{
  "schemaVersion": 1,
  "disclosures": [
    {
      "name": "TestBlockMutexProfileInlineExpansion",
      "class": "host-fatal",
      "signature": "",
      "reason": "Awaits `sync.Mutex.Lock` + `runtime/pprof.inlineC` from its OWN goroutine (pprof_test.go:2795) while the goroutine it started sits in inlineC's mu.Lock(). MEASURED ALONE on the published host (Release, tiering off, --run ^name$ -timeout 2m): exit 2 in 123 s, attributed fail at pprof_test.go:2784, then the package-level `test binary died on an unrecovered panic in a goroutine`. This is the row SUB-Q27 measured as the package's host-killer at alphabetical position 2, behind which 180 rows went unmeasured. Carries 3 of the Go side's 183 verdict rows. Host-fatal, not a verdict disclosure, for the reason the package note gives: the host DOES attribute a fail verdict carrying the panic text before it dies, so what is missing is the SURVIVAL, not the verdict, and no per-test capture can supply it. Withdrawn from BOTH sides by one --skip/-skip regexp and COUNTED IN DISCLOSED."
    },
    {
      "name": "TestBlockProfile",
      "class": "host-fatal",
      "signature": "",
      "reason": "Drives seven table cases whose f -- blockChanRecv, blockChanSend, blockChanClose, blockSelectRecvAsync, blockSelectSendSync, blockMutex, blockCond -- each start a goroutine that awaits the blocked MAIN goroutine's own frames while main is parked on the channel, mutex or cond that goroutine was going to release. MEASURED ALONE: exit 2 in 122 s, attributed fail at pprof_test.go:816. Carries 3 verdict rows (top level, /debug=1, /proto). Host-fatal, not a verdict disclosure, for the reason the package note gives: the host DOES attribute a fail verdict carrying the panic text before it dies, so what is missing is the SURVIVAL, not the verdict, and no per-test capture can supply it. Withdrawn from BOTH sides by one --skip/-skip regexp and COUNTED IN DISCLOSED."
    },
    {
      "name": "TestMutexProfile",
      "class": "host-fatal",
      "signature": "",
      "reason": "Reaches blockMutexN, which awaits N goroutines blocked in `sync.Mutex.Lock` carrying `runtime/pprof.blockMutex` frames, from a goroutine, while the test is parked. MEASURED ALONE: exit 2 in 125 s, attributed fail at pprof_test.go:1232. Carries 4 verdict rows (top level, /debug=1, /proto, /records). Host-fatal, not a verdict disclosure, for the reason the package note gives: the host DOES attribute a fail verdict carrying the panic text before it dies, so what is missing is the SURVIVAL, not the verdict, and no per-test capture can supply it. Withdrawn from BOTH sides by one --skip/-skip regexp and COUNTED IN DISCLOSED."
    },
    {
      "name": "TestMutexProfileRateAdjust",
      "class": "host-fatal",
      "signature": "",
      "reason": "Reaches blockMutex -- the TestMutexProfile shape with count 1. MEASURED ALONE: exit 2 in 122 s, attributed fail at pprof_test.go:1351. Carries 1 verdict row. Host-fatal, not a verdict disclosure, for the reason the package note gives: the host DOES attribute a fail verdict carrying the panic text before it dies, so what is missing is the SURVIVAL, not the verdict, and no per-test capture can supply it. Withdrawn from BOTH sides by one --skip/-skip regexp and COUNTED IN DISCLOSED."
    },
    {
      "name": "TestProfileRecordNullPadding",
      "class": "host-fatal",
      "signature": "",
      "reason": "Calls blockMutex to produce a mutex-profile event before it reads any of the five record readers it checks for null padding, so it dies on the helper before its own subject is reached. MEASURED ALONE: exit 2 in 123 s, attributed fail at pprof_test.go:2838. Carries 6 verdict rows (top level, /BlockProfile, /GoroutineProfile, /MutexProfile, /MemProfile/inUseZero=false, /MemProfile/inUseZero=true). Host-fatal, not a verdict disclosure, for the reason the package note gives: the host DOES attribute a fail verdict carrying the panic text before it dies, so what is missing is the SURVIVAL, not the verdict, and no per-test capture can supply it. Withdrawn from BOTH sides by one --skip/-skip regexp and COUNTED IN DISCLOSED."
    },
    {
      "name": "TestProfilerStackDepth",
      "class": "host-fatal",
      "signature": "",
      "reason": "Reaches goroutineDeep, blockChanDeep and blockMutexDeep through produceProfileEvents, each awaiting a deep foreign stack. MEASURED ALONE: exit 2 in 122 s, attributed fail at pprof_test.go:2544 -- and this arm produced TWO package-level death events, one per panicking goroutine, which is the plainest statement that the shape is structural rather than a single accident. Carries 5 verdict rows (top level, /block, /goroutine, /heap, /mutex). Host-fatal, not a verdict disclosure, for the reason the package note gives: the host DOES attribute a fail verdict carrying the panic text before it dies, so what is missing is the SURVIVAL, not the verdict, and no per-test capture can supply it. Withdrawn from BOTH sides by one --skip/-skip regexp and COUNTED IN DISCLOSED."
    }
  ],
  "notes": [
    "SIX host-fatal entries, all of one shape, all minted by Q43 on 2026-09-04 and each earned by its own isolation measurement: the awaitBlockedGoroutine closure described next. A SEVENTH entry -- TestGoroutineProfileLabelRace, a HANG rather than a crash -- was minted with them and RETIRED on 2026-09-05 (its record is the last note). Withdrawing the seven is what made the package measurable at all: on the 2026-09-04 tree the converted host went from 2 verdicts against 183 Go rows to 147 verdicts, 120 of them matching, in 48 seconds.",
    "SIX host-fatal exclusions, minted together (Q43, 2026-09-04), are the complete reverse-reachability closure of runtime/pprof's awaitBlockedGoroutine helper -- computed with go/packages under the same conservative any-use-of-a-same-package-function rule the converter's own capability analysis uses -- and EACH was independently measured to take the host down when run alone. Withdrawing fewer would not make the package censusable: the next member kills the run the same way at the next alphabetical position. Together they carry 22 of the Go side's 183 verdict rows.",
    "THE MECHANISM. awaitBlockedGoroutine (pprof_test.go:1023) polls runtime.Stack(buf, all=true) for the regexp (?m)^goroutine \\d+ \\[<state>\\]:\\n(?:.+\\n\\t.+\\n)*runtime/pprof\\.<fName> -- it waits for a FOREIGN goroutine's own INTERIOR STACK FRAMES to appear in the all-goroutine dump. The CLR offers no way to walk another thread's stack, which the converted runtime.Stack(all) already states rather than hides: a foreign goroutine renders as its header, a ForeignStackPlaceholder line and its `created by` line. So the regexp can never match on any input, the helper's `for { Gosched(); Stack() }` loop never terminates, and the time.AfterFunc(t.Deadline()-1s) timer Go arms for exactly this case fires and panics ON A GOROUTINE. golib reproduces Go's semantics faithfully -- an unrecovered panic in a goroutine kills the binary -- so the process dies and every test after it in its phase is lost. The capability is absent BY CONSTRUCTION, not unimplemented: no increment of runtime/pprof can supply it.",
    "WHY AN EXCLUSION RATHER THAN A DISCLOSED VERDICT. The host already attributes a verdict before it dies -- Goroutine.ObserveUnhandledPanic writes `<test> fail -- panic on a goroutine started by <test>` with the panic text -- so a verdict is not what is missing. Survival is, and no per-test capture can supply it: the polling loop keeps spinning whatever is done with the timer's panic, so containing it converts a bounded crash into an unbounded hang and a package-deadline kill that discards every verdict in the package. That is the measured reflect TestOffsetLock shape core/testing/TestHost.cs carries as the reason its own containment policy was REVERSED, and the reason golib's Goroutine.ContainUnhandledExceptions refuses a PanicException outright.",
    "MINT RULE, checked: no committed proof page under docs/validation/current names any of the six, so the withdrawal cannot shrink a row some platform runs successfully. SIGNATURE, deliberately empty on all six: a withdrawn test produces no verdict on either side to pin against, and the loader refuses a signature only for this class for exactly that reason. RETIREMENT: all six retire together the day runtime.Stack(all) can render a foreign goroutine's interior frames; until then the entries simply stop applying if it ever can.",
    "THE SEVENTH ENTRY WAS A HANG, THE CLASS'S FIRST, AND IT IS RETIRED (2026-09-05, by the measurement its trigger named). TestGoroutineProfileLabelRace did not crash: its /reset subtest spun forever because the converted goroutine profile withheld LABELS (SUB-Q27's measured pointer-staleness defect) and the subtest's exit condition is a label appearing in the profile text. Measured alone it produced ZERO verdicts and consumed the whole package deadline (exit 1 after 182 s under -timeout 3m); in the full run it left 36 rows without a C# verdict. It sat in host-fatal rather than in the converter's unsupportedRuntimeCapabilities gate on the gate's own bar -- 'add an entry ONLY for something provably unavailable, never for something merely unimplemented' -- and the label half was unimplemented, not unavailable. RATIFIED, and the class WIDENED with it (coordinator ruling 2026-09-05, recorded at hostFatalClass in testConversion.go): host-fatal admits a crash OR a deadline-consuming hang, because the charter is 'every test after it in its phase is lost too' and a test that never returns costs exactly that. RETIREMENT, measured: the labels re-entered at the Q44 union (golib hands a reference-bearing box's registered order TOKEN out as its unsafe.Pointer number, so the labelMap pointer resolves to its box across a collection instead of naming a moved address; pprof_impl.cs fills labels[i] from the goroutine registry's slot). Re-measured at that union with this entry deleted, gated ^(TestGoroutineCounts|TestGoroutineProfileLabelRace)$, Release, tiering off, oracle go1.23.12: TestGoroutineProfileLabelRace PASS in 58 ms (/reset 42 ms, /churn 6 ms) and TestGoroutineCounts PASS in 10.7 s with its label half now reached -- 4/4 verdicts matching. The entry is deleted rather than annotated because host-fatal retires by deletion; this note keeps the history so the shape is not re-derived. The six above are unchanged by it: their capability (a foreign goroutine's interior frames in runtime.Stack(all)) is a different door."
  ]
}
