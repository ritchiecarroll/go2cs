{
  "schemaVersion": 1,
  "disclosures": [
    {
      "name": "TestCaller",
      "class": "runtime-capability",
      "signature": "incorrect symbol info",
      "reason": "runtime-capability: symtab_test.go's assertion requires f.Entry() < pc -- the function's entry address always strictly precedes a call site's return address within it. go2cs has no addresses, only opaque per-call-site PC tokens (the deliberate model managed_impl.cs's header documents throughout); FuncForPC(pc) mints a *Func whose Entry() returns that SAME token, so Entry() == pc by construction and the < assertion can never hold. Inventing a value strictly less than the token (e.g. subtracting a constant) would satisfy the assert with a number that means nothing this host can stand behind -- the same class of laundering the ManagedPointerTokens/reflect pointer-identity disclosures already refuse. Falsifiable in the parts that matter: the same assertion also checks ok, Name()'s suffix, and the file/line range: if any of THOSE ever mismatch, the row is a real defect, not this disclosure -- only the Entry()>=pc arm is pinned. Measured 2026-09-02: ok=true, Name()=the correct qualified name, file/line correct, Entry()==pc exactly (e.g. 27==27) on every reported iteration."
    },
    {
      "name": "TestPinnerConstStringData",
      "class": "runtime-capability",
      "signature": "not marked as pinned",
      "reason": "runtime-capability: the test's own comment states its subject -- 'const string data is not in span'. It asserts that unsafe.StringData of a string LITERAL reads runtime.isPinned == true before, during and after a Pinner.Pin/Unpin, which is true in Go because RODATA is not a heap span: spanOfHeap answers nil and the non-Go-pointer arm answers pinned. golib has no RODATA -- a literal's bytes are a heap byte[] (rooted for the process's life but movable) and unsafe.StringData returns an element reference into it -- so the truthful managed answer for that storage is 'a Go heap object, not pinned', and that is what the row reads on its FIRST assertion, before Pin is ever called. Answering true for storage nothing pinned would be fabrication (this class's anti-laundering clause). A truthful form exists in principle -- literal storage on the Pinned Object Heap plus a registry of literal-backed arrays so isPinned can recognise them -- and is REFUSED as one row's consumer paying a corpus-wide, unpriced allocation-policy change; that form is this entry's rejoin clause. Only the first-assertion arm is pinned: Pin and Unpin on that pointer must still not panic, so a regression in the non-Go-pointer no-op reads as a defect, not as this disclosure. Design and prediction: docs/phase4/DESIGN-runtime-pinner.md section 6.1."
    },
    {
      "name": "TestPanicSystemstack",
      "class": "host-fatal",
      "signature": "",
      "reason": "host-fatal HANG member (the class was widened to admit a deadline-consuming hang beside a crash, coordinator ruling 2026-09-05, recorded at hostFatalClass in testConversion.go; this row RATIFIED as a member by coordinator ruling 2026-09-05 over C1's Q46 reading). The test re-execs the host with the argument testPanicSystemstackInternal; the child's crash_unix_test.go init() starts one goroutine and then calls testPanicSystemstackInternal on the initializing thread, each meant to print \"x\\n\" and then deadlock on a runtime lock inside systemstack so that a SIGQUIT under GOTRACEBACK=crash prints both blocked stacks. MEASURED (solo, Release+TC0, 3 m deadline; Go=pass): the converted child prints ONE x, because the goroutine started inside init() cannot enter testPanicSystemstackInternal until the CLR type-initializer lock is released by the module initializer it was started from -- and that initializer is deadlocking itself by the test's own design (dotnet-stack: main thread = <Module>..cctor -> initΔ3 -> lock2 spinning; goroutine = Goroutine.Run with no body frame, its thread in a futex wait). Go runs init-started goroutines concurrently with init; the CLR serializes every other thread's first touch of an initializing type behind its constructor. The parent therefore never completes io.ReadFull(pr, 4), never sends SIGQUIT, never reaches its deferred Process.Kill, and consumes the package deadline; the deadline's external SIGKILL orphans a child that a hand-sent SIGQUIT kills at once (its SIGQUIT is unblocked and caught, SigPnd empty). NOT a missing feature: the class censused in the pinned GOROOT (a go statement lexically inside func init()) is 2 production + 2 _test.go files and exactly ONE deadlocks, by its own design -- the semantic remedy (a package init that does not hold the type-initializer lock while it runs) is a converter design question recorded on the board with a population of one, not built ahead of a second case. Withdrawn from BOTH sides by the one --skip/-skip regexp and COUNTED IN DISCLOSED. Retires if a second case ever moves the design."
    },
    {
      "name": "TestEmptySlice",
      "class": "host-fatal",
      "signature": "",
      "reason": "host-fatal HANG member, class REPRESENTATIONAL (coordinator ruling 2026-09-07 admitting the adjChunks pair; the class FIELD ratified to host-fatal at the same ruling once the schema was read at testConversion.go -- only host-fatal reaches hostFatalSkipExpression and only host-fatal may carry an empty signature, and a hanging test has no failure text to pin, so any other class is either refused at load or needs an invented signature that can never match). The test's FIRST statement is adjChunks() (mfinal_test.go:149, called at :168), an unbounded for{} that allocates *objtype -- objtype is [320]byte, reference-free -- until two live objects sit at exactly adjacent addresses, uintptr(unsafe.Pointer(c)) + 320 == uintptr(unsafe.Pointer(d)). SetFinalizer, runtime.GC and the <-fin receive below it are therefore NEVER REACHED: despite its file this is not a finalizer row, and the finalizer-deadlock reading of it was retracted in full once the spin was measured. Solo at a 5 m deadline the host burns 0.99 cores sustained across 255 s with the working set growing 125.4 -> 157.7 MB and emits no terminal action, against Go passing the same test in the same comparison (i9). MEASURED on both sides. Native Go at the pinned go1.23.12 finds the pair at ITERATION 2, minimum delta exactly 320 over 191 of 197 consecutive gaps, because Go's size class for a 320-byte object IS 320 and Go stores no per-object header. The CONVERTED path has a hard floor of 344 with ZERO hits at 320 across 199,999 pairs (i9, mailbox d0687ff2), and 344 is independently DERIVED from the CoreCLR x64 object layout as 8 sync block + 8 method-table pointer + 8 length and padding + 320 payload. Two distinct non-overlapping objects are separated by at least the size of the first, so 320 is below the smallest gap this object model CAN produce and the search cannot terminate. REPRESENTATIONAL rather than unimplemented: the assertion references the FOREIGN system's own heap layout, which a correct port answers differently by construction; the row's other verdicts are exactly what an unbuilt implementation would satisfy, so this is NOT an E3 row exclusion and the row stays in the denominator. Withdrawn from BOTH sides by the one --skip/-skip regexp and COUNTED IN DISCLOSED. RETIRES when a 320-byte Go allocation has a 320-byte managed footprint."
    },
    {
      "name": "TestEmptyString",
      "class": "host-fatal",
      "signature": "",
      "reason": "host-fatal HANG member, class REPRESENTATIONAL (coordinator ruling 2026-09-07 admitting the adjChunks pair; the class FIELD ratified to host-fatal at the same ruling once the schema was read at testConversion.go -- only host-fatal reaches hostFatalSkipExpression and only host-fatal may carry an empty signature, and a hanging test has no failure text to pin, so any other class is either refused at load or needs an invented signature that can never match). The sibling of TestEmptySlice and the same defect one function over: the test's FIRST statement is adjStringChunk() (mfinal_test.go:182, called at :197), the identical unbounded for{} hunting the same 320-byte adjacency between a string's backing bytes and an *objtype, so SetFinalizer and the <-fin receive below it are NEVER REACHED. It was absent from the first gated run's stuck set only because \"Slice\" sorts before \"String\" and the run hung at the first member; PREDICTED on that ground and then CONFIRMED solo -- same three results-file events, same package-deadline terminator, same 0.99-core spin (i9, mailbox 6003d31), so this member is measured and not inferred from its sibling. MEASURED on both sides. Native Go at the pinned go1.23.12 finds the pair at ITERATION 2, minimum delta exactly 320 over 191 of 197 consecutive gaps, because Go's size class for a 320-byte object IS 320 and Go stores no per-object header. The CONVERTED path has a hard floor of 344 with ZERO hits at 320 across 199,999 pairs (i9, mailbox d0687ff2), and 344 is independently DERIVED from the CoreCLR x64 object layout as 8 sync block + 8 method-table pointer + 8 length and padding + 320 payload. Two distinct non-overlapping objects are separated by at least the size of the first, so 320 is below the smallest gap this object model CAN produce and the search cannot terminate. REPRESENTATIONAL rather than unimplemented: the assertion references the FOREIGN system's own heap layout, which a correct port answers differently by construction; the row's other verdicts are exactly what an unbuilt implementation would satisfy, so this is NOT an E3 row exclusion and the row stays in the denominator. Withdrawn from BOTH sides by the one --skip/-skip regexp and COUNTED IN DISCLOSED. RETIRES when a 320-byte Go allocation has a 320-byte managed footprint."
    },
    {
      "name": "TestCrashWhileTracing",
      "class": "host-fatal",
      "signature": "",
      "reason": "host-fatal CRASH member (coordinator ruling 2026-09-07). A crash produces no row to pin, so this is the skip-list kind rather than a disclosure of a failing verdict. FACT ABOUT TODAY'S HOST: the test binary dies on an unrecovered panic in a goroutine at this row -- index 104 of 883 in file order -- and the death SHADOWS every later test, leaving 799 rows UNMEASURED; excluding this row alone raised terminal verdicts from 84 to 104 (i9, measured, ungated vs gated at master 5a27a8972). ROOTED end to end and the root is NOT a host defect: runtime.StartTrace is hand-owned on linux and windows and refuses by name because the CLR has no per-thread g (runtime/<goos>/trace_impl.cs), runtime/trace.Start early-returns on that error BEFORE spawning its writer goroutine so nothing is ever written to the child's stdout, the parent's traceparse.NewReader then fails version.ReadHeader's Fscanf on the \"go 1.\" literal, and t.Fatalf aborts the test BEFORE cmd.Wait(); testenv's t.Cleanup cancels the context, os/exec's watchCtx -- still parked because Wait never drained resultc -- takes ctx.Done() and calls cmd.Cancel, whose else branch t.Logf's after the test has completed, and Go's own testing panics on a post-completion log. Every step from the Fatalf onward is FAITHFUL GO (internal/testenv/exec.go:199 and :205, os/exec/exec.go:772); Go never reaches the failing header read because Go's tracer works. The entry is the UNBLOCK, not the answer: it turns 799 unmeasured rows into measurable ones in one cut, and the tracer capability itself remains open work rather than a settled frontier. RETIRES when the test re-admitted produces a terminal verdict. AMENDMENT 2026-09-13 (C1) -- THE 1-6/7-8 MARKING, ruled at f5b35f351 as one dated amendment to be written only once i9's item-4 tail was in. LINKS 1-6 OF THE CHAIN ABOVE ARE READ: source-read at named lines on both sides (runtime/<goos>/trace_impl.cs, runtime/trace/trace.cs, internal/trace/reader.go, crash_test.go). LINKS 7-8 -- testenv's t.Cleanup cancel reaching os/exec's parked watchCtx, and the cmd.Cancel else-branch t.Logf that Go's own testing panics on -- were marked CONSEQUENCE-NOT-MEASURED: a reading of the code's own consequence, not a run. THE TAIL IS IN AND IT DOES NOT MEASURE THEM AT THIS ROW: i9 0dd133719 on landed master 44f858717 (Release, tiered false, .NET 10.0.11, rc=1, 169 s) reaches 185 of 880 verdicts with 695 never reached, and this row is not among them. NOR CAN ANY RUN MEASURE THEM HERE, IN EITHER CONFIGURATION, AND THAT IS THE FINDING RATHER THAN A GAP: run SOLO, the row's cleanup fires while the test is still live, so the late log cannot happen -- measured 2026-09-06, the row fails cleanly in 2.33 s and the host lives (src/tests/GolibTests/LogAfterCompleteTests.cs:34-39, which names this row as the WRONG instrument for exactly this reason); run FULL, this entry's own name enters hostFatalSkipExpression's regexp (src/go2cs/testConversion.go:6600) and withdraws the row from both sides. The entry forecloses its own confirming measurement. WHAT IS MEASURED is links 7-8's TERMINAL SHAPE, at the next door rather than this one: TestLockOSThreadNesting dies on \"panic: Log in goroutine after TestLockOSThreadNesting has completed\", thrown at src/core/testing/TestExecution.cs:418 after the ancestor walk at :407 finds no live ancestor (root-read by C1 at 18a34299f; the walk is Go's logDepth shape, landed 1aff2a352 and an ancestor of the tail's own tree) -- the same post-completion-log kill links 7-8 describe, and faithful, since Go panics on that condition too. SO THE MARKING STANDS AS: 1-6 READ; 7-8 READ AND MECHANISM-CONFIRMED AT A SIBLING DOOR, NEVER MEASURED AT THIS ROW, AND NOT MEASURABLE AT IT. Nothing above this amendment changes, the RETIRES trigger included."
    }
  ]
}
