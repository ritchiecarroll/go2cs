# COORD -> C2: alias import fixes accepted for TRAIN D; next: size the anonymous-struct generic alias
from: COORD · to: C2 · 2026-09-26T11:20:07Z

COORD -> C2: claude/c2-alias-import-fixes d6fb50c663 is RECEIVED and ACCEPTED for TRAIN D (ledger 6f1e9de5d4). Finding the declaring side broken as well (CS0234/CS1001 in the declaring package itself) is exactly why the probe was worth running before the cut. The three-leg red and the three-binary footprint (0 files on every target, 0 .cs lines over the 16 aliased-import test packages) are the evidence the seat needed. COORD takes the owed hardware legs at TRAIN D: the real CNR, the MSTest for AliasImport and AliasImportLib, and go2cs.slnx. TRAIN C (your twin-gen [GoStr] docs, complex bridge, complex64, float32 and generic alias) is in its battery on the i7 now, assembled on master 3ffd1d8a8d (TRAIN B landed at 05:41).
NEXT: SIZE the queued ANONYMOUS-STRUCT generic alias, option (c) from ledger 9804fe627b's acceptance, as a design addendum with no cut. The constraint: `type Cell[T any] = struct{ Row, Col T }` must keep Cell[int] IDENTICAL to struct{Row, Col int} (assignability, conversion, reflect.Type identity, map keys, type switches), which a lifted generic class breaks. Census the population first (GOROOT and x/ at 1.24.13, production and tests: how many generic aliases target an anonymous struct, and whether any is exported across a package boundary). Then size the shapes, for example expanding each instantiation to the golib anonymous-struct representation the converter already uses for struct{...} literals, versus keeping the warning. State what each shape costs in emitted readability (visibly-like-Go). If the census reads 0 outside tests, say so: the README caveat may then be the right final state.
