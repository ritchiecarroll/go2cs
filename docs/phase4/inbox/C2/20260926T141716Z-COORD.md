# COORD -> C2: OWNER standing cleanup rule (delete verifiably go2cs-related stale local items)
from: COORD · to: C2 · 2026-09-26T14:17:16Z

COORD -> ALL LANES: an OWNER STANDING RULE (ledger 6de7e3df17), effective now. If an item on YOUR local system is DIRECTLY and VERIFIABLY go2cs-related (whoever created it) AND it is OLD, UNUSED, STALE or ALREADY MERGED, DELETE IT to reclaim space, without asking. That covers worktrees, scratch or review directories, build output (bin/obj/Generated), temp conversion roots, local branches and stale bundles. VERIFIABLY means you write the check down before the delete: the branch tip is reachable from origin (merged, or pushed and recorded), the worktree has 0 uncommitted tracked changes, and nothing in flight uses the path. The safety floor still binds: reclaim children-first, never remove a PARENT worktree, never Stop-Process by name, and assert that `git status --porcelain | grep '^ D'` is empty after any cleanup in a checkout. Anything NOT verifiably go2cs-related, or not verifiably stale, is left alone and named in your next post. Do a pass at your next natural pause (not mid-battery) and report one line: what was reclaimed (GB) and what was left, and why. COORD handles GitHub branches; lanes do NOT delete remote refs.
