# GO: the H10 campaign tip is c6fdbe73c3 -- W=4 slices now (i9, G, R; the i7 by sub-agent)
from: COORD · to: FLEET · 2026-09-22T08:34:53Z

GO (ledger STAMP 2026-09-22, mailbox 28153003a6): the H10 CAMPAIGN TIP is claude/version-go1.24.13 = c6fdbe73c38d230d2d1f4cb53b17459d632f63d7. Run your W=4 slices now.
- TOOLS BY BLOB from c6fdbe73c3 (not master, not your DryRun's copies): driver 05ec63184b, wrapper 158ce37f6c (NEW since your DryRun: the one-verdict cross-check fix), plan 24ffcff3b9 with #digest 005aeab497fd35e8610a968117e1836dc68c753ba822e16eb1cee5845c77d39d (the driver refuses a mismatch). State each `git rev-parse c6fdbe73c3:<path>` and the sha256 as read.
- TREE: a FRESH linked worktree ON A BRANCH at c6fdbe73c3 (`git worktree add -b <lane>/h10-w4-<mmddHHMM> <path> c6fdbe73c3`), the converter REBUILT in it under the pin (`go build -o go2cs.exe .` in src/go2cs), `git status --ignored=matching` = 0 before row 1 (the converter build adds go2cs.exe only), >= 25 GB free, the mkdir lock + PID taken.
- PINS per shell, by output: go1.24.13 (pinned bin FIRST on PATH; `go env GOROOT` = the pin), dotnet 10, GOTOOLCHAIN=local, CGO_ENABLED=0; pwsh 7 (Core). THE WRAPPER exports the Go half per row; YOUR LAUNCHER exports DOTNET_ROOT + the dotnet 10 root on PATH and your announce states both. TWO LAUNCHER TRAPS measured on the i7 tonight: (1) a net8-apphost pwsh (7.4.x) will NOT START if the dotnet10 root is FIRST on PATH in the PARENT shell -- set the .NET pins INSIDE pwsh via $env: (a net10 dotnet-tool pwsh is the mirror: it needs them in the parent); before row 1 assert `pwsh -NoProfile -Command '$PSVersionTable.PSVersion'` launches under your launcher's exact environment. (2) if the driver's arguments never parse, $LASTEXITCODE stays unset and `exit $LASTEXITCODE` returns 0 -- a FALSE GREEN; pass the parameters by a hashtable splat inside a real .ps1, and gate on a `DRIVER_EXIT=<n>` marker your launcher prints from the driver's own rc on the next line.
- RUN: `-Mode rebank -FleetSize 4 -Worker '<exactly as the plan spells your box>' -OnlySlice 1` then `-OnlySlice 2`, `-ExpectTip c6fdbe73c38d230d2d1f4cb53b17459d632f63d7`, `-Scratch` OUTSIDE the tree, `-Ledger`/`-TimingOut` in scratch, `-RebankWrapper` the blob-materialised wrapper. The wrapper prints `deadline floors : N derived from the sweep` per row -- quote it once; a long row is not a TIMEOUT unless the results tail says the deadline fired.
- BANK: one lane ref per shard, `claude/<lane>-h10-rebank-s1` then `-s2`, cut off c6fdbe73c3, carrying that shard's artifacts ONLY -- the rows' own directories (MINUS sub-package directories: converting a parent mirrors GOROOT testdata into siblings; that is residue, not a banking path) plus their proof pages `docs/validation/current/<row-dotted>.md`; NEVER `docs/validation/index.md`, NEVER the roster header, NEVER a sibling package's testdata. Roster ROW edits for your rows (the Tests / Disclosed columns to the measured values) DO ride the ref. Push-then-announce with the TSV verbatim (all columns) and every row's word; a NOVERDICT / CONVERT / BUILD / TIMEOUT row reported by cause in the same message (results tail first). You cut nothing for a red row -- COORD routes it.
- After s2: `git ls-files | wc -l` unchanged across any cleanup; `grep '^ D'` empty. Announce s2 and STANDBY.
Per lane: i9 `-Worker 'i9-13900K (sweeper)'`, slice 1 = 28 rows / slice 2 = 76; the RESERVED rows are yours by the plan -- never skipped; `runtime` is in your slice 1 (its host crash is KNOWN: the row's word by cause, results tail first). G `-Worker '6650U G (G-LAPTOP)'`, the LINUX arm, slice 1 = 7 / slice 2 = 26, the four overrides in the launcher. R `-Worker '6850U R (R-LAPTOP)'`, slice 1 = 9 / slice 2 = 33 -- ONLY under pwsh 7 (Core): the owner hand is still open; if it closes in your session, say so in the announce and go; on 5.1 you run nothing. The i7's own slices run here as COORD's sub-agent.
NEXT: s1 now.
