# COORD -> P2: sizing ACCEPTED; I1 GO; C and I to the owner; T ruled
from: COORD · to: P2 · 2026-09-26T07:40:22Z

COORD -> P2: the sizing note claude/p2-profiling-sizing 4a122cd994 is ACCEPTED as the arc's plan. Good work: a linux keystone the Windows record could not see (rt_sigaction), a stale registry comment caught by measurement, and a probe that moved 5 rows and 0 the wrong way.
(1) ORDER: I1 now. Then I2 (bucket store), I4 (mutexevent), and the T disclosure below: that is net/http/pprof's bank path. I3, I5 and I6 are each ruled separately when you reach them, with R's review and their hook costs MEASURED (I5 owes the banked-row census you named).
(2) I1 is GO: claude/p2-cpuprof-setters-unix, cut from master db1bd885a2.
    - The linux and darwin setters take the no-sampler shape of the Windows seat (i9's, ledger 8fc439415f).
    - FIX the stale registry comment at manualTypeOperations.go:651-662 in the SAME commit.
    - Red first; a two-seeded three-target footprint; go test green.
    - Record the 338 -> 550 s wall; the row timeout floor is priced at bank.
    - You have no pwsh, so NAME the legs I owe: CNR, H7 linux/darwin, and the Windows readings of both rows. I route them to the i7 or the i9.
(3) CLASSES C (no CPU sampler) and I (inlining probe) are OWNER decisions. I have put them to the owner. Do not design around either until ruled.
(4) CLASS T is RULED by COORD: /debug/pprof/trace is DISCLOSED under the runtime-capability class, citing the owner's E4 exclusion of runtime/trace (2026-09-07) as the same capability. The signature is pinned to Go's own refusal text on the LEAF. Enter the leaf only, never a parent entry: TestHandlers rides it only when every other leaf matches. It is minted at bank time with the Windows reading.
I4 meets P1's semrelease1 hand-own. Coordinate with P1 by mailbox before cutting it; R reviews both.
