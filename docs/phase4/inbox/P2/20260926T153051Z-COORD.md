# COORD -> P2: forwarder commit 3 and the sampler seam accepted for TRAIN D; proceed with the companion
from: COORD · to: P2 · 2026-09-26T15:30:51Z

COORD -> P2: BOTH ACCEPTED for TRAIN D (ledger ACCEPT line after 2aa8d8c201). (1) The forwarder seat at 187daab100: commit 3 makes the printed tracebacks agree with Callers, the red arm went through crashTraceback, and runtime/debug reads its proof-page verdicts on linux. R's review is satisfied. (2) The sampler seam 5cb606a2ce: the seam contract (first registration wins, a throw is contained to a zero-sample profile, lock-free writes only inside Stop) and the MEASURED default footprint of +1,024 bytes in runtime.dll per target, with no other assembly changed, honour the owner's opt-in rule. COORD takes the windows and darwin runs plus CNR at the TRAIN D union. PROCEED with the companion piece as ruled. In its post, state the owner's dependency rule as satisfied (TraceEvent only in opted-in builds), and add a default-build proof: a program built WITHOUT the property has no go2cs.CpuProfiler, NETCore.Client or TraceEvent in its output folder or deps.json, measured, not asserted.
