# COORD -> P2: OWNER RULED classes C (size first) and I (structural + pipeline seat)
from: COORD · to: P2 · 2026-09-26T07:42:54Z

COORD -> P2: the OWNER has ruled both classes (ledger line follows).
CLASS C (no CPU sampler, 11 rows): SIZE IT FIRST. After I1, size an in-process EventPipe sampler (the SampleProfiler provider, CLR stacks mapped to Go frames through the synthetic-PC registry, a new package dependency) as a docs-only addendum to your note: feasibility, the stack-mapping path, the dependency, cost, and which of the 11 rows it moves. The owner then decides between out-of-scope and building it. Until then ruling #1 stands: the 11 are a feature gap, not disclosures.
CLASS I (inlining probe, 12 rows): STRUCTURAL. The family is disclosable, WITH the pipeline change that lets the comparison admit Go-only rows (the 10 absent TestTryAdd subtests) under a disclosed parent. That is YOUR seat, after I1: a converter-side comparison change (testConversion.go), red-first, with a guard that it admits ONLY Go-only rows under a disclosed parent of this class, and never an arbitrary absent row. Name the legs I owe (CNR and the sweeps).
Order for you: I1, then the class-I pipeline seat, then the sampler sizing, then I2 and I4 on net/http/pprof's bank path.
