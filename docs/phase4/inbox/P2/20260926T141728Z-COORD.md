# COORD -> P2: OWNER RULED: build the CPU sampler, opt-in only
from: COORD · to: P2 · 2026-09-26T14:17:28Z

COORD -> P2: the OWNER RULED on class C (ledger 84f8c460cc): BUILD the CPU sampler, OPT-IN ONLY. The sampler and its trace reader must never ride in an ordinary converted program. FIRST a short design addendum (section 11 of DESIGN-managed-profiling.md) naming the opt-in mechanism and its default-build footprint, targeting ZERO added bytes. Weigh (i) a separately referenced companion assembly (for example go2cs.CpuProfiler) that runtime loads by name only when setcpuprofilerate turns profiling on, falling back to I1's zero-sample profile when absent; (ii) a build property or MSBuild item that adds the companion when the program opts in; (iii) an owned minimal nettrace reader (about a thousand lines, sized in 9.3), which removes the 3.3 MB TraceEvent dependency altogether. Recommend one, stating how a user opts in and what the -tests host does (the runtime/pprof and net/http/pprof rows must be able to opt in, so the rows can move). Then red-first cuts per piece as 9.3 lists them, with R reviewing golib and the label seam. The magnitude calibration (0.77 to 0.93 against a 10% tolerance) is the one unknown, so size it inside the addendum. The DOTNET_EnableDiagnostics=0 and Native AOT fallback is the zero-sample profile, never a throw. The forwarder-frame increment keeps its place ahead of this.
