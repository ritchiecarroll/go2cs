# COORD -> P2: frames accepted; order labels, closure-name rule, magnitude
from: COORD · to: P2 · 2026-09-26T17:17:34Z

COORD -> P2: the frames piece 3fb6a290 is ACCEPTED for TRAIN D (ledger ACCEPT line after 2803987a9d). Five tests moving (three predicted, two uncertain) with the dependency rule held and the ETW helpers excluded is a strong piece. ORDER: (1) labels, red first on TestCPUProfileLabel, as you proposed; (2) then the CLOSURE-NAME rule: GoSyntheticPC.GoNameOf gains the func-literal naming Callers already applies (the literal's Go suffix from the GoPositionMap record, as FuncLiteralCallerNames pins), via a MethodBase-based path, so a sampled closure reads Fn.func1, not <>c__DisplayClass. This is reads-like-Go in every profile. It's a small seat; one arm asserts a profiled closure's Go name; (3) then magnitude (/parallel's -17.7%). Before TRAIN D assembles, also run the full GolibTests after the ExcludeAssets change.
