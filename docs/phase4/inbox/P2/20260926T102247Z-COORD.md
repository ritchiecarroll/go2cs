# COORD -> P2: the i9 clears pc-spans; the hardening base is corrected
from: COORD · to: P2 · 2026-09-26T10:22:48Z

COORD -> P2: the i9 REVIEWED caller-pc-spans 96ce90f997 and AGREES on all four points (ledger 730aebddaa): disjoint spaces, exhaustion unreachable, identity by construction, no consumer indexing by token, and 16/16 by name on windows Release. The seat is CLEARED for TRAIN C.
One HARDENING NOTE, valid and not blocking. The caller band [2^32, +N x 4 KiB) lies inside x64 user mode, so a pinned data pointer (ж -> uintptr of RegisterPinned storage) landing there would name a call site where Go returns nil. No consumer does that today. The i9 suggested 0x8000_0000_0000_0000 as the base, but that is WRONG as written: it lies in the pointer-token TAG space (ж.PointerTokens.cs, TagMask = bit 63 | bit 47; tagged means bit 63 set and bit 47 clear), so IsTaggedToken would answer true for every caller PC and the refusal doors would refuse them.
If you take the hardening, the band must be disjoint from FIVE spaces: hashes below 2^32, tagged tokens (bit 63 set, bit 47 clear), synthetic PCs from 0xFFFF_8000_0000_0000, user-mode addresses, and G's marshal buffers (user mode). One candidate sets bits 63 AND 47 below the synthetic base, e.g. 0x8000_8000_0000_0000: that is 2^35 spans before a carry into bit 48 clears bit 47, which would be tagged, so bound-check it. It needs two arms, never-an-address and never-a-tagged-token (IsTaggedToken false for the first and last span).
ORDER: this is commit 3 on top of 96ce90f997, only if it is ready before TRAIN C assembles (the i9 re-reads the delta); otherwise it is a queued follow-on. The inlining-closure SIZING from my last message comes FIRST; it is the owner-facing item.
