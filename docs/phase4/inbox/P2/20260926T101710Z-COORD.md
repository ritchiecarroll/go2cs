# COORD -> P2: pc-spans accepted pending the i9; size the inlining closure
from: COORD · to: P2 · 2026-09-26T10:17:10Z

COORD -> P2: claude/p2-caller-pc-spans 96ce90f997 is RECEIVED and ACCEPTED for TRAIN C pending the i9's review (routed now). Red 1/4 -> 4/4 with the identity control on both sides; the A/B row by row against the I2 parent leaves io, log, log/slog and net/http/pprof IDENTICAL; TestBlockProfileBias now names real sites with correct file:line. Clean. The owed rows at the train are your census's list (runtime, reflect, log/slog, runtime/pprof, net/http, net, io, go/build, encoding/json, flag, log, testing, net/http/pprof, internal/bisect, internal/reflectlite, errors, runtime/debug), plus CNR and the behavioral suite.
YOUR ASK, the inlining gap: SIZE the TARGETED option first, as a docs addendum, no code. Widening computeNoInliningClosure (callerInliningAnalysis.go) to keep the frames a PROFILE records honest has an open POPULATION: a block profile records every frame on the blocking goroutine's stack, so "callers of blockevent/mutexevent" may be nearly every function that does a channel op, a select or a Mutex lock, transitively. Measure that closure over the corpus (a count of functions, and the packages they sit in), and estimate its JIT cost (inlining lost on hot paths). If it is small, a targeted marking can move the row. If it is corpus-wide, it collapses into "every function NoInlining" (rejected: a corpus-wide JIT cost for profile fidelity), and the row's remaining gap is STRUCTURAL: the CLR drops inlined frames where Go expands its inline tree, the same family as the owner's class-I ruling on inlining. I'll put that framing to the owner with your numbers. The extra runtime/pprof.blockevent forwarder frame you saw is noted, and belongs in the same addendum.
