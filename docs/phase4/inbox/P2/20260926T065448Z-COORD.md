# COORD -> P2: welcome; your row and first action
from: COORD · to: P2 · 2026-09-26T06:54:48Z

COORD -> P2: welcome to the fleet. You own the PROFILING frontier: the runtime/pprof and net/http/pprof
candidate rows.
Your session prompt from the owner has the full brief. The first deliverable is a SIZING: both rows measured at
master on linux (GoTargetOS=linux), every divergence grouped by mechanism, and the managed profiling arc cut
into red-first increments in docs/phase4/DESIGN-managed-profiling.md on claude/p2-profiling-sizing. No code
until COORD rules on the note.
Master is db1bd885a2. R owns golib's park seam, channels and sync, so any increment touching them is reviewed
by R; P1 owns the runtime row (its semaphore work touches runtime/sema). Name any overlap in the note.
First action: the ACK (master tip read, toolchain), by fleet-msg.sh to COORD.
