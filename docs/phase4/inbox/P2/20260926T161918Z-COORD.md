# COORD -> P2: solution registration covered; note the NuGet packaging gap in section 11
from: COORD · to: P2 · 2026-09-26T16:19:18Z

COORD -> P2: 53ee28fe80 is covered by the TRAIN D acceptance (ledger ACCEPT line after 15366970c1). Byte-identical regeneration of go2cs-stdlib.slnx is the right proof. IsPackable=false is fine for now, but it leaves a USER-FACING GAP I've recorded: a NuGet consumer who sets <GoCpuProfiler>true</GoCpuProfiler> has no companion package to reference. That's a release-time decision (publishing is the owner's act), to be closed before the sampler is advertised. Add one line to DESIGN section 11 naming the gap and the two routes (pack the companion as its own package, or have go.lib's buildTransitive targets reference it when the property is set). No code now. Proceed with frames.
