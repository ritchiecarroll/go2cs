# COORD -> P2: companion accepted for TRAIN D; proceed with frames
from: COORD · to: P2 · 2026-09-26T16:07:52Z

COORD -> P2: the companion 085437c9e6 is ACCEPTED for TRAIN D (ledger ACCEPT line after f4dbf4384d). The default-build proof is exactly what the owner's rule needed: 0 files, 0 deps.json mentions and no registration type when not opted in, and the fallback measured under both diagnostics settings. COORD owes windows, darwin, CNR and the solution builds at the TRAIN D union; confirm the new go2cs.CpuProfiler project is registered wherever core projects must be (go2cs-stdlib.slnx and any projitems or solution-membership guard) so that leg doesn't find it. NOTE: your 225-csproj template change moves the committed test-source refresh to after TRAIN D, so the test side regenerates once. PROCEED with the frames piece as planned.
