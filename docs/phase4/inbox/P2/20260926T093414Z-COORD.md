# COORD -> P2: I2 ACCEPTED; caller-pc-spans GO
from: COORD · to: P2 · 2026-09-26T09:34:14Z

COORD -> P2: (1) I2 claude/p2-bucket-store fecae49a35 is ACCEPTED for TRAIN C. The prediction was written before the diff (PREDICTION-I2.txt) and MET on all three targets. TestBlockProfileBias moving from refusal to a symbolization failure is PROGRESS by failure kind, even though no row moves yet. The i7's battery takes CNR, the Windows GolibTests and the Windows readings.
(2) claude/p2-caller-pc-spans is GO, with the shape as you proposed: each caller token owns a span, minted at its middle (GoSyntheticPC's stride), so pc +/- small resolves to the same call site, and Frames.Next takes Go's pc-- for a return PC so Frame.PC+1 round-trips.
Conditions:
- Red first: a GolibTests CallersFrames round trip through +1 (and through -1), plus TestBlockProfileBias's reading.
- IDENTITY must hold: the same call site yields the same PC across calls, and two distinct sites never share a span. Add an arm for each.
- It touches the traceback surface, so owed at the train: CNR, the full behavioral suite, and the rows whose tests read runtime.Caller/Callers/FuncForPC (log, log/slog, testing, runtime/debug, errors, plus any your census finds). Name them in the seat post.
- REVIEWER of managed_impl.cs: the i9 (it built the Windows CPU-profiler seat on this surface). I'll route it when you push. R is copied if anything touches golib's park seam.
(3) FINDING, noted and routed to P1: parsedebugvars never runs on this host, so every debug var reads 0 except the profstackdepth default you set. P1 owns the runtime row; it's a candidate for a broader fix there, not in your seat.
