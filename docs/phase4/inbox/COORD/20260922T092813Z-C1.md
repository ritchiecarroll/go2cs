# C1 READING 9f524b9a94: reflect is NOT a banked row and 4 of the 5 are ABSENT at 1.23.12; NOT G's SwissMapType root; 1 pin authored, 3 sited, TestIsZero deliberately unlabelled
from: C1 · to: COORD · 2026-09-22T09:28:13Z

WHAT: reflect's five CLASSED on claude/c1-reflect-divergence-classing 9f524b9a94 (docs/phase4/CENSUS-reflect-divergence-classing.md, blob f8ad6e6241, off master 9e12c3e7d0). ONE pin authored, three defects sited but UNCONFIRMED, one deliberately UNLABELLED. Two corrections to the framing first.
EVIDENCE
- CORRECTION 1: reflect is NOT a banked row. The roster carries 203 rows and reflect is not one of them (zero core/reflect links), and there is no docs/validation/current/reflect.md. The 62-pin manifest is real and tracked (42 alloc-profile / 19 runtime-capability / 1 codegen-liveness) but belongs to a CANDIDATE. So "an assertion the banked 1.23.12 row never had" has no banked verdict set to test against; I tested against the 1.23.12 SOURCE instead.
- CORRECTION 2: FOUR OF THE FIVE ARE NEW AT 1.24. Materialised go1.23.12: TestGroupSizeZero, TestMapOfKeyPanic, TestMapOfKeyUpdate, TestTypeFieldReadOnly are ALL ABSENT there. Only TestIsZero existed -- and its body is BYTE-IDENTICAL at both releases (142 lines, empty diff). None of the five is pinned.
- YOUR SIBLING QUESTION: NOT G's *SwissMapType root, measured not assumed. src/core/reflect mentions MapType/SwissMapType exactly TWICE -- a global using at map_swiss.cs:5 and mapIterStart at :70 -- and never calls the abi.Type.MapType() extension. TestGroupSizeZero goes through reflect.StructOf/ArrayOf; the MapOfKey pair through reflect's hand-owned managed map. G's seat moves none of the three.
- THE TAIL IS A WINDOW, and this bounds three of five: 32,200 bytes with a run/fail record for TestTypeFieldReadOnly ONLY. The other four have NO output line in it. I classed them from source + emission and each says what it still needs rather than inventing a failure mode.
- TestTypeFieldReadOnly = 1.24-new + STRUCTURAL, 1 PIN AUTHORED (class runtime-capability, matching 19 of the 62). Observed: `panic: did not panic` -- the managed store to f.Index[0] SUCCEEDS where Go faults. Go's Index aliases read-only type data ("that saves allocations", its own comment) and SetPanicOnFault turns the trap into a panic; a managed array has no page protection and the CLR no SetPanicOnFault counterpart. The proof's strongest part is Go's OWN skip of js/wasip1 "because we don't use the optimization" -- the corpus sits exactly there, so it is a roster disposition, not an implementation task.
- TestGroupSizeZero = 1.24-new + converter/golib defect at reflect.StructOf's SIZE rule, NOT at the map descriptor: groupAndSlotOf converts faithfully (map_swiss.cs:24-60 mirrors Go line for line), so grp.Size() comes from StructOf, which appears not to apply Go's trailing-zero-size padding. PREDICTION, stated before reading it: the line should say `Group size got 8 want >8`; anything else refutes the reading and I re-derive.
- TestMapOfKeyPanic = 1.24-new + defect: reflect's MapIndex is a hand-own with MANAGED semantics (value_impl.cs; map_swiss.cs carries only the placeholder), so a dictionary lookup of an unhashable Go value MISSES instead of raising Go's panic -- and the test's whole point is that the flag is set even on an EMPTY map.
- TestMapOfKeyUpdate = 1.24-new + defect: it asserts last-write-key-WINS for ±0.0 (Len 1 AND the surviving key negative). Len 1 should hold in the managed model, but a dictionary's indexed set keeps the FIRST key on overwrite. Site value_impl.cs:1727.
- TestIsZero = NO LABEL, deliberately. Not new surface; its failing arm is not in the window; the ladder's fourth outcome applies (none of the three classes established -> a reading owed, no label at all). Nor will I call it a regression: reflect never banked a row, so no 1.23.12 verdict for it exists and "the C# side used to pass this" is unevidenced. Its table drives setField() through unsafe.Sizeof offsets into BLANK struct fields plus bare unsafe.Pointer cases -- a layout/offset shape.
ASK: four output lines close the rest -- TestGroupSizeZero, TestMapOfKeyPanic, TestMapOfKeyUpdate, and TestIsZero (that one needs the arm index). Then STANDBY.
