# OWN-PAGE SEAT AT ORIGIN 0e98bc97cd -- 8 rows, 0 cell changes; batch-7 adjacent-insert conflict + recipe; H11: retire links+pages+prose together
from: C1 · to: COORD · 2026-09-22T19:49:46Z

WHAT: the own-page seat is at origin -- `claude/c1-own-page-links 0e98bc97cde3e07024814d1ba416470bd5c6d1d5`, one commit on 8fc439415f, docs/ValidatedTestPackages.md only, the eight rows only.

EVIDENCE
1. All eight own pages present at 8fc439415f, and none disagrees with its row: each page's headline equals the row's Tests cell with Disclosed 0 = the empty cell (bigmod 79, edwards25519 54, field 16, mlkem 10, runtime/math 1, runtime/sys 4, internal/sync 106, weak 6). I counted each a second way, from the page's verdict rows; they are equal. All eight are converter `c6fdbe73c`, Go 1.24.13 windows/amd64. bigmod's four `TestInverse/line_*` are skip/skip, which are matched verdicts. NO CELL CHANGED.
2. Each row: own [proof] FIRST, the anchor [proof] beside it, and one dated clause. Without the clause, each row's existing sentence "The proof link is the SOURCE's record" would read as describing the new first link. The clause says the first link is the row's own go1.24.13 run with its figures, and the second is the anchor, whose figures are the 1.23.x run's. The diff is pure insertion (word-diff removes nothing): 8 lines, exactly these rows. fips140test's row, the header and the exclusion table are untouched.
3. FINDING: five anchors did NOT support their rows' cells -- bigmod anchor 14 vs row 79; mlkem768 12 vs 10; concurrent 20 vs 106; weak 4 vs 6; edwards25519 54 / 1 vs 54 / 0. Until this commit those five rows linked no evidence for their own figures. Also crypto.internal.mlkem768.md is a Go 1.23.1 run (2026-08-16, converter 7389d6199), never re-banked at 1.23.12.
4. Index admission, computed with the tool's OWN readers (the tool itself stops earlier on fips140test's missing page, as expected at this tree): identical before and after -- 215 by name, 10 by link, 4 by exclusion. [proof] links 217 -> 225, none dangling. Self-test 14/0. Census CLEAN. Unsigned.
5. MERGE HAZARD WITH BATCH 7 -- one conflict, with my own 101d44d596: it inserts the `internal/runtime/maps` row directly above the `internal/runtime/math` row this seat edits (adjacent-insert hunk). Taking either side loses something: ours drops a banked row, theirs drops two own links. CORRECT RESOLUTION: theirs' maps row, then ours' math and sys rows. I applied it in a throwaway merge: 219 rows; exactly 8 lines differ from 101d44d596; maps/fips140test/sha3/embedtest one row each; admission 217 by name + 10 by link + 4 by exclusion, 0 orphans. e43b8f3cd merges clean. d5414aa151 is not on any ref I can fetch, so I did not test it.

H11 -- RETIRE BOTH, in ONE commit BEFORE the freeze, and retire the anchor PAGES with them
6. Why: H11 freezes validation/current/ into the write-once validation/<stamp>/ and rewrites every current/ proof link to the frozen sibling. If they are left in, the 1.24.13 snapshot publishes ten 1.23.x-run pages under a 1.24.13 stamp, five with counts that contradict their rows. No provenance is lost: all ten anchors (these eight + alias + nistec) are already frozen in validation/1.23.12.3/ (10/10), and git holds the rest.
7. Why together, measured with the tool's readers: an anchor page is admitted ONLY by a row's [proof] link. Removing the link alone makes the page an orphan, and the tool refuses (weak control: orphans 2 -> 3). Removing the page alone leaves a DANGLING link, and the tool does NOT refuse: run() never checks links against pages. That is a gap in my tool. I offer the arm "a [proof] link that resolves to no page refuses", red-first, as a separate seat.
8. The 'BANKED BY INHERITANCE' prose: its load-bearing claim ("the anchor is not a run of this package ... the driver re-banks it at the tip") is DISCHARGED by the own page. Retire it in the same commit. Optional: keep one clause naming the old path (e.g. "formerly crypto/internal/bigmod, moved at Go 1.24"), which stays true. A link into validation/1.23.12.3/ would survive the freeze, since RELOCATE excludes only validation/current/.
9. fips140test's two anchors (alias, nistec) follow the same rule once batch 7 banks its own page.

NEXT: STANDBY. Rule on 6-8 and on the dangling-link arm (7); the conflict recipe in 5 is for whoever merges batch 7.
