# PUSHED claude/c2-defined-complex64-typed-const 8707a96433 -- width by underlying kind, footprint 0
from: C2 · to: COORD · 2026-09-26T08:26:06Z

PUSHED new ref claude/c2-defined-complex64-typed-const 8707a96433, one commit on e454b62ddb (seated in TRAIN C).

FIX: in visitValueSpec, a COMPLEX-kind const's width comes from the declared type's UNDERLYING basic kind, not from `csTypeName == "complex64"`. So `const k C64 = 0.25` reads `0.25F + 0F.i()`. Plain complex64, complex128, UntypedComplex and every complex128-backed type render as before.

RED-FIRST (local, unofficial, linux flavour): DefinedComplexConst gains typed consts of C64 (real, integral and complex values; package-level and local), C128 and C128b. At e454b62ddb it is RED on exactly the 4 C64 consts (CS0029); the C128 and C128b consts compile and stand as controls. With the fix: 0 errors, and the output equals Go's.

CENSUS (go/types, GOROOT, 3 targets, prod + internal + external tests; positive-controlled on a probe 9/9): 0 typed consts of any defined float or complex type.

FOOTPRINT (two seeded roots from one archive of e454b62ddb, both binaries):
- -stdlib windows / linux / darwin: 0 files changed (written 1856 / 1926 / 1926 per arm). As predicted.
- -tests over fmt, reflect and testing/quick: 0 .cs lines. Only go2cs_test_manifest.json provenance moved (converter revision and input digest), and that file is untracked.
- Behavioral, 737 projects transpiled by both binaries (a CNR stand-in; there is no PowerShell here; the same 8 are unmeasured in both runs): only DefinedComplexConst/main.cs moves, 8 lines = the 4 C64 consts, and the fixed emission equals the golden. Its package_info.cs map re-encodes because the Go changed.
- go test ./... ok.

OWED (hardware): the real CNR (check-no-regression.ps1), the behavioral MSTest for DefinedComplexConst, and the stdlib and go2cs.slnx builds at TRAIN C (the corpus bytes are unchanged, so they should read as for e454b62ddb).

SUGGEST: the float32 twin. `const f F32 = 0.25` with `type F32 float32` emits a double (`0.25`), which is CS0266. It has two causes: visitValueSpec.go:934 picks the width by `csTypeName == "float32"`, and only the plain-float32 `const` arm appends the `f` suffix. GOROOT has 0 sites (same census). It is left out of this seat because it is not the one-line fix.

STANDBY.
