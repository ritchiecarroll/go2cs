# C2 O1 SURVIVAL CENSUS ANNOUNCE: 3b53fec4f2; 476 today, 2,766 (57%) with func-value adapters; the fmt format path hinges on 8 value sites
from: C2 · to: COORD · 2026-09-24T17:23:33Z

C2 -> COORD: O1 SURVIVAL CENSUS ANNOUNCE. Pushing claude/c2-literal-cache-draft c555d91c55 -> 3b53fec4f2 (fast-forward, existing ref, announced first). Read-only; new probe docs/phase4/probes/c2-o1-survival (RESULTS.md is the record).

POPULATION: the production literal arguments to a noescape string parameter of a static callee. It reproduces the J rows exactly: windows 4,835 (2,344 + 2,491 format), linux 4,802, darwin 4,803. string(b): 111/115/115, matching I6.

SURVIVORS (windows, sites; format position in brackets):
- S0 today: 476 (81).
- S1 with sstring's gaps closed (enumerator, copy, spread): 528 (81).
- S2 = S1 plus an adapter lambda at func-value sites: 2,766 = 57% (2,233 = 90% of the format population). linux/darwin within 5.
- THE LEVER IS THE ADAPTER. fmt.Errorf/Sprintf/Fprintf/Printf are each used as a value once or more (7 sites, 1 in production: text/template funcs.go:51), and so is parsenum (fmt/export_test.go:8). One value use fixes a delegate signature, and the fixed point fails the whole format path.

WHY THE REST FAILS (S2 first reason, 2,069 sites):
- hand-owned 989: runtime.throw alone is 726;
- cascade 522;
- binder 262;
- reflection-exported method 139;
- generic 44, box 33, closure 30, map key 29, composite 13, other 8.
CASCADE ROOTS:
- stringslite.Index passes its needle to the generic bytealg.IndexRabinKarp: 248 sites (strings.Index/Contains/Cut/Split/Count/Replace);
- the hand-owned bytealg.IndexString/IndexByteString: 72;
- Builder/Buffer.WriteString (binder): 97;
- log.Printf (closure): 66.

FMT PILOT (§8.4 step 1):
- Literals enter through the EXPORTED entries (Errorf 920, Sprintf 446, Fprintf 286, Printf 39 = 1,694 directly). An unexported-only pilot changes no count; flipping the exported format parameters is a visible signature change, the owner's call.
- The flip list is 33 parameters (31 on linux/darwin), including utf8.DecodeRuneInString and RuneCountInString.
- Preconditions: the spread (buffer.writeString appends s...); adapters at 8 value sites (one takes Printf's PC, so identity changes); the explicit u8 -> sstring operator.

RESIDUAL (windows):
- 2,069 failing, plus 3,248 leaking in Go, plus 3,481 with no parameter to retype.
- Outside call arguments: about 20,000 stored/returned/assigned literal occurrences (composite elements 17,881).
- Reconciled exactly with the call-argument rows on all 3 GOOS.

PREDICATES AND CONTROLS: all in the README. go test . controls every class both ways on a fixture, and was made to fail once (dropping the interface-variable arm named `boxes`), then restored byte-identical.

LEGS: a read on the linux VM; no .NET, nothing converted or compiled. Survival is a prediction about C#; the pilot's own build is the first read.
Not in the design file: C1's §8.4 appends at the same spot on another branch. SUGGEST: COORD places a §8.5 pointer after C1's seat.
Gates: go test -count=1 ./... green; identifier census clean on all 14 files. Next: arm C once COORD posts the 1.24.13.2 SHA.
