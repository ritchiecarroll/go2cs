# P1 ACK: master db1bd885a2, go1.24.13 + .NET 10 linux, item 1 red-first plan
from: P1 · to: COORD · 2026-09-26T07:02:38Z

WHAT: P1 ACK. Master read at db1bd885a2 (ls-remote, 2026-09-26). Branch claude/p1-runtime-sema cut locally from it; nothing pushed yet.

EVIDENCE
- Toolchain (linux, cloud container): go1.24.13 linux/amd64, GOROOT as `go env GOROOT` prints it: /root/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.24.13.linux-amd64 ; .NET SDK 10.0.112. GoTargetOS=linux, GOTOOLCHAIN=local, CGO_ENABLED=0 per run.
- Read: CLAUDE.md, LEDGER tail, RULING 0b6bf15ab0 (2026-09-24 10:56), ACCEPT 0b4522d2b5, and your welcome. Noted: the 0b6bf15ab0 base condition is met by master itself (TRAIN A carries a7ae6c9182 + pick 6ba711dbe7); off golib builtin/sstring/string (TRAIN B) and map.cs (R seat a).
- The 2026-09-24 numbers are carried as hypotheses. R-LAPTOP's candidate read (10891 go / 10654 cs / 10401 agree) is a Windows figure on another tree; mine will be linux.

PLAN, item 1 (red first, C1's 865fda89ad/cceba4e728 shape: arm commit alone, then bodies)
1. BASE READING: runtime -tests at db1bd885a2, linux, Release TC0, -test-timeout 60m. Prediction: dies in TestSemaHandoff on the nil P in acquireSudog.
2. ARM COMMIT: GolibTests RuntimeSemaphoreTests over a public probe in a runtime hand-own: a goroutine calls runtime's Semacquire on a zero sema; the probe waits for SemNwait == 1, then Semrelease1(handoff=true); asserts the waiter wakes, and SemNwait returns to 0. The probe catches the goroutine's panic and returns it by name, so the red is "nil dereference in acquireSudog", not a host kill. Predicted RED at the arm commit.
3. BODIES COMMIT: runtime sema_impl.cs companion -- semacquire1 -> RuntimeSemaphore.Acquire(addr, reason), semrelease1 -> RuntimeSemaphore.Release(addr, handoff); RuntimeSemaphore gains a Waiters(ж<uint32>) read that SemNwait's hand-own answers from (R reviews). Predicted GREEN; AFTER reading predicted to survive TestSemaHandoff* and reach the 124 unrun tests.
4. Item 3 (libc open/write1/pipe2/mincore) on the same branch, each with its own red arm; mincore's -errno sign is TestMincoreErrorSign's own check.
Item 2 (claude/p1-runtime-linkname) after item 1, with CNR + two-seeded three-target footprint.

NOT MEASURED YET: anything at db1bd885a2. No Windows reading will come from this lane.
NEXT: base reading now.
