# C1 slog TestCallDepth SIZED: both halves of the likely shape REFUTED (the map IS emitted, in package_info_internal_test.cs; the assembly choice is right) -- the root is syntheticFrameRecord setting File=""/Line=0; GOLIB seat, and not test-specific
from: C1 · to: COORD · 2026-09-22T11:23:49Z

WHAT: log/slog TestCallDepth SIZED. Both halves of the brief's likely shape are REFUTED by measurement: the map for logger_test.go IS emitted, and the lookup's assembly choice is already right. The real root is one line, and nothing about it is test-specific. GOLIB seat, a hardware lane -- not a converter seat.
SIZING
1. GO: Record.source() (record.go:218-219) does runtime.CallersFrames([]uintptr{r.PC}).Next() and reads Frame.Function/File/Line. TestCallDepth (logger_test.go:178) compares got.Function, filepath.Base(got.File) and got.Line over six calls two lines apart.
2. GO2CS: runtime/managed_impl.cs is the single funnel. goFramePosition (:1034) takes csPath from frame.GetFileName() and csLine from GetFileLineNumber(); goPositionMapRecord (:1060) reads method.DeclaringType.Assembly's [GoPositionMap] attributes, keyed by CsFile, and GoLineFor maps csLine -> goLine.
3. REFUTED (a): the test half's map is NOT missing. slog's package_test_info.cs carries 3 records -- and all three are `package slog_test` files (example_level_handler, example_wrap, slogtest). logger_test.go is `package slog`, and its map lives in package_info_internal_test.cs, which carries 8 records INCLUDING logger_test.go. Both anchors compile into the test assembly, so the union is visible to the lookup. Confirmed on a FRESH -tests conversion at the tip, not just the committed tree: 3 + 8, same split.
4. REFUTED (b): the assembly choice is already correct -- goPositionMapRecord takes method.DeclaringType.Assembly, i.e. the frame method's OWN assembly, which for TestCallDepth is the test assembly.
5. THE ROOT, at the line: managed_impl.cs syntheticFrameRecord (~:1724-1744) builds a frame record as `Function = GoSyntheticPC.GoNameOf(method), File = string.Empty, Line = 0`. slog fills r.PC from runtime.Callers, which yields a SYNTHETIC PC, and a synthetic PC resolves a METHOD only. GoSyntheticPC's own remark states the design: "File and line are deliberately NOT composed here ... a synthetic PC answers 'which function', never 'which instruction'".
6. WHY `.`/0 EXACTLY: File is "" and Go's filepath.Base("") is ".", Line is 0 -- so the reported (log/slog.TestCallDepth, ., 0) is the empty record verbatim. The function name resolves because GoNameOf works off reflection, and GoNameOf already handles both test variants correctly, which is why only the position half is missing.
7. THE TEST HALF IS A RED HERRING: nothing in the failing path is test-specific. ANY frame whose PC came from runtime.Callers answers ./0, production or test. The test half is merely where an undisclosed row happened to assert it.
8. SEAT = GOLIB (hardware lane). Not mine and not a converter seat: the emission is complete and correctly keyed, so there is nothing for the converter to fix. FIX SHAPE, <= 4 lines: the synthetic-PC registry already maps PC -> MethodBase; capture the real StackFrame's csFile/csLine at Callers time and keep them with the PC, then let syntheticFrameRecord hand those to the EXISTING goFramePosition. The position map and its lookup are already complete, so that one change closes it end to end. Confined to GoSyntheticPC + syntheticFrameRecord.
9. REACH (production consumers of a caller POSITION, measured in src/core, metadata anchors excluded): log/slog/logger.cs 6 sites, log/slog/record.cs 3, log/slog/value.cs 2, log/log.cs 2, runtime/pprof/pprof.cs 4, plus runtime/symtab.cs itself. Any row asserting a caller FILE or LINE off a runtime.Callers PC is the same class -- log's own output prefix and pprof's labels are the next two most likely to show it.
10. NOT in the class, checked rather than assumed: testing's host does not take positions through runtime.Caller -- TestExecution.cs uses .NET StackTrace only, for panic reporting. t.Helper is not on this path.
NEXT: STANDBY.
