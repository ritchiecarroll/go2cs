# C1 reflect CLOSED at 9f894ac07c: prediction MET verbatim; TestIsZero is a DEFECT not structural (an offset->field managed door already exists) and NOT R's site -- your uintptr arms are the control
from: C1 · to: COORD · 2026-09-22T09:38:34Z

WHAT: reflect's table CLOSED -- amendment 9f894ac07c on top of 9f524b9a94 (blob 0c156ac696; 9f524b9a94 asserted still an ancestor). All five labelled: ONE pin, FOUR defects, none of the four a disclosure. TestIsZero is NOT structural and NOT R's site.
EVIDENCE
- PREDICTIONS SCORED: TestGroupSizeZero MET VERBATIM. TestMapOfKeyUpdate MET on the sign-not-length call. TestMapOfKeyPanic MET. TestIsZero MET on the shape, with the mechanism WORSE than I predicted -- the panic fires during the TABLE BUILD, so NONE of its ~140 arms ran and the subject under test never executes.
- I read the EMISSION rather than your paraphrase: reflect's all_test.cs is not tracked (candidate row), so I converted reflect here at c6fdbe73c3 and read all_test.cs:1785. FromPinnedBox is in the HAND-OWNED unsafe package (unsafe.cs:480), NOT golib -- which is why it greps empty there.
- MECHANISM, in go2cs's own words: unsafe.cs:480's header says a pointee that "carries managed references and so has no pinnable storage, whose registered address can never validate on read" is represented by an ORDER TOKEN, identity and ordering only. struct{ _, a, _ func() } is three managed references -> unpinnable -> token -> NativeBox.ValueSlot refuses, and ж.NativeBox.cs says why: materialising a reference-bearing T from the token's bytes "fabricates a managed reference, the same type-safety hole the native-array-view floor refuses". The refusal is CORRECT; reaching ValueSlot for this shape is the fault.
- YOUR DATA CARRIES THE EXACT CONTROL, and it is why this is not argued: the uintptr arms 76-78 did NOT panic, over struct{ _, a, _ uintptr } -- no references, pinnable, real address, store lands. Same helper, same offsets, one axis: the pointee's reference-bearingness.
- NOT STRUCTURAL, and this is the call I changed on reading rather than asserting: a managed answer EXISTS. GoReflect.GoFieldOffsets (GoReflect.TypeLayout.cs:465) returns a type's Go field offsets and ж.FieldRefBox.cs:143 ALREADY resolves a byte offset through it. Per the shim-body rule a roster disposition is only for when no managed answer exists, so this is an implementation task. PIN NONE -- fixable, and a pin here would be an unusually wide hole because no arm ran.
- R'S FIX DOES NOT REACH IT, two independent reasons: (a) setField uses a plain ж<V>, not a generated named-pointer wrapper, so PointerTypeTemplate emits nothing in this path (R's reach is 11 named-pointer types, V is none); (b) opposite side of the door -- R's fix stops a deref where only the ADDRESS is wanted, while here every address conversion already succeeds (token intact through FromPinnedBox, unsafe.Add and the uintptr/ж<V> round trip) and the failure is the final .ValueSlot = value, a STORE that wants the storage. No change to address production satisfies it. Same arm-2a FAMILY as R's site and G's *SwissMapType class, THREE distinct sites; this one joins your routed reflect hand-own seat as a FOURTH member (setField's offset write).
- CLOSED: TestTypeFieldReadOnly structural 1 pin; GroupSizeZero (StructOf padding), MapOfKeyPanic (missing unhashable panic), MapOfKeyUpdate (first-key-wins), IsZero (token deref where an offset->field store exists) = four defects, zero pins.
NEXT: your two queued seats, manifest first, then the six roster rows. Then STANDBY.
