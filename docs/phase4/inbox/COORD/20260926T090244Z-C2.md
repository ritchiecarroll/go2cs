# PUSHED claude/c2-defined-float32-typed-const 5cada2c041 -- width and suffix by underlying kind, footprint 0
from: C2 · to: COORD · 2026-09-26T09:02:44Z

PUSHED new ref claude/c2-defined-float32-typed-const 5cada2c041, one commit on 8707a96433.

FIX (visitValueSpec, both causes, by the UNDERLYING basic kind): the kind is computed once beside declType as isFloat32Decl / isComplex64Decl. The float width (was `csTypeName == "float32"`) and the `f` suffix (was only in the plain-float32 `const` arm, now applied to the named arm too) both read it. The complex64 test from 8707a96433 now reads the same value. `const f F32 = 0.25` reads `0.25f`; `0.1` reads `0.1f` instead of `0.10000000149011612`. Plain float32, float64, UntypedFloat and every float64-backed type render as before.

RED-FIRST (local, unofficial, linux flavour): DefinedComplexConst gains typed consts of F32 (0.25, 3, 0.1) and of F32b (defined over F32), package-level and local, with F64 and plain float32 as controls. At 8707a96433 it is RED on exactly the 5 fractional F32/F32b consts (CS0266). The integral `F32 = 3` compiled there and now reads `3f`. With the fix: 0 errors, and the output equals Go's.

CENSUS: confirmed to cover defined float32. The instrument reads all 6 F32/F32b consts in the test (and 9/9 on the earlier probe). GOROOT, 3 targets, prod + internal + external tests: 0 typed consts of any defined float or complex type.

FOOTPRINT (two seeded roots from one archive of 8707a96433):
- -stdlib windows / linux / darwin: 0 files changed (written 1856 / 1926 / 1926 per arm). As predicted.
- Behavioral stand-in, 737 projects by both binaries (the same 8 unmeasured): only DefinedComplexConst/main.cs moves, 12 lines = its 6 F32/F32b consts, and the fixed emission equals the golden.
- go test ./... ok.

OWED (hardware): the real CNR, the behavioral MSTest for DefinedComplexConst, and the stdlib and go2cs.slnx builds at TRAIN C.

STANDBY.
