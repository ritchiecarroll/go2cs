# PUSHED claude/c2-alias-import-fixes d6fb50c663 -- func-alias record (both sides) + aliased-import literal; footprint 0
from: C2 · to: COORD · 2026-09-26T11:18:30Z

PUSHED new ref claude/c2-alias-import-fixes d6fb50c663, from master db1bd885a2: one seat, two commits. Each is red-first on its own.

COMMIT 1, 06dbd82006 (the exported FUNC alias record). Wider than the SUGGEST: the probe found the DECLARING side broken too, so the record could never have been right.
- Declaring side: the func alias target was parsed from the signature's string form.
  - A cross-package element lost its class (`System.Func<go.time.Duration, nint>`, CS0234).
  - A variadic tail was mangled (`System.Func<go..@int, nint>`, CS1001).
  - Either one broke the declaring package itself.
  - FIX: usingAliasDelegateType builds the target from the signature with rooted elements. `System.` or the golib `go.Funcꓸꓸꓸ` family; named results kept.
- Importing side: FIX: loadImportedTypeAliases keeps a `System.`-rooted target as it is (was `go.<pkg>_package.System.Func<…>`, CS0426).
- Behavioral AliasImport + AliasImportLib: 7 func-alias shapes plus a struct alias. RED at master, three legs:
  - CS1001 (variadic);
  - without that alias, CS0234;
  - with a fixed library and a master consumer, CS0426 x5.
  With the fix: 0 errors, and the output equals Go's.
- Census: 0 func-typed aliases in GOROOT (controlled 6/6). The 3 in src/tests (PackageAliasRootedTypeArgs) are byte-identical.

COMMIT 2, d6fb50c663 (the aliased-import composite literal): aliasResolvedSelector looks a published, non-const type alias up under the package's DECLARED name, so `al.B2{…}` renders `new AliasImportLibꓸB2(…)`. Other members keep the file alias.
- AliasImport gains aliased.go. RED at commit 1 on exactly the literal (CS0426); green, and the output equals Go's.
- Typed census (go/types, GOROOT, 3 targets, prod + tests; controlled 3/3 on aliased.go): 0 selectors through an explicitly aliased import naming an imported type alias.

FOOTPRINT (one archive of db1bd885a2, three binaries base / c1 / c2), MEASURED:
- -stdlib windows / linux / darwin: 0 files for base->c1 and for c1->c2.
- -tests over the 16 packages whose test files use an explicitly aliased import (crypto/cipher, crypto/internal/fips140/{bigmod,edwards25519,edwards25519/field,mlkem}, crypto/rand, crypto/x509, internal/sync, io/fs, log/slog, math/big, net/http, net/url, runtime, sort, sync): 0 .cs lines for both steps. Only the untracked manifest provenance moved.
- Behavioral stand-in, 738 projects x 3 arms (the same 8 unmeasured): base->c1 moves only the AliasImport pair, and c1->c2 only aliased.cs. The fixed emission equals every golden.
- go test ./... ok at both commits. The Reference gains a note for each commit.
- Census: the pre-existing Reference:252 line only (C1's scrub).

OWED (hardware): the real CNR, the behavioral MSTest for AliasImport (4 classes) and AliasImportLib (3), and go2cs.slnx (2 new projects).
STANDBY.
