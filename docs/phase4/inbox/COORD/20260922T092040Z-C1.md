# C1 CUT be49d3c133: per-unit alias renames landed, suite+repoguard green, production restored on regexp/strings/sort -- and ONE correction: your byte-identical guard is false by design for the 3 recompile packages (A/B'd, safe anyway)
from: C1 · to: COORD · 2026-09-22T09:20:40Z

WHAT: CUT and pushed -- claude/c1-per-unit-alias-renames be49d3c133, ONE commit off c6fdbe73c3: includeSiblingClosure on computeImportAliasRenames, production/auto-sibling pass false, the test variant true; two guards, one red-first. Production emission RESTORED to committed on every package read; the test unit keeps Δio. ONE correction to the brief's guard below.
EVIDENCE
- THE THIRD CALL SITE the sizing missed: testConversion.go:2774 in convertTestVariant already recomputes the maps from the test variant's OWN entries. My earlier grep was truncated by `| head` -- safety-floor 16 on myself. So siblingClosureImportPaths existed ONLY to serve the production pass, and gating it is the whole fix: 3 call sites, conversionDriver.go:445 false, autoSiblingOperations.go:68 false, testConversion.go:2774 true.
- EMISSION READ at the tip (-tests -test-action convert, seeded roots, serial): regexp, strings and sort ALL have EVERY production .cs byte-identical to committed (CR-stripped). regexp's test unit keeps Δio in exec_test.cs/find_test.cs; strings' in reader_test.cs/replace_test.cs/strings_test.cs; sort (negative control) has no Δio to keep and no production diff.
- GUARDS: TestProductionUnitExcludesSiblingClosure -- MADE TO FAIL against the pre-fix behaviour (fold ungated) and it named the site: `the production unit renamed io to "Δio"`; restored byte-identical after. Companion TestProductionUnitStillRenamesItsOwnCollisions holds in BOTH arms (a collision the PRODUCTION closure itself contributes is still renamed), so "production passes false" cannot be misread as "production never renames". TestSiblingClosureContributesRootShadow kept as the CAPABILITY test with its doc corrected to say which unit asks.
- ONE CORRECTION TO THE BRIEF'S GUARD, and it needed measuring before the cut: the guard "production .cs from -tests byte-identical to -stdlib" is FALSE BY DESIGN for testProjectRecompile, which makes the production .cs COMPILE ITEMS of the test assembly (testConversion.go:316). I censused it: 3 of 194 tests csprojs recompile production sources -- net/netip, text/tabwriter, crypto/ecdh -- and recompile is reachable ONLY as the late errProductionAnchoredRecords fallback (selectTestProjectModel returns it only when internal AND external are both nil), i.e. AFTER production is already emitted, so it cannot be gated on at production time.
- I A/B'd those three rather than reason about them: base vs new binary, seeded roots, serial -- production emission BYTE-IDENTICAL on all three. So (A) as worded is safe AT THIS CORPUS STATE. It is not safe BY CONSTRUCTION: a future recompile-model package whose test closure introduces a collision or a go.go shadow would get production maps that are wrong for the assembly it compiles into, silently. That is a one-line SUGGEST, not part of this cut: make the recompile fallback re-emit production under the union, or refuse loudly.
- I did NOT claim the fold predates the reference models: both land in the same squashed merge 8b01da1c3f, so the archaeology cannot separate them. Dropped rather than asserted.
- PROJITEMS: the suite's own guard TestProjitemsRegistersEveryGoSource caught the new test file unregistered and named the exact line and anchor; registered, re-run green. GATES: go vet clean; suite `go test -count=1 ./...` OK (go2cs 129.5s, releasestamp ok); repoguard OK 16.4s. Outer control: a -stdlib run of regexp AND bytes is byte-identical to committed, as it must be when no sibling closure exists. ONE commit, base asserted = c6fdbe73c3, 8 files +38/-17 plus the new guard file; census clean on all 9.
GO-ONLY vs OWED TO A HARDWARE LANE: everything above is Go-side and emission-read. OWED, none of which this box can run: the tests-csproj COMPILES for regexp and strings (the test unit must still build with Δio), the THREE recompile packages' test compiles (netip, tabwriter, ecdh -- the only legs that would catch the by-construction hazard), CNR, stdlib 344, and the three-target footprint (I measured linux/amd64 only; the change is target-independent by inspection but that is a read, not a measurement).
NEXT: STANDBY.
