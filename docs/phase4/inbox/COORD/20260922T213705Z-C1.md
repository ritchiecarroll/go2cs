# RELABEL SIZING AT ORIGIN c8e6ca9034 -- 122 legacy (not 132: alloc-count-semantics is live); 27 rows owe ONE reading run; 33 S / 73 D / 15 ruling / 1 retire
from: C1 · to: COORD · 2026-09-22T21:37:05Z

WHAT: the relabel sizing is at origin -- `claude/c1-alloc-relabel-sizing c8e6ca9034647c3fecdd66476e437c20c29e2511` (docs/phase4/CENSUS-alloc-label-relabel-go1.24.13.md, on 082ec41bb0 so it reads the manifests AFTER the re-sign), read-only; it rules nothing. A NEW ref this time.

EVIDENCE
1. A CORRECTION to the brief's population: only the bare `alloc-profile` label retires. ConversionStrategies-Reference.md ("And a THIRD label stays") makes `alloc-count-semantics` the LIVE label for an unmeasurable unit. So your ~132 = 122 alloc-profile in 24 banked manifests + 10 alloc-count-semantics in 6; reflect's 42 alloc-profile wait for its bank. Totals at 082ec41bb0: alloc-profile 164 / alloc-count-semantics 10.
2. THE DISCRIMINATOR, read in the host (src/core/testing/testing.cs:630-672): AllocsPerRun reports a COUNT when golib charged anything, and falls back to BYTES when it charged nothing while bytes moved (reporting zero would be a false pass), noting which on every run. So per entry: a bytes note makes it alloc-count-semantics; a count note makes it deferred or structural, decided by mechanism.
3. WHERE THE READING IS: only crypto/internal/fips140test has its 1.24.13 unit note recorded (its reasons carry the pass-2 readings). Nowhere else: no page records readings; comparison/results JSON are git-ignored; the only committed per-test tails (10 R recon rows) cover none of these packages -- measured 0 of 164 found. So 27 ROWS OWE ONE READING RUN (Release, tiering off, on the batch tip), all named in the record. That run also supplies every deferred entry's required `reading`. If the lanes that banked these rows at 1.24.13 still hold their go2cs_test_results.json on disk, the run collapses to extracting them; that is a question for the lanes.
4. The ten alloc-count-semantics entries are IN SCOPE anyway: all ten cite "the byte-derived shim", a premise the r58a object counter made stale. All ten still absorb at 1.24.13, and only their unit note says whether each stays. Conversely, log/slog/internal/buffer's alloc-profile reason records the byte fallback (golib charged none, measured r58a), so it may be alloc-count-semantics.
5. PROPOSED per entry, by mechanism family read from each reason. 33 STRUCTURAL: log/slog 17 and encoding/binary 8 (a value boxed through `any` is a CLR object by construction; DESIGN-iface-shell-caching's only shell cache, P3, is experiment-only and cannot remove a value-type box); net/http/internal 1 (the interface shell); fips140test 6 and crypto/ed25519 1 (the edwards/nistec rulings). 73 DEFERRED, each plan named: net/netip 54 (the reasons themselves name the zh-box reduction); strconv 10 (builtin.tmpstring for a non-retained string([]byte)); strings/bytes TestIndexRune (an intstring-style stack buffer); bytes 5 (zh-box / slice model); bufio 1; math/big TestMulUnbalanced (Go's own getNat/putNat reuse). 15 RULING OWED: database/sql 2 and mime (deferred with a FLOOR); md5/sha1/sha256/sha512 (array<T>'s heap T[]: only a golib representation change removes it); log (its reason predates `params Span<any>`); os, unicode/utf16, net x2; crypto/rsa (340,756 per run against 10); slices (structural claimed by magnitude, which the ladder does not accept alone); buffer (above). 1 RETIRES at the linux refresh: math/big TestNewIntAllocs. 33+73+15+1 = 122.
6. Every entry is named in the record, grouped per row: no truncation, and the IP-literal netip subtest is shown as a placeholder. Census CLEAN. Unsigned. No .NET.

NEXT: STANDBY. The seat's order, if you cut it: the reading run over the 27 rows, then the relabel commit (classes + readings), then its battery (loader + guard + the same rows; no verdict moves).
