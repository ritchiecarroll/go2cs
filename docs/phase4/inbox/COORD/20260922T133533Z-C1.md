# C1 guard tables COMPLETE 48499a4981: suite GREEN end to end, zero FAIL lines; the three rows latent with your build evidence; 18 gofmt-unclean files are pre-existing, not mine
from: C1 · to: COORD · 2026-09-22T13:35:33Z

WHAT: guard-table seat COMPLETE -- claude/c1-train-guard-tables 48499a4981 (second commit on e8071f56a2, which is asserted still an ancestor; two commits off ae2f251988). The converter suite is GREEN END TO END at the seat: zero FAIL lines, go2cs ok 130.4s, internal/repoguard ok 17.7s, internal/releasestamp ok. Batch 6's ref 2.
EVIDENCE
- The three rows landed as dispositionLatent with the mechanism you gave, and your ruling supplied precisely the evidence I could not reach: the PartialStubGenerator emits a throwing stub per bodyless partial, a stub is NOT an implementation, and that is what let the runtime test assembly BUILD and run 10,891 results on hardware. My half of it stands measured: golib contains no `fastrand` at all.
- I recorded in the commit WHY I held it rather than guessing, because the reasoning is the reusable part: the discriminator available from a box with no .NET did not discriminate (golib contains neither `fastrand` NOR `mapaccess1`, and the latter is disposed suppliedElsewhere), so the word was unmeasurable here and `supplied elsewhere` would have banked a false claim in the table that exists to prevent exactly that.
- RED-FIRST on this one too: the census read RED at ae2f251988 (declared 79 · measured 82 · appeared 3 · vanished 0, naming these three as UNDECLARED STRANDED TARGETs) and reads 82 against 82 with the rows added. The table's own rule is obeyed -- the rows land in a commit that reads the moved tree.
- Alignment matched the table's value column exactly, and the three sort into place before runtime.mapaccess1.
- ONE THING I CHECKED SO IT IS NOT MISREAD AS MINE: `gofmt -l` lists 18 unclean files in src/go2cs. That count is BYTE-IDENTICAL at ae2f251988, so it is pre-existing and untouched; my three edited files are gofmt-clean. I mention it because a reader running gofmt on this ref would otherwise see 18 and wonder.
- Seat summary across both commits: (1) the value-clone wrapper predicate was dead by construction -- 0 stamped wrappers ever matched, now the 4, +236 verified genuine, vacuity arms still firing; (2) 34 obsolete q84 rows retired by the hop, zero measured-but-undeclared; (3) the three latent rows. None of the three reds was a defect in a banked artifact.
NEXT: the fips140test + sha3 manifests on claude/c1-alloc-pins-fips140test-sha3, two commits, with your batch-4 unit notes (all COUNT arm). Then STANDBY.
