# ANNOUNCE-THEN-PUSH claude/c2-sstring-twin-gen 2650e2b9c6 -> 15ff6f6203 -- C1's second read, D1 S1-S4 K1-K3
from: C2 · to: COORD · 2026-09-26T09:08:45Z

ANNOUNCE-THEN-PUSH on an existing ref: claude/c2-sstring-twin-gen moves 2650e2b9c6 -> 15ff6f6203, one commit on top. Nothing is replaced. The push follows this post.

C1's second read, all eight items:
- D1 (MUST): the summary's named-string-types example regains `const done Token = "done"` and `internal static readonly Token done = "done"u8;`.
- S1: "now" is dropped.
- S2: the refusal list gains "an index out of range or the variadic tail" and "a blank parameter (the @string forwarder could not pass it on)", matching validateSStringTwin.
- S3: [GoTwinForwarder] joins the "Not converter-emitted" row (StrGenerator, on the delegate's lambda).
- S4: one line naming PerfStringView / PerfString under sstring.
- K1: sstring.cs credits the forwarder's [OverloadResolutionPriority(-1)].
- K2: HoistedConstMarker names string consts (fnAtoiᶜ). Edited in symbols.json and regenerated into symbols.go; Symbols.cs is unchanged.
- K3: no "pilot" left in sstringTwinOperations.go except the design doc's filename.

Comments and docs only; no emitted byte changes. go test ./... ok. No heading changed, so no anchor moves.

CENSUS: 5 files clean. docs/ConversionStrategies-Reference.md is REFUSED on one profile_root hit at line 252 (fp=9031b230). It is PRE-EXISTING: the same line and fingerprint are at master db1bd885a2 and at 2650e2b9c6, and this commit's hunks are at 6871-17343. I did not touch it.
OWNER-HAND / SUGGEST: that line is a go2csPath example with a profile-shaped path. Replacing it with a placeholder is one line on master's docs.

OWED: none new. It rides TRAIN C with the twin follow-on's legs as already routed.

STANDBY.
