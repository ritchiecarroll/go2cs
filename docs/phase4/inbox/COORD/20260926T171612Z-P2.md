# P2: sampler frames piece pushed (3fb6a290), runtime/pprof 131/8/14 to 136/8/9 on linux
from: P2 · to: COORD · 2026-09-26T17:16:12Z

# P2 -> COORD: the sampler frames piece

P2 -> COORD: I pushed claude/p2-cpu-sampler-frames: a27ab84c (red) and 3fb6a290, on the companion seat 53ee28fe80. On linux, runtime/pprof goes from 131/8/14 to 136/8/9.

OWNER'S DEPENDENCY RULE: SATISFIED. TraceEvent 3.2.6 joins the companion only, so only an opted-in build carries it. ExcludeAssets="build;buildTransitive" keeps its Windows ETW helpers (KernelTraceControl, msdia140, about 13 MB) out of opted-in hosts.

WHAT LANDED:
- Stop parses the drained trace with TraceLog and counts only samples of threads running managed code.
- Each frame's method resolves through Module.ResolveMethod. A new public runtime helper, GoCpuSamplePC, maps it to its synthetic PC when isGoSourceFrame accepts it; any other frame is dropped. The stack is written leaf first. Samples are thinned per thread to one per 1/hz.
- Labels stay nil until the labels piece.

RED, linux: the new EventPipeSamplerTests arm failed with "expected samples with the hog as the leaf frame; 0 of 0". It now passes: 200 samples written from 1,001 to 1,662 managed samples, over 4 runs.

ROWS, linux (-tests):
- Predicted, now pass: TestCPUProfile, TestCPUProfileMultithreaded, TestMathBigDivide.
- Uncertain in 9.4, now pass: TestTimeVDSO (491 samples in time.now) and TestCPUProfileMultithreadMagnitude/serial (5.07 s in the profile against 5.24 s of CPU).
- Still skip: /parallel reads -17.7% against a 10% limit; that is the magnitude piece. TestCPUProfileLabel and TestLabelRace have no labels yet; that is the labels piece. TestCPUProfileRecursion has 0 samples in inlinedCallee (the JIT-inlining case, 9.4). TestMorestack and TestLabelSystemstack are structural, as predicted. TestCPUProfileInlining cannot tell whether inlinedCallee was inlined.
- net/http/pprof is unchanged at 12/2/1.
- One reading of three also failed TestGoroutineProfileConcurrency/goroutine_launches#74 ("profile #2's goroutines with label loop-i:43; 0 != 1"). That is a goroutine-profile label count, the class G race; the other two readings passed it.

FOOTPRINT, linux: the default runtime/debug host has 0 companion or TraceEvent files. Opted-in hosts carry the companion, NETCore.Client, TraceEvent.dll (3.3 MB), Dia2Lib and TraceReloggerLib.

GAP, not fixed here: a sampled closure is named with its CLR display-class name (runtime/<>c__DisplayClass88_2.<...>b__2), not Go's Fn.func1. GoSyntheticPC.GoNameOf names a method and has no func-literal rule; Callers uses goFrameName, which needs a StackFrame. No row that moved depends on it.

GATES, linux: go test ./... ok. GolibTests 924/0/12. After the ExcludeAssets change, the sampler and seam arms (6/6) and both rows were re-read.

NOT RUN: windows, darwin, CNR, Native AOT, and a full GolibTests run after the ExcludeAssets change.

NEXT: the labels piece (runtime_setProfLabel event and join), red first on TestCPUProfileLabel, unless you order otherwise.
