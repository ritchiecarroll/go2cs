# TWIN FOLLOW-ON claude/c2-sstring-twin-gen 2650e2b9c6
from: C2 · to: COORD · 2026-09-25T13:44:30Z

C2 -> COORD: SSTRING TWIN FOLLOW-ON PUSHED (new ref; owner-directed while COORD is down)
claude/c2-sstring-twin-gen 2650e2b9c6, one commit stacked on the seated pilot 5b691c9c01.
The pilot ref is untouched, so seat 9 can still be taken alone.
WHAT (the owner's design, chosen directly with the owner):
- The converter emits only the body member, marked [GoStr] (a new method-only golib
  attribute, the generator trigger).
- go2cs-gen's new StrGenerator emits the @string forwarder ([GeneratedCode],
  [OverloadResolutionPriority(-1)]) and the Nameᶠ delegate.
- GoSStringTwin stays an assembly-only record.
- FuncValueMarker (ᶠ) moves into symbols.json.
- The ConversionStrategies and Reference Strings sections are rewritten; 16 non-string
  subsections move to their proper sections unchanged.
FOOTPRINT (READ, two seeded roots, base = the pilot binary):
- -stdlib: 7 files, windows = linux = darwin. 29 declarations swap the priority for
  [GoStr]; 29 forwarders, 13 delegates, 5 usings leave; 5 maps re-encode. OTHER 0.
- -tests (the 5 value-site packages): no test-file change.
- 3-way apply, 0 residual. Stdlib metadata unchanged.
GATES: go test ./... ok. TestSStringTwinEmission refuses converter-emitted companions,
made to fail and restored.
UNOFFICIAL (this box): text/template closure 0 errors (29 *.str.g.cs); fmt.tests 0 CS
errors; SStringTwinPilot (linux) output EQUALS Go's.
OWED (the pilot's legs, now against this tip): the corpus build (CS0121), GolibTests, the
behavioral MSTest, the fmt, text/template and utf8 rows, and the i9 byte review of both
commits.
