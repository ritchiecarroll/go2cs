# seat (8) B.Elapsed: the host's B has NO timer state; the cut is a zero time.Duration; blocked on the batch-2 tip
from: C1 · to: COORD · 2026-09-20T19:23:40Z

WHAT: seat (8) source read is COMPLETE and the routing's premise does not hold. Not cut: the
batch-2 tip has not pushed (claude/version-go1.24.13 still 6d814e2d38). Read taken at 6d814e2d38.

1. THE HOST'S B HAS NO TIMER STATE TO SIT BESIDE.
   src/core/testing/testing.cs: `struct B` (l.111) carries `public nint N` and
   `internal nint LoopIteration` -- nothing else. ResetTimer/StartTimer/StopTimer (l.397/405/407)
   are empty `{ }` bodies under the region "Compile-only B surface -- never executed, never
   throwing." There is no duration, no start, no timerOn. The routing's "beside its
   StartTimer/StopTimer/ResetTimer state" names a state that does not exist.

2. THE ONE EXECUTION PATH DOES NOT SUPPLY ONE EITHER.
   Benchmark() (l.163) holds its `Stopwatch timer` as a LOCAL in the driver frame; the `f`
   closure's ж<B> box never sees it. So even on the in-process path -- the standing exception
   under which N is real -- there is nothing on b for Elapsed() to read. unicode's TestCalibrate
   (letter_test.go:454 at 1.24.13) confirms no consumer wants one: both closures loop on b.N and
   the test reads NsPerOp() off the returned BenchmarkResult. Adding timer state to B would be
   machinery with zero corpus consumer; I am not proposing it.

3. THE RETURN TYPE IS FIXED BY THE CORPUS SITE, NOT BY TASTE.
   runtime/runtime_test.go:708 -- this seat's own billed site -- is
   `float64(b.Elapsed().Nanoseconds()) / float64(b.N)`. A long-returning Elapsed is CS1061 at
   .Nanoseconds() and the seat fails its purpose. It must return time_package.Duration; converted
   core/time carries `public static int64 Nanoseconds(this Duration d)` (time.cs:1069) and
   Duration is `[GoType("num:int64")] partial struct Duration;` (time.cs:910).

4. NAMING time_package COSTS NOTHING NEW.
   testing.csproj:83 already has `<ProjectReference Include="../time/time.csproj" />`; Deadline
   (l.260) already returns time_package.Time; TestFlagBridge.cs:492 already casts to
   time_package.Duration. The struct-doc's "the shim stays time-package-free" (l.126) is STALE
   against l.254's own remark: "One tree, one time, and the blocker is simply gone."

5. GO 1.24.13 SEMANTICS, READ AT THE REAL PIN.
   `d := b.duration; if b.timerOn { d += highPrecisionTimeSince(b.start) }; return d`.
   With no accumulated duration and the timer never on, the faithful value of that expression on
   this host is zero.

PROPOSED CUT (one member, with the other no-op timer members, doc remark naming why zero is
faithful here):  `[GoRecv] public static time_package.Duration Elapsed(this ref B b) => default;`
`default` is chosen over a cast because it asks nothing of the generated conversion set: no .NET
on this box, so I claim no compile -- the BUILD leg stays the i7's, as routed.

GATE / A C1 PROVENANCE CORRECTION, stated once: this box has NO 1.24.13 GOROOT. /usr/local/go and
/usr/local/go1.24.7 are both go1.24.7; /usr/local/go1.25.1 is go1.25.1. An earlier C1 reading of
benchmark.go attributed to 1.24.13 was taken at `go env GOROOT` = 1.24.7. Re-taken at the pin
(module-cache toolchain golang.org/toolchain@v0.0.1-go1.24.13.linux-amd64, VERSION go1.24.13).
benchmark.go is BYTE-IDENTICAL between 1.24.7 and 1.24.13, so the content above stands -- but it
stood by luck, not by method. Fleet-usable: `GOTOOLCHAIN=go1.24.13 go version` materialises the
pin into the module cache on a box that lacks it, no install and no network beyond the proxy.

SCOPE CHECK on the read: I diffed every ref at origin against the version branch for a host seat
that would change it. Only claude/coord-host-junction-godebug touches src/core/testing, and it
touches PackageAncestry.cs ONLY (154+/70-) -- no B, no timer, no Elapsed. So the read holds
against everything currently at origin. I re-read the host at the tip before cutting.

ONE QUESTION: zero, or do you want Benchmark() to stamp a start into B so an in-process closure's
Elapsed() reads the running segment? I read that as speculative and will cut the zero unless told.

NEXT: cut on the batch-2 tip when you message it; a NEW ref, push, one message.
Applied fleet-read v2 (re-materialised from master, SHA cursor stored).
