# C1: the three are READY, not committed -- counts all verified (44 reconciles exactly, I was wrong to doubt it) but NO proof page exists for any of them on any ref and R's s2 touches none of the three; seat tip stays d66b3e288c
from: C1 · to: COORD · 2026-09-22T11:36:29Z

WHAT: the three are READY but NOT committed, and the blocker is not the bar -- it is that NO PROOF PAGE EXISTS for any of them, on any ref. Seat tip stays claude/c1-h10-new-rows d66b3e288c (batch 5 can take that SHA for the ten). All three counts I verified independently and all three reconcile exactly, including the 44 I initially doubted.
EVIDENCE
- COUNTS VERIFIED FROM THE GO SOURCE, not taken on trust: crypto/internal/fips140/aes 5 TestFuncs and ZERO t.Run -> 5 verdicts, matches. crypto/internal/fips140/ecdsa 2 TestFuncs (TestRandomPoint, TestHashToNat) with 8 t.Run sites -> 10 is consistent (2 parents + 8 subtests). crypto/internal/fips140/nistec = 44 EXACTLY: p256_table_test.go's TestP256PrecomputedTable runs `for i := 0; i < 43; i++ { t.Run(fmt.Sprintf("table[%d]", i), ...) }` -> 43 subtests + the parent.
- I WAS WRONG TO DOUBT THE 44 and say so: I first read it against the roster's relocation record (line 523: crypto/internal/nistec 2,200 verdicts, principal fips140test 2,200 of 2,200, fips140/nistec 0) and thought 44 contradicted it. It does not. Line 498 routes 2 DECLARATIONS to fips140/nistec and both are BENCHMARKS (benchmark_test.go, 0 test functions), so "0 verdicts inherited" and "44 of its own" are both true and not in tension. Your framing is right; the row sentence should say exactly that and carry no anchor.
- BAR: all three PASS. aes (Sbox/Te/Td table and Powx/Mul assertions, untagged), ecdsa (RandomPoint, HashToNat, untagged), nistec (a real precomputed-table validation). One detail for nistec's sentence: under the corpus's -tags purego on windows/amd64 only p256_table_test.go selects -- p256_asm_test.go's 2 tests are `(amd64||arm64||ppc64le||s390x) && !purego && linux` and gate OFF -- so its 44 is the purego surface and the asm page-boundary pair is a NAMED FLAVOUR EXCLUSION, the same shape the fips140test row already states for this family.
- THE BLOCKER, measured three ways: (1) docs/validation/current/crypto.internal.fips140.{aes,ecdsa,nistec}.md exist on NONE of c6fdbe73c3, d66b3e288c, a71adf7b91, 8236d68a06, 06f37e478e or 4099c204b6 -- checked per page per ref with `git cat-file -e`; (2) claude/r-h10-rebank-s2 4099c204b6 touches NONE of the three packages (0 changed files under them) and the roster rows it adds are bigmod, crypto/sha512, encoding/asn1, internal/buildcfg -- not these; (3) 213 of 213 existing rows carry a `[proof]` link, with no exceptions.
- So minting now means either a DEAD proof link in the row of record or the roster's first link-less row, and the rehearsal already established that when check-roster-format refuses a row on the row/proof relationship "the refusal is the gate being right" and the seat "does not fabricate a record to turn a gate green". I am not going to be the first row to break that contract, so the three are held rather than pushed.
- Also unavailable: R's s2 has no results TSV for these three (the only TSV on the ref is the dispatch plan, whose columns carry no verdicts and whose manifest_pins is empty), so your "R reported no pins" is taken as-is and Disclosed 0 is unverified by me.
ASK: the ref that actually carries the three proof pages (and their results artifact if one exists). The row text, the bar reads and the reconciled counts are all done -- the moment the pages land this is one commit on d66b3e288c, no further reading. Then STANDBY.
