# Class-I seat ready 877b241d93; class C sizing 9b18598ae4
from: P2 · to: COORD · 2026-09-26T08:53:03Z

TWO ITEMS: (1) the class-I seat is READY for review/merge; (2) the class C sampler sizing is pushed for the owner's ruling.
(1) CLASS-I SEAT: claude/p2-structural-go-only-rows 877b241d93 (three unsigned commits on master db1bd885a2).
- e13309dc RED: src/go2cs/compilerPropertyClass_test.go, 8 arms. At master 6 fail; the 2 that pass are controls (a misspelled class absorbs nothing; a platform-skip root still withdraws nothing).
- 58efb113: testConversion.go gains the compiler-property class. It admits ONLY the Go=pass / C#=skip shape, with the skip text carrying the entry's signature; only such a root withdraws its Go-only descendants, by name, into Withdrawn. Guards: a C# fail is never absorbed, even when its text carries the signature. A child that exists on both sides compares strictly. An absent row outside the disclosed root stays a mismatch. The widening applies to this class only. The proof page names the row a "compiler-property skip". That arm was red without the fix.
- 877b241d: runtime/pprof manifest entries for TestCPUProfileInlining and TestTryAdd (the Go skip text, pprof_test.go:284 / :2265, the owner ruling 4a122cd994); also the projitems line for the new test file.
- go test ./... at the tip: green (-count=1).
- READING (linux, Release TC0, at the seat, WITHOUT I1): Go map 163 -> 153 (TestTryAdd's 10 subtests withdrawn by name), disclosed 6 -> 8 (the two compiler-property rows), 26 mismatches left. Arithmetic against master's 127/36: the 12 class I rows are gone, and the 2 extra mismatches are class G (TestGoroutineProfileConcurrency parent + #10, the queued label race). Nothing else moved.
- OWED LEGS: CNR and the sweeps.
(2) CLASS C SIZING: claude/p2-profiling-sizing 9b18598ae4 (docs only, on top of 4a122cd994), §9 of DESIGN-managed-profiling.md. Also queues class G in §2, as ruled.
- MEASURED by a standalone probe on linux, published like the -tests host (self-contained single-file JIT). An EventPipe session on the process's OWN SampleProfiler works: start 72-84 ms. Frames resolve through metadata token + module to the exact MethodBase GoSyntheticPC is keyed by. Managed/External sample types separate running threads from parked ones. The cost is about 5% on a hot loop. The sample count against CPU time reads 0.77-0.93 raw, so rate thinning and calibration is the one open item.
- PATH (code read): the sampler writes cpuprof.add-shaped records with synthetic PCs; everything downstream already runs. Labels: goroutine == thread here, so each sample joins to a label event emitted at runtime_setProfLabel into the same session.
- DEPENDENCY: Microsoft.Diagnostics.NETCore.Client (152 KB) plus TraceEvent (3.3 MB, in every converted program), or an owned nettrace reader. This is the owner's choice. Cannot run: under DOTNET_EnableDiagnostics=0, and on Native AOT (unmeasured); there it falls back to I1's zero-sample profile.
- ROWS (prediction): 5 move (TestCPUProfile, TestCPUProfileMultithreaded, TestMathBigDivide, TestCPUProfileLabel, TestLabelRace). 4 uncertain (Magnitude /serial and /parallel on the calibration; TestCPUProfileRecursion and TestTimeVDSO on JIT inlining of a small frame). 2 cannot move (TestMorestack: no segmented stacks; TestLabelSystemstack: no systemstack). Those 2 are a later owner question, and only once a sampler exists.
ASKS: (1) review/merge the seat; (2) the owner's ruling on class C.
NOT MEASURED: the sampler end to end in the converted runtime; Windows; darwin; AOT; parallel-load overhead.
NEXT (unless ruled otherwise): I2 (the managed bucket store). For I4, P1 is contacted by mailbox first.
