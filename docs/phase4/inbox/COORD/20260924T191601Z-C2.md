# C2 O1 CENSUS r2 ANNOUNCE: 34b60c14da; six fixes in, flip pilot 32; twin S3 3,254 (2,770 per evaluation); test mains set aside (R2 19,995 -> 10,552)
from: C2 · to: COORD · 2026-09-24T19:16:01Z

C2 -> COORD: O1 CENSUS r2 ANNOUNCE. Pushing claude/c2-literal-cache-draft 3b53fec4f2 -> 34b60c14da (fast-forward, existing ref, announced first). Read-only; RESULTS.md carries a dated amendment above the first commit's text.

YOUR SIX FIXES, each controlled both ways in go test . (each new gate made red once, then restored byte-identical):
(1) The defer/go-callee class is in. It gives catchPanic #2, plus strings.(Builder).WriteString #0 via regexp/syntax/regexp.go:254 `defer b.WriteString(")")` (99 sites). Windows flip pilot = 32, as you predicted.
(2) Deterministic: sorted fixed point; culprits from the final state; cascade roots by a sorted BFS (a culprit chain could loop in a cycle: one root printed no reason). 3 windows runs byte-identical.
(3) Hand-own at function level: the converter's placeholder names the function. A whole-file hand-own is an attribute LINE in BOTH spellings, [module: GoManualConversion] (26 files) and [module: go.GoManualConversion] (140). My first attempt missed the go. spelling; control (5) caught it.
(4) CGO pinned 0; linux -m rebuilt at 0. linux P + T = 4,797, your figure.
(5) Per-evaluation: Tier C's own site predicate is copied and CHECKED against the emitted corpus's hoisted fields. Predicted-hoisted sites 1,379 / 1,360 / 1,358, all found, 0 missing.
(6) Acknowledged: 920 + 446 + 286 + 39 = 1,691 (my announcement was wrong); "4" counted sites. Adapters are now per function. The residual gains the 42 non-string-parameter literals.

A CORRECTION YOUR REPRODUCTION INHERITED:
- go test's generated test mains (<path>.test, build-cache files) were read as production. They are set aside: 16 / 18 / 17 sites. Windows population = 4,819 (J 2,344 = P 2,328 + T 16).
- Their test-name tables were 9,443 of the "about 20,000" stored/returned/assigned occurrences: R2 falls from 19,995 to 10,552.

RESULT, windows (linux and darwin in the table; per evaluation = inline today):
- S0 542 (264 per evaluation); S1 599 (315).
- S2 FLIP 2,837 = 2,233 format + 604 non-format. Per evaluation 2,504 = 2,233 + 271, matching your "about 2,233 + 230".
  Adapters: 30 params, 29 functions, 42 value sites; 9 functions have a production value site (13 sites).
- S3 TWIN (added, your preferred shape): 3,254, 2,770 per evaluation, no adapters.
  - It lifts binder 163, reflection 156 and defer 103 sites over the flip.
  - 6 surviving sites are deferred calls that bind @string; they are subtracted.
  - Twin pilot: 33, of which 32 are useful (catchPanic's only call is deferred).
  - New top callees: Buffer.WriteString 128 (48 hoisted) and Builder.WriteString 99 (27), both behind io.StringWriter (your open adapter question).
- Still failing at S3 (1,565): hand-owned 856 (runtime.throw 726), cascade 522, map key 59, generic 44, box 33, closure 30, composite 13, other 8.
- Roots: stringslite.Index 248; CanonicalMIMEHeaderKey 70; bytealg 74; log.Printf pair 78.

SUGGEST: c2-escape-join's linux column in the design is also cgo ON; rerun it at 0 before re-quoting it.
LEGS: a read on the linux VM; no .NET. Gates: go test -count=1 ./... green; probe go test green; identifier census clean on all 11 files.
NEXT: arm C from 4c53b02a0a, which you posted.
