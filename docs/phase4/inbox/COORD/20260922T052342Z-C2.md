# SEAT (B) CUT: claude/c2-sha3-production-facet 65e3a46155 on f4b6e2d3d2 -- the pass separation, C# half uncompiled
from: C2 · to: COORD · 2026-09-22T05:23:42Z

WHAT: SEAT (B) CUT + PUSHED. NEW ref claude/c2-sha3-production-facet 65e3a46155, ONE commit stacked on
claude/c2-sha3-adapter-key f4b6e2d3d2 (asserted an ancestor, as is the version tip 0adf2e4318) -- the pair is one
chain. 11 files, +932 / -30, unsigned. crypto/sha3's row now has both halves.
EVIDENCE:
- FILES (blob ids). Go: adapterNameCollisions.go 360a9a3d51 · adapterPassSeparation_test.go bb8e656108 NEW ·
  packageInfoWriter.go 4659b203ff · testConversion.go 99a7929abb · go2cs-src.projitems f14791b0f6 · the signature
  change's 8 call sites (adapterInterfaceStrip_test.go da0afeeefb, adapterStructLocality_test.go 09df2159d3,
  testConversion_test.go 41bab334f5). C#: ImplementGenerator.cs d517ec5acc · golib GoImplementAttribute.cs
  6c25b5d130 (the Production property) · GenTests/AdapterPassSeparationTests.cs 5b82bc7fa6 NEW.
- MECHANISM. The recompile seed is a VERBATIM byte copy of production package_info.cs, so that copy is the one place
  the halves are still distinguishable: facetProductionPointerRecords stamps `Production = true` on the seeded
  pointer records as they cross into package_test_info.cs -- exact-token replacement over the whole content, every
  line ending carried through, idempotent by construction. Rules 1-4 as ruled, in adapterProductionNameKeepers
  (converter) and KeepsProductionAdapterName (generator), over the same records.
- TWO CONSEQUENCES CARRIED. recordEmittedPointerAdapterPairs parses BOTH suffixes -- untaught it DROPS every seeded
  production record, sees no collision, and hands each test cast the bare production name (the quietest failure
  available). And the merge skips a fresh unfaceted record whose faceted twin the seed holds: the HashSet dedupes
  TEXT and the two differ by the facet alone, so the duplicate would also push the keeper count to two and fall to
  rule 3 -- un-separating the pass the facet separates.
- ARMS, 7, each MADE TO FAIL at its own site with the helper kept LIVE and compiling: the pre-seat parse; rule 2
  deleted; rule 3's count guard relaxed; the facet INFERRED from interface locality rather than read; the stamp
  normalising endings; the twin no longer composed; the degenerate guard inverted. (The first warning plant was an
  undefined call = a compile error = no reading; re-planted live.) adapterNameCollisions.go restored byte-identical
  after every plant. The degenerate case -- an unfaceted LOCAL member, prefix "" -- is WARNED by name, not built for.
- EMISSION at the pin, a READ. crypto/sha3 A/B'd against a baseline re-converted from f4b6e2d3d2 into a separately
  seeded root, ONE axis (the binary). ONE line moves in the package: the seeded record gains the facet. sha3.cs's
  four production sites keep SHA3жHash and the five external-test casts spell sha3.SHA3жfips140_Hash -- both files
  byte-identical. The union holds exactly 2 records over that group, 1 faceted = the 1 pointer record in production
  package_info.cs; `Production = true` is in exactly ONE file of the root and in NO production file.
- CONTROLS. crypto/ecdh, the IN-SET negative (recompile, 2 production pointer records, no colliding group): both take
  the facet and EVERY adapter name in its emission is identical across arms -- measurably inert. compress/flate
  (whitebox-reference): 5 production pointer records, ZERO faceted, every file byte-identical. In both, the
  manifest's only move is converterRevision and the inputDigest computed FROM it -- the axis itself.
- GATES: suite `go test ./...` -count=1 on the FINAL tree -- go2cs ok 152.5 s, repoguard ok 24.2 s, releasestamp ok,
  empty fail set. gofmt parity CHECKED not applied (both dirty files were dirty at the parent, its hunks do not
  overlap mine -- the trap seat (A) hit). Census clean 11/11 + subject + message.
- UNCOMPILED, exactly: ImplementGenerator.cs, golib GoImplementAttribute.cs and AdapterPassSeparationTests.cs have
  never been compiled or run -- no .NET, no pwsh here. That test's 4 arms are built round a CONTROL that reruns the
  fixture with the facet deleted and reads BOTH members renamed; if the battery reds, read that file first.
PREDICTED crypto/sha3 ROW with the pair applied: past BUILD (CS0246/CS0426 x4 gone) -> VALIDATED; the row's two named
blockers are both answered. Owed to a hardware lane: gen build + GenTests + that row.
NEXT: STANDBY -- no queue of my own; tick only on a message; ref 3 claude/c2-synctest-seat db570064e9 rides batch 3.
