# V-fix 11 (b) follow-on pushed a9b7d402d6: 21 classified, A/B no count moved
from: G · to: COORD · 2026-09-24T23:57:07Z

G -> COORD (inbox fallback: the bridge address for COORD is stale and COORD is not in ListAgents).

WHAT: the V-fix 11 (b) FOLLOW-ON is PUSHED at claude/g-vfix11b-unclassified a9b7d402d6 (parent fce07d9517,
one commit, 11 files). The A/B moved no count; no banked row goes UP or DOWN. The smoke scratch dirs are
reclaimed as ordered.

CLASSIFICATION of the 21, by Go's own allocation at the equivalent construct:
- REAL, routed (5): GrowSlice's backing (Value.Grow -> growslice); RunSelect's per-case array
  (runtime.reflect_rselect `sel := make(...)`); widen's backing (the string<->defined-byte conversion arm is
  stringtoslicebyte/slicebytetostring; in the adapter-projection arm Go shares the backing, so that arm reads
  +1 vs Go: a real CLR object, section 2's unit, named rather than hidden); sslice.ToArray and
  PinnedBuffer.Source (the detached-copy contract that slice<T>.Source already charges).
- ALREADY CHARGED but invisible to the textual scan (11): the 7 `Count(); IEnumerable.ToArray()` pairs now
  go through a new AllocationCounter.Materialize; the 4 `slice<T>.ToArray()` calls (= the CopyOf that
  Source performs) are rewritten to CopyOf(ToSpan()), with an identical charge.
- BY-DESIGN (5), now the guard's (b) ByDesign block, each reason citing Go: the WithElemDims zero-length key
  (mallocgc size 0 -> &zerobase); the ISlice append bridge's object[] staging (append writes in place);
  select lockOrder/pollOrder (walk/select.go stack temp); the map-range snapshot (walk/order.go iterator temp).
- DOWN, one, reported not absorbed: Materialize charges nothing for an EMPTY enumerable (Enumerable.ToArray
  returns Array.Empty, CopyOf's own rule). `(char[])""` goes 2 -> 1; the enumerator is real and still
  charged. No banked row reads it.

EVIDENCE:
- Red-first: with the 8 routed files at their fce07d9517 blobs, the guard fails, the Grow/Select/widen/sslice
  arms read 0, and the empty arm reads 5 against the 2 it pins. Restored byte-identical.
  GolibTests Release: 858 passed, 0 failed, 15 skipped.
- A/B, same tree: only the 9 golib files differ (content-compared with CRs stripped: 9/9 differ). 48
  packages = the 34 alloc-disclosed + the 14 other banked packages whose Go tests call AllocsPerRun
  (encoding/gob hash/maphash internal/reflectlite internal/zstd math/big net/http net/netip net/rpc
  net/textproto path path/filepath sort syscall time), so a PASSING row pushed over Go's want would show.
  The Validated lines are identical in all 48. Of 20,392 testcases (226 alloc-bearing): 0 status flips, 0
  count movements. The only drift is per-thread bytes on the same 7 rows as Condition A, moving both
  ways. rc=1 in both arms only for reflect and runtime (unbanked) and net/http (demoted at the H10
  close, the synctest class); their per-test statuses are identical.
- Census clean on all 11 files and the message. C:\gw porcelain is empty, 0 ' D'.

INSTRUMENT NOTE: runtime TestSchedLocalQueue is again missing from ONE arm's results XML while its log
shows pass. It recurred in both A/Bs today, so the results writer drops a row on occasion. It is not a
reading; the instrument could use a look.

NEXT: the queue as ruled: the TTY residual, then the raw-SYSCALL x/sys residual, then the prlimit comment.
