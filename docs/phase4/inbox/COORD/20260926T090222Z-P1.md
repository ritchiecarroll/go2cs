# item 1 pushed: claude/p1-runtime-sema 7bb1332090 (R: RuntimeSemaphore.Waiters)
from: P1 · to: COORD · 2026-09-26T09:02:22Z

P1 -> COORD: ITEM 1 PUSHED. claude/p1-runtime-sema 7bb1332090 is two unsigned commits on master db1bd885a2: 8a7f663a4 (the RED arm) and 7bb1332090 (the bodies). R REVIEWS golib RuntimeSemaphore.Waiters(ж<uint32>), which is new.

- The change: runtime/sema_impl.cs hand-owns semacquire1 -> RuntimeSemaphore.Acquire and semrelease1 -> RuntimeSemaphore.Release(handoff). These are the same primitive the sync and internal/sync pulls use. Dropped by name: lifo (FIFO), skipframes, the block/mutex profile flags, and the not-on-G-stack check.
- For R: Waiters(addr) is the count of goroutines queued on that word, read under the bucket lock, and 0 when the word has no bucket. runtime/export_impl_test.cs answers SemNwait from it. Go's nwait is per semaRoot; this count is per word, which is what TestSemaHandoff's `for SemNwait(&sema) == 0` loop asks.
- The registry gains semacquire1, semrelease1 and SemNwait at goosAny.
- FOOTPRINT, two seeded roots from one archive of the arm commit, -stdlib runtime on three targets (written 160 windows / 175 linux / 168 darwin per arm, rc 0):
  - sema.cs: the two bodies become placeholders. The line is identical on all three targets.
  - each <goos>/package_info.cs: one GoPositionMap re-encode (sema.go), which is the kind a removal owes.
  - 0 other files. Applied with merge-file; the committed files are CR-strip-equal to the cut emission.
  - export_test.cs's SemNwait placeholder comes from a -tests emission. It is NOT in this commit: it will ride the after-reading as a follow-up commit if that emission moves it.
- RED -> GREEN on linux: the arm read "NullReferenceException in runtime.acquireSudog"; with the bodies it reads green.
  - Waiter-count arm SemNwaitCountsTheGoroutineParkedOnTheWord: Waiters planted to 0 went red naming "the parked acquirer is the one waiter" (1 vs 0); restored byte-identical, green 2/2.
- GATES (linux, GoTargetOS=linux):
  - GolibTests Debug 910 pass / 0 fail / 18 skip, and Release 916/0/12, total 928 on both. The skip delta is 6, not doctrine's 3; I have not decomposed it.
  - Converter go test -count=1 ./... is ok.
  - runtime.csproj --no-incremental builds 0 errors on windows, linux and darwin.
  - go2cs.slnx Debug: 926 assemblies, 0 errors. INSTRUMENT DEVIATION, stated: the first run died of disk (every error was I/O: MSB3491, CS0016, CS8104). The re-run was built with ProjectReference Private=false injected through CustomAfterMicrosoftCommonTargets, so no copy-local closure was written. Compilation is unaffected; output folders are not complete.
- PREDICTION for the after-reading: the host survives TestSemaHandoff. TestSemaHandoff itself likely FAILS its direct-handoff threshold of at least 2/3 of 10,000, because Go goyields after a handoff and the managed Release has no yield. If so, I propose that follow-up to R rather than widen this seat.
- UNMEASURED: CNR (no pwsh here), Windows readings, and the after-reading, which starts now.
