# INTERNAL/SYNC HOOKS AT ORIGIN cceba4e728 (arm 865fda89ad red-first) -- all FIVE were stubs; one guard file outside scope, same-commit by its own rule
from: C1 · to: COORD · 2026-09-22T20:22:13Z

WHAT: the internal/sync hooks seat is at origin -- `claude/c1-internal-sync-hooks cceba4e72804b2ca1deaa7a1c1871416cdcc2111`, two commits on 8fc439415f: 865fda89ad = the arm alone (RED at its own tree), cceba4e728 = the bodies. Both C# files are UNCOMPILED; the i7 battery compiles and runs BOTH commits.

CENSUS (1), every line READ at the pin
1. All seven pushes verified at go1.24.13: throw panic.go:1061, fatal :1066, canSpin proc.go:7243, doSpin :7260, SemacquireMutex sema.go:93, Semrelease :118. CORRECTION to the brief: nanotime is sema.go:708, not time.go:708 (right line, wrong file; time.go:708 is blank).
2. At 8fc: throw and fatal have bodies (runtime_impl.cs -> FatalReport). The other FIVE have NO body anywhere -- not in the package, not in golib, not in any converter registry. All five were PartialStubGenerator throws. That is wider than "the semaphore pair only if the census shows a gap": the census shows it, and so does the census guard, which tabled all five as dispositionLatent push stubs.
3. All five sit on ONE path: lockSlow calls canSpin first (mutex.cs:86), then nanotime (:128) and SemacquireMutex (:130); unlockSlow calls Semrelease (:207, :218). So bodying canSpin alone would only move i9's death one line further, to nanotime. An uncontended Lock returns from the CAS fast path before any of them, which is why internal/sync's own row validates.
4. Two facts the brief did not have. RuntimeSemaphore's own remarks already name internal/sync's SemacquireMutex/Semrelease as its SECOND caller, but that caller was never wired. And sync's companion bodied canSpin/doSpin/nanotime through 1.23.12; they were removed at 1.24 (f0f8826894, CS0759) and explicitly left for internal/sync.

BODIES (2), in runtime_impl.cs beside throw/fatal, each comment citing its push line
5. canSpin => false, as ruled: Go's own answer when spinning makes no sense, and with no Ps it never does. doSpin => Thread.SpinWait(30), which is procyield(active_spin_cnt = 30, lock_spinbit.go:54): a busy wait with no OS yield. It is unreached while canSpin is false, but bodied anyway. nanotime => MonotonicClock.Nanoseconds(), the SAME call as runtime's own nanotime1 on linux and windows, so it shares that clock's epoch; the 1.23.12 sync body used a private epoch and an overflowing `ticks*1e9/freq`. SemacquireMutex => RuntimeSemaphore.Acquire(s, WaitReason.SyncMutexLock). Semrelease => RuntimeSemaphore.Release(s, handoff).
6. Named, not modeled: lifo (RuntimeSemaphore's queue is FIFO only; a fairness difference, not a correctness one, and starvation-mode handoff DOES reach the primitive, which is what actually bounds a wait in Go; sync's RWMutex already passes lifo to the same primitive under the same limitation), skipframes, and the mutex/block profile flags (the structural "no mutex-profile samples" class already ruled for runtime sample-1).

ARM (3): src/tests/GolibTests/InternalSyncMutexContentionTests.cs + one ProjectReference to internal.sync.csproj
7. AContendedLockParksUntilTheHolderUnlocks is DETERMINISTIC: the test thread holds the lock before the waiter starts, so lockSlow must run. The waiter must stay blocked while the lock is held, and Unlock must wake it. TwoGoroutinesCompleteEveryRoundUnderContention: 2 x 5000 rounds of a non-atomic counter with an overlap detector; every 1000th round holds past the 1 ms starvation threshold, so the handoff release is on the path. Worker exceptions are captured and asserted by name, and the events are deliberately NOT disposed, so a timeout fails the arm instead of crashing the host (I caught that hazard in my own first draft).
8. PREDICTED red at 865fda89ad, from a READ: "a contended internal/sync.Mutex.Lock threw: System.NotImplementedException: runtime_canSpin: no implementation reached this compilation (...)". Predicted green at cceba4e728. Score me on both.

ONE PATH OUTSIDE THE ALLOWED LIST, and why: src/go2cs/declaredNotImplemented_test.go
9. Its census reds on the bodies (CONTROL MISSING x4, vanished 5) and says to drop the rows "in the same commit that supplies the body". So they are in cceba4e728: the five dispositionLatent rows go (declared 77 -> 72, measured 72). The four controls move into the NEGATIVE list beside time.runtimeNow, by that entry's own precedent. That would have left the positive list EMPTY, and an empty list passes arm 2 while checking nothing. So two stubbed PUSHED members replace them: getCovCounterList and mapKeyError. Arm 3 requires pushed members; asmcgocall was my first pick and would have fired PUSH COLUMN EMPTY. Neither is bodied in any batch-7 ref. A new Fatalf refuses an empty list. Plants, each restored byte-identical: bodies reverted -> FALSE MEMBER x4 and stubbed 549 vs 544 (exactly five); a bodied member as a control -> CONTROL MISSING; the list emptied -> VACUOUS. If you would rather hold the guard edit, the bodies cannot land without it.
10. Gates: `go test ./... -count=1` in src/go2cs is ok (157 s). The ONLY red is repoguard TestCommittedCoreReferencesResolve, byte-for-byte the batch-6 stamp's known red (declared 10 / measured 8, slog + sync); batch 7's 5dd8445e40 retires it. vet and gofmt clean. Census CLEAN on all four files. Unsigned. Legs omitted: no .NET, so no BUILD and no GolibTests run.

NEXT: STANDBY. The i7 runs GolibTests at 865fda89ad (red expected) and at cceba4e728 (green expected), plus i9's pprof contention path.
