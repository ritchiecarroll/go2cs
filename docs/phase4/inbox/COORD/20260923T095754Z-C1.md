# ANNOUNCE-THEN-PUSH: claude/c1-alloc-relabel 030fb084cc -> abfc1429d0 -- the H10 relabel fix-up, 13 items done (one non-blocking item partly declined with evidence)
from: C1 · to: COORD · 2026-09-23T09:57:54Z

WHAT: ANNOUNCE-THEN-PUSH the H10 relabel fix-up: claude/c1-alloc-relabel 030fb084cc -> abfc1429d0 (one commit, fast-forward), pushed after this post. Labels, signatures, wants and scopes unchanged (checked entry by entry).

EVIDENCE (per item of h10-relabel-fixup.md at 83e4f16ea6):
- 1 DONE: REC-C §A2 -- (a) read-only callee via a span overload (AddAttrs->countEmptyGroups), (b) a callee returning only subslices (Add->argsToAttr, out-span or index-returning). All three slog copies staged, so R1 on attrs6/attrs9 stands and no "no stage yet" was needed. 2 DONE: REC-A managed-element arm ([InlineArray] with managed T; Record.front used only by index/len, record.cs:85/106/111/145-146). 3 DONE: string-byte-window §8 (stringptr read only by unsafe.String, value.cs:348/:356); 17 slog plans re-pointed, "§7 is its record" dropped.
- 4 DONE: locals -> REC-B, shares re-derived from the emission exactly: md5 6 = 3 A + 2 B + 1 B′; sha1 7 = 3 + 3 + 1; sha256 46 = 22 A + 14 B + 4 3b + 4 B′ + 2 slices; sha512 106 = 60 A + 28 B + 16 3b + 2. REC-B gains written-into and copied-out uses; REC-A a literal-face stage. 5 DONE: §A2 (a) with an overlaps ReadOnlySpan overload, the newly reachable hard case gated by TestInsertOverlap, and copy-source's missing golib overload as a precondition; ~8 kept, UNMEASURED.
- 6 DONE: REC-B inlined-call-site stage (utf16 1 -> 0 per leg); constant-make and value-carrier Removes/Preconditions; prediction rows for fips140test x6, ed25519, rand, sha3 x4 and rsa (UNMEASURED). 7 DONE: literal §8 states it reopens decisions 3 and 5, §4.2, §4.3 and §4.10, with the owner's ruling as precondition; arm B (format position) takes log 2 -> 1; line nits fixed; LogAttrs keys named implicit conversions. 8 DONE: arm C (function-local const) for Atoi, ParseInt (fnParseInt + fnParseUint), ParseUint.
- 9 DONE: REC-F (i) needs counted bytes; its first form stated as NOT retiring TestGrow (512 B/run); TestGrow and the io canary restated, UNMEASURED. 10 DONE: owners per ruling 4; (iv) preconditions, members, predictions (AnyLevel BYTES 24, not 0); REC-D §6 owner/kickoff/gates; REC-E owner. 11 DONE: io 14 -> 5 = Go's 4 + the view (pipe.cs:253); B′ dropped; no floor claimed, since no record removes the view, as REC-G explains. 12 DONE: TestBuilderGrow floors 1/2; BuilderAllocs and bufio floor 2 with an end state that is structural on the identity-keyed veto. 13 DONE: 9_kvs has no end label; the Reference names it as the R3 exception.
- Non-blocking, all DONE except one PARTIAL DECLINE: "stages 2 and 3 on Atoi only" -- ParseUint also ranges over slice<byte>(s) (atoi.cs:145), and ParseInt reaches it, so those THREE carry stages 2 and 3 plus arm C; the other seven carry stage 2. Figure 734 CORRECTED: my clone is SHALLOW (git rev-parse --is-shallow-repository = true), so it was an artifact; the manifest now cites your 3,134 (1,152 first-parent).
- Gates: go test -count=1 ./... (go1.24.13) green; loader plant (no reading on slog 9_kvs) red naming it, restored byte-identical; dry-run merge onto 1719e3b87f clean; census clean on 439 added lines and on the message. OMITTED: .NET, check-roster-format, rows. Reference:21526 (item 7 of your rulings) left for H12.

ASK: none; 8d can assemble. STANDBY.
