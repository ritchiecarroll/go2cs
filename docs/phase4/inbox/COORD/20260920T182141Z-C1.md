# the json publicization seeding seat is cut: claude/c1-publicize-internal-test-seed bf7e31d037 (7 files +416/-0, suite ok, two one-axis reds, the row's emission read before/after, population 7 packages/9 types)
from: C1 · to: COORD · 2026-09-20T18:21:41Z

**WHAT.** The json publicization seeding seat is cut and pushed: `claude/c1-publicize-internal-test-seed`
`bf7e31d037b30a429b6793951bb0fa55f35c9882`, off the version branch at `6d814e2d38`, 7 files +416/-0,
zero deletions, no mode change, census clean on every changed file and the message, unsigned.
Batch 2 has NOT landed on the version branch yet (it is still at `6d814e2d38`), so the merge-before-gates
step is not yet possible — the gates below are against the base alone.

**EVIDENCE.**

```
  THE ROW, an emission read on the real source at go1.24.13 (no dotnet here):
    before   [GoType] partial interface isZeroer {            encode.cs:917
    after    [GoType] public partial interface isZeroer {
    whole-package footprint: EXACTLY that modifier -- encode.cs 2 lines, package_info.cs 2
    (the metadata partial moves internal -> public group). Nothing else in the package moved.
  THE POPULATION, the number my sizing left open: 531 stdlib package dirs scanned, linux/amd64
    tags, syntactic -> 7 packages / 9 types. A small CLASS, not a one-row fix:
      crypto/internal/fips140/mlkem fieldElement · encoding/json isZeroer · internal/reflectlite
      structType · internal/runtime/maps table · os processStatus · runtime g mutex
      profBufReadMode · strconv decimal
  GATES: converter suite `go test ./...` ok, repoguard ok. TWO one-axis reds, both restored
    byte-identical: the seeding CALL removed -> the 2 SUBJECT arms fail, controls green; the
    scan's exportedness gates dropped -> the NARROWNESS control + the scan arm fail, subjects
    green. The projitems guard caught the new arm file first; registered, BOM kept, no CRLF.
```

**THE SHAPE**, one line, because it is not a new mechanism: the production conversion already scans
its in-package test half for reference spelling (`siblingTestFuncMethodNames`) and storage shape
(`siblingTestAddressedGlobalNames`); this adds ACCESSIBILITY to the SAME parse, with predicates that
mirror `collectPublicizedTypes` one for one and a resolution against the real production scope that
is the narrowness gate — a test-only type resolves to nothing, and an external `<pkg>_test` file
never reaches the scan at all.

**OWED TO THE i7 — every BUILD leg, since there is no dotnet on this box**: the `encoding/json` row
`-tests -test-action all` past BUILD; the other six packages of the class by BUILD; the two-seeded
corpus diff on this push. The population above is a syntactic census under one platform's tags, so
treat it as an upper bound on what those legs meet, not a prediction.

**NEXT.** Idle. Route the next seat, or tell me to re-gate this one once batch 2 lands on the version branch.
