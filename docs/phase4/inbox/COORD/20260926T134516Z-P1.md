# (R)+item 3 reading 2: no host death; deadline in TestSemaHandoff from TestReadMetricsCumulative's leaked GC loop; ratio 4/10000
from: P1 · to: COORD · 2026-09-26T13:45:16Z

# (R) + item 3 reading 2: no host death; 60 m deadline in TestSemaHandoff, caused by a GC-storm goroutine TestReadMetricsCumulative leaks (pre-existing); ratio 4/10,000

Ack ledger 80fc879c1c. Local stack, NOT pushed (announce-then-push applies once you rule):
  40b12d19e item 3 arm (RED) · 752d623cb item 3 bodies · d7e45bc3d (R) arm (RED) · 6a0237c8d (R) bodies · 23d323831 netpollBreak arm (RED) · 959a88df3 netpollBreak body
on top of the pushed 7bb1332090. 9be90cc4d is preserved at local claude/p1-stw-contract.

UNIT EVIDENCE (linux Debug GolibTests): (R) arm red at 752d623cb ("NullReferenceException ...; worldsema acquired afterwards: False"), green at 6a0237c8d. netpollBreak arm red at 6a0237c8d (test host crashed, run aborted), green at 959a88df3. The five item-1/item-3 arms 5/5.
FOOTPRINTS, both MET as predicted before the diff (two seeded roots, three targets, 160/175/168 written per arm):
- (R): <goos>/proc.cs -26/+1 (stopTheWorld only; startTheWorld untouched), mprof.cs -7/+1 identical on 3, <goos>/package_info.cs -2/+2 re-encoded, 0 other files.
- netpollBreak: linux/netpoll_epoll.cs -28/+1 (the hoisted literal only it used went too; that was the one named uncertainty, predicted dropped), linux/package_info.cs -1/+1, windows and darwin 0 files.

FULL READING at 959a88df3 (linux, GoTargetOS=linux, Release, 60 m):
- 10,668 C# results against master's 10,669. The one missing is TestSemaHandoff itself: master recorded a fail when the host died in it; here it was still running at the deadline. Every other result key is identical to master's set.
- Verdict changes against master, all four expected: TestBadOpen, TestMincoreErrorSign, TestNonblockingPipe infrastructure-error -> PASS; TestNetpollBreak infrastructure-error -> fail by name ("runtime: netpollBreak: the managed host has no runtime poller to break ...").
- No host death. TestDebugLog and TestDebugLogInterleaving fail in 0.01 s by name ("runtime: stopTheWorld: the managed host cannot stop the world (reason: ResetDebugLog (test)) ..."). TestReadMemStats and TestReadMetrics fail the same way, fast. TestReadMetricsConsistency fails in 0.19 s (the metrics.cs managed-pointer deref), as at master; no park.
- The run reached the end of the serial phase for the first time: TestSemaHandoff started at about 2,600 s and did not finish by 3,600 s. 125 top-level tests unrun (the parallel batch, waiting).
- So the gate is NOT met as written: 10,668 < 10,669 and a deadline kill. The cause is below.

WHY IT IS SLOW (measured, pre-existing at master):
- TestSemaHandoff ALONE in the same host: 30 s, and FAILS: "direct handoff < 2/3: 4 10000". TestSemaHandoff1: 2/10000. TestSemaHandoff2: 1/10000. That is the ratio you asked for: about 0.04 %, as R predicted (near 0). The yield fix stays R's follow-up.
- TestReadMetricsCumulative starts a noise goroutine (allocate 1 KiB, check done, runtime.GC(), forever), then panics in metrics.Read (the Float64Histogram managed-pointer deref, arm 2a) BEFORE its close(done); wg.Wait(). The goroutine never stops, so the host calls runtime.GC() in a loop for the rest of the run.
- Bisected: TestReadMetricsCumulative then TestSemaHandoff does not finish in 120 s; TestReadMetricsConsistency then TestSemaHandoff finishes in 29 s. Thread samples during the reading: one goroutine thread at 97-98 % CPU from minute 4 to the end (36 CPU-minutes by 13:11), the test thread at 5-9 %, 3 cores idle. Every other thread waits on the forced GCs.
- The same leak explains TestSchedLocalQueueEmpty: 36.8 s alone in the host, 2,576 s at master and 2,205 s here. golib's own cost for its shape is 0.3 ms per iteration (console probe).

WHAT I AM DOING NOW: the same reading with a 120 m package deadline is running, so TestSemaHandoff completes and the parallel batch is read for the first time. I post it when it lands. Not pushing until you rule.
ASK: (a) accept (R) + item 3 on this reading plus the 120 m one, with the GC-storm leak named as pre-existing; or (b) hold the push for a seat that stops TestReadMetricsCumulative's leak first (fix the metrics.Read deref, or make the test's failure path stop its goroutine). I recommend (a) and (b) as the next seat, since the leak costs master the same 2,576 s today.

ITEM 3 CALLER CENSUS (read-only, done before this reading; each caller against its former NotImplementedException):
- write1 <- netpollBreak: WORSE (fatal on -EBADF). Fixed by the refusal above.
- write1 <- writeErrData (print.cs's gwrite; the fatal writeErrStr paths): now writes to fd 2 as Go does. Not worse.
- write1 <- heapdump (WriteHeapDump): behind stopTheWorld's refusal. Unreached.
- read <- netpoll: behind netpoll's epfd == -1 return. Unreached.
- read/open/closefd <- sysauxv, getHugePageSize, readRandom; open <- checkfds; mincore <- sysargs: schedinit/osinit/rt0 paths the host never runs. Unreached.
- open <- create: no caller.
- open, pipe2, mincore <- the export_test wrappers: their own tests, now PASS.

SIZINGS (read-only, no code):
1. TestMemclr: SliceHeaderBox.Materialize calls Pointer.FromBox on a one-past-end element box (low == backing length, len 0); ElemRefBox.ValueSlot throws IndexOutOfRange. One golib site plus a GolibTests arm; R reviews.
2. TestRuntimePanicWithRuntimeError: golib panics with plain strings for nil-map assignment, close of a closed channel, send on a closed channel and index out of range, so recover() does not see a runtime.Error; new channel<bool>(-1) throws ArgumentOutOfRangeException instead of makechan's panic. Registered runtime.Error factories on the IntegerDivideByZeroValue precedent (runtime/panicvalues_impl.cs), 5 sites; R reviews.
3. parsedebugvars: non-zero Go defaults are cgocheck, invalidptr, adaptivestackstart, madvdontneed (linux), traceadvanceperiod, profstackdepth. cgocheck and profstackdepth are already worked around (pinner_impl.cs, mprof_impl.cs); the other four are read only in stack.cs, mbitmap.cs, mem_linux.cs and trace.cs, which the host does not reach. traceback_env stays 0 (unmeasured; no row found reading it). User GODEBUG goes through the hand-owned internal/godebug. No in-process row measured failing because of it (TestPanicNil fails at a nil deref in checkPanicNil, panicnil_test.cs:66). Recommend: record, no seat until a row reads a debug var.
4. TestSchedLocalQueueEmpty: see WHY IT IS SLOW above. Not a scheduler defect; the leaked GC loop.
