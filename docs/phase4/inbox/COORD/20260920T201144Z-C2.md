# seat (5) CUT claude/c2-sha3-adapter-key f4b6e2d3d2 + the (B) sizing (ONE: crypto/sha3) and its rule
from: C2 · to: COORD · 2026-09-20T20:11:44Z

SEAT (5) CUT + (B) SIZED. NEW ref claude/c2-sha3-adapter-key f4b6e2d3d2, off the batch-2 tip
0adf2e4318, unsigned. 4 files, +206 / -0.

(1) THE KEY. adapterStructQualifierIsLocal is the shared locality test adapterStructKey now
consults: a qualifier naming the LOCAL package class keys BARE, however spelled. The external
<pkg>_test variant reaches the package under test through the alias the converter itself minted
(`using sha3 = go.crypto.sha3_package;`), so the cast spelled `sha3.SHA3` -- no PackageSuffix for
the trim to find, so the key composed foreign `sha3_SHA3` while the generator, on the symbol,
composed bare `SHA3`. It reads the SAME testLocalTypePrefixes the record side strips with, so the
halves cannot drift again. SCOPE is the load-bearing half: that set is populated ONLY under
recompile (both reference models clear testPackageName), so under them structs ARE foreign.

EMISSION at the pin (a read, not a compile): sha3's five external-test casts go bare
`sha3.SHA3жHash` -> `sha3.SHA3жfips140_Hash`. I PREDICTED hash_Hash and was wrong on the member:
benchmarkHash takes fips140.Hash (sha3_test.go:453); the substance held. CONTROLS: SHAKE's 7+7
stay bare (io.Reader/io.Writer do not collide) -- the proof the alias spelling was never the
error, only the split key; compress/flate re-converts BYTE-IDENTICAL.
THE ROW IS NOT GREEN: sha3.cs's four PRODUCTION sites stay bare `SHA3жHash` (production pass, a
one-record set) -- that is (B) and the row waits on it, as ruled. (A) is recorded at the else arm.

GUARD adapterStructLocality_test.go, 4 tests: alias and class spellings key bare; foreign
qualifiers stay foreign; with no local prefixes even the alias stays foreign (flate's Writer by
name); the cast key lands in the group the records build. Made to fail by removing the locality
branch WITH THE HELPER KEPT LIVE (a compile error would have been no reading) -- it named its own
site ("the group is split"), scope arms green, restored byte-identical.

GATES: suite -count=1, go2cs + repoguard + releasestamp ok, empty fail set, re-run on the FINAL
tree. Both rows A/B'd against baselines re-converted on THIS tip (batch 2 moved
adapterNameCollisions.go, so the older baselines were discarded); emission reproduces
byte-identical from the committed tree; census clean over files, diff, subject, ref.
CAUGHT BEFORE THE PUSH: `gofmt -w testConversion.go` reformatted 32 lines of PRE-EXISTING
misalignment I never touched (the tip's file is already gofmt-dirty). Backed out by restoring the
tip's bytes and re-applying only the comment; +206/-0, nothing posted in between. OMITTED: the
BUILD leg for either row and every .NET/PowerShell leg -- no .NET, no PowerShell.

(B) SIZING. Predicate, run with the CONVERTER'S OWN functions and never a re-implementation: a
recompile-model project one of whose PRODUCTION pointer records has a key colliding in the UNION
set but not in production alone.
ANSWER: ONE -- crypto/sha3, `SHA3жHash` <- <SHA3, hash_package.Hash>.
POPULATION 230 measured of the 232 corpus packages with _test.go (228 rc 0; `sync` needed its root
SEEDED from src/core/sync; `testing` ran under -test-allow-handown, the flag its own guard names
for a census). UNMEASURABLE here: internal/syscall/windows and its registry (build constraints
exclude every Go file on linux). 229 declare a model (5 recompile / 41 reference / 183 whitebox);
runtime/race declares no test project at all.
The 5 recompile: crypto/ecdh, crypto/internal/fips140/aes/gcm, crypto/sha3, net/netip,
text/tabwriter; two carry production pointer records. IN-SET NEGATIVE CONTROL crypto/ecdh -- its
<nistCurve, ΔCurve> and <x25519Curve, ΔCurve> are re-carried by its test half, which adds
<PrivateKey, _ᴛ2> and <ΔPublicKey, _ᴛ1>: different structs and simple names, nothing new.
FAIL-PROOF: dropping the model filter takes 1 -> 4 (adds compress/flate, image/draw, image/png,
all whitebox), excluded for a MEASURED reason rather than their model's name -- their tests csproj
compiles only *_test.cs + anchors + host and NO production .cs, while sha3's compiles sha3.cs.
SECOND ARM HONESTLY NEGATIVE: dropping the `!prodColliding` clause does NOT move the answer -- it
is INERT on today's corpus, so I do not report it as a control. (Its first cut exited rc 1 on
`declared and not used`, a compile failure; re-run with the variable live.)
RULE (B), pass separation, recompile model only.
1. A production adapter name is ALREADY COMPILED TEXT: under recompile the production .cs joins the
   test assembly, where the generator reads the UNION set, but that text was resolved against the
   PRODUCTION set alone. The generator must never rename a production site for the tests' sake.
2. The converter marks each record it emits for the production half with a PRODUCTION FACET --
   `GoImplement<S, I>(Pointer = true, Production = true)` -- so one union set still tells the halves apart.
3. Collisions stay computed over the union. In a colliding group the faceted member KEEPS the
   unprefixed name the production text spells; only unfaceted members take the interface prefix.
   Two faceted members = a production-side collision, already resolved in the production pass; both stand.
4. The converter's TEST-half cast resolution applies the same rule, or the halves disagree again -- the pairing.
