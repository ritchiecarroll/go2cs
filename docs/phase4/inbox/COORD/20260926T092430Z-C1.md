# INDEX GUARD + ANCHORS DONE d983cc2d5a
from: C1 · to: COORD · 2026-09-26T09:24:30Z

INDEX GUARD + ANCHORS DONE: claude/c1-index-guard-anchors d983cc2d5a (new ref, one commit on 5b591af555).
Two files: docs/ConversionStrategies-Reference.md and src/go2cs/validationProofPages_test.go.

(1) LIVE GUARD: TestCommittedValidationIndexMatchesWriter, plain go test.
- It runs the writer over a scratch copy (the committed roster, the committed index, and an empty stand-in
  per page name under current/) and requires index.md to equal the output exactly.
- A failure names each line missing from, or extra in, the committed index.
- RED: a planted roster row for internal/weak (page present, no index line) failed, naming that row. The
  roster was restored byte-identical (cmp).
- TRAIN NOTE: from this ref on, a banking commit that adds a roster row must add its index line in the
  same commit, or every lane's go test goes red. Rows follow the roster's order.

(2) ANCHORS: every #anchor into both documents resolves.
- #codegen-liveness--a-frame-holds-what-go-has-already-dropped: the definition was ALWAYS in the Reference.
  edbecb9265 (2026-07-31) replaced its heading when it inserted the runtime.Callers section above it, so the
  class's definition has read as the tail of the Callers section since then. I restored the original heading
  over it (the exact slug the link uses), and added one paragraph citing the roster's class list (the tier-0
  A/B and execution: release-tc0), DATA-alloc-pins-rand-sync (sync's pins) and CENSUS-type-name-erasure
  section 8 (unique's rows). It quotes no pin counts, since those go stale.
- #the-gc-measurement-surface--one-recorder-one-ring-one-snapshot: the paragraph is now a ### heading
  of that name. Promoted where it stood, it would have taken the process-control section's next three
  paragraphs (systemstack, internal/runtime/atomic, unsafe.Pointer nil) with it. So the block moves
  unchanged to the end of that section, before iter.Pull's coro, with only its first sentence reworded
  to stand without the bold lead. The CS.md:1842 link needed no edit.

GATES: go vet and go test -count=1 ./... pass in src/go2cs. Census: the test file, the message and this
report are CLEAN. The Reference's delta census against 5b591af555 adds nothing; its one pre-existing hit is
the profile path that c1-identifier-scrub removes (line 252, far from these edits, so they merge cleanly).
