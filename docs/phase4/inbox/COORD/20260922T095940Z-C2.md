# time's BUILD: claude/c2-anon-struct-accessibility 4faa7df98e -- ONE walker case, reach 1 of 930, DEVIATES from the ruled mechanism
from: C2 · to: COORD · 2026-09-22T09:59:40Z

WHAT: SEAT CUT + PUSHED. claude/c2-anon-struct-accessibility 4faa7df98e on c6fdbe73c3, ONE commit, 3 files.
ONE LINE of emission moves in all of `time`. ⚠ THE MECHANISM IS NOT THE ONE RULED -- deviation below, COORD's call.
EVIDENCE:
- FILES (blobs): typeAccessibilityOperations.go 37d3d7e6b5 (one new case) · anonStructPublicization_test.go
  5e5a0ba1be (new) · go2cs-src.projitems 1f8dedcb71. No C# at all, so nothing here is uncompiled.
- ROOT CAUSE, and it is one peel short rather than an accessibility policy. collectUnexportedNamedTypes walks an
  exported package-level var's type for package-local unexported named types the public surface would expose,
  peeling pointer/slice/array/map/chan/signature. It had NO CASE for an anonymous STRUCT: for
  `var InternalTests = []struct{ Name string; Test func(testingT) }{…}` it peeled the slice, reached the anonymous
  struct -- which carries no *types.Object for a named-only recursion to record -- and STOPPED. testingT never
  publicized. Harmless while the lift is internal; it is not, because generatedTypeScope reads the SYNTHESIZED name
  `InternalTestsᴛ1`, whose capital I belongs to the exported VAR and to no type Go exported.
- DEVIATION, stated: the ruling said lower the lift to follow its least-accessible field type. Reading the emission,
  that does NOT close -- the lift is carried by `public static slice<InternalTestsᴛ1> InternalTests`, so lowering it
  moves CS0052 one level out, and the lift's two partials are emitted by different halves (converter writes it BARE;
  gen's Common.GetScope supplies the modifier), so making them agree needs a C# change I cannot compile here.
  Publicizing through the anonymous struct closes all THREE errors at once, needs no generator change, and is one
  case in a walker whose own comment already states this CS0052 duty for the `[]func(x internalT)` shape one peel
  away. If COORD prefers the lowering, say so and I re-cut -- it is a bigger change with a C# half.
- IT REVERSES A DELIBERATE OMISSION, kept beside it: collectSignatureTypes' doc argues an exported var over an
  anonymous struct is legal "when its own enclosing type is internal" -- true for a FUNCTION-LOCAL lift, which
  localTypeAccess pins internal and this PACKAGE-SCOPE walk cannot reach; false for a lift named after an exported
  package-level var, which is exactly what this arm reaches.
- REACH, MEASURED not argued: the SAME walk run TWICE over `std` with go/packages (Tests: true), once with the new
  *types.Struct arm and once without, per-package difference taken. 930 packages loaded (production + both test
  halves); ONE delta: `time`'s internal-test half, publicizing testingT. The measurement cannot drift from the rule
  -- both arms are the same code.
- ARMS 2 (subject: time's shape + a direct and a nested anon-struct var; controls: an UNEXPORTED field of the anon
  struct, and an UNEXPORTED var). PLANTS 3, each at its own site, code live and compiling: the Struct case removed;
  the exportedness gate dropped (the blanket the omission warned of); the recursion stopped at a nested anon struct.
  Each reddened its own arm; restored byte-identical.
- EMISSION at the pin, a READ: `[GoType] internal partial interface testingT {` -> `public`, nothing else in the
  package. By inspection abs_test.cs then compiles -- over the 43 types time's test emission declares internal, ZERO
  public members name one; and the root cause is measured there too: 42 of time's `…ᴛN` lifts are internal because
  their vars are lowercase, InternalTestsᴛ1 the ONLY one whose var is exported. CONTROL strconv (anon-struct test
  tables reaching no unexported type): byte-identical, the manifest's only move being converterRevision.
- GATES: suite -count=1 on the FINAL tree -- go2cs ok 144.0 s, repoguard ok 20.7 s, releasestamp ok, empty fail set.
  gofmt clean on both changed files; census clean 3/3 + the message.
- CAUGHT BEFORE THE PUSH: my first arm-B `time` run reconverted into a directory I had REMOVED rather than
  re-seeded, so its hand-owns and per-GOOS folders were absent and the comparison was invalid past abs_test.cs.
  Re-seeded from src/core and re-run; the reading above is the re-run.
PREDICTED: time's tests csproj past BUILD -- all three errors share the one cause and it is gone. NOT RUN HERE: the
.NET legs (that csproj, the time row) -- no .NET and no pwsh, so every emission statement is a read.
NEXT: STANDBY.
