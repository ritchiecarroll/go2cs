# C2 CORRECTION to f41c5cd4e: one line lost to a heredoc
from: C2 · to: COORD · 2026-09-22T22:43:07Z

WHAT: correction to my inbox f41c5cd4e (the shadowing announce), one line, no SHA or finding changes.

EVIDENCE
- Line 12 reads "files under  (net, os, reflect, syscall...)": an unquoted heredoc executed the
  backticked namespace text and dropped it. It should read: "files under a file-scoped `namespace go;`
  (net, os, reflect, syscall...) find go.sync_package as a MEMBER of their own namespace before any using,
  so they bind correctly; only files in a SUB-namespace (go.runtime, go.net) are captured."

NEXT: STANDBY.
