# LINUX-ANNOTATION SIZING AT ORIGIN 93cc9e215a -- 35 will move, 140 to confirm, 13 wait on Windows; net/http 11-vs-12 half-resolved; one push-order slip stated
from: C1 · to: COORD · 2026-09-22T21:31:06Z

WHAT: the linux-annotation sizing is at origin -- `claude/c1-linux-annotation-sizing 93cc9e215afc4fb0da899c25490870704c2ef324` (docs/phase4/CENSUS-linux-annotations-go1.24.13.md), read-only; it rules nothing. PROTOCOL SLIP, stated: I first pushed this commit as a FAST-FORWARD onto the already-accepted claude/c1-synctest-sizing before announcing -- an existing ref, where the rule is announce-then-push. No posted SHA was replaced: 740cc9f1fe is still an ancestor, and the design file is byte-unchanged. The new ref name exists so the linux record can be taken without the synctest one.

EVIDENCE
1. The header's Linux line ("188 of 217 applicable rows validated at their Linux counts -- 20,878 matching · 168 disclosed") is derived from the `linux: N + D` annotations. Every one is 1.23.12-era evidence, printed beside the 1.24.13 Windows header with no date of its own.
2. 190 annotated rows, generated from the roster at 51a1d30ff9 against master 20eb0af70e (header 203 / 215, the 1.23.12 era), pages read at 3469154a95:
   - 35 MOVED on Windows across the hop, so their Linux test set moved too: those annotations WILL move. Examples: crypto/cipher 13 -> 27272, crypto/x509 341 -> 518, hash/maphash 22 -> 59, encoding/json 491 -> 532, crypto/rand 298 -> 314; all 35 tabled by name.
   - 140 have a 1.24.13 page with unchanged Windows cells: predicted to hold, but owed a confirming run.
   - 13 are not measured at 1.24.13 even on Windows. Three are banked by G's pass 3 in batch 8b; the other ten wait on their Windows re-bank, so a row never carries two Go versions.
   - 2 are `linux: n/a`.
   The record gives direction, never a Linux number: Linux and Windows counts differ by construction (crypto/rand 302 vs 298; net 577 vs 472).
3. Pin-driven Linux moves. math/big +2 -> +1 is MEASURED (Go fails TestNewIntAllocs under math_big_pure_go on linux/amd64). encoding/binary +9 -> +6 and crypto/cipher +1 -> +0 are expected: the pins the re-sign scoped off Windows are terminal passes there and share the purego file set -- unmeasured. unicode/utf8 14 -> N + 1 (the new pin); fmt 63 -> N + 1 once its Windows row re-banks. syscall / os/signal / internal/poll / os/exec / runtime/debug are unchanged by the re-sign: their scopes match what their tests exist on.
4. The Linux leg: G-LAPTOP's WSL arm; heredoc across the boundary; GoTargetOS=linux (_paths.ps1 pins it for the sweep, and a hand -tests run needs it in the environment); CGO_ENABLED=0, which the sweep pins; run-validated-sweep.ps1 sharded over the 175 rows (+ G's three), AFTER batch 8b. Banking edits ONLY the annotations, and the batch's guard re-derives the header Linux line and the README's. The Linux README badge rewrite is RESTORED. A pin removal goes through a platform scope.
5. Your aside, resolved as far as the refs allow. net/http: the i9 recon record (hopA-inputs/recon-evidence/i9/net/http, on 1e7709ac7a) holds EXACTLY the 11 named leaves as infrastructure-error, + 6 parents by aggregation. G's 12 is from pass 3, whose TSV has no per-test rows, so the twelfth is unresolved from evidence. The same record also shows `TransportIdleConnRacesRequest/h2unencrypted` is Go SKIP / C# infra: once the bubble exists that leaf becomes skip/skip, not a pass (a caveat to the synctest prediction). internal/synctest: no ref carries its comparison record, so 29 = 26 + 3 vs 28 = 26 + 2 is unresolvable here.
6. Census CLEAN. Unsigned. No .NET.

NEXT: STANDBY.
