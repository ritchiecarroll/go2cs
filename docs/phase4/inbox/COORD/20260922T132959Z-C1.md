# C1 guard tables e8071f56a2: 2 of 3 fixed -- the value-clone wrapper predicate was DEAD BY CONSTRUCTION (0 stamped wrappers ever seen) and red 3 was 34 OBSOLETE rows not new ones; red 1 held one word short, disposition unmeasurable here
from: C1 · to: COORD · 2026-09-22T13:29:59Z

WHAT: guard-table seat -- claude/c1-train-guard-tables e8071f56a2, ONE commit off ae2f251988. TWO of the three reds FIXED; the THIRD held one word short, and not because of an artifact. NONE of the three is a defect in a banked artifact, so nothing needs unbanking. Two corrections to the brief's diagnoses below.
EVIDENCE
- I reproduced all three at ae2f251988 first (full `go test -count=1 ./...`, not a filtered run -- my first filtered attempt saw only two because TestCommittedCoreReferencesResolve lives in internal/repoguard, a different package).
- (2) FIXED, and the guard was BLIND TO ITS OWN POPULATION: goTypeWrapperRe required `[GoType(…)]` then whitespace then `partial`, so it cannot match a declaration carrying a SECOND attribute or an access modifier -- and a STAMPED wrapper always carries [GoValueClone] BY DEFINITION. Measured: old pattern sees 0 stamped bodiless wrappers, widened sees exactly the 4. So valueCloneMintedMembers (which ALREADY contains "Value") had NEVER admitted a real corpus type -- dead from the day it was written, surfacing only now that the first stamped wrappers exist. The four artifacts are CORRECT.
- ANTI-VACUITY, since a widened predicate is worthless if it stops catching: all +236 extra matches verified to be genuine bodiless [GoType] wrappers; the attribute group excludes newlines; and the guard's OWN planted-fixture and vacuity arms (TestValueCloneStampScannerFiresAndAdmits, TestValueCloneVacuityArmCanFire) both still PASS.
- (3) FIXED, and the OPPOSITE direction from the brief: nothing new references an untracked directory. The failure is DECLARED BUT NOT MEASURED -- 34 obsolete rows the hop resolved, all retired relocation sources (runtime/internal/math x28, crypto/internal/nistec x3, crypto/internal/bigmod x2, crypto/internal/edwards25519/field x1). I checked the other direction explicitly: ZERO measured-but-not-declared, so no new reference hides behind them. Removed exactly those 34 by matching the guard's own output, not by hand-listing; the 10 real stale references stay declared.
- (1) HELD, and I correct MYSELF here: the three are runtime.fastrand / fastrand64 / fastrandn, bodyless partials in runtime/rand_test.cs -- so they DID arrive with the runtime test artifacts, exactly as you said. I first read the census's arrow as the stub's location and called them production; that was wrong. Adding the rows is right.
- WHY I STOPPED ON IT: the DISPOSITION is a claim about what supplies the body, and that table's own comment says a row "is disposed by its MECHANISM and read by its MEASUREMENT". golib contains NEITHER fastrand NOR its dispositionSuppliedElsewhere sibling mapaccess1, so that discriminator does not discriminate; settling it needs a build of the runtime test assembly, which this box cannot run. Writing `latent` where "supplied by golib" is true, or the reverse, banks a false claim in the table that exists to prevent exactly that. Three rows are ready; one word is owed.
- GATES at the seat: go vet clean; go test -count=1 ./... -> internal/repoguard ok 17.5s, internal/releasestamp ok, and the ONLY failure left is the held census. Red-first on both fixes: measured RED at ae2f251988 before, green after.
ASK: rule the disposition for the three (or give me a hardware lane's build of the runtime test assembly) and they land as a second commit on e8071f56a2. NEXT: the fips140test + sha3 manifest seat with your batch-4 unit notes; then STANDBY.
