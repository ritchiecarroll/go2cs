# PUSHED claude/c2-generic-type-alias 9804fe627b -- generic aliases unalias at use; footprint 0 measured
from: C2 · to: COORD · 2026-09-26T10:08:21Z

PUSHED new ref claude/c2-generic-type-alias 9804fe627b, one commit on master db1bd885a2. As ruled.

FIX:
- visitTypeSpec: a generic alias emits `// type A[T any] = Box[T]` and nothing else (no global using, no GoTypeAlias record).
- genericAliasTarget (typeNameResolution): types.Unalias(t) for a *types.Alias with type args or params, called first by getAliasQualifiedTypeName and getFullyQualifiedTypeName.
- convCompositeLit: literal type syntax naming a generic alias renders the target.
- A plain alias never reaches the gate.

ANONYMOUS-STRUCT ARM: needs MORE than the lift, so it is reported separately (as ruled). A lifted generic type would make `Cell[int]` a different type from `struct{ Row, Col int }`, which is option (c). The declaration emits the comment plus a conversion warning and no lifted type. It is not in the behavioral test; the unit test pins the declaration.

RED-FIRST (local, unofficial, linux flavour):
- Behavioral GenericTypeAlias + GenericTypeAliasLib + GenericTypeAliasBystander (the first go 1.24 behavioral modules). At master the library fails with 9 CS errors (7 x CS0234 go.T, 2 x CS0307) and both consumers fail on it. With the fix: 0 errors, and both outputs equal Go's.
- TestGenericTypeAliasRendersItsTarget: RED at master (16 lines).
- TestPlainTypeAliasKeepsItsName (the gate guard): green at master and after, RED when the gate is widened to every alias. Restored byte-identical after both controls.

FOOTPRINT (two seeded roots from one archive of db1bd885a2), MEASURED as predicted:
- -stdlib windows / linux / darwin: 0 files.
- -tests over the 10 packages whose test files declare any alias (internal/poll, internal/reflectlite, internal/runtime/maps, math/bits, net, net/netip, os, reflect, runtime, slices): 0 .cs lines. Only the untracked test-manifest provenance moved.
- Behavioral stand-in over 739 projects (the same 8 unmeasured): only the 3 new projects move, and their fixed emission equals the goldens.
- go test ./... ok (after registering the new test in go2cs-src.projitems).

DOCS (same commit): README:110's exception removed; ConversionStrategies summary and Reference gain "Generic Type Aliases"; DESIGN-generic-type-aliases.md gains a dated block with the measurement.
CENSUS: 34 of 35 staged text files are clean. The Reference refuses on the pre-existing line 252 (C1's scrub owns it); my hunk is at ~9386.

OWED (hardware): the real CNR, the behavioral MSTest for the 3 new projects (4 classes, the lib 3), go2cs.slnx (3 new projects), and the stdlib builds if you want them (no byte moved).

SUGGEST: a PLAIN cross-package alias in a composite literal through an ALIASED import renders `new pl.B2(…)` (CS0426); a canonical import gets `new PALibꓸB2(…)`, which works. Pre-existing at master, non-generic.
NEXT per your queue: the exported func alias record seat. STANDBY until you confirm.
