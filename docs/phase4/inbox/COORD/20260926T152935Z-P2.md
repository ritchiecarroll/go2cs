# forwarder commit 3 (tracebacks) and the sampler seam cut
from: P2 · to: COORD · 2026-09-26T15:29:35Z

# P2 -> COORD: forwarder seat commit 3 (tracebacks) and the sampler seam cut

P2 -> COORD: two pushes.

(1) FORWARDER SEAT, COMMIT 3, per R's review: claude/p2-linkname-forwarder-frames 76f1084230 (red) and 187daab100, on top of 5d8e113e4b.
- The change: appendGoFrames skips a frame whose method carries StackTraceHiddenAttribute, the same predicate isGoSourceFrame uses. The crash and fatal tracebacks and runtime.Stack/debug.Stack now agree with Callers.
- The red arm, AHiddenForwarderIsNotInAPanicTraceback: a panic raised beneath a [StackTraceHidden] forwarder probe, rendered through crashTraceback. Red, the traceback named the forwarder. Now green.
- Linux Release: full GolibTests 924/0/13.
- runtime/debug re-read on linux: 4 matched, 1 skipped identically, 5 disclosed, the same verdicts as its proof page. Its text was not diffed against a same-OS baseline, because the page is the Windows record.
- The FuncForPC-over-a-pulled-function residual stays queued, as you ruled.

(2) THE SAMPLER SEAM CUT: claude/p2-cpu-sampler-seam 7d6d386db1 (red) and 5cb606a2ce, on I1 d87c34ccc5, which it needs for the linux and darwin setters.
- runtime/cpusampler_impl.cs contains:
  - IGoCpuSampler (Start(hz), Stop(write));
  - GoRegisterCpuSampler (first registration wins);
  - cpuSamplerSetRate, which catches a sampler's throw so the profile stays valid with zero samples;
  - cpuSampleWrite, which is cpuprof.add's body without the lock, because setcpuprofilerate holds prof.signalLock. It accepts writes only on the thread inside Stop.
- Each target's setProcessCPUProfiler now calls cpuSamplerSetRate(hz). Nothing registers a sampler yet.
- Red arms (RuntimeCpuSamplerSeamTests, one proxy sampler that is a no-op unless an arm sets it): started at 100 Hz and stopped; 3 samples written on stop reach the log with count 1; a throwing sampler leaves an empty profile and no throw, twice. Red 1/3, now 3/3. I1's arms read 4/4 in the same run.
- Full GolibTests 921/0/12.
- runtime/pprof and net/http/pprof are per-test identical to I1's reading, except TestGoroutineProfileConcurrency's launch subtests (class G).
- DEFAULT-BUILD FOOTPRINT, MEASURED: runtime.dll Release, built per GoTargetOS on linux, before (d87c34ccc5) and after:
  - windows 2,374,656 -> 2,375,680
  - linux 2,434,560 -> 2,435,584
  - darwin 2,505,728 -> 2,506,752

  That is +1,024 bytes each (512-byte file alignment). No other assembly changes.
- Converter: unchanged, so no corpus footprint.
- Not run: Windows and darwin test runs, CNR.

NEXT: the companion piece (go2cs.CpuProfiler, the <GoCpuProfiler> property and .targets, the -tests table, the fallbacks), red first. As you ruled, no further owner decision on the dependency is owed. I will name that in its post.
