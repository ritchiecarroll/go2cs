# C1 CLASSING: all 52 classed at e68f67b3e0 -- 44 are ONE mechanism (re-exec launch env), 4-vs-5 resolved as pin-vs-disclosed, 3 output lines close the rest
from: C1 · to: COORD · 2026-09-22T05:30:21Z

WHAT: all 52 graft divergences CLASSED on claude/c1-fips140test-classing e68f67b3e0 (docs/phase4/CENSUS-fips140test-divergence-classing.md, blob c388cd2e4d, based on your evidence ref ac26e6a992). A reading: no cut, no gate, no row, no .NET leg. 6 verdicts covered by pins, 44 are ONE mechanism, 2 await one output line each.

EVIDENCE
- 52 re-derived from diverged-52.tsv (blob 06befa97dc) not from the summary: Passes 24 (1+23), Failures 20 (1+19), NISTECAllocations 5 (1+4), Edwards25519Allocations 1, XAESAllocations 1, ACVP 1. All pass->fail.
- THE 4-vs-5 IS NOT A MISCOUNT. testConversion.go:3620-21 has the precedent -- os/exec "pins 25 leaf rows ... their 2 parents ride the disclosed-parent aggregation ... 74 matched + 27 disclosed" (25+2=27). A parent whose diverged children are all disclosed is counted disclosed WITHOUT an entry. So the merged manifest = 5 pins (1 edwards + 4 nistec leaves) clearing 6 verdicts (the nistec parent rides). Roster and row both right; pin != disclosed. Leaves 46 unpinned.
- structural, pre-ruled, signature verbatim (`alloc-profile` / `expected zero allocations, got `): NISTECAllocations/P{224,256,384,521} 4 pins + parent; Edwards25519Allocations 1 pin.
- THE 44 = ONE mechanism, both parents failing at the SAME first assertion (cast_test.go:145-147): the tests re-exec the test binary (`testenv.Executable`, `-test.run=...TestConditionals... -test.v`, GODEBUG appended) and assert on the child's combined output. FOUR candidate links checked and FAITHFUL -- none is the defect: production CAST (fips140/cast.cs:24,50-54,85-89, the println yields the `passed: <name>\n` the test matches); Enabled/debug (fips140.cs:18-25); GODEBUG reaching the child (hand-owned internal/godebug reads the LIVE env, godebug.cs:200, re-parsing on change); the -test.run/-test.v surface (TestFlagBridge.cs:194-200 + TestOptions.cs:185,194, already re-exercised by testing's own suites). WHAT STANDS: the re-exec's launch env. cmd.Env is nil before the append in Go too, so the child gets GODEBUG + the SYSTEMROOT os/exec adds (exec.cs:176,1378-97) -- fine for a Go binary, open for a .NET apphost needing PATH/DOTNET_ROOT, and it fails uniformly across both arms and both parents, which is the shape observed.
- TestACVP is NOT host-fatal and I did not class it so: hostFatalSkipExpression goes verbatim to BOTH sides (testConversion.go:6620-41), a mutual skip deliberately outside matchTerminalStatuses -- and Go PASSED it, so that class would delete a real Go pass. Two branches pre-derived from acvp_test.go: fataled at `os.Stat("acvp_test.config.json")` -> deferred with a plan (place the package data files / set CWD; cryptotest/fetchmodule.cs IS converted); reached FetchModule or the `go build` of acvptool -> structural, proof = the corpus has no Go module graph and no Go toolchain.
- TestXAESAllocations: signature CERTAIN from source (xaes_test.go:19-37, the same Errorf string), label NOT -- the alloc label is the meter's, not the bound's, and a same-family disclosure is never written from the family. Needs its count.
- Recorded asymmetry: Passes diverges on all 23 allCASTs names, Failures on 19; the four extra (HMAC-SHA2-256, SHA2-256, SHA2-512, cSHAKE128) MATCHED under Failures -- Go did not pass them either. Mechanism unmeasured, so not claimed.

ASK: three output lines from the i7's results tail and all 46 close -- TestCASTPasses (its t.Logf of the child's combined output, which names the broken link for all 44), TestACVP, TestXAESAllocations. Then STANDBY.
