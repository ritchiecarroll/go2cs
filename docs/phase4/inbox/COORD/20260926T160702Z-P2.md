# the sampler companion piece
from: P2 · to: COORD · 2026-09-26T16:07:02Z

# P2 -> COORD: the sampler companion piece

P2 -> COORD: I pushed claude/p2-cpu-sampler-companion: a970c755ce (red), bfd5ff90f1 and 085437c9e6, three commits on the seam 5cb606a2ce.

OWNER'S DEPENDENCY RULE: SATISFIED. Only an opted-in build carries the diagnostics dependency, as ruled; no further owner decision is owed. This piece adds only Microsoft.Diagnostics.NETCore.Client. TraceEvent arrives with the frames piece, inside the same companion.

WHAT LANDED:
- src/core/go2cs.CpuProfiler: a hand-owned library with EventPipeSampler. Start opens a SampleProfiler session on the process's own diagnostic port and drains it in the background; Stop stops the session and waits for the drain. A session that cannot open leaves the zero-sample profile and throws nothing. No samples are written yet; that is the frames piece.
- GoCpuProfiler.targets: under <GoCpuProfiler>true</GoCpuProfiler> it adds the companion reference and one linked GoCpuProfilerRegistration.cs, a [ModuleInitializer] in the program's own assembly.
- Converter: cpuProfilerTestPackages (runtime/pprof, net/http/pprof). test-csproj-template.xml sets the property through a new marker and imports the .targets under it.
- The 225 COMMITTED *.tests.csproj take the template's 6 new lines (+1,350, nothing else). None is tabled, so each build is unchanged. A -tests run of runtime/debug re-emits the Import block identically; its other 4-line csproj diff is the linux flavour of a Windows-banked file and predates this.

RED ARMS, linux:
- EventPipeSamplerTests: "a session opens on this process and drains a non-empty trace" failed on the stub. "A session that cannot open leaves no samples and no throw" passed.
- TestTestCsprojCpuProfilerOptIn failed on the template.
- Now 2/2 and green. The seam's 3/3 hold in the same run.

DEFAULT-BUILD PROOF, MEASURED on linux -tests hosts:
- runtime/debug, not opted in: 0 files named *CpuProfiler*, *NETCore.Client* or *TraceEvent* under bin/tests, and 0 of 1 deps.json mentions them. Its tests.dll carries no GoCpuProfilerRegistration type.
- runtime/pprof and net/http/pprof, opted in: 4 such files each, 1 of 1 deps.json mentions them, and the pprof tests.dll carries the registration type.

FALLBACK, MEASURED: the published runtime/pprof host, run with -test.run 'TestCPUProfile$|TestMathBigDivide', gives the same result with DOTNET_EnableDiagnostics=1 and =0: rc 0, TestCPUProfile skipped with "total 0 CPU profile samples", no throw, 11.65 s against 11.60 s. NOT measured: direct evidence that the session opened inside that host. The golib arm shows a session opening on this box.

GATES, linux:
- go test ./... ok.
- GolibTests 923/0/12.
- runtime/pprof (131/8/14) and net/http/pprof (12/2/1) are identical per test to the seam reading.
- runtime/debug: 4 validated, 5 disclosed.

Not run: windows, darwin, CNR, and Native AOT.

NEXT: the frames piece. TraceEvent parses the drained trace, frames join through GoSyntheticPC, and Stop writes the samples. It is predicted to move TestCPUProfile, TestCPUProfileMultithreaded and TestMathBigDivide.
