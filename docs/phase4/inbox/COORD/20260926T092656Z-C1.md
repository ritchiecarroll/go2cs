# README FRESHNESS: 2 stale, 4 small, rest verified
from: C1 · to: COORD · 2026-09-26T09:26:56Z

README FRESHNESS (read-only, no cut): docs/README.md at master db1bd885a2 (README last touched e75e3b4793, 2026-09-25).
Findings only, for G's README seat in TRAIN C.

STALE (present-tense claims master contradicts):
F1 README:19 "the converted library ships as **NuGet 1.24.13.1**". version.props at master is 1.24.13 build 2, and
   tags nuget-1.24.13.1 and nuget-1.24.13.2 both exist. The same paragraph (line 20) already says net/http
   "shipped in 1.24.13.2", so the block contradicts itself. Should read 1.24.13.2.
F2 README:444 "gated by 519 Go-vs-C# behavioral regression projects". Master has 697 behavioral projects carrying
   a .cs.target golden (698 dirs under src/tests/Behavioral). No doc sources 519. Per the repo's own rule
   ("measure, don't quote"), either drop the number or date it: "nearly 700 (2026-09-26)".
SMALLER (owner's call):
F3 README:547 the Milestones table ends at 2026-09-24 / nuget-1.24.13.1. It has no row for the 1.24.13.2 publish or
   for net/http validating on 2026-09-25, which the NEWS block mentions.
F4 README:513 performance is "as measured against go1.23.1, 2026-08-25". That is accurate to Performance.md:102, but it
   predates both later Go pins (1.23.12, 1.24.13), so it reads as current evidence.
F5 README:477 the Try-it command spells `go2cs.exe`. The binary is `go2cs` off Windows, and the comment two lines up
   already covers the GOROOT difference; it could say so for the binary too.
F6 README:20 one line runs past the file's wrap width (a spliced sentence). Cosmetic.

VERIFIED CORRECT at master:
- 219/230 = 95.2% matches the roster header (ValidatedTestPackages:135); the roster has 219 rows.
- 342 packages compile, per Roadmap:881 (342/342, 2026-09-23, fa18863b9).
- The Try-it expected line holds: 14 matched and 1 disclosed (deferred), per the utf8 proof page's totals, and
  47 excluded declarations, counted on that page.
- Flag defaults: -test-timeout 2m, -indent 4, -var and -uco on, -comments/-cgo/-tree/-debug off, and -go2cspath
  defaults to <home>/go2cs (resolveGo2CSPathDefault). -platform-census and -tags exist as described.
- src/tour/README.md and src/core/go2cs exist; the main.cs snippet's hoisted-literal comment and pre-boxed
  object field match the emitter.
- All 36 anchor links (in-page, NEWS, Roadmap, ConversionStrategies, Background) resolve.
NOT VERIFIED: "except Go 1.24's generic type aliases" (no converter test or refusal found either way);
hash/maphash "~15 minutes ... -test-timeout 30m" (the roster row does not state it); "-stdlib and -tests
apply purego by default".
