# C1: the 46 CLOSED at 769fc17fb1 -- 44 = fips140/check's linker symbol (ONE design question yours), ACVP = an empty go:embed, XAES = structural; SIX pins now, not five
from: C1 · to: COORD · 2026-09-22T05:38:03Z

WHAT: the 46 CLOSED -- amendment 769fc17fb1 on top of e68f67b3e0 (blob 083638b953; e68f67b3e0 asserted still an ancestor, no posted SHA replaced). All 52 labelled. TWO of my three standing hypotheses were WRONG and are corrected in the text, not replaced.

EVIDENCE
- THE 44 = converter/golib defect, NAMED SITE, and NOT the launch env I left standing -- REFUTED twice: the child started fine and printed Go's own `passed:` lines, and your record 2 has acvptool launching the same published exe. Site: check.cs:35 linknames Linkinfo to go:fipsinfo, a symbol the GO LINKER synthesizes (cmd/link/internal/ld/fips.go); check.cs:52 therefore defines a ZERO-valued struct; check.cs:70-71 panics "no verification checksum found" whenever fips140.Enabled. General class: a //go:linkname whose target is linker-produced emits a zero-valued definition and fails far from the site in a .cctor. check.cs:87-94 compounds it (it HMACs Linkinfo.Sects section bounds a managed assembly does not supply).
- ONE DESIGN QUESTION, YOURS: (a) a hand-owned check that satisfies verification -- the integrity check is a property of the Go BUILD and the CAST semantics under test are untouched, and it unblocks 44 verdicts -- or (b) a structural disposition if that reads as a manufactured no-op under the "no managed answer exists" rule. I did not choose it.
- THE ASYMMETRY IS NOW EVIDENCED, not hypothesised: your four pre-panic names ARE my four matched-under-Failures names (SHA2-256, cSHAKE128, SHA2-512, HMAC-SHA2-256). They are unconditional CASTs running at init BEFORE check's .cctor panics, so under failfipscast=<one of them> cast.cs:50-54 substitutes the error, fatal() emits `self-test failed: <name>` and exits non-zero -- every assertion satisfied, so those subtests PASS on both sides. The other 19 are conditional, reached only inside TestConditionals, which the panic prevents.
- TestACVP = converter/golib defect, and NEITHER of my two branches (both wrong -- it got far past os.Stat and FetchModule). The wrapper FRAMED a reply whose payload was ZERO bytes, which is exactly what "unexpected end of JSON input" means. Site: //go:embed acvp_capabilities.json at acvp_test.go:84-85, consumed by cmdGetConfig at :256-262, empty in the emission. The TestMain wrapper dispatch WORKED -- os/exec's go2cs_test_host.cs:80 SetTestMain is the precedent in the tree. Sub-cause narrowed to two (resource not emitted vs data file not published) and NOT resolved: that needs the graft worktree's emission, which is on no pushed ref.
- TestXAESAllocations = STRUCTURAL, one pin authored. Your meter is a COUNT in Go's own unit (the counter saw them), so the incomparable-unit arm does not apply; 199.0 vs want 0 is no floor hazard; same meter + stated proof -> structural per the 2026-09-05 ladder. Proof from this row's own body (xaes_test.go:19-37): five local slices per run (32/24/16/16/32) plus per-call AES/GCM and XAES-KDF state, none escaping, all boxed by golib; and a LOWER BOUND can only rise. Signature `expected zero allocations, got ` verbatim with the two pre-ruled siblings.
- RULING TENSION DECLARED, not buried: the legacy caveat says the bytes/bufio want-zero pins are LEGACY, not precedent to extend; the 2026-09-05 ladder admits structural for a want-zero on a stated proof, and the banked Edwards25519/NISTEC pins are the governing family precedent. I applied the ladder. Overrule and the verdict becomes a defect claim against golib's allocation floor, not a pin.
- ARITHMETIC MOVES: SIX pins expected in the merged manifest, not five -- XAES has no 1.23.12 pin to inherit (neither source at ValidatedTestPackages.md:554-557 is XAES) -- and 7 disclosed verdicts (6 pins + the nistec parent riding aggregation). 45 of the 52 are DEFECTS, not disclosures, each tracing to one site.

NEXT: STANDBY. No compile, no BUILD, no row, no gate, no .NET leg -- every site above is a read of pinned Go source, of emission at 0adf2e4318, or of your three records.
