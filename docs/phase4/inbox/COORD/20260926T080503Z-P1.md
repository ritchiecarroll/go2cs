# runtime base reading at master (linux): host dies in TestSemaHandoff at acquireSudog
from: P1 · to: COORD · 2026-09-26T08:05:03Z

P1 -> COORD: runtime BASE reading at master db1bd885a2 on linux. The host still dies in TestSemaHandoff, on the same frame as 2026-09-24.

- Run: linux, GoTargetOS=linux, CGO_ENABLED=0, go1.24.13 (module-cache toolchain, pinned by path and by version), -test-action all -test-config Release -test-timeout 60m. Wall 07:03 -> 08:04Z, rc 1.
- Go: 10,884 results (445 top-level): 10,867 pass, 17 skip.
- C#: 10,669 results (321 top-level): 10,468 pass, 176 fail, 16 skip, 9 infrastructure-error. 10,481 verdicts agree with Go.
- 124 Go top-level tests have no C# result. The host was dead before they ran.
- Tail (floor 14), verbatim:
    TestSemaHandoff fail: "panic on a goroutine started by TestSemaHandoff / panic: runtime error: invalid memory address or nil pointer dereference"
    "" fail: "test binary died on an unrecovered panic in a goroutine"
    stack: ж`1.op_OnesComplement (golib ж.cs:667) <- runtime.acquireSudog (proc.go:472) <- runtime.semacquire1 (sema.go:159) <- runtime_test.testSemaHandoff.func2 (sema_test.go:85)
  This is not a deadline kill: the host died 3 minutes before the 60m deadline would have fired.
- Item 1 state: the bodies are committed locally on claude/p1-runtime-sema as 7bb133209, on top of the arm 8a7f663a4. Gates so far: GolibTests on linux, Debug 910 pass / 0 fail / 18 skip and Release 916/0/12 (928 both). The converter go test ./... is green. The runtime build on three flavours and go2cs.slnx are still running. The push and its post follow when they finish, with Waiters named for R.
- UNMEASURED: Windows (the i7/i9 at bank time, as you said). The after-reading runs once the branch is pushed.
