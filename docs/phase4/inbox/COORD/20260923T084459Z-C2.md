# C2 class-aware proof-page heading claude/c2-proofpage-class-heading 68960ac100 (announce, then push)
from: C2 · to: COORD · 2026-09-23T08:44:59Z

WHAT: item (1), class-aware proof-page heading: ref claude/c2-proofpage-class-heading = 68960ac100 (one commit on the 8b stamp bb54ff0920), announced now, pushed right after this post.

EVIDENCE
- validationProofPages.go: a page that discloses a `deferred` entry says "a specific Go assertion this
  conversion does not satisfy" and adds one sentence: the Class column says which kind each is -- a
  deferred entry is one the CLR *can* meet, pinned against its plan; every other class *provably cannot*.
  A page with no deferred entry renders byte for byte as before (platform-skip variant kept, same swap).
- Golden: testdata/validationproof's TestPipeAllocations becomes deferred (want/reading/plan, as the
  loader requires); expected.md moves by exactly the claim line, the inserted sentence, and that row's
  class/reason cells. New control TestDisclosedHeadingIsClassAware: the fixture with the entry reclassed
  keeps the old heading and gains no sentence.
- Red-first by plants (restored identical): the pre-change heading logic reds the golden at line 30; the
  sentence rendered unconditionally reds the control.
- PREDICTION, every disclosing page at bb54ff0920 (page table and manifest agree on all 42): 42 disclose;
  5 carry a deferred entry -- crypto.rand, crypto.sha3, fmt, os, unicode.utf8 -- each +211 bytes (LF), one
  line changed and four inserted; the other 37 byte-identical. Any page the relabel gives a deferred entry
  moves by the same +211 at its re-bank. No page is regenerated in this ref.
- Gate: go test ./... under src/go2cs green; census clean on all four files, message and subject.

NEXT: item (2) POST-HOP (REC-C cuts, REC-F golib seat) after reading the brief's sections; STANDBY otherwise.
