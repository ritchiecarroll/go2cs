# seat (3) internal/synctest cut -- claude/c2-synctest-seat db570064e9
from: C2 · to: COORD · 2026-09-20T18:32:11Z

SEAT (3) internal/synctest CUT. NEW ref claude/c2-synctest-seat db570064e9, off the base
6d814e2d38, unsigned (no GPG key here). Both defects are one converter change each.

ROW: internal/synctest -tests -test-action convert rc 0 at the pin (GOROOT go1.24.13, VERSION
read in-process). PAST BUILD IS NOT CLAIMED -- no .NET here.

(a) iter.Pull. convCallExpr's named func-type ARGUMENT rule reached a GENERIC parameter
    UNSUBSTITUTED: getFunctionSignature returns the callee's own *types.Func type, so
    Pull[V any](seq Seq[V]) arrives as Seq[V] with V unbound and the rule declined -- there
    was nothing concrete to construct. instantiatedParamType (already in the file, used by
    three other rules) reads the instantiation go/types recorded. Emission at the site:
    iter.Pull(seqʗ1) -> iter.Pull(new iter.Seq<time.Time>(seqʗ1)). CS0411 and the CS0029 on
    the pair deconstruct were ONE root; both go with it.

(b) CS0136. visitRangeStmt's deferRangeVarBox excluded range-over-func by an ARM TEST
    (yieldFunc <= -1), not by any property of the range, so a heap-boxed yielded variable was
    declared at the loop's own scope where the foreach declares the same name -- and the box
    was shared across iterations, which is not Go's per-iteration semantics for a stored &i.
    The loop now iterates a temp and opens its body with the fresh box and the copy into it,
    byte-identically to the slice/array/map arm's shape. One- and two-value yield forms both;
    each variable decided independently; string/chan/int arms unchanged.

FOOTPRINT on the row: TWO hunks, one per defect. TestIteratorPush's box moves into the loop;
TestIteratorPull's call gains the wrap. TestIteratorPull's OTHER hoisted `now` -- where the
hoist IS the declaration, no foreach to collide with -- is unchanged: the control for (b).
Re-converted from the committed tree: byte-identical.

GATES RUN HERE: converter suite `go test ./...` -count=1 -- go2cs 113.469s ok,
internal/repoguard 16.039s ok, internal/releasestamp ok, empty fail set. Both new tests made
to FAIL by regressing their own rule (3 arms: the arm test restored -> names boxedYield; the
substitution dropped -> names pullImplicit; the two-value branch reverted -> names
boxedYieldPair), each source file restored BYTE-IDENTICAL after. Census CLEAN over the three
changed files, the staged diff, the subject, the ref name and this message.

OMITTED, and why: the BUILD gate for the row and EVERY .NET/PowerShell leg -- no .NET and no
PowerShell on this box, so this is a read, not a compile. The disk-constrained legs (stdlib
solution, CNR, the two-seeded corpus diff) are the i7's.

BATCH 2 IS NOT ON THE BASE. origin/claude/version-go1.24.13 was still 6d814e2d38 at 18:33Z,
0 ahead, so the gates above ran WITHOUT it. The merge and a re-gate are still owed.
