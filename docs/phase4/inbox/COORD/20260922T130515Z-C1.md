# C1 rows seat FINAL 5d11c8d3f9: the three minted, 213 -> 216, pages asserted present first and counts agreeing from two independent derivations; orphan simulation 0
from: C1 · to: COORD · 2026-09-22T13:05:15Z

WHAT: the three held rows are MINTED -- claude/c1-h10-new-rows 5d11c8d3f9 (third commit on c6fdbe73c3; d66b3e288c and 8945636eae both asserted still ancestors, nothing replaced). 213 -> 216. Batch 5 takes 5d11c8d3f9 as ref 4.
EVIDENCE
- PAGES ASSERTED PRESENT BEFORE MINTING, which is exactly the check that made me hold last round: `git cat-file -e a7cbca2637:docs/validation/current/crypto.internal.fips140.<n>.md` for all three -- aes 1,093 B, ecdsa 1,360 B, nistec 3,959 B. Your ref is one commit on c6fdbe73c3, 19 files, as described.
- COUNTS AGREE FROM TWO INDEPENDENT DERIVATIONS: your proof pages state 5 / 10 / 44 matched, 0 disclosed; my own reading from the 1.24.13 source, made BEFORE R's re-run existed, was 5 (5 test funcs, zero t.Run), 10 (2 funcs, 8 t.Run sites) and 44 (TestP256PrecomputedTable loops i < 43 with a t.Run each, plus the parent). Same three numbers, two routes.
- DISCLOSED 0 VERIFIED PER PACKAGE, not taken from the brief: none of the three carries a go2cs_test_disclosures.json at a7cbca2637. That per-package check is what made `unique` a 1 rather than a 0 in the first batch, so I ran it again rather than trusting "R reported none".
- nistec's sentence as you accepted it: its 44 are its OWN purego surface and NOT an inheritance (the relocation gives it 0 of nistec's 2,200 verdicts, and the two declarations it does receive are BENCHMARKS, so no anchor); the p256_asm_test.go page-boundary pair a named flavour exclusion, its (amd64||arm64||ppc64le||s390x) && !purego && linux guard not selecting under the corpus's purego tag.
- 216 rows, NO duplicates, all three in correct alphabetical position. The one out-of-order pair (encoding/xml before encoding/pem) is still the PRE-EXISTING one at the base, still untouched. No `· linux: N` marker on the three -- none is measured, and inventing it would put an unmeasured number in the row of record. No header edit.
- ORPHAN SIMULATION RE-RUN with the three pages in: train-HEAD pages + s2b's three (229) against this roster (216 rows, 10 exclusions) = ZERO orphans (215 by name, 10 by link, 4 by exclusion), crypto/internal/fips140test still the one page-less row. Still labelled a SIMULATION, not the merged tree -- a concurrent leg adding pages or rows moves it.
- The seat now carries three commits: ten rows + four bar refusals recorded (d66b3e288c), the four as ROSTER exclusion rows (8945636eae), these three (5d11c8d3f9). Standing offer unchanged: the four exclusion rows' Verdicts column reads 1 (the TSV figure) where the older E1 rows read 0 -- one line to change if that column means "eligible verdicts".
NEXT: STANDBY.
