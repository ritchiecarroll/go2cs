# P2: go2cs.CpuProfiler registered in both solutions (53ee28fe80)
from: P2 · to: COORD · 2026-09-26T16:17:26Z

# P2 -> COORD: the sampler companion is registered in both solutions

P2 -> COORD: I pushed 53ee28fe80 to claude/p2-cpu-sampler-companion, on 9ea33098 (red), which sits on 085437c9e6. The solution-membership leg will now find go2cs.CpuProfiler.

OWNER'S DEPENDENCY RULE: SATISFIED. Nothing here changes who carries the diagnostics dependency; only an opted-in build references the companion.

WHAT LANDED:
- solutionGenerator.go: GenerateSolutionFile lists core/go2cs.CpuProfiler/go2cs.CpuProfiler.csproj at the solution root, after golib, when the tree carries it. The walk no longer collects it into an import-path folder.
- The committed src/go2cs-stdlib.slnx (root, after golib) and src/go2cs.slnx (in /core/, after golib) each gain one line.
- go2cs.CpuProfiler.csproj sets IsPackable=false. push-nuget.ps1 packs go2cs-stdlib.slnx, and the NuGet route is option (iii), which is deferred. Without this, membership would have published a package.

RED, linux: TestSolutionListsTheCpuProfilerCompanionAtTheRoot failed on 9ea33098. The companion was collected and listed twice, once under a /go2cs.CpuProfiler/ folder. The red's folder assertion also matched the project path itself; 53ee28fe tightens it to the folder element.

MEASURED, linux:
- A regeneration from the real core csproj layout reproduces the committed go2cs-stdlib.slnx byte-identical.
- dotnet sln list shows the companion in go2cs-stdlib.slnx (345 projects).
- go test in src/go2cs is ok, and go vet is clean.

OTHER GUARDS CHECKED: projitemsIntegrity_test covers only go2cs-src.projitems, and this piece adds no converter _test.go file. check-solution-integrity.ps1 checks tests/Behavioral membership and the core reference graph; the companion is not in the emitted graph. deploy-core.ps1 collects every non-test csproj under core, so it picks up the companion without a change.

NOT RUN: a build of either solution, windows, darwin, CNR, and check-solution-integrity.ps1 (no pwsh on this box).

NEXT: the frames piece, as planned.
