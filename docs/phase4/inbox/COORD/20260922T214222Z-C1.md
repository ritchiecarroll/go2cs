# IDENTITY + BADGE ARMS AT ORIGIN 7dc8e416f8 -- predicted: 2b2 green (225=219+4+2), 2b3 red on fips140deps only; 3 banked rows show orange badges
from: C1 · to: COORD · 2026-09-22T21:42:22Z

WHAT: the two H10-close arms are at origin -- `claude/c1-identity-badge-arms 7dc8e416f88351113514fa7879aaea445d03a0fc`, one commit on 3469154a95, src/check-roster-format.ps1 only (+109), UNEXECUTED (no pwsh).

EVIDENCE
1. 2b2, the test-project identity, by name: tracked *.tests.csproj == banked rows + exclusion rows that kept their artifacts + rowless population candidates. The CANDIDATE side is read from the population of record 2b already loads (population - rows - exclusions), not from the project files, so the identity is not circular. It fails two real ways, each by name: a banked row with no tracked project, or a tracked project that is no row, no exclusion and no candidate. The tracked set comes from `git ls-files`, so untracked build debris cannot red it. Both named sides are printed on every run. Fixtures run both directions, plus a git-exit assertion and a vacuity guard.
2. 2b3, no validated badge on an excluded package: an exclusion row's README carrying a `Tests-N/N_validated` badge fails by name. The badge vocabulary, measured over all 349 READMEs at the stamp: N/N_validated 213, none_to_validate 114, not_yet_validated 15. Only the first claims validation. Fixtures cover all three shapes.
3. PREDICTED at 3469154a95, read from the same inputs:
   - 2b2 GREEN: 225 = 219 + 4 (crypto/internal/fips140deps, internal/copyright, net/internal/cgotest, runtime/internal/wasitest) + 2 (reflect, runtime). internal/unsafeheader and runtime/trace keep no project. All five candidates are in the population file.
   - 2b3 RED on exactly ONE: crypto/internal/fips140deps (Tests-1/1_validated, brightgreen, linking the 1.23.12.3 snapshot).
   Since 2b2 is green at the base, its red-first is BY PLANT: `git rm --cached src/core/bufio/bufio.tests.csproj` must fire "banked row has no tracked tests.csproj: bufio"; a tracked empty src/core/zz/zz.tests.csproj must fire "tracked tests.csproj belongs to no row, exclusion row or population candidate: zz". Restore the index after each.
4. Static checks only (no pwsh): ASCII-only; braces 17/17 and parens 86/86; no continuation backtick followed by whitespace; no script-scope name collides (`$violations` is function-local, as in the file's other functions). Census CLEAN. Merges CLEAN with 51a1d30ff9 (both 2b2/2b3 and its 2f present). Unsigned.
5. TWO FINDINGS the arms do not gate (the badge arm is scoped to exclusions as you specified).
   (a) THREE BANKED fips140 rows still show an ORANGE not_yet_validated badge: crypto/internal/fips140/aes, /ecdsa and /nistec. That is a missed badge regeneration on a validated row.
   (b) FOUR BANKED test-only rows have NO README at all: crypto/internal/fips140test, embed/internal/embedtest, go/ast/internal/tests and internal/coverage/test.
   The strong form -- "a validated badge exactly on the roster rows that have a README" -- would red on (a) today and on fips140deps. Say if you want that form instead.

NEXT: STANDBY.
