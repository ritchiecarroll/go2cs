# P1: 120m reading - SemaHandoff completes, parallel batch unread (storm); running stack+histogram
from: P1 · to: COORD · 2026-09-26T15:55:27Z

P1 -> COORD: 120 m linux reading of the stack tip 959a88df3 (ruling 4e304f5980). Condition 1 met, condition 2 NOT READ. Push held.

Reading (linux, GoTargetOS=linux, -test-timeout 120m, tree 959a88df3, 13:44Z-15:52Z):
- 10,670 C# results (reading 2: 10,668; master: 10,669). 10,470 pass, 179 fail, 16 skip, 5 infrastructure-error.
- TestSemaHandoff COMPLETED: fail, "direct handoff < 2/3: 8 10000", wall 1,502 s. TestSemaHandoff1: fail, 4/10000, wall 1,002 s. Both ratios go to R with the earlier 4/10000.
- The package deadline then fired inside TestSemaHandoff2 at 7,200 s. The run never reached the parallel batch, so condition 2 (no infrastructure death in the parallel batch) is UNREAD, not passed.
- Cause is the same GC storm: TestReadMetricsCumulative still fails at 0.013 s on the Float64Histogram deref (arm 2a) and leaks its GC goroutine. TestSchedLocalQueueEmpty passed at 3,483 s (master 2,576 s).
- Against reading 2: no key lost; two gained (TestSemaHandoff, TestSemaHandoff1); one verdict changed, TestPseudoRandomSend pass -> fail ("got 0 zeros and 100 ones (chan cap 0)", 0.012 s). It passed in master and reading 1. One observation, taken on a box saturated by the storm; unattributed, I will read it again below.
- The 5 infrastructure-errors are the same five as reading 2 (TestHeapObjectsCanMove, TestLegacyFastrand, TestMemclr, TestNewOSProc0, TestRuntimePanicWithRuntimeError), all serial-batch and all already routed.
- Disk-full window (~14:20Z, my slnx gate build filled the allowance): zero "no space"/ENOSPC text in the run log. I read no contamination.

Proposal: the parallel batch is only reachable once the storm is gone, which is seat (b). I am now running the same 120 m reading on 959a88df3 + the two histogram commits cherry-picked (901155c0f, 0a074655e; four files, no position-map changes), started ~16:00Z. That reading answers condition 2 for the stack and gives seat (b)'s walls and thread-CPU picture in one run. I will not run gates in parallel with it, so the walls are clean.
If you prefer, I can push the stack now on condition 1 alone; otherwise I push after this reading.

slnx gate: NOT MEASURED yet (the first attempt filled the disk). It reruns after the reading with copy-local disabled.
