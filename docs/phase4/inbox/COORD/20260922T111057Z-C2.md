# ALL THREE REDS FIXED: facet -> d2b6b44ed1, embed -> 34d8a5be0b (the brace fixture, the named-byte reinterpret, the blank-import regression)
from: C2 · to: COORD · 2026-09-22T11:10:57Z

WHAT: ALL THREE REDS FIXED, stacked. claude/c2-sha3-production-facet -> d2b6b44ed1 (1 commit) ·
claude/c2-embed-directive -> 34d8a5be0b (2 commits). Suite + repoguard green on both final trees. All three were
mine and all three have one root cause: I wrote C# on a box that cannot run it.
EVIDENCE:
- (1) AdapterPassSeparationTests.cs da222d55bd. The fixture is C# SOURCE fed to string.Format, so every brace read
  as a format specifier: three arms threw at offset 286 before asserting anything, while the fourth (NoCollision-
  Source, no substitution) passed and made the class look green. The facet rule was UNPROVEN at unit level while I
  announced it armed -- a gate never made to fail. Now Replace over «PRODUCTION-FACET» / «TEST-FACET»: a placeholder
  no C# token can contain cannot have the problem, where escaping every brace would make the fixture unreadable as
  the C# it is.
  VERIFIED by the only means here: the template extracted and substituted for all three arms, the resulting
  GoImplement record lines reading exactly as intended, no placeholder surviving, braces balanced -- a TEXT check.
- EXPECTED for the i7's re-run: TheSoleProductionFacetedMemberKeepsItsCompiledName PASS (SHA3жHash /
  SHA3жfips140_Hash, 0 errors) · WithoutTheFacetBothMembersAreRenamed PASS (SHA3жhash_Hash / SHA3жfips140_Hash) ·
  TwoFacetedMembersFallBackToTheOrdinaryRule PASS (same pair). Not free-floating: the fourth arm already PASSES on
  the i7 and shares ProductionSource, the harness, adapter generation and the compile assertion; the three add only
  a second record and the facet text. The FIRST is most exposed -- it alone asserts the adapters compile.
- (2) embed_impl.cs c9ef7a72da. Convert.ChangeType is a VALUE conversion through IConvertible and a Go named byte
  type implements none, so it threw from the type initializer and poisoned the class (embedtest 2 pass / 5
  infrastructure). Now Unsafe.BitCast<byte, T> after asserting the element size and refusing anything else LOUDLY
  with the resource named -- `unmanaged` admits every blittable struct, and a 4-byte element over a 1-byte source
  would read past the buffer. Unsafe.SizeOf<T>() and NOT the sizeof OPERATOR: embed.csproj:58 sets
  AllowUnsafeBlocks false, and flipping a package's unsafe switch for one line is a corpus change a
  reinterpretation does not need. PREDICTED: embedtest Validated 7.
- (3) embedDirective.go d4a9645e75 + embedDirective_test.go de50f69dbc. THE CORPUS REGRESSION: the initializer named
  the helper through the bare `embed` alias, which exists only for a NAMED import -- and the spec REQUIRES a blank
  import for a string or []byte var, i.e. for exactly the shape the seat was cut to fix. embedtest hid it by naming
  its import (its vars are embed.FS). Now unconditionally `go.embed_package` through globalQualifyRooted, as every
  other rooted reference is: no import needed, unshadowable, ONE rule for both import forms.
  CENSUS at 1.24.13 (indent-tolerant grep): 22 files blank-import embed, 21 under cmd/ which the corpus does not
  contain, ONE corpus instance -- crypto/internal/fips140test/acvp_test.go, and one is enough because it compiled
  before my seat. (COORD read 21/20/1; the corpus figure agrees, the cmd/ figure differs by one, immaterial.)
- ARM for (3): a real `-tests` conversion of BOTH a blank-import and a named-import fixture, asserting the emitted
  initializer names go.embed_package and the regression's own spelling appears nowhere -- both forms, so the rule
  cannot later be split. Made to fail by restoring the bare alias: both subtests red, restored byte-identical. Arm 9
  asserted the old spelling and reddened on this change, the arm doing its job; it now asserts the qualified form.
- EMISSION at the pin, a READ: acvp_test.cs:84 is `go.embed_package.ΔEmbedBytes<byte>(asm, "go.embed/crypto/
  internal/fips140test_test/", "acvp_capabilities.json")`; NO bare `embed.` identifier survives in the file; its
  tests csproj carries the ProjectReference to core/embed/embed.csproj so the name resolves. embedtest unchanged but
  for the same qualification: 14 initializers, 11 resource items, the named-type casts intact.
- GATES: facet ref go2cs ok 147.4 s / repoguard 33.8 s; embed ref go2cs ok 140.8 s / repoguard 20.9 s; releasestamp
  ok, empty fail sets; gofmt clean; census clean on all four files and both messages. STILL UNCOMPILED:
  AdapterPassSeparationTests.cs and embed_impl.cs, never compiled here -- which is how all three shipped. The i7's
  re-run is the measurement, not my prediction.
NEXT: STANDBY.
