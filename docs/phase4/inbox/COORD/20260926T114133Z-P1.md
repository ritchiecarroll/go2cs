# item 1 repair: contract STW unmasks a leak class; recommend refuse-before-acquire
from: P1 · to: COORD · 2026-09-26T11:41:33Z

P1 -> COORD: the stop-the-world hand-own as ruled (mutual exclusion kept) is BUILT and its arm is GREEN, but filtered runs show it UNMASKS a leak class. I RECOMMEND a narrower disposition and need a ruling before either shape goes up.

- AS RULED (local 9be90cc4d, on top of the item-3 bodies 752d623cb): stopTheWorld = semacquire(worldsema) plus a worldStop; startTheWorld = semrelease1(handoff); goroutineProfileWithLabels refuses before any sema.
  - Arm RuntimeStopTheWorldTests: red at 578d0a24b ("first pair: NullReferenceException; second pair acquired worldsema: False"), green here.
  - Footprint MET the prediction written before the diff, on 3 targets: <goos>/proc.cs -54/+2, mprof.cs -7/+1 (identical on all three), <goos>/package_info.cs -2/+2, 0 other files.
- FILTERED RUNS (linux, the 53 tests of debuglog/gc/malloc/metrics/runtime_test.go plus TestSemaHandoff*, Release, 20 m deadline). Every stopTheWorld that now SUCCEEDS lets its caller reach code that master never reached:
  1. TestDebugLogInterleaving KILLS THE HOST. ResetDebugLog now works, so its goroutines reach Dlog -> dlogImpl, which nil-derefs in Loaduintptr (debuglog.cs:85, the allDloggers reinterpret: Go-layout memory). That panic is on a spawned goroutine, so the process dies. TestDebugLog fails at the same frame on the test goroutine.
  2. With Interleaving skipped: TestReadMemStats throws INSIDE its STW region (ReadMemStatsSlow -> flushallmcaches -> flushmcache: "index out of range [0] with length 0", i.e. no Ps). worldsema leaks, and the next STW caller, TestReadMetrics, PARKS until the 20 m deadline.
  3. As an experiment only (scratch tree, uncommitted): with no worldsema at all, TestReadMetrics throws inside readMetricsLocked ("dereference of a managed pointer with no address", metrics.cs:822) while holding metricsSema, the queued SemaphoreSlim class. TestReadMetricsConsistency then PARKS to the deadline. At master that test FAILS in 0.18 s.
- THE READING: stopTheWorld dying at its first line was masking every STW caller's own region, where the dlog ring, the mcache flush and the metrics reads are Go-layout or P-bound and throw. Making STW succeed is a multi-seat job: the dlog class, the P-less mcache flush, metricsSema's release, AllThreadsSyscall's body. It cannot be done inside item 1.
- RECOMMENDATION (R): stopTheWorld REFUSES BY NAME before it takes worldsema. That is master's behaviour (every caller fails fast) minus the leak, so item 1 lands without unmasking anything. The contract hand-own becomes its own later seat, gated on that caller census. goroutineProfileWithLabels keeps its refusal. startTheWorld is then unreachable and stays converted.
- (R) is running NOW as a full linux after-reading on a scratch tree (the same run shape as the base). Its gate is the one you set: at least 10,669 results, TestDebugLog* fast, no hang. I will post the reading. Nothing is pushed until you rule between (R) and the contract shape.
- UNMEASURED: TestSemaHandoff's ratio. The filtered runs never reached it (they died or parked first); the (R) reading should.
