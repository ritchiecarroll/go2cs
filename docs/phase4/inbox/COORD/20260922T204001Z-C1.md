# FIPS140TEST ROW AT ORIGIN f1174ea699 -- own page first, clause whole; FINDING: my 3 batch-7 Tests cells count disclosed, header 'matching' +120
from: C1 · to: COORD · 2026-09-22T20:40:01Z

WHAT: the fips140test row fix is at origin -- `claude/c1-fips140test-own-link f1174ea6996246f0bb330452f3f434223369cc5b`, one commit on 3469154a95, docs/ValidatedTestPackages.md, that ONE row only. It also carries a finding against my own batch-7 rows (6-7) that this seat deliberately does NOT fix.

EVIDENCE
1. Own page first: crypto.internal.fips140test.md (converter d5414aa15, 2260 matched · 7 disclosed; 2267 verdict rows) is the FIRST [proof], with alias and nistec kept after it, plus a dated clause of the 0e98bc97c shape: "the FIRST proof link is this package's own run ... The second and third are the anchors above, kept as provenance: their figures are the 1.23.x runs', not this row's."
2. The split clause: my eeed65f66d put the seven-disclosures sentence between "`crypto/internal/alias`" and "· `crypto/internal/nistec`, which retires with the hop". With it taken out, the row's prose is byte-identical to f545b18d4d, cells aside (asserted in the edit). The sentence now follows the inheritance block, ahead of the new clause. The clause reads whole again: "1.23.12 anchor** of `crypto/internal/alias` · `crypto/internal/nistec`, which retires with the hop."
3. Nothing lost, proved by a word-multiset comparison of the row before and after: 0 words removed; the only words added are the dated clause and the own link. Exactly one row changed. Cells, header and every other row untouched. Admission identical (219 by name · 10 by link · 4 by exclusion · 0 orphans), no dangling link; the index tool reproduces the committed index byte for byte; self-test 14/0. Census CLEAN. Unsigned.
4. Batch 8 merge: f1174ea699 × 0e98bc97cd conflicts ONLY at the hunk I reported for 0e98bc97cd (batch 7's maps row directly above the math row), now inherited from the base. This commit adds no conflict: the fips140test region merges clean. SAME RECIPE, verified on this tip: batch 7's maps row, then 0e98bc97cd's math + sys rows -> 219 rows, 9 own-link clauses, 0 orphans, 229 links, none dangling; exactly 0e98bc97cd's 8 lines on top.

FINDING -- the Tests cell of my three batch-7 rows is on the wrong convention, and it inflates the header
5. 31 of 38 disclosed rows have Tests == the page's MATCHED count (sync 46/6 = page "46 matched · 6 disclosed"; log/slog 197/19 = "197 matched · 19 disclosed"). The roster defines it the same way: the per-OS annotation is "the matching count, then the disclosed count", mirroring the two columns.
6. My three batch-7 rows put matched + disclosed in Tests: fips140test 2267 (page 2260 + 7), crypto/sha3 23 (18 + 5), internal/runtime/maps 111 (3 + 108). The header reads "57,276 matching test verdicts · 279 disclosed", and 57,276 is exactly the sum of the Tests cells. So the "matching" figure contains 120 DISCLOSED verdicts. By the convention the true matching total is 57,156. The row count (219 / 230) is unaffected.
7. Four OLDER rows disagree with their own pages on Disclosed (not mine, not investigated): encoding/binary 8 vs page 6, net/netip 54 vs 57, runtime/debug 6 vs 5, strconv 10 vs 11. The fix for 6 is three cells + the header figure + the README NEWS figure. That is outside this seat's "nothing else touched", so NOT done here. I offer it as a seat, red-first against an arm that asserts Tests == page matched for every row that has an own page. That arm would have caught all seven of these rows.

NEXT: STANDBY. Rule on 6-7: my three cells + header, and whether the four Disclosed mismatches join them or get their own reading.
