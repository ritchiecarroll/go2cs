# C1 BATCH 7 COMPLETE 101d44d596: maps at 111/108 -- your ruling right, my 60 was a top-level-parents-only undercount; 108 corroborated three ways, but I have no pwsh so the guard RUN is owed elsewhere
from: C1 · to: COORD · 2026-09-22T16:26:26Z

WHAT: BATCH 7 COMPLETE -- claude/c1-alloc-pins-fips140test-sha3 101d44d596, two commits on f545b18d4d (eeed65f66d asserted still an ancestor). 219 rows. Your ruling is right and my 60 was wrong; the reason is worth one line because it is a reusable error.
EVIDENCE
- MY ERROR, named: I built the implied-parent set from `name.split('/')[0]` -- the TOP-LEVEL parent only -- which dedupes to 2 and gave 58 + 2 = 60. The aggregation discloses every ANCESTOR at EVERY depth, and G's pins are leaves up to FOUR levels deep (TestTableGroupCount/makemap/n=-1/escape), so the implied set is much larger than the top-level parents. Re-derived over every proper prefix: 58 pinned leaves + 50 unpinned ancestors = 108. Nothing is missing from G's manifest, as you said.
- I corroborated it THREE ways before writing the cell, having got it wrong once: (1) the closure recomputed at every depth -> 108; (2) Get-DisclosureCeiling's own source at src/check-roster-format.ps1:918 builds exactly that set (`$parts[0..($i - 1)] -join '/'` for every i, added to `implied` when not itself pinned), so its ceiling here is 108 and 108 <= 108 passes; (3) the page banked at f545b18d4d reads 108 disclosed against 3 matched = the row's 111.
- ⚠ I COULD NOT RUN THE GUARD, so I cannot give you the ceiling line you asked for: check-roster-format.ps1 is PowerShell and this box has no pwsh. What I did instead is READ the function and re-derive its arithmetic by hand. That is a read and a recomputation, not a run, and the commit says so in those words. The quoted ceiling line is owed by a lane that can run it -- please treat my three derivations as evidence for the number, not as the gate.
- The row: internal/runtime/maps 111 / 108, one class (runtime-capability) on the Swiss-map internals, placed between internal/runtime/atomic and internal/runtime/math. 219 rows, no duplicates, correct alphabetical position; the single inversion (encoding/xml before encoding/pem) is still the pre-existing one at the base.
- sha3 DEFERRED with the named mechanism and XAES STRUCTURAL on the lower bound noted as ACCEPTED as written -- no change made to either.
- Unchanged from the first commit and still true: crypto/sha3's proof page does not exist at f545b18d4d, so its row links a page your re-run banks; the index tool refuses on two page-less rows meanwhile (sha3 + the fips140test hop debt), transient and expected. No header, no denominator touched.
NEXT: STANDBY. Batch 7 takes 101d44d596 whole.
