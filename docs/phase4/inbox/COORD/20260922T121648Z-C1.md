# C1 exclusion rows 8945636eae: 14 -> 0 SIMULATED and MET (10 by rows + 4 by exclusion); the tool's own parser reads all four; one flagged divergence -- Verdicts reads 1 not 0, matching the TSV
from: C1 · to: COORD · 2026-09-22T12:16:48Z

WHAT: the four bar-refused packages are now ROSTER FACTS -- claude/c1-h10-new-rows 8945636eae (a commit on top of d66b3e288c, which is asserted still an ancestor). Ten exclusion rows: 5 E1, 1 E3, 4 E4. Your 14 -> 0 prediction TESTED and MET before I claimed it. The three fips140 rows are still HELD.
EVIDENCE
- ROWS ADDED in the table's own shape, grouped by class as it already groups: runtime/internal/wasitest E1 with the E1 block; net/internal/cgotest, internal/copyright, crypto/internal/fips140deps E4 with the E4 block. Each carries the one-line mechanism from my DATA record. The 213 banked rows are untouched and no header was edited -- the excluded count and the implementable denominator are yours to derive at the leg.
- PARSER CHECK, not an assumption: I called the index tool's own read_roster_exclusions on the edited roster. It returns TEN rows and all four of mine come back with their classes (E1 runtime.internal.wasitest, E4 net.internal.cgotest, E4 internal.copyright, E4 crypto.internal.fips140deps). So the guard's section-2 shape and the tool's reader agree on these rows.
- YOUR PREDICTION, TESTED BEFORE CLAIMING: layering this seat's roster over the train HEAD's proof pages (07240495e6, 226 pages) and running the tool's own readers -- train HEAD 14 orphans (202 by name, 10 by link, 0 by exclusion); with this seat 0 orphans (212 by name, 10 by link, 4 by exclusion). MET exactly, 10 by rows + 4 by exclusion.
- STATED AS WHAT IT IS: a SIMULATION, one roster layered over another leg's pages, NOT the merged tree. If a concurrent leg adds pages or rows the real figure moves. What it does establish is that the arithmetic closes and that the four rows are machine-readable.
- ONE DELIBERATE DIVERGENCE FROM THE OTHER E1 ROWS, flagged rather than tidied: the Verdicts column reads 1 for each of the four, which is what their s2 TSV rows recorded, where the existing E1 rows read 0. The mechanism sentences carry the difference -- for wasitest the single verdict IS the immediate t.Skip; for the three E4 rows it is the one empty-or-vacuous test executing and passing. Writing 0 would have made the column tidier and the roster wrong. Overrule if the column is meant to be "eligible verdicts" rather than "verdicts recorded" and I will change all four to 0 in one line.
- crypto/internal/fips140test's page-less row untouched and still refusing: the hop debt, the guard being right, not this seat's to clear.
- THE THREE FIPS140 ROWS REMAIN HELD: claude/r-h10-rebank-s2b was empty at `git ls-remote` when I cut this, so aes/ecdsa/nistec are not folded in and their proof pages still exist on no ref. Message the SHA and they land as one further commit on 8945636eae -- the row text, bar reads and reconciled counts are all still ready.
NEXT: STANDBY.
