# SIZED, NO CUT: 9 in scope (5 shapes, 3 new at 1.24.13 on G's lane); converted mode is stricter than the gate of record; my 9210bc85f4 broke a ruled parity -- 2 rulings
from: C2 · to: COORD · 2026-09-22T20:03:08Z

WHAT: SIZED, NO CUT -- reading the Go gate changed the premise, and my landed 9210bc85f4 broke a ruled parity. Two
rulings asked. Shapes only below; every value was read in public GOROOT source.
EVIDENCE:
- SCOPE: 15 packages; 247 committed test artifacts at 8fc439415f + 274 GOROOT 1.24.13 test sources staged under a
  scratch src/core path (converted mode judges path SHAPE, not tracking); master census 998fc49d03. Harness
  controlled positively: the pre-seat definition yields slog's four.
- COMMITTED: 6 refusing hits in 2 files. GOROOT 1.24.13: 9 -- the same 6 plus 3 NEW in os/user/lookup_unix_test.go,
  selected only on linux with CGO off, i.e. G's lane; the committed os/user came from a Windows emission, so G's
  PASS 3 re-emission is what brings them. The other 29 tree-wide refusals are outside these packages.
- (i) net/http serve_test (TestRedirect) unc_slash x2, one fp: a schemeless URL, host = the foo-bar placeholder
  plus a dot-com. (i) syscall exec_windows_test (TestEscapeArg) unc_backslash x2, one fp: a UNC host that COMPOSES a
  word with the already-admitted server placeholder. (i) os/user lookup_unix_test (TestFindGroupId,
  TestLookupUserPopulatesAllFields) home_unix x3, two fps: home dirs of two synthetic accounts in Go's fake passwd.
- (ii) syscall exec_windows_test (TestEscapeArg): profile_root x2, one fp -- a profile segment that is a non-ASCII
  personal first name, Go's pick for testing UTF-16 escaping. Policy (a personal name in a shared admit set) AND
  mechanism: this box's mawk counts it as 10 bytes and never folds its capital, gawk-UTF8 (the i7) would.
- (iii): NONE. WHY NO CUT for (i): profile_placeholder is "carried exactly" from the Go gate's fleetPlaceholderSegments, which is
  consulted on EVERY tracked non-test file. Mirroring fixture names into it widens the gate of record everywhere to
  clear test data.
- THE DECISIVE FACT: the Go gate never runs the STRUCTURAL pass on *_test.cs (fleetIsUpstreamFixture -- only the
  denied-token pass; its own comment: "skipping neither is 2,500 false positives"). So all 9 already PASS the gate of
  record; converted mode refuses them only because it downgrades 2 arms where the gate downgrades every structural
  arm for test sources.
- RULING 1: should converted mode MIRROR fleetIsUpstreamFixture -- mark profile_root, home_unix, unc_backslash and
  unc_slash [CONVERTED-CONTEXT], with the denied-token and runtime arms still refusing? One mechanism clears these 9
  and the 29, matches the gate of record by construction, and a denied host inside any of those shapes still refuses
  by its token arm -- the mitigation both gates already rely on. If yes I cut it with arms: each structural arm
  reports, never refuses, on a converted source; a planted denied token inside a UNC and a profile path in a
  converted source still refuses.
- MY DEFECT: 9210bc85f4 added the hex branch to the census's unc_escape but NOT to the Go gate's
  fleetIsUnicodeEscapeSegment, whose doc reads "PARITY with the census's unc_escape admit ... ruled 2026-09-20". I
  did not read the Go gate. Measured with a throwaway probe (never committed): on a NON-test path the Go gate
  refuses a hex-escape UNC host (findings=2) that the census now admits; on a *_test.cs path it runs no structural
  pass (0). So the pre-push census is now WEAKER than repoguard on that one shape on non-test surfaces -- not a
  false green at the gate of record, which still catches it, but the two disagree. And I told you (a) blunted the
  arm; against the gate's own design, (a) is what the gate of record already does.
- RULING 2, the repair: (a) mirror the hex branch into fleetIsUnicodeEscapeSegment -- x plus exactly two hex, no
  tail, a Go arm red-first -- restoring parity; or (b) revert the census hex branch, right if Ruling 1 is yes, since
  slog then clears by the downgrade and the admit has no job left. I lean (b) if Ruling 1 is yes, (a) if no. Either
  is small; I cut on your word.
ASK: Rulings 1 and 2. NEXT: STANDBY.
