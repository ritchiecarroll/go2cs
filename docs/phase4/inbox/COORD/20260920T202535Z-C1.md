# seat (9) CUT: claude/c1-struct-conv-construction da6cf0f5dd -- empty-struct conversion constructs; 1-line A/B, 3 neuters, suite+repoguard green
from: C1 · to: COORD · 2026-09-20T20:25:35Z

WHAT: seat (9) CUT and pushed. claude/c1-struct-conv-construction = da6cf0f5dd, ONE commit on
0adf2e4318 (asserted an ancestor), 3 files, +287/-0, UNSIGNED (no secret key here).

DIAGNOSIS, read at the tree rather than taken: I converted unique with -tests into a fresh scratch
root and read handle_test.cs:65 myself. It is `testHandle(Ꮡt, ((testZeroSize)new EmptyStruct()));`,
exactly as G sized it. The reason is one line up, at :45 -- `[GoType] internal partial struct
testZeroSize {}`. A [GoType] with NO underlying argument is a struct DEFINITION, not a wrapper
(compare `[GoType("@string")] testString` in the same file), so it declares no conversion operator
for the cast to bind, and golib's EmptyStruct is a different struct entirely. CS0030.

WHY NEITHER EXISTING ARM REACHES IT (you asked me to look first, and the look paid):
- the composite-underlying hop (convCallExpr ~1441) is restricted to Map/Slice/Array, and its own
  comment says why -- a struct underlying has no nameable C# cast target. Deliberately excluded.
- the constructor route (~1504) wants an EXPORTED test-file-declared target over an UNEXPORTED
  NAMED struct. Here the target is unexported and the argument is ANONYMOUS. Two misses, not one.
So there was no existing rule to join; the arm is new, placed beside those two.

THE RULE: target and argument both named/unnamed structs with ZERO fields and not identical ->
`new T()`. A zero-field struct has exactly one value, so the construction IS the conversion --
total, not an approximation. And the shape is the corpus's own: 200 zero-field [GoType] structs
exist in src/core and 15 sites ALREADY emit `new X()` over 7 of them (`new sigset()` x8), in a tree
that compiles clean. That is the closest thing to a compile proof I can offer from here.

THE ONE JUDGMENT CALL: `new T()` DISCARDS the operand's emission, so the arm is gated on an operand
that cannot carry computation -- an empty composite literal, or isPureReadExpr (your existing
helper, reused unmodified; a CompositeLit is not one of its cases, and widening it there would
change the contract the string-view analysis depends on). `T(f())` does NOT take the arm: it keeps
the cast and fails LOUDLY at compile time rather than silently dropping the call. I chose loud over
convenient. Say so if you want it the other way.

POPULATION at the pin: `T(struct{}{})` appears 6 times in GOROOT/src; FIVE are function calls
(reflect's `V` is `var V = ValueOf`, so 4532/4534/7520 are not conversions) or uncompiled testdata.
unique's is the only genuine one. Reverse spelling `struct{}(x)`: no struct site at all -- maphash's
`chan struct{}(nil)` is R's bare-chan class, already closed.

A/B ON THE ROW, base binary vs fixed, same input, a FRESH output root each (never two into one):
exactly ONE emitted line changes across the whole package --
`((testZeroSize)new EmptyStruct())` -> `new testZeroSize()` -- plus go2cs_test_manifest.json's
converterRevision and inputDigest, which move whenever the binary does. Nothing else in 20 files.

GUARD (emptyStructConversion_test.go, registered in projitems, BOM kept, LF preserved): four
positives and THREE controls, each read at its OWN emitted statement via a line-binding helper, so
no control can pass vacuously off another arm's positive. Proven against three neuters:
  arm disabled         -> all four positives report their pre-fix emission, and the first reports
                          `((zeroNamed)new EmptyStruct())` -- the corpus emission verbatim, so the
                          fixture reproduces the real defect rather than resembling it;
  operand gate off     -> the CALL control reports `new zeroNamed()`, the call silently dropped;
  field-count gate off -> the field-bearing control reports `new pairA()`, X silently zeroed.
Each neuter restored byte-identical by sha256.

GATES: suite `go test -count=1 ./...` -> ok go2cs 124.5s, ok repoguard 18.0s UNCACHED, 0 FAIL,
exit 0. gofmt clean, go vet clean. Census CLEAN on the staged diff. git status unfiltered shows only
the three intended paths, no deletions; three files staged BY NAME, never -A. Build output was moved
out of the worktree before staging.

NOT RUN, not claimable here: any C# compile. The BUILD leg -- unique CS0030 -> 0, with the CS0411
class already retired by R's batch-3 seat -- is the i7's.

NEXT: idle. Standing by for the rehearsal's fips140test evidence.
