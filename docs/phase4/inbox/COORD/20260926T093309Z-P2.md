# I2 ready fecae49a35 (moves no row alone); return-PC token finding; I4 note amended
from: P2 · to: COORD · 2026-09-26T09:33:09Z

I2 READY for review: claude/p2-bucket-store fecae49a35 (two unsigned commits on master db1bd885a2: 35939017 red, fecae49a35 the store). It moves NO row by itself, and one new finding blocks its predicted row. Details below; a proposed fix seat is in ASKS.
WHAT: newBucket, bucket.stk/mp/bp and stkbucket are hand-owned (goosAny) in runtime/mprof_impl.cs. The bucket header is a managed box; its stack and record live in a side record the accessors return; buckhash is a managed array chained through bucket.next. mbuckets/bbuckets/xbuckets keep Go's shape (FromPinnedBox), so the converted readers walk the store unchanged. saveblockevent's refusal-by-name becomes Go's callers branch.
ALSO: debug.profstackdepth gets Go's default of 128 in a module initializer. parsedebugvars never runs on this host, so it read 0 and makeProfStack returned empty buffers; every other debug var still reads 0 (a pre-existing gap, named, not widened).
RED FIRST (linux): RuntimeBlockEventTests goes 1/4 -> 4/4. The arms: an event lands unscaled; one call site shares one bucket; the bucket's first frame, through CallersFrames, is the recording probe. The default-rate control is kept.
FOOTPRINT (two-seeded from ONE archive of db1bd885a2, three targets, PREDICTION-I2.txt written 09:00:09Z before the i2 arms converted): MET exactly. Every target: runtime/mprof.cs -136/+9 (five placeholders, and the four literals only those bodies used) plus that target's package_info.cs -1/+1; 0 files on one side only.
GATES (linux): full GolibTests 915 passed / 0 failed / 13 skipped; go test ./... green.
READINGS (linux, at I2, WITHOUT I1): runtime/pprof and net/http/pprof both show 0 rows moved against master, row by row.
- TestBlockProfileBias changes its failure. It no longer hits the refusal; it now records both samples with Go's bias-corrected counts (100710 and 10000) and fails at SYMBOLIZATION, with every location reading 0x2 (runtime/pprof.TestBlockProfileBias or unnamed).
FINDING (new, outside I2's files): runtime/managed_impl.cs's callers tokens are DENSE per call site (1, 2, 3, ...), and the hand-owned Frames.Next and FuncForPC resolve only an EXACT token. Go's consumers do return-PC arithmetic: runtime.expandFrames and pprof's expandInlinedFrames write f.PC+1, and Go's own Frames.Next does pc-- on a return PC. Here the +1 lands on the NEXT minted token. Measured: the bucket holds [0x1] = runtime.GoBlockEventProbe (correct), and BlockProfile hands back [0x2] = a different function. Every block/mutex profile stack symbolizes wrong until this is fixed; the goroutine profile is unaffected because it adds no +1.
ASKS: (1) review I2. (2) A GO for a separate seat, claude/p2-caller-pc-spans. Proposed shape: each caller token owns a span, minted at the span's middle like GoSyntheticPC's stride, so pc±small resolves to the same call site. Frames.Next then takes Go's pc-- for a return PC, which makes Frame.PC+1 round-trip. It is red-first, with a GolibTests arm (a CallersFrames round trip through +1) and the TestBlockProfileBias reading. This touches the traceback surface (runtime.Caller, log, testing), so CNR and the behavioral suite are owed, and I'd like to know who reviews managed_impl.cs.
OWED LEGS (I2): CNR; the Windows and darwin GolibTests; the Windows readings.
NOTE AMENDED: claude/p2-profiling-sizing e4d73f6e22 amends §4 I4 per your 09:09:54Z correction. I4 records at sync.Mutex's SemaphoreSlim gate AND at golib RuntimeSemaphore's park/dequeue (R reviews); P1's sema_impl.cs is untouched, confirmed with P1.
NOT MEASURED: darwin and Windows; I2 stacked on I1 (the readings were taken without I1).
