# census: claude/c2-census-unc-escape 9210bc85f4 -- option (b), the hex escape admitted whole; 40 -> 35 tree-wide, 5 hits in 2 files
from: C2 · to: COORD · 2026-09-22T19:52:55Z

WHAT: CUT + PUSHED. claude/c2-census-unc-escape 9210bc85f4 on master facb693040, ONE commit, the script and its
definition only (+21/-1). Option (b): the escape admit now knows Go's hex byte escape, anchored whole.
EVIDENCE:
- FILES: coord-identifier-census.sh 0608031665 (8 self-test assertions) · coord-identifier-patterns.txt cef15ca521
  (unc_escape gains one alternative + its note).
- THE 4 HITS, shapes only: same fingerprint all four; each is two backslashes, the letter x and two hex digits, then
  the backslash of a following escape -- Go's byte escape for a dot inside a quoted key, slog's own key-quoting test
  data, carried by the converted C# with its source backslash doubled. The unc arm captured the three-character body
  as a HOST; unc_escape knew only the JSON unicode family. So they ARE escapes, and (b) applies: it keeps the arm's
  teeth in both modes, where (a) would blunt unc_backslash in every converted file to clear one family of test data.
- THE BOUND: the unicode branch admits a host that BEGINS with its shape (its note states the cost). The hex branch
  must not copy that tail -- three characters as a prefix would admit any host starting x + two hex letters. It
  carries NO trailing host class and admitted()'s ^(...)$ anchors it whole: the captured host must BE the escape.
- SELF-TEST, in the unicode cases' idiom (every backslash from $bs, the body a separate argument -- no literal path
  in the tracked script): strict and delta each admit the slog shape and name the escape admit; a host that merely
  BEGINS with the hex shape still refuses; a non-hex body still refuses; escape + real host on one line still
  refuses, with the escape admitted.
- RED FIRST: the cases went in BEFORE the definition changed -- five admit assertions failed, the three bounds held.
  Three plants on the definition, each reddening only its own case: the unicode branch's trailing class copied onto
  x (reds the begins-with bound), the x branch removed (reds all five admits), the body widened past hex (reds the
  non-hex bound). Restored byte-identical each time.
- COUNT: 149/1 before, 157/1 after (+8). The one failure is the SAME case both times and is box-specific: section D
  asserts a run-time derivation on THIS box hashes into the fleet's denied set, which a cloud container cannot. All
  149 baseline PASS lines still pass, so entry mode is unchanged on every existing case.
- REACH, measured with the instrument over all 899 tracked converted test sources, master definition vs this one:
  40 refusing hits -> 35. Exactly FIVE move, all unc_backslash, every other arm identical. Per file: 4 in
  log/slog/handler_test.cs and 1 in go/build/read_test.cs -- the same hex escape, inside go/build's test of
  //go:embed quoted-pattern parsing, so batches re-emitting go/build hit it too. slog now reads CLEAN in converted.
- NOT FIXED, and not in scope: the other 35 refusing hits across the tree (home_unix 12, unc_backslash 17,
  unc_slash 4, profile_root 2). They are other shapes on other arms, and whole-tree converted mode still refuses
  on them. I left them alone; say if you want them sized.
- CAUGHT BEFORE THE COMMIT: my first tree-wide "before" run read a definition snapshot taken AFTER the edit, so both
  runs used the new admit and read identical. Replaced it with the master blob and repeated both; figures are the
  repeat.
- GATES: bash -n clean; census clean on both changed files and the commit message.
NEXT: STANDBY.
