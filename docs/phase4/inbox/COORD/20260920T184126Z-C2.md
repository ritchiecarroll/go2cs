# seat (5) sha3 sites read -- the ruled root confirmed, plus two elements it does not reach
from: C2 · to: COORD · 2026-09-20T18:41:26Z

SEAT (5) crypto/sha3 -- SITES READ at 6d814e2d38 (a read, not a compile). The ruled root is
confirmed AND it is not the whole of it: two more elements change the fix's shape, so they go
to you BEFORE I cut. Nothing pushed; I hold for the batch-2 tip.

CONFIRMED, the ruled root. sha3's external test carries `using sha3 = go.crypto.sha3_package;`
and spells its casts `new sha3.SHA3жHash(...)`. splitAdapterStructReference gives qualifier
`sha3`; adapterStructKey trims only a `_package` SUFFIX, so an ALIAS qualifier survives verbatim
and the key composes `sha3_SHA3жHash`. The records are BARE (`GoImplement<SHA3, hash_package.
Hash>`), so their key is `SHA3жHash`. The generator's AdapterStructKey resolves the SYMBOL --
container `sha3_package` == packageClassName -> bare `SHA3` -- and its own doc says the two
"must agree or the collision groups diverge". They diverge. hash_package.Hash and
fips140_package.Hash both map to `SHA3жHash`, so the generator prefixes BOTH (AdapterInterface-
Prefix is "" only for a LOCAL interface; neither is), and NO member named `SHA3жHash` is emitted.

(A) THE RECOMPILE MODEL NEVER REACHES anchoredAdapterMemberName AT ALL. testConversion.go:1358
    branches on `model == testProjectWhiteboxReference`: that arm captures with an anchorClass
    and calls resolveAdapterNameMarkers WITH the anchor; the else arm (:1362-1363) passes
    neither. flate is whitebox-reference -- which is exactly why it works, and why its sites read
    `flate_test_package.bytes_BufferжWriter`. sha3's manifest says testProjectModel `recompile`,
    so it takes the else arm: emittedAdapterPairAnchors is never populated for it, and the
    locality test inside anchoredAdapterMemberName could not fire even if reached. A shared
    locality resolver WITH anchoredAdapterMemberName therefore does not, by itself, reach sha3.

(B) A SECOND BROKEN SITE CLASS, from pass separation rather than locality. Under recompile the
    tests csproj compiles `sha3.cs` and `package_test_info.cs` but NOT `package_info.cs`
    (crypto.sha3.tests.csproj:104-108). sha3.cs's four production sites read bare `SHA3жHash` --
    a name computed against the production record set (ONE record, no collision) -- while the
    generator in that compilation reads the TEST record set, where it collides. So the
    production file carries a name resolved against a record set the generator will not use
    there. The alias key is not involved; this one is the pass boundary.

CONTROLS held. IN-PACKAGE: SHAKE takes io.Reader and io.Writer -- different simple names, no
collision -- so `sha3.SHAKEжWriter`/`ReaderжReader` are correct bare TODAY and must not move;
they are the proof the alias spelling itself is not the error, only the group-key divergence is.
FOREIGN: flate's own Writer is recorded `global::go.compress.flate_package.Writer`, whose
qualifier DOES carry the suffix, so both sides compose `flate_Writer` and agree; flate's
`bytes_Readerжio_Reader` / `bytes_Readerжflate_Reader` are a colliding group resolving correctly.

ASK: does the ruling cover (A) and (B)? I can cut the locality resolver alone, or the resolver
plus the recompile arm taking the anchored path, or all three. Name it and I cut on your tip.
