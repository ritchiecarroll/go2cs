# seat (8) CUT: claude/c1-testing-b-elapsed a5249dc702 on the batch-2 tip -- B.Elapsed zero Duration + the stale doc sentence
from: C1 · to: COORD · 2026-09-20T20:10:34Z

WHAT: seat (8) CUT and pushed. claude/c1-testing-b-elapsed = a5249dc702, ONE commit on the
batch-2 tip (0adf2e4318 asserted an ancestor), ONE file, 32+/3-, UNSIGNED (no secret key here).

THE CUT (src/core/testing/testing.cs):
1. `[GoRecv] public static time_package.Duration Elapsed(this ref B b) => default;` placed with
   the other no-op timer members, after StopTimer, with the remark as ruled.
2. The stale BenchmarkResult sentence ("the shim stays time-package-free") replaced, per your
   ruling, pointing at Deadline and Elapsed instead.

CONSUMER CENSUS at the corpus pin -- every .Elapsed() site in GOROOT/src with its ENCLOSING
declaration, which is the reading that decides the capability question:
  runtime/runtime_test.go:708    func BenchmarkMutexCapture(b *testing.B)   <- the runtime row
  testing/benchmark_test.go:212  func ExampleB_ReportMetric()               testing is hand-owned
  testing/benchmark_test.go:242  func ExampleB_ReportMetric_parallel()      testing is hand-owned
  testing/benchmark.go:392       func (b *B) stopOrScaleBLoop()             Go's own impl
ZERO Test functions reach B.Elapsed. Naming the enclosing decl mattered: a bare site list would
have left "could a Test observe the zero?" open, and it is the whole question.

DECISION I MADE AND AM FLAGGING: supportedTestCapabilities() is NOT widened. The list carries
B.N and B.Loop (plus testing.Benchmark and BenchmarkResult.NsPerOp) precisely because a Test can
reach them through testing.Benchmark -- TestCalibrate's shape. B.Elapsed has no such Test, so by
the census above widening would admit and exclude nothing, and charter §9 wants that measurement
BEFORE a widening, not a widening on faith. B.ReportAllocs is the standing precedent: declared on
the host, deliberately absent from the list, and asserted absent by
TestUnsupportedTestingCapability. The sole converted consumer is a benchmark DECLARATION, gated by
KIND in discoverTestDeclarations and never registered, so the zero is never observed. Say the word
if you want it listed anyway and I will cut that separately with the §9 measurement.

RESOLUTION CHECK: the file is `namespace go;` and its usings do not name time_package -- it
resolves as go.time_package by namespace walk, exactly as T.Deadline's `time_package.Time` does in
this same class at line 261, which compiles today. testing.csproj:83 already carries
../time/time.csproj, so no project reference is added.

GATES RUN: preconditions re-read AT THE TIP before writing (testing.cs is byte-identical across
batch 2 -- batch 2 touched only PackageAncestry.cs in that directory): struct B still N +
LoopIteration; the three timer members still empty bodies; Benchmark's Stopwatch still a driver
local; no pre-existing B.Elapsed (the file's one "Elapsed" is Stopwatch's, line 181). CRLF 812/812
preserved, no BOM introduced, zero curly apostrophes (the file's convention, 0 at HEAD). Census
CLEAN on entry and subject, shared arms materialised from origin/master. git status unfiltered
empty; ONE file staged by name, never -A.
FLOOR 13 on the apply tool: re-running it REFUSES ("hunk-2 target occurs 0 times") and leaves the
file byte-identical by sha256 -- the guard was made to fire, not assumed.

NOT RUN, and not claimable from here: any compile. No .NET on this box. The BUILD leg -- runtime's
tests project past the missing member -- is the i7's, batch 3 or its successor, as routed.

NEXT: idle for routing. My earlier three refs ride batch 3 unchanged.
