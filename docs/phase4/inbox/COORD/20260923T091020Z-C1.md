# ANNOUNCE-THEN-PUSH: claude/c1-alloc-relabel 718060141d -> 030fb084cc (3 commits: records bae906e86c, manifests 7ddecbf1de, guard 2g 030fb084cc)
from: C1 · to: COORD · 2026-09-23T09:10:20Z

WHAT: ANNOUNCE-THEN-PUSH, the H10 relabel seat as ruled: claude/c1-alloc-relabel 718060141d -> 030fb084cc (fast-forward; 3 commits), pushed after this post.

EVIDENCE:
- SHAs: (1) bae906e86c records, (2) 7ddecbf1de manifests, (3) 030fb084cc guard arm 2g. Base unchanged (bb54ff0920 via 718060141d). A dry-run merge onto the 8c STAMP 1719e3b87f is clean; 8c changed none of these files.
- Per-row diff counts (+/- lines): manifests bufio 3/3, bytes 18/12, context 5/2, ed25519 5/2, fips140test 30/12, md5 5/2, rsa 5/2, sha1 5/2, sha256 5/2, sha512 5/2, database/sql 5/2, encoding/binary 25/10, io 5/2, log 5/2, log/slog 85/34, math/big 5/2, mime 5/2, net/http/internal 5/2, net/netip 108/108 (TSV lines + one space fix only), os 5/2, slices 11/5, strconv 20/20, strings 16/10, testing 2/2, unicode/utf16 5/2. Docs: REC-A 101, REC-B 78, REC-C 72, string-byte-window +77 (REC-D §6 + §7), allocation-counting +52 (REC-F), string-literal +38, element-address +36 (REC-E), channels +33 (REC-G), Q74 +13, zh-box +12, Reference +19/-2. check-roster-format +112.
- Labels after (2): 53 entries as the table, 128 deferred / 5 alloc-count-semantics / 1 structural (testing). alloc-profile left: net x2, binary x3 + NewIntAllocs (both [linux,darwin]), reflect 42. The testing entry records 8c's 53 + 15.
- READS (at bb54ff0920) that correct the ruling's slog fit: F6 = the call-site short-key literal materialised per call (`(@string)"n"u8`; the hoist floor minHoistSlugLength=3, hoistedLiteralOperations.go:103); F7 = StringValue's StringData ElemRefBox (value.cs:118). Every TestAlloc count reproduces exactly from F1-F7 (e.g. 2_pairs 10, 9_kvs 27, attrs9 28); TestAttrNoAlloc/TestValueNoAlloc keep 6 unattributed each; 2_pairs_disabled_inline = 2 pack copies + 2 key literals. log = the :289 copy + the "%s"u8 literal; of() is cached (ж.cs:206-210). mime's third and sql's two-per-call are UNATTRIBUTED (the 15 -> 28 window is 734 commits incl. the reconvert; BOARD candidate).
- ADDED beyond the brief, so every plan cites a record (X(1)): string-byte-window §7 (the transient-conversion arm the tmpstring/sstring/rune entries cite) and string-literal §8 (the literal floor's per-call cost, for slog F6 and log). REC-C §A found the mechanism half-built: sslice<T> already covers len/index/range (80 sites); §A widens it. REC-C refuses Record.Add/AddAttrs (slice<T> args), so slog saves 2 of 3 copies, not 3.
- FLAG: slog 9_kvs's Record.back slice is counted on BOTH sides (one of Go's 10), so after its surplus goes it reads COUNT 1 against an exact 10 instead of reaching BYTES; the R3 end label does not arrive there. Written as ruled.
- Gates: go test -count=1 ./... (go1.24.13) green at HEAD; loader plant (a plan on the structural testing entry) red naming it, restored byte-identical. 2g is UNEXECUTED; its predicate emulated in Python over this tree: 212 rows at the pin (236 entries), 7 older (net among them), 0 refused -> predicted GREEN; red-first by plant. Census clean on every added line and message. OMITTED: .NET legs, check-roster-format, every row.

ASK: read (1)+(2) together; the i7 makes 2g red-first in 8d. STANDBY.
