# item 1 HOLD: semaphore-holder census, the STW four are the population
from: P1 · to: COORD · 2026-09-26T10:13:51Z

P1 -> COORD: the semaphore-holder CENSUS promised in the HOLD. Every non-test runtime semacquire on linux sits behind stopTheWorld/stopTheWorldGC or behind a path already made unreachable. So the four STW functions are the whole population for this class.

- worldsema via stopTheWorld (proc.cs:1433), the path the after-reading hit. Reached by:
  - export_test helpers: ResetDebugLog x2, ReadMetricsSlow, ReadMemStatsSlow, CountPagesInUse, PageCachePagesLeaked.
  - heapdump.go (debug.WriteHeapDump).
  - mprof.go's GoroutineProfile paths (mprof.cs:1347/1414/1561). None is hand-owned in the registry; that is read, not measured.
  - os_linux.go:766 AllThreadsSyscall, the PRODUCTION path under syscall.Setuid/Setgid and friends on linux.
- gcsema + worldsema via stopTheWorldGC (proc.cs:1475): the same body underneath.
- mgc.go's gcStart/gcMarkDone (startSema, gcsema, worldsema, markDoneSema): unreachable, because runtime.GC is hand-owned and nothing on this host drives the pacer.
- trace.go's traceAdvanceSema/traceShutdownSema/doneSema: unreachable (StartTrace refuses, StopTrace is a no-op).
- goroutineProfile.sema (mprof.cs:1344): taken just BEFORE its stopTheWorld, so it leaks the same way when that call throws.
- Outside the class: metricsSema is already a SemaphoreSlim hand-own (managed_impl.cs:374). That is a separate, pre-existing leak class if readMetrics throws between metricsLock and metricsUnlock. I did not create it and have not measured it.
- So the fix population is the four STW entry points plus goroutineProfile's own sema acquire. Every stopTheWorld panic at master today (proc.cs:1574, `gp.m.p.ptr()`) leaves worldsema held, and after item 1 the NEXT caller of any of those paths parks forever.
- UNMEASURED: whether GoroutineProfile is reached by a runtime or pprof row on linux, and whether anything in the row calls AllThreadsSyscall.
