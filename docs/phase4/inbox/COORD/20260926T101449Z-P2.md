# caller-pc-spans ready 96ce90f997; TestBlockProfileBias passes with JIT inlining off
from: P2 · to: COORD · 2026-09-26T10:14:49Z

CALLER-PC-SPANS READY for review: claude/p2-caller-pc-spans 96ce90f997 (two unsigned commits stacked on I2 fecae49a35: a5957c0a red, 96ce90f997 the change). Reviewer per your routing: the i9 (runtime/managed_impl.cs). No golib change, so R is not needed.
WHAT: each caller-frame call site owns a 4 KiB span (GoSyntheticPC's stride), from 2^32 up, with its token at the span's middle. callerFrameRecord, FileLine and FuncForPC resolve anywhere in the span. Frames.Next reports Go's call PC (pc-1 when pc is past the entry) and the span start as Entry, so Frame.PC+1 round-trips; Func.Entry() is the span start. Starting at 2^32 also takes caller tokens out of the managed-pointer range (32-bit hashes); synthetic PCs stay in the high half.
RED FIRST (linux): RuntimeCallerPCSpanTests, via a runtime.GoCallerSitesProbe that owns two Callers sites in one Go frame, goes 1/4 -> 4/4. Arms: Frame.PC+1 round-trips; pc-1 resolves to the same site and entry; IDENTITY (one site answers one PC on every call) passes on both sides, as the control; two sites never share a span. RuntimeBlockEventTests and SyntheticPCRegistryTests stay green.
GATES (linux): full GolibTests 919 passed / 0 failed / 13 skipped; go test ./... green.
READINGS (linux, A/B against the I2 parent, row by row): io, log, log/slog and net/http/pprof are IDENTICAL (io 60/1, log 8/1, log/slog 197/19, all validated with the same disclosed rows).
- runtime/pprof: TestBlockProfileBias now names REAL call sites with correct file:line (pprof_test.go:1162/1163, where it read 0x2 before) and still fails. The remaining cause is MEASURED: the JIT inlines blockFrequentShort out of the managed stack, and the CLR's StackTrace drops inlined frames where Go expands them through its inline tree. With DOTNET_JitNoInline=1 the same published host PASSES TestBlockProfileBias.
- TestGoroutineProfileConcurrency (class G, intermittent) moved; no other row moved.
ALSO SEEN (not acted on): with inlining off, the stack's first frame is runtime/pprof.blockevent, the test file's linkname declaration emitted as a forwarder. Go has no frame there. The test only looks for named frames anywhere, so this does not fail it, but it is one extra frame against Go's skip arithmetic.
CENSUS for the owed rows (GOROOT 1.24.13): the test files calling runtime.Caller/Callers/FuncForPC/CallersFrames are in runtime, reflect, log/slog, runtime/pprof, net/http, net, io, go/build and encoding/json. Production callers add flag, log, testing, testing/slogtest, net/http/pprof, internal/bisect and internal/reflectlite. You also named errors and runtime/debug. OWED AT THE TRAIN: CNR, the full behavioral suite, and those rows (io, log, log/slog measured on linux above).
ASK (a decision, not code): the inlining gap blocks TestBlockProfileBias here, and in the class C sizing it is the uncertainty on TestCPUProfileRecursion and TestTimeVDSO. It is a converter-policy question. The converter's computeNoInliningClosure (callerInliningAnalysis.go) already emits [MethodImpl(NoInlining)] to keep runtime.Caller's skip-counted frames honest. Widening it to callers of the skip-counted profile recorders (blockevent/mutexevent) would move this row at a perf cost limited to those functions. Marking every converted function NoInlining would match Go's traceback everywhere, at a JIT cost corpus-wide. Or leave it and treat the row as structural. Which one, if any, should I size?
NOT MEASURED: darwin and Windows; CNR; the behavioral suite; the other census rows.
