# C2 REC-F (iii)+(iv): MakeNoZero census + predictions, 645a8e1f62
from: C2 · to: COORD · 2026-09-24T06:20:40Z

C2 -> COORD: REC-F (iii)+(iv) SEAT ANNOUNCED; push follows this line.
REF claude/c2-rec-f-golib 97060327a7 -> 645a8e1f62, off fa18863b94, UNMERGED until the 1.24.13.1 release.
 97060327a7 (posted earlier as PARKED/WIP; the title stays, a posted SHA is not replaced) is the change:
 (iv) NewArray(0) -> Array.Empty<T>(), uncharged; (iii) MakeNoZero through AllocationCounter at Go's
 size class (rule measured against go1.24.13 for n = 1..70,000). 645a8e1f62 is its record:
 DESIGN-allocation-counting.md §9.1 plus probes/c2-mnz-census/.
CENSUS (dynamic, not a call graph): Go's own tests of the 46 std packages that call AllocsPerRun, run
 on a go1.24.13 GOROOT patched to count MakeNoZero inside each measurement window. Positive control
 first: Grow(18) = 1 call, 24 B per run. 112 of 119 sites observed over 411 calls (second pass at
 GOMAXPROCS=1 for the "GOMAXPROCS>1" skips); the 7 unexecuted were classified by reading their
 closures (js x3, Windows-only x2, zstd data, reflect's own skip): none can reach MakeNoZero.
 The converted callers match Go's one to one (builder.cs:68; bytes.cs:602/697/724/754).
RESULT 4 sites, 7 calls, each exactly 1 MakeNoZero per run, all strings/bufio. No (iv) member reaches
 it, so the halves compose by addition.
PREDICTIONS after (iii), COUNT per run, UNMEASURED:
 strings TestBuilderAllocs 2 -> 3; bufio TestReadStringAllocs 2 -> 3;
 strings TestBuilderGrowSizeclasses 3 -> 3 (the regrow gives way to the counted buffer; 4 falsifies
 the size class); TestBuilderGrow growLen=0 2 -> 2 (the unit-note leg), growLen>0 legs 3 -> 4.
 None moves toward its want: Go's one real allocation becomes visible, as the members' plans already
 say. The re-pins must ride the same merge, or a by-hand sweep comparison flags all four as moves away
 from the want. (iv): C1's predictions stand as C1's; Go's own counts are recorded beside them
 (e.g. 9_kvs Go 10). (iv)'s reach beyond its members is not censused; the one away-move class is
 the COUNT->BYTES seam (TestAnyLevelAlloc is the named instance).
GATES go test ./... rc=0 on the tree; identifier census clean on all four files and the message
 (two net/netip IP-literal subtest names masked in the TSV).
LOCAL, unofficial, not a gate: ZeroLengthBackingTests 4/4 on linux. A full GolibTests pass was cut
 at 900 s; its 35 failures all load kernel32 (the Windows-flavour corpus on linux).
OMITTED LEGS (hardware lane owns them): GolibTests on Windows; the strings/bufio/log/slog rows before
 and after on Windows and Linux; a re-read of every AllocsPerRun unit note; CNR; one go2cs.slnx build.
NEXT REC-C §B (extendslice) on claude/c2-rec-c-append-make. STANDBY after it.
