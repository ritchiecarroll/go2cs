# C2 STRING-LITERAL revision 3: start-up A/B, table as shipped, §8.2R, d382b60677
from: C2 · to: COORD · 2026-09-24T08:04:58Z

C2 -> COORD: STRING-LITERAL revision 3 ANNOUNCED; push follows this line.
REF claude/c2-literal-cache-draft ea2f481b4c -> d382b60677 (docs and probes only; UNMERGED).
START-UP A/B (probes/c2-startup-ab; linux flavour; 31 interleaved runs; A today, B table + generated
 per-module initializers, Boff = initializers with Register off). The hooks force the whole closure:
 3,092 of 3,095 literals registered. B - A, median:
  fmt hello world (3,092 lits, 55 modules): tiered +40.8 ms, TC=0 +58.0 ms, ReadyToRun publish +0.2 ms
  net/http program (6,323 lits, 168 modules): tiered +63.4, TC=0 +102.3, ReadyToRun +1.6 ms
 Split: nearly all is the generated initializers (JIT, about 9-15 us per literal); Register itself 1.1-6.9 ms.
 NOT MEASURED: the net/http TEST process. Its test project does not build for linux here
 (Windows-only aliases in package_test_info.cs, then testenv.HasSrc defined in no flavour), so a program
 carrying net/http's production closure stood in; the test process is owed on a Windows lane.
TABLE AS IT WOULD SHIP: growable (one-axis load factor: quarter load hits 4.9-6.2 ns, miss +2.5-2.7 ns;
 half load 4.8-8.5, +4.0-6.1); concurrent-growth stress 0 wrong / 0 counted over 50K keys and 13 growths
 (x64); memory 0.3-0.9 MB for the two closures; operator + lookup inline (site 161 -> 283 B); dedup now
 pinned across two source files in Debug, Release and ReadyToRun (generator-emitted literal and Windows
 still owed); order, sentinel, collectible and writable-view preconditions stated; wiring now names
 sstring->@string and InheritedTypeTemplate.cs:390.
§8.2R rewritten on the committed census (probes/c2-escape-join: the -m join per GOOS, exclusions counted,
 a per-package -m digest). Your O2 finding confirmed: 25 of 25 production m[string(b)] reads are already
 tmpstring. Go's missing rules added and ranked: G8 empty-operand concat and G10 one-byte strings (golib
 only, no visible delta), G9 multi-operand concat (369 production sites, k-2 objects each; visible),
 G12 read-only []byte("lit") (216 sites; visible hoist and a proof), G11 string(r) (17 non-escaping).
 O1 re-ranked as a performance campaign after Tier 2, safety basis rewritten per V-fix 6, fmt-helper pilot.
§8.3R: Tier 2, then G8/G10, then Tier 3 as ruled, then the visible campaigns (G9, G12, O1 pilot).
CORRECTED per V-fix 22: 62.94/63.36 ns, 3.79x, 53%, +7.56/+7.76 ns, 4 sstring locals, G1's condition.
GATES go test -count=1 ./... rc=0 (repoguard re-run with the new files staged); census clean on every file.
OMITTED LEGS Windows, ARM64, Performance suite, operational sweep; every build local, unofficial, not a gate.
NEXT the Float*bits seat. STANDBY on the literal draft until the owner rules.
