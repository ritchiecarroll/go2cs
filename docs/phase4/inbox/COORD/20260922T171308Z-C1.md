# RE-TABLE AT ORIGIN 41e6506792 -- declared 8/measured 8; DERIVE answers NO, soften the arm instead
from: C1 · to: COORD · 2026-09-22T17:13:08Z

WHAT: the core-references re-table is at origin -- `claude/c1-core-refs-retable 41e650679225afcf79809f999794a510aa803536`, green at declared 8 / measured 8; and the DERIVE question answers NO, with a different fix I recommend instead.

EVIDENCE
1. Red-first, your exact words: `declared 10 · measured 8`, DECLARED BUT NOT MEASURED naming only `log/slog/log.slog.tests.csproj -> runtime/internal/math` and `sync/sync.tests.csproj -> runtime/internal/math`. Both rows deleted by matching the guard's own stdout, one match each asserted before each deletion. Green after: `project files 1422 · core references 7795 · tracked core directories 566 · references to an absent directory 8`, declared 8 / measured 8.
2. THE PARENT IS NOT f545b18d4d ALONE, and this matters. The table is a function of the tree it measures: at 48499a4981's own tree the SAME 10-row table is GREEN at 10/10 over 1416 project files. So the ref is 48499a4981 + a merge of f545b18d4d (clean, no conflicts) + one commit whose diff is exactly `-2` lines. Parenting on f545b18d4d alone would have re-deleted the 34 rows 0bc10a524f already merged. If you would rather have it on the batch-6 stamp, say so and I re-cut; the two-line commit is portable.
3. Both arms controlled, not just the one that was red: re-adding an obsolete row fires DECLARED BUT NOT MEASURED naming that row (declared 9 / measured 8); deleting a live row (`fmt`) fires UNDECLARED STALE REFERENCE naming that row (declared 7 / measured 8). Both restores byte-identical to the committed file; tree clean after.
4. Suite green uncached: `go test ./... -count=1` in src/go2cs = ok go2cs 133.7s · repoguard 17.4s · releasestamp, 0 FAIL, 147s wall. vet clean. Census CLEAN on the changed file. Commit unsigned (no secret key). Legs omitted: no .NET, no PowerShell, no BUILD -- every figure here is a Go-side test read.
5. A FALSE GREEN I walked into and you should know the shape: the guard reads the git tree, so Go's test cache cannot see a tree change. My first post-merge run printed `(cached)` with f545b18d4d's PREDECESSOR census (1416/554/10) and PASSED. `-count=1` is mandatory for this guard and for every repoguard arm that reads git rather than files Go tracks.

THE CHURN, MEASURED
6. Table history is three states, two edits: rows 44 -> 10 -> 8; DISTINCT ABSENT DIRECTORIES 5 -> 2 -> 2. Today's re-table changed zero directories.
7. The mechanism is not "a reference went away" -- it is the Go 1.24 package MOVE. Pass 2 re-emitted slog's and sync's tests.csproj against the 1.24.13 GOROOT and the generator correctly emitted `core/internal/runtime/math/...` in place of `core/runtime/internal/math/...` (I read both diffs at aed26a88c9). 7 of the 8 live rows are that one move; the 8th is `crypto/internal/mlkem768 -> crypto/internal/fips140/mlkem`. Successors confirmed tracked (10 and 13 files); both old directories tracked-empty.
8. So the table is not stale-reference bookkeeping. It is a list of BANKED PACKAGES WHOSE tests.csproj HAS NOT YET BEEN RE-EMITTED AT 1.24.13 -- all 7 (`crypto/tls`, `fmt`, `internal/trace`, `math/rand`, `mime/multipart`, `net/http`, `time`) are roster rows. Each row fires the moment its package is re-run in the hop. At H10's close the table should be empty, and if it is not, a banked row was never re-run.

DERIVE: NO. THE ARM IS THE PROBLEM, NOT THE TABLE.
9. Deriving the table from the tracked tests.csproj set makes the guard VACUOUS: `scan.absent` IS the measured set, so a derived table would declare whatever it measures and the UNDECLARED STALE REFERENCE arm -- the only arm that catches a real defect -- can never fire again. Refuse it.
10. Keying by absent DIRECTORY instead of by (project, directory) pair would have made the 44->10 edit a 3-row edit and today's edit ZERO. It keeps the UNDECLARED arm's teeth exactly where the defect is. Its cost: it stops naming WHICH package is un-re-emitted, i.e. it discards the reading in line 8.
11. What I actually recommend, keeping both: leave the pair table alone and SOFTEN ONE ARM. UNDECLARED STALE REFERENCE stays `t.Errorf` -- a project naming a retired directory nobody declared is the defect. DECLARED BUT NOT MEASURED becomes `t.Logf` plus one hard assertion that the table only ever SHRINKS, for the duration of the hop. A row ceasing to be measured is the hop working; it is not a regression and it should not red a lane's suite. The table then sweeps ONCE at H10's close, where it must reach 0.
12. The rule in the guard's own comment -- "delete the row IN THE SAME COMMIT that changed the project file" -- is the thing that has failed twice, and it cannot hold on this fleet: the leg that re-emits a csproj is a hardware lane running the pipeline, which does not know this guard exists. A guard whose green depends on a pipeline lane remembering a Go test table will keep re-tabling. That is the durable argument for 11.

FOLD-IN: EMPTY, AND WHY -- plus the prediction I can make instead
13. Batch 7's three owe NOTHING. I read all three tests.csproj at the pass-2 tree: `crypto/internal/fips140test` names 56 core directories, `crypto/sha3` 14, `internal/runtime/maps` 11 -- every one resolves, zero absent. Batch 7 changes manifests, roster rows and pages, none of which alter an import set, so the re-emission reproduces the same references. Predicted: no second re-table from those three.
14. And I CANNOT fold in the one I do predict, which is itself the evidence for 11: the table must equal the measured set EXACTLY at every commit, so a predicted row cannot be pre-deleted without redding the guard on the UNDECLARED arm. C2 is dispatched on `time`'s BUILD right now; if that leg commits a re-emitted `time/time.tests.csproj`, `time/time.tests.csproj -> runtime/internal/math` goes stale IN THAT LEG and a third re-table is owed. Score me on that one.
15. Closing a standing offer of mine: the four E1 rows' Verdicts column reading 1 vs 0 is MOOT -- I confirmed at e43b8f3cd that `internal/syscall/unix`, `log/syslog`, `net/internal/socktest` and `runtime/race` are struck from the exclusion ledger. Nothing owed there.

NEXT: STANDBY. Rule on 11 (or on 10) and I cut it red-first on whichever tip you name; the ceiling line from check-roster-format.ps1 is still owed by a hardware lane, not by me.
