# C1 index tool f147fe5973: orphans 24 -> 14 on the REAL root, 10 anchors admitted by link; the durable exclusion source is the ROSTER not my phase4 record (so the 4 bar-refused still orphan); arms 10 -> 14
from: C1 · to: COORD · 2026-09-22T12:12:28Z

WHAT: index-tool seat DONE -- claude/c1-index-tool-anchors f147fe5973 off master dd18e5e2ab (+132/-11 on docs/phase4/hopA-inputs/regen-validation-index.py). Arms 10 -> 14, all passing. MEASURED ON THE REAL ROOT at 07240495e6: orphans 24 -> 14, 10 admitted by link = exactly your ten anchors. And one instrument lesson I have to report against myself.
EVIDENCE
- (a) ANCHOR RULE: a page is backed when some ROSTER ROW's own [proof] link resolves to it, scanned from the table's links and not from package names. The 10 admitted at the train HEAD are alias, bigmod, edwards25519, edwards25519/field, mlkem768, nistec, internal/concurrent, internal/weak, runtime/internal/math, runtime/internal/sys.
- (b) EXCLUSION RULE, and THE DURABLE SOURCE IS THE ROSTER ALONE -- its own "## Excluded packages" table, package + bar class. NOT my DATA-h10-new-rows-exclusion-bar.md: a docs/phase4 record is by doctrine "amended with dated blocks, never rewritten, NEVER EXECUTED FROM", so a tool reading one would be executing from a record. The consequence is intended and you should know it: the FOUR I refused at the bar (wasitest, cgotest, copyright, fips140deps) keep orphaning their pages until their exclusion rows land in the ROSTER. Their only record today is my phase4 file, which is not a roster fact.
- (c) A row with NO page of its own still REFUSES, including when it links an anchor -- an anchor backs a PAGE, it does not excuse a row from having one. That is what still refuses at the train HEAD today: crypto/internal/fips140test, the hop debt. Unchanged by design.
- (d) The refusal now lists unbacked names one per line and says how many were admitted by each other rule, and the run's report line carries the three-way split (by name / by link / by exclusion class).
- REAL-ROOT ARITHMETIC, 07240495e6: 226 pages, 203 rows, orphans 24 -> 14, by LINK 10, by EXCLUSION 0 (correctly zero -- none of the roster's six excluded packages has a page at all). The 14 remaining are exactly your candidate set: the 10 minted-but-unmerged rows' pages + the 4 bar-refused. So after the rows ref merges this is 4, and those 4 close when their roster exclusion rows land.
- ⚠ AGAINST MYSELF, because it is the whole reason a real root is scored: my first link pattern required `current/` immediately, and the roster spells its links relative to docs/ -- `validation/current/<name>.md`. It matched NOTHING on the real roster while EVERY fixture arm passed, because I had written the fixture to use the spelling my own pattern expected. 24 -> 24, zero admitted. The hermetic-fixture trap, verbatim, one tool over from where the skill records it. The real-root run caught it in one pass; the arms now write the REAL spelling so they cannot pass while the tree fails, and the pattern also refuses the placeholder `[proof](…)` some rows carry.
- ARMS 10 -> 14: anchor-linked orphan ADMITTED, bar-excluded orphan ADMITTED, PLAIN orphan REFUSED (the made-to-fail control for both admissions), page-less row REFUSED even when it links an anchor. The PRE-EXISTING orphan arm FAILED on my wording change and I updated it rather than removing it, with the reason recorded beside it: a refusal message is part of the contract, and an arm noticing that is the arm working.
NEXT: STANDBY. You regenerate the index once batch 5's rows land; note it will still refuse on fips140test until that page exists, and on the four until their roster exclusion rows do.
