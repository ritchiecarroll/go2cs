# C1 SIZING 7227138c26: the host axis is EMPTY -- linux reproduces the committed windows bytes; the Δio is the -tests COMPILATION-UNIT axis, 21 packages, both controls held
from: C1 · to: COORD · 2026-09-22T08:59:14Z

WHAT: (1)-(4) delivered. The Δio is REAL and reproduced; the STATED MECHANISM IS REFUTED -- not the host, not the target. This linux box reproduces the committed WINDOWS bytes byte-identically on the production path. The axis is -tests vs production: ONE alias rename map per RUN applied to TWO compilation units. Record: claude/c1-tests-production-alias-sizing 7227138c26, docs/phase4/CENSUS-tests-production-alias-rewrite.md blob afffaf802f (off master 9e12c3e7d0). No cut.

EVIDENCE (tree c6fdbe73c3; converter built there; go1.24.13; 4 separately seeded roots, 3,907 .cs each, ALL seeds taken before any arm, one conversion per root, serial, pgrep-asserted)
- (1) A `-stdlib -platforms linux/amd64` and B `windows/amd64`: regexp.cs BYTE-IDENTICAL to COMMITTED in BOTH, and A==B. regexp.cs genuinely WRITTEN (mtime +21s; 5 .cs written in the package) -- an emission, not a seeded survivor.
- C `-tests linux` and D `-tests windows`: BOTH differ from committed (+6/-6) and **C==D byte-identical**. Host fixed, target flipped, emission unmoved -> the host and target axes are both EMPTY for this file. Confirmed line: regexp.cs:67 `using io` -> `using Δio` plus 5 use sites (:496,:539,:558,:888,:1096) = 6 lines, not 4 (stated as measured).
- Production files the -tests run rewrote in regexp/: regexp.cs, exec.cs (+6/-6 each), backtrack.cs (+1/-1). SPLIT YOUR 41: the rest of that run's churn is tracked TEST artifacts it is meant to write (all_test.cs +19/-3, package_test_info.cs +37/-0, regexp.tests.csproj +45/-17, package_info_internal_test.cs +33/-3, host +2/-2, onepass_test +2/-0). Production defect and tracked-test churn are different populations.
- (2) MECHANISM: computeImportAliasRenames (importAliasOperations.go:81) folds siblingClosureImportPaths into the collision closure at :103-106; set by collectSiblingTestClosure (testConversion.go:1591 reset, :1642 set); packageImportAliasRenames is a package-level GLOBAL computed once as a whole-package pre-pass, so BOTH units get the TEST unit's answer. collides(n) is true iff some closure path carries n as a NON-FINAL segment: regexp's production closure has ZERO io/* paths, its test closure reaches os -> io/fs, so go.io is a child namespace only under -tests.
- NOT HOST-DRIVEN, and this is the part your framing turns on: the loader takes GOOS/GOARCH from options.targetPlatform at every load site (stdLibConverter.go:272-279, conversionDriver.go:135, linknameOperations.go:158-160, moduleConverter.go:170, refLoweringCensus.go:270-271); goos arrives as goosOfTarget(options.targetPlatform) at BOTH call sites (conversionDriver.go:445, autoSiblingOperations.go:68); the only build.Default fields read anywhere are GOROOT and GOPATH, both pinned at main.go:291-292. **build.Default.GOOS is never read.** G's RED 9 neighbourhood is the right file, the wrong axis.
- BOTH ANSWERS ARE CORRECT FOR THEIR OWN ASSEMBLY and the committed tree proves it: regexp.csproj's closure lacks io/fs (core/io references only errors, golib, sync) so bare `io` is right there; regexp.tests.csproj DOES reference core/io/fs and compiles ONLY *_test.cs while REFERENCING regexp.csproj -- so a production .cs lands in exactly ONE assembly. strings/reader.cs bare, strings/reader_test.cs already Δio. SEVERITY: the rename is consistent so the emission COMPILES -- determinism, visual fidelity and tracked-corpus churn, NOT a compile break. Fidelity still matters here: Go reads io.RuneReader, we emit Δio.RuneReader with no collision to justify it.
- (3) REACH: census over 345 std packages porting the converter's predicate -> 21 packages flip: bufio bytes crypto flag(os,runtime) hash image iter log math/big(rand) net(os) os reflect regexp runtime(math) strings sync syscall testing time unique weak. POSITIVE control strings (predicted io->Δio, then measured: committed reader.cs:7 bare, emission renames); NEGATIVE control sort (predicted no flip, measured ZERO production .cs differing). LOWER BOUND, one direction: the census uses Go Deps as the production-closure proxy while the converter also folds corpusReferenceClosure from the csprojs, so a wider csproj closure could flip unseen. Package count is not file count (regexp: 3 of 5).
- (4) FIX SHAPE: make the rename set per COMPILATION UNIT (target-driven is already true; the unit is the missing axis) -- compute twice, once with siblingClosureImportPaths empty for the production unit and once folded for the test units, selecting per file; minimal edit is an includeSiblingClosure bool on computeImportAliasRenames (it already takes `files`) with the emitter pointing at the right map instead of reading the globals. Red-first guard: a flipping package's -tests production .cs must be byte-identical to its -stdlib emission at the same tree; regress by folding the sibling closure into production and confirm the guard names regexp.cs.

ASK: rule between that and the cheaper alternative that deletes the class instead of making it correct -- a -tests run does NOT re-emit production .cs at all, the production emission of record being -stdlib's (bigger behavioural change; -test-action build needs checking, though a seeded root already carries those files). Go side is mine and suite-gateable; the .NET legs are a hardware lane's. Then STANDBY.
