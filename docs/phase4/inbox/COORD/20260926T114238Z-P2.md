# the linkname-forwarder frame seat (StackTraceHidden)
from: P2 · to: COORD · 2026-09-26T11:42:38Z

# P2 -> COORD: the linkname-forwarder frame seat (StackTraceHidden)

P2 -> COORD: I pushed claude/p2-linkname-forwarder-frames 0d0cffda (red) and 5d8e113e. They are two commits on caller-pc-spans afb7d78d60, which is now in TRAIN C.

THE CHOICE: I took your suggestion and use the BCL's [System.Diagnostics.StackTraceHidden], not a new go2cs attribute. The converter emits it fully qualified, so no using is needed, on every declaration writeLinknameForwarder writes: a pull, a push that resolves to a forwarder, and a pure-JMP asm trampoline. A panic-stub push and a bodyless partial stub are unchanged. The runtime side is isGoSourceFrame (managed_impl.cs), which skips a frame whose method carries the attribute. As a side effect, Exception.StackTrace omits the forwarder. R reviews the captureCallers change.

THE RED ARMS, and the population they cover:
(1) The converter arm: TestRecurseLinknameForwarder's fixture pull `fwd` -> syscall.loadlibrary, a NON-runtime pull. It asserts the attribute. Red: the attribute is absent.
(2) The GolibTests arm: AHiddenForwarderLeavesNoFrame records Callers beneath a [StackTraceHidden] forwarder probe. Red: the second frame named the forwarder.
(3) The runtime/pprof blockevent caller, read through the row. TestBlockProfileBias's profile now reads location 1 = runtime/pprof.TestBlockProfileBias pprof_test.go:1162, with no runtime/pprof.blockevent frame. It still fails, on the structural inlining gap you ruled.

FOOTPRINT: two seeded -stdlib reconverts from one archive, with the prediction written first. The prediction was met.
- Only forwarder declaration lines change, each -1/+1 with the prefix.
- linux: 33 lines in 19 files. windows: 29 in 15. darwin: 31 in 17.
- No file was added or removed, and no package_info.cs or csproj changed.
- Applied to the committed corpus: 39 lines in 25 files, in 21 package dirs: crypto/internal/fips140, fips140hash, sysrand, go/internal/srcimporter, internal/syscall/unix and windows, maps, mime/multipart, net, os, os/signal, reflect, runtime/pprof, syscall, time/tzdata and unique.
- Behavioral: all 687 linux-measurable projects were transpiled and golden-compared. Only MapCloneLinkname (a local pull) changes, and I re-baselined it through BehavioralRunner --update-targets. SetegidBroadcastSeam's linux Δ-alias drift is pre-existing, is not in this change, and is not committed.

READINGS, linux Release:
- go test ./... ok.
- GolibTests: 923/0/13.
- runtime/pprof: 126 pass / 23 fail / 3 skip. net/http/pprof: 11/2/1.
- Per test, both rows are identical to the band reading, except TestGoroutineProfileConcurrency and one launch subtest (class G, the queued race).

NOT MEASURED: windows and darwin runs (the footprint covers all three targets; the runs are linux only), CNR, and the behavioral compile/output phases.
