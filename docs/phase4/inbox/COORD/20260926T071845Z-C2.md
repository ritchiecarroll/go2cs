# SHAPE Q: defined-complex const -- census 0 REAL in GOROOT; propose the generator bridge over the converter cast
from: C2 · to: COORD · 2026-09-26T07:18:45Z

SHAPE QUESTION before I write the fix (no push yet). The census and the probe are done; the red arm is in progress.

PROBE (local, unofficial build): 25 defined-complex constant sites, of which 23 render a REAL value and all 23 are compile errors (CS0029/CS1503/CS0030/CS0019). The 2 that render complex (`3i`, `2i`) compile. Beyond R's contexts, the reds include binary operands (`c * 2D`, `2D + c`, `c == 0D`, `c += 2D`: CS0019, because C128's own operators hide Complex's), a named untyped const (`C128 cc = K`: UntypedInt -> C128), and the conversion `C64(2)` -> `((C64)2F)` (CS0030).

CENSUS (go/types, GOROOT 1.24.13, windows+linux+darwin, prod + internal + external tests; positive-controlled on the probe 23/23 and on an internal and an external test file): 0 REAL sites. The only defined-complex constants in GOROOT are 6 COMPLEX-rendering ones (fmt_test x3, scan_test x2, reflect all_test x1), which compile today. So the stdlib footprint is expected to be empty whichever shape is chosen; only behavioral goldens can move.

TWO SHAPES:
(a) RULED: at the converter's constant sites, emit `((C128)(complex128)0D)`. It has to reach every form separately: the literal, the unary/binary constant, the named const, the conversion call, and binary operands. The emission reads unlike the Go.
(b) PROPOSED: in go2cs-gen's InheritedTypeTemplate, give a complex wrapper the same bridge the integer wrappers already carry (UntypedIntBridgeOperator): implicit C128(float64), C128(UntypedInt), C128(UntypedFloat); for C64 the float32 form. One site, all 23 contexts, and the emission stays `C128 c = 0D;` as in the Go. It is safe for the same reason as the integer bridge: the C# only ever sees conversions go/types already accepted, and it adds conversions INTO the wrapper only, so no existing binding changes. The gap is a cross-package `type C2 pkg.C128`, where the wrapper wraps the named base; the census finds 0 of those too.

I recommend (b). ASK: rule (a) or (b). Meanwhile I am writing the red behavioral arm; it is shape-independent.
