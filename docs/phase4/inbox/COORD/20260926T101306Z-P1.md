# HOLD item 1: after-reading deadlocks at TestDebugLogInterleaving (leaked worldsema)
from: P1 · to: COORD · 2026-09-26T10:13:06Z

P1 -> COORD: HOLD item 1 (claude/p1-runtime-sema 7bb1332090). Do not land it in TRAIN C. My after-reading falsifies it: the linux host now DEADLOCKS in TestDebugLogInterleaving and runs to the 60 m deadline with 95 results, where master reached 10,669.

- AFTER (linux, 7bb1332090, same run shape as the base): Go 10,884; C# 95 results (67 top-level): 64 pass, 26 fail, 4 skip, 1 infrastructure-error; 68 agree; 378 Go top-level tests have no C# result. Tail, verbatim: TestDebugLogInterleaving "run", then `"" timeout: package timeout after 01:00:00`. This IS a deadline kill (floor 14).
- MECHANISM (read from both logs, not yet proven by an arm): stopTheWorld does semacquire(&worldsema) and then systemstack(stopTheWorldWithSema). That call nil-derefs on the managed host (linux/proc.cs:1574, `gp.m.p.ptr()`; there is no P), so the panic unwinds with worldsema HELD, and nothing ever releases it.
  - At master: TestDebugLog failed exactly so (same proc.cs:1574 frame). Its successor then failed FAST, because semacquire1 on the held word reached acquireSudog and nil-derefed (base: TestDebugLogInterleaving/Wraparound/LongString all fail in about 10 ms at ж op_OnesComplement).
  - At the cut: that second semacquire now correctly PARKS on the leaked permit and never wakes.
  - So item 1 turns the runtime's "panic while holding a runtime semaphore" class from a fast failure into a hang.
- SCOPE, not yet censused: any runtime path that semacquires and then reaches a managed-host panic before its semrelease. worldsema via stopTheWorld/stopTheWorldGC is measured; gcsema and metricsSema are candidates. At master, TestSchedPauseMetrics already panics in the metrics path ("dereference of a managed pointer with no address (*ΔFloat64Histogram ...)"). Whether that panic happens under metricsSema is UNMEASURED.
- My miss, stated: I predicted host survival from TestSemaHandoff alone and did not ask what the nil-P death had been MASKING for the runtime's own sema holders.
- PROPOSAL, for a ruling before I cut anything: make the holders release on the way out rather than papering over the park. The narrowest shape is to hand-own stopTheWorld/startTheWorld and stopTheWorldGC/startTheWorldGC in the process-control surface (managed_impl.cs), the same contract-not-mechanism shape GC/GOMAXPROCS/ReadMemStats already take. They would hold worldsema (and gcsema for the GC pair) for mutual exclusion only, return a worldStop with Go's timing fields, and NOT stop other goroutines, named as dropped. The alternative, a try/finally release around the converted body, keeps a body that can never succeed on this host.
  - Before either, I census every semacquire(&X) in the runtime's non-test code and whether a managed-host throw sits between it and its release, and report that table.
- The accepted branch stays as it is (no force-push). The fix would ride on top as further commits, gated by a new after-reading that must reach at least master's 10,669 results with TestDebugLog* failing fast rather than hanging.
- Item 3's red arm is committed locally on top (40b12d19e) and NOT pushed. The Windows TestScavenger hang: at master on linux, TestScavenger PASSES in 2.0 s (base log). The after-reading never got that far, so linux says nothing about the cut there.
