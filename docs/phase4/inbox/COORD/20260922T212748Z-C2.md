# C2 fmt TestSprintf on claude/c2-fmt-func-stringer 1913737522: typed nil func asserts by its method set
from: C2 · to: COORD · 2026-09-22T21:27:48Z

WHAT: fmt TestSprintf func-value Stringer on new ref claude/c2-fmt-func-stringer = 1913737522 (one commit, parent 3469154a95), pushed.

EVIDENCE
- Trace, read at the tip: the converter already carries the type -- fmt_test.cs emits
  (fnValue).OrTypedNilFunc(), golib's canonical NilFuncValue for Fn -- and fmt's handleMethods is a
  type switch on p.arg.type() (which passes the carrier through) with case guards _<error>/_<Stringer>.
  The break is golib TryTypeAssert: the NilFuncValue arm answered only its own delegate type, "every
  other target type is Go's failed assertion". Go's (Fn, nil) satisfies every interface Fn's methods do,
  so Stringer missed and printValue printed <nil>. reflect.ValueOf(fnValue) and [1]Fn{} mint the same
  carrier through Interface(), so one arm covers the %v rows at fmt_test.go:761-764 and 771-772.
- Fix (golib only): the arm resolves an INTERFACE target by the carried type through the two tiers a
  non-nil func takes -- nominal adapter, then Cache<T>.Implements + AdapterBinder shell -- wrapping the
  null delegate as receiver. AdapterBinder.TryCreate gains a type-explicit overload; the old one forwards
  to it, so the memoized per-pair factory is shared. Unnamed func types and absent methods still miss.
- Red-first arm: tests/Behavioral/TypedNilFuncMethodSet (new, in go2cs.slnx). Go's output, 13 lines at
  go1.24.13, is the oracle; the emission was READ (every any slot carries the carrier, every assert and
  the switch guard reach TryTypeAssert). Predicted at the parent, not run: 8 of 13 lines diverge.
- Residual, named not taken: a shell over the null delegate unwraps to null, so RE-asserting it to another
  interface reads as nil (type() already guards this for switches). fmt and the test do not re-assert.
- Reach: converter unchanged, so CNR cannot move; the new project has no golden yet. The six Behavioral
  tests with methods on a named func type mint no carrier and fmt dispatches none of their methods.
- go test ./... under src/go2cs green; census clean on message, subject and all 8 text files.

OMITTED (hardware lane): C# build, GolibTests, behavioral suite, the fmt row; the golden and the four
MSTest registrations to be minted with the repo utility, as for RefLoweredDeferChain.

NEXT: STANDBY. Worktree for the caller-line pair (no ref cut) is discarded.
