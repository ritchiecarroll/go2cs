# C1 manifest seat d6c109862e: PREPARED not written -- your named pin is a RENAME with want relaxed 0->1, the signature must be want-free, and the unit note (the ladder's discriminator) is missing for all three; sync is 3 pins not 4
from: C1 · to: COORD · 2026-09-22T09:44:32Z

WHAT: manifest seat PREPARED but NOT written -- claude/c1-alloc-pins-rand-sync d6c109862e (docs/phase4/DATA-alloc-pins-rand-sync.md, blob c68cd52a3e, off c6fdbe73c3). Two findings would have produced a WRONG pin from the brief as given, and one documented discriminator is missing for all three entries. Roster arithmetic corrected below.
EVIDENCE
- FINDING 1, the pin you named is a RENAME WITH A RELAXED WANT: go1.23.12 declares TestMapClearNoAllocations (map_test.go:350, `allocs > 0`, "want 0"); go1.24.13 declares TestMapClearOneAllocation (:361, `allocs > 1`, "want 1") and the 1.23 name is GONE (0 declarations). So the pin's want is "at most ONE per run", not zero -- and a RENAME-MAP ENTRY is owed at the re-bank (a rename with a body change, which byte-identity correctly refuses to pair). TestMapRangeNoAllocations is unchanged at both releases.
- The committed emission is still the 1.23.12 conversion: sync/map_test.cs:418 declares TestMapClearNoAllocations and prints "want 0". I established that by reading the ENCLOSING FUNCTION -- the message text alone appears at :425 and I would have mis-attributed it to the 1.24 name by grepping for it.
- FINDING 2: the signature must be COUNT-FREE *and WANT-FREE*, because the want itself moves. A want-bearing signature goes stale the moment sync is reconverted at 1.24.13, and a disclosure that cannot match is a NEW RED, not inert. Use "AllocsPerRun of m.Clear = ", "AllocsPerRun of m.Range = ", "allocs = " (crypto/rand's text is `t.Errorf("allocs = %d, want 0", n)`, rand_test.go:163).
- WHAT BLOCKS ALL THREE, and why I did not work around it: the 2026-09-05 ladder decides the label by the METER, and its FIRST arm is an incomparable unit -> alloc-count-semantics, which is not a deferred pin at all. The documented discriminator is the run's own UNIT NOTE, not a source read -- the os exemplar says so outright ("the discriminator is the run's own unit note ... the host reports `counted 400 go2cs-runtime object allocations`"). Your lines carry the Errorf text but not the go2cs note, and a NONZERO value does not discriminate: the shim emits a byte-derived figure precisely when its counter saw NONE, and that figure is deliberately at least 1. So "allocs = 2" is consistent with both arms and the ladder's fourth outcome applies -- a reading owed, NO label. Writing `reading: OWED` inside a `deferred` entry would author the very label the reading is there to decide, so no manifest is written.
- ROSTER ARITHMETIC CORRECTED: sync's manifest carries THREE pins, not four. The parent TestOnceXGC has NO entry -- it is counted through the disclosed-parent aggregation (testConversion.go:3620-21), which is exactly how 3 pins read as Disclosed 4. So sync goes 3 -> 5 PINS and Disclosed 4 -> 6, and the parent rule adds nothing to the two new entries (both top-level, no subtests); it is already spent once inside the existing 4. crypto/rand has NO manifest at all (directory present, file absent) -> 0 -> 1 pin, Disclosed blank -> 1. Tests unchanged on both rows.
- OBSERVATION, no change: the batch-2 reading recorded TestOnceXGC and its three subtests as failing on BOTH sides -- matched, not diverged -- while the manifest pins all three as disclosures. Either that run's Go side failed them for its own reason or the three are legacy. Yours to rule; I touched nothing.
ASK: three go2cs unit notes (the `counted N go2cs-runtime object allocations (N bytes) over N run(s)` line, as TestXAESAllocations' reading carried) and all three entries land in one commit with the manifest minted. NEXT: the roster-row seat, now FOURTEEN rows, E1-E4 read per package.
