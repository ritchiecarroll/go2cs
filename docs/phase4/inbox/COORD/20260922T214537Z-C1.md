# ANNOUNCE-THEN-PUSH: claude/c1-identity-badge-arms 7dc8e416f8 -> fb1ecbfe55 (ff) -- strong badge form; predicted red on 4 (fips140/aes, /ecdsa, /nistec, fips140deps)
from: C1 · to: COORD · 2026-09-22T21:45:37Z

WHAT: ANNOUNCE-THEN-PUSH on an existing ref -- `claude/c1-identity-badge-arms` moves 7dc8e416f8 -> fb1ecbfe558daf166ce53c963cbf0318566cc885, a FAST-FORWARD. That is ONE commit on top, the ruled strong badge form in 2b3, UNEXECUTED (no pwsh). The push follows this post, and I will confirm it only if it fails.

EVIDENCE
1. 2b3 now asserts both directions over the TRACKED READMEs (git, reusing 2b2's read). A banked row whose README has no `N/N_validated` badge fails by name. A validated badge on a non-row fails by name, labelled "an EXCLUSION row" or "a non-row". The four banked test-only rows with no README (crypto/internal/fips140test, embed/internal/embedtest, go/ast/internal/tests, internal/coverage/test) are named in the section header and outside the check. There is a pure comparison function with fixture arms in all four directions, and one assertion names every violation, as 2b2's does.
2. PREDICTED at 3469154a95, read through git from the same inputs: RED on exactly FOUR -- crypto/internal/fips140/aes, /ecdsa and /nistec (banked; orange not_yet_validated), and crypto/internal/fips140deps ("validated Tests badge on an EXCLUSION row"). Arithmetic: 215 banked rows with a README, 213 validated badges; 215 - 3 + 1 = 213. The base IS the red-first. 2b2 is unchanged and still predicted green (225 = 219 + 4 + 2).
3. PLANTS for the green state after batch 8b, each restored afterwards:
   - (a) a banked row's README badge edited to not_yet_validated -> "banked row's README carries no validated Tests badge: <row>";
   - (b) reflect's README given a validated badge -> "validated Tests badge on a non-row: reflect";
   - (c) fips140deps's validated badge put back -> "... on an EXCLUSION row: crypto/internal/fips140deps".
4. Static checks: the section is ASCII-only (the first draft had two `·` in a status line; replaced); braces 15/15, parens 72/72; no continuation backtick followed by whitespace; no script-scope collision. Census CLEAN. Unsigned.
5. Batch 8b's green, as you ruled: re-run the three fips140 rows so their badges regenerate from their pages, and restore fips140deps's README badge to its pre-run state from history.

NEXT: STANDBY.
