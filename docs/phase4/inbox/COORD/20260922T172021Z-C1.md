# RULING 11 CUT -- claude/c1-core-refs-retable 5dd8445e40 (ff on 41e6506792); growth fails, shrinkage logs
from: C1 · to: COORD · 2026-09-22T17:20:21Z

WHAT: ruling 11 is cut and at origin -- `claude/c1-core-refs-retable 5dd8445e40fa00e2b542c242eec1864b6e3ace03`, one commit on 41e6506792, which is still an ancestor at origin (fast-forward; no posted SHA replaced).

EVIDENCE
1. Red-first at 41e6506792, then the SAME two plants after the change, same tree, `-count=1` throughout:
   - declared-not-measured (`sync` re-added): BEFORE `FAIL` / "DECLARED BUT NOT MEASURED", declared 9 measured 8. AFTER `PASS` / "RETIRED, SWEEP IT: sync/sync.tests.csproj -> runtime/internal/math", `declared 9 · measured 8 · retired (logged, not gated) 1`.
   - undeclared absent (`fmt` row deleted): BEFORE `FAIL` / "UNDECLARED STALE REFERENCE: fmt/fmt.tests.csproj -> runtime/internal/math". AFTER `FAIL`, same arm, same name, `declared 7 · measured 8 · retired 0` -- unchanged, still gated.
   - clean: `PASS`, `declared 8 · measured 8 · retired (logged, not gated) 0`.
   Both restores byte-identical to the committed file after every plant.
2. Shape as ruled: UNDECLARED STALE REFERENCE keeps `t.Errorf`; the second arm is `t.Logf` with a counted summary; NO second file -- the committed table is its own baseline, and "only shrinks" is enforced by the UNDECLARED arm alone.
3. Both arms carry their own running instruction naming `-count=1` and why (the guard reads the git tree, so Go's test cache cannot see a tree change), and the test's header comment repeats it.
4. The close condition is stated in `staleCoreReferences`' own comment, in your words and mine: at H10's close the table is SWEPT TO ZERO, and a row still standing then names a banked roster row that was never re-run at 1.24.13. The comment also records what every row names (the two 1.24 MOVES, seven rows and one), and that the replaced rule -- delete the row in the same commit that changed the project file -- cannot hold because the re-emitting leg runs the pipeline and does not know this guard exists.
5. Gates: suite `go test ./... -count=1` in src/go2cs = ok go2cs 132.7s · repoguard 17.5s · releasestamp, 0 FAIL, 134s wall. vet clean. gofmt clean. Census CLEAN on the changed file. Diff is one file, +37/-8. Commit unsigned (no secret key). Legs omitted: no .NET, no PowerShell, no BUILD -- every figure is a Go-side test read.

ONE CAVEAT, NOT A REQUEST TO REOPEN
6. `t.Logf` output is printed for a PASSING test only under `-v`. So under a plain `go test ./...` a retired row is now INVISIBLE, and the sweep depends on someone reading a verbose run or on the H10 close sweep you ruled. That is a consequence of 11 working as designed, not a defect -- I am naming it so the close sweep is not assumed to be prompted by anything. If you want it visible without `-v` the only honest lever is a hard assertion, which is the thing 11 removes; I would leave it as ruled and rely on the close condition.

NEXT: STANDBY. Ref 3 for batch 7 is 5dd8445e40 whole.
