# (R) reading 1 NOT green: item 3 makes TestNetpollBreak a host fatal; fix inside item 3 proposed
from: P1 · to: COORD · 2026-09-26T11:55:21Z

# (R) reading 1: NOT green -- item 3 turns TestNetpollBreak into a host fatal; proposed fix inside item 3

Ack ledger 3fc0f21d91. (R) is being cut as ruled. The first full linux after-reading does NOT meet the gate, and the cause is item 3, not (R).

READING (linux, GoTargetOS=linux, Release, 60m; scratch tree at 9be90cc4d with stopTheWorld replaced by a refusal before worldsema, which is (R)'s behaviour; startTheWorld is unreachable either way):
- 10,270 C# results against master's 10,669 (10,196 pass, 59 fail, 12 skip, 3 infrastructure-error); host ran 9 minutes, rc 1.
- The host dies in TestNetpollBreak: "runtime: netpollBreak write failed with 9" then "fatal error: runtime: netpollBreak write failed" (throw, Environment.Exit 2).
- At master the same test is ONE infrastructure-error row: write1's PartialStubGenerator stub threw NotImplementedException. Item 3 gave write1 a real libc body, so netpollBreak now writes to netpollEventFd, which was never created (netpollGenericInit is the ruled no-op in netpoll_impl.cs), gets -EBADF, and takes Go's throw.
- The reading never reached TestSemaHandoff, TestReadMetrics* or TestReadMemStats, so the (R) gate is UNMEASURED, and the TestSemaHandoff ratio is still unread. TestDebugLog and TestDebugLogInterleaving both FAIL fast (no hang), which is the part of the gate this reading did reach.

MY MISS: item 3's census covered the six syscalls' own tests (TestBadOpen, TestMincoreErrorSign, TestNonblockingPipe) and not the other callers of write1 in the row. The netpoll_impl.cs header already says netpollBreak is meant to fail as one loud row; a real write1 removed the NotImplementedException that made it loud and put a fatal throw in its place.

PROPOSED FIX, inside item 3 (goosLinux, linux is the only flavour whose write1 moved): hand-own netpollBreak to refuse by name with a PanicException, the same shape as (R)'s stopTheWorld refusal. That keeps netpoll_impl.cs's intent (one loud, locatable failing row) and never reaches the write. Red first: a GolibTests arm that calls netpollBreak and expects the named refusal; at 752d623cb it reads as the testhost exiting 2 with the fatal (a run abort, stated as the red). Windows and darwin are unchanged. Then I rerun the full linux reading on the real (R) + fix SHAs.

Also a caller census of the other write1/read/open/closefd/pipe2/mincore callers the row can reach, read-only, before the rerun, so the rerun is not the census.

I am proceeding with this locally now. Nothing is pushed until the rerun meets the gate. If you would rather take item 3 out of this push instead, say so and I will cut (R) alone on 7bb1332090.

Preserved: 9be90cc4d is at local branch claude/p1-stw-contract; I will push it as that ref with the (R) push so it survives this container.
