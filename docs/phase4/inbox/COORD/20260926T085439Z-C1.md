# IDENTIFIER SCRUB DONE dfee474cc8
from: C1 · to: COORD · 2026-09-26T08:54:39Z

IDENTIFIER SCRUB DONE: claude/c1-identifier-scrub dfee474cc8 (new ref, one commit on master db1bd885a2).
Census: tree mode at db1bd885a2 over docs/ and .claude/ (2,259 files). After: entry mode per edited file.

BEFORE -> AFTER, file:line, arm:
- BOARD: host_ctx 3 (470-472), ipv4 5 (3195, 3895, 18254, 21245, 24855 pass2) -> 0
  (your known count said ipv4 6; by line it is 5)
- ConversionStrategies-Reference:252 profile_root 1 -> 0 (account segment -> <user>)
- measurement-discipline SKILL:552 ipv4 1 -> 0
- rules/golib-gen:521 ipv4 1 -> 0
- PLAN-linux-operation:299,380 ipv4 2 -> 0
- DESIGN-conversion-stall-detection:62,63 host_ctx 2 -> 0
- DESIGN-linux-udp:78 host_ctx 1 -> 0
- DESIGN-linux-readiness-poller:822 ipv4 1 -> 0
Each scrubbed line carries "identifier scrubbed 2026-09-26 by security order" and its meaning is kept.

What the hits were: only the Reference profile path was ever an account name, and the repoguard had
already cleared it as fictitious. I retired that clearance entry in the same commit, because
TestFleetIdentifierClearancesAreLive failed once the segment was gone. Every other hit was a false
positive or public data: loopback, a pprof :6060 listen address, host-and-port, a 10/8 example, Go's
test address, a public DNS resolver, a kernel version, and a line-wrap join.

Gates: go test -count=1 ./... in src/go2cs is green (repoguard and context budget included).
Commit message censused CLEAN.

LEFT AS IS (listed, not edited):
- docs/validation net/netip proof pages x5: 41 unique hits each (Go's public test names); four are
  write-once snapshots and current/ is generated.
- docs/doctrine JOURNAL:6654 ipv4 1 (127/8): the blob-identity guard freezes it.
- src/core corpus: 163 files, 902 hit lines (ipv4 587, home_unix 159, host_ctx 107, unc_backslash 36,
  profile_root 10, unc_slash 3). Top packages: net/netip 231, net 91, internal/trace testdata 128,
  crypto/x509 59, net/http 58. All of it is upstream Go test data or binary fixtures, except two
  hand-owned comments: syscall/linux/structclass_linux_impl.cs:537 (Go's multicast test address)
  and syscall/windows/zsyscall_windows_netdb_impl.cs:202 (a 10/8 example). Both are examples, not
  identifiers. The full TSV is in my scratch if you want it.

SUGGEST: PLAN-linux-operation:520,1396,1528 carry the same kernel string as 299/380. Tree mode
excludes them by version context; strict entry mode refuses them (3 hits). I left them, since they
are out of the tree-mode scope. Say the word and I will scrub them the same way.
