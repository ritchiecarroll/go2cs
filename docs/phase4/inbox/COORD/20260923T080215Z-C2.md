# C2 POST-HOP atomic acquire loads + runqempty on claude/c2-atomic-acquire-loads 8ef7f65ec8
from: C2 · to: COORD · 2026-09-23T08:02:15Z

WHAT: POST-HOP atomic/runqempty seat on new ref claude/c2-atomic-acquire-loads = 8ef7f65ec8 (one commit on R's 287dbe4236), pushed.

EVIDENCE
- (1) Load, Load64, LoadAcq, LoadAcq64, LoadAcquintptr, Load8 registered in manualConversionFuncs and
  hand-owned in atomic_impl.cs (blob 8ccb095b74): Volatile.Read for 32-bit/uintptr/8-bit, Interlocked.Read
  for the two 64-bit (as Loadint64 does; 64-bit Volatile.Read is not atomic on a 32-bit CLR). GOARCH: the
  corpus emits only atomic_amd64.cs (all targets amd64); no other GOARCH file is converted.
- (2) runqempty hand-owned in runtime2_impl.cs (blob 825343974d), registered goosAny: runnext == 0
  answered as Volatile.Read(ref pp.runnext.m_ref) is null; Go's loop unchanged. Not a golib Reinterpret
  special case: golib cannot name runtime's Δguintptr, and a number-view of a managed reference is what
  the family refuses by design. Census: proc.cs:6282 x3 GOOS is the only guintptr-family reinterpret.
- (3) FOUND BY THE EMISSION: the bare `Load` key also displaced generic `Pointer[T].Load` (types.cs), since
  isManualFuncDeclInPackage read a generic receiver as none. Fixed at the root (methods keyed Recv.Name);
  registry census: no other bare key names a generic method, so nothing else moves.
- EMISSION, two seeded -stdlib roots x3 targets (frozen seed, serial, rc 0, 1862/1941/1936 both arms),
  identical per target: atomic_amd64.cs -43/+6 (6 placeholders, blob 3aabf3a2b1) + its map record;
  runtime/<goos>/proc.cs -18/+1 (runqempty placeholder) + its proc.go map record. Applied whole where the
  committed file == base emission; in runtime/<goos>/package_info.cs only the proc.go record, each carrying
  one foreign drift line (profBufReadMode internal vs public). Behavioral: 735 identical -> CNR EMPTY.
- ARMS, red-first by plants (restored identical): TestAtomicLoadsAreAcquireLoads (structural, derives the
  family from GOROOT per target) red with registrations removed, with base bodies, with one plain body;
  TestNoReinterpretOfAManagedReferentPointerType red on the base corpus (3 sites), green re-emitted;
  TestGenericReceiverMethodIsKeyedByItsReceiver red 2 assertions without the fix. go test ./... green.
- No deterministic behavioural hang arm is included: I cannot run C# here, and a hoist is not a
  deterministic red.
- manualTypeOperations.go was not gofmt-clean at the base (R's pointerMask block); I added no finding.

OMITTED (hardware lane): C# build, GolibTests, the runtime row (TestSchedLocalQueueEmpty exercises both
halves: hang at base via (1), arm-2a via (2) once (1) lets it through). NEXT: STANDBY.
