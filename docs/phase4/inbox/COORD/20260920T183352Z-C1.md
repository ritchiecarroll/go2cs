# seat (4) cut: claude/c1-testonly-bridge-anchor-using 77f978074f -- the white-box bridge anchor no longer imports a production class that was never emitted (embedtest CS0234); 2 files +222/-2, suite ok, two one-axis reds on two different guards, site control container/list unchanged
from: C1 · to: COORD · 2026-09-20T18:33:52Z

**WHAT.** Seat (4) is cut and pushed: `claude/c1-testonly-bridge-anchor-using`
`77f978074f18d6ba98e2dcd36b7a2dca689bc7df`, off the version branch at `6d814e2d38`, 2 files +222/-2,
zero deletions, no mode change, census clean on both files and the message, unsigned. Batch 2 still
has NOT landed on the version branch (tip is `6d814e2d38`), so the gates are against the base alone.

**EVIDENCE.**

```
  THE SITE: internalTestPackageInfoSeed's production `using static` was written unconditionally --
    the ONE site of the family where visitFile.go:100, referenceModelTestPackageInfoSeed and
    testConversion.go:4267 all already consult productionClassEmitted.
  WHY ONE ROW: the bridge anchor is written ONLY for a MIXED suite (both variants). Of the stdlib's
    test-only packages at go1.24.13, embedtest is the only mixed one -- 8 by `go list` over std,
    or 7 if you drop crypto/.../seccomp which has no test files; I give the predicate, not a number.
  THE ROW, emission read: the line is GONE, the bridge's own using static stays, whole-package
    delta is that ONE line plus the manifest stamp.
  GATES: suite ok, repoguard ok. TWO one-axis reds, each restored byte-identical, each reaching a
    DIFFERENT guard -- caller removed -> the subject fails on the class name; seed removed -> it
    fails on the EMPTY-NAME assertion (`using static go.;`). That assertion exists because without
    it the second red PASSES: a malformed line is still "not the production class".
  SITE CONTROL (the load-bearing one): container/list -- production half present, both variants, so
    it REACHES the changed line. Anchor still written, still imports go.container.list_package,
    emission byte-identical.
  REACH CONTROL: the six other test-only packages -- emission byte-identical. ⚠ None of them writes
    a bridge anchor at all, so it shows the fix does not REACH them and cannot show it is narrow AT
    the site. Stated as what it is.
```

**⚠ THE PART WORTH MORE THAN THE FIX.** `testOnlyPackageEmission_test.go` already guarded this
question, with a one-axis fixture and its own control — and its runner **fails if an external
variant appears**, because it reproduces `crypto/internal/fips140test`. The fixture was one variant
short of the shape that breaks, so the bridge anchor was written by no path it took. A guard whose
fixture cannot reach the site is the vacuity shape one layer out from where we have been finding it.
The cut adds the mixed fixture beside the old one, same one-axis design, same real `-tests` wiring.

**NEXT.** Idle. The BUILD leg is the i7's. Route the next seat.
