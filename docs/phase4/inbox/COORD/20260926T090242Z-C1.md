# SECOND READ c2-sstring-twin-gen 2650e2b9c6: 1 fix, 4 small
from: C1 · to: COORD · 2026-09-26T09:02:42Z

SECOND READ: C2's string docs at claude/c2-sstring-twin-gen 2650e2b9c6 (branch base 4c53b02a0a, not db1bd885a2).
Verdict: accurate, present tense, no pilot-era wording. One real defect (D1) and four small items; the rest checks out.

FIX BEFORE MASTER:
D1 ConversionStrategies.md:875-884 (named string types): the example drops `const done Token = "done"` and its C# line
   `internal static readonly Token done = "done"u8;`, but still uses `done` (878, 883). The Reference keeps both (6842/6847).
   Restore the two lines.
SMALL (C2's call):
S1 Reference:6921 "`fmt.Sprintf("xxx"u8)` now copies nothing": "now" is change-log wording. Drop it.
S2 Reference:6950-6954: the refusal list omits two validateSStringTwin refusals (sstringTwinOperations.go:225-245):
   a blank parameter (the forwarder cannot pass it on) and an index that is the variadic tail or out of range.
S3 Reference:17330-17342: the attribute table says "the vocabulary is exactly this", but it has no [GoTwinForwarder]
   (written by StrGenerator on the delegate's lambda). It belongs in the "Not converter-emitted" row.
S4 Guards the old strings text named that the Reference now names nowhere: behavioral GenericCompositeLiterals,
   GenericStringTypeArg, NestedGenericTypes; performance PerfString/PerfStringView (and CS.md drops the ~11-12x figure).
   Short was the brief; one line naming the perf pair under sstring would keep the evidence reachable.
CODE COMMENTS, not docs (same seat if C2 wants):
K1 golib/sstring.cs:228-234: the implicit-u8 comment says "the twin's [OverloadResolutionPriority(1)] resolves it". At the tip
   the priority is -1, on the generated @string forwarder (StrTemplate), so the sstring member wins the tie.
K2 symbols.go:70: the HoistedConstMarker comment says "hoisted big-constant" only; ᶜ now also names string consts.
K3 sstringTwinOperations.go:9, :158-166 still say "pilot" (header, and the method-value panic text).

CHECKED AND CORRECT at the tip:
- [GoStr] Sprintf matches fmt/print.cs:268.
- StrGenerator/StrTemplate match the Reference 6911-6918 block: the forwarder shape, priority -1, fully qualified types,
  the Funcꓸꓸꓸ family, and the [GoTwinForwarder] lambda.
- Value sites: text/template funcs.cs:40, fmt errors_test.cs:31, SStringTwinPilot main.cs:35-54.
- FuncForPC name and traceback skip: GoSyntheticPC.cs:177 and runtime managed_impl.cs:860/1650.
- Refusals: hand-owned (visitFuncDecl.go:369), no body, generic, non-string, closure, defer/go, local-bind.
- Registry: 29 functions and 32 parameters, as stated. The <SStringTwins> record and the defer/go lambda form match.
- ᶜ const hoist: name, ordinal fnAtoiᶜ1, private/internal and named type (visitValueSpec.go:1064-1090 and 1497-1522);
  strconv atoi.cs:260 as printed.
- Implicit u8 operator (sstring.cs:235); every sstring table row against its operators; named-string wrapper members
  (InheritedTypeTemplate.cs:382-434).
- Every cited test exists: 19 behavioral, plus TestSStringTwinEmission, TestNoSStringTwinMethodGroupInCorpus and
  TestLocalStringConstHoistsUnderItsOwnName.
MOVED SUBSECTIONS: all 16 land byte-identical, by a script that compares each block, in Slices and Arrays (3),
  Pointers (3), Struct Types (1), Interfaces (4, plus the #### under the last), Multi-Result Values (1) and
  Constant Values (4). The other 17 old Strings subsections are rewritten into the section's 10 by design.
ANCHORS: every #anchor into both docs resolves, except Reference:20553 and CS.md:1834. Both were broken before C2's branch.
