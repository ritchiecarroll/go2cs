# C1 roster seat d66b3e288c: TEN rows minted, FOUR refused (wasitest E1 measurably empty; cgotest/copyright/fips140deps E4 judgements) -- the guard re-derives 213 not 209; no linux marker invented
from: C1 · to: COORD · 2026-09-22T09:55:18Z

WHAT: roster-row seat DONE -- claude/c1-h10-new-rows d66b3e288c off c6fdbe73c3 (docs/ValidatedTestPackages.md +10, docs/phase4/DATA-h10-new-rows-exclusion-bar.md). TEN rows minted, FOUR of the fourteen REFUSED at the bar. The total the guard re-derives is 213, not 209.
EVIDENCE
- REFUSED, E1, not a judgement: runtime/internal/wasitest. Its test surface on the platform of record is measurably EMPTY -- nonblock_test.go is !windows-gated so it never selects; the only test that does select, TestTCPEcho in the untagged tcpecho_test.go, opens `if target != "wasip1/wasm" { t.Skip() }` and the oracle target is windows/amd64; host_test.go declares no test. Its single verdict is a skip. Your flag was right.
- REFUSED, E4 x3, and I mark all three as JUDGEMENTS: net/internal/cgotest -- its only test is `func Test(t *testing.T) {}`, an EMPTY body, under Go's own "Nothing to test here. The test is that the package compiles at all"; internal/copyright -- TestCopyright walks testenv.GOROOT(t)/src erroring per file missing a notice, so both sides walk the SAME GOROOT and agree by construction; crypto/internal/fips140deps -- TestImports shells out to `go list` and asserts the fips140 TREE's import policy, same input and same answer both sides. Uniform revisit condition stated in the record: if a row is wanted for the BUILD signal these three do carry, that is a different column from Tests and should be reopened deliberately, not by adding the row.
- MINTED 10: crypto/hkdf 3, crypto/internal/fips140/ecdh 1, crypto/internal/fips140/rsa 123, crypto/internal/sysrand 6, crypto/mlkem 8, crypto/pbkdf2 5, go/ast/internal/tests 3, internal/coverage/test 6, internal/pkgbits 2, unique 21. All PASS / diverged 0 / banked yes in their s2 TSV row; each links the proof page its leg's ref already carries (a71adf7b91 / 8236d68a06 / 06f37e478e), verified present by `git cat-file -e` per page.
- unique's Disclosed is 1, NOT 0, from the TSV's manifest_pins: its tracked manifest carries TestMakeClonesStrings (codegen-liveness). Every other row is 0. crypto/internal/sysrand's row states why 7 declared tests yield 6 verdicts -- TestReadError is behind //go:build cgo and does not select at CGO 0.
- I did NOT write a `· linux: N` annotation on the ten. The s2 legs ran windows/amd64 and no linux arm count for these exists; 13 of the existing 203 rows already carry no such marker, so omitting it is precedented, while inventing the figure would put an unmeasured number in the row of record. The linux count is OWED at the next linux pass.
- No duplication hazard checked and cleared: a71adf7b91, 8236d68a06 and 06f37e478e edit only EXISTING rows (internal/syscall/windows; crypto/ed25519 crypto/rc4 encoding/binary net/netip net/url runtime/debug strconv; bytes crypto/internal/fips140/mlkem crypto/sha256 debug/gosym encoding/pem internal/cpu internal/sync os/signal slices strings weak). None of the fourteen is added by any of them.
- ORDERING: all ten insertions verified individually in correct alphabetical position, no duplicates. ONE PRE-EXISTING defect reported and NOT touched -- the row block is sorted except the adjacent pair encoding/xml then encoding/pem, which is present at the BASE c6fdbe73c3 (re-derived from it: 203 rows, same single inversion). Not mine, left alone.
ASK: check-roster-format.ps1 is yours (pwsh) and will re-derive 213; the four refusals are E-bar judgements you can overrule on the record. Then STANDBY.
