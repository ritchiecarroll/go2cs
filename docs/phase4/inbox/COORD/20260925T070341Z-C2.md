# TWIN PILOT PUSHED claude/c2-sstring-twin-pilot 5b691c9c01
from: C2 · to: COORD · 2026-09-25T07:03:41Z

C2 -> COORD: SSTRING TWIN PILOT PUSHED (new ref, push-then-announce)
claude/c2-sstring-twin-pilot 5b691c9c01, one commit on claude/c2-arm-c 9baa1e563f.
SHAPE: per ruling b919919c96. The sstring member carries the body under priority 1;
the @string member forwards [GoTwinForwarder]; 13 canonical `<Name>ᶠ` delegates; the
u8 operator is implicit.
LIST: 32 params in 29 functions (the 33 less catchPanic, as announced). Gaps closed in
golib, not cut: sstring range (ref struct enumerator) and spread (ꓸꓸꓸ).
RECORDS: 9 GoSStringTwin records (7 fmt, 2 utf8); package_info and embedded metadata.
go2cs-gen copies the priority onto the ж overload of the 13 [GoRecv] twins.
FOOTPRINT (READ; two seeded roots from one archive of 9baa1e563f):
- -stdlib: 8 files per target, windows = linux = darwin line for line: fmt x5,
  utf8 x2 and text/template/funcs.cs:40.
- -tests (fmt, text/template/parse, runtime, reflect, fips140test): the 8 value-site
  lines, nothing else.
- Behavioral census, 738 projects (8 unmeasured, the same as arm C): only
  BlankIdentifierCollision:89 (`_ = fmt.Sprintf`) and the new SStringTwinPilot moved.
- 3-way apply, 0 residual. runtime/import_test.cs keeps its committed initΔ3 (the
  linux host numbered it Δ5: foreign drift).
GUARD: TestNoSStringTwinMethodGroupInCorpus scans core and the behavioral goldens. RED
on the unmodified corpus at all 8 sites, and at BlankIdentifierCollision:89; a
regressed funcs.cs:40 is named; restored byte-identical.
UNIT: TestSStringTwinEmission, RED at 9baa1e563f. A hook in the wrong place was caught
by its consumer-package arm: an early package-selector arm emitted
`f := fmt.Sprintf` as the method group. go test ./... ok; vet 0.
UNOFFICIAL, this box, not gates: the text/template closure builds with 0 errors (no
CS0121); fmt.tests C# compiles; SStringTwinPilot built for GoTargetOS=linux runs, and its
output EQUALS Go's, including `same pointer: true` and FuncForPC `fmt.Sprintf`.
OWED LEGS (yours):
1. Corpus build on windows, stdlib.slnx and go2cs.slnx (the CS0121 check with twins).
2. GolibTests (sstring enumerator and spread, GoNameOf, frame skip).
3. Behavioral MSTest: SStringTwinPilot, BlankIdentifierCollision.
4. Member rows. fmt TestCountMallocs: each row whose format is a non-empty literal
   predicts -1 (15 of 16, including Sprintf("xxx") 2 -> 1); Sprintf("") unchanged.
   Also: utf8.RuneCountInString callers lose the class-enumerator allocation.
5. The fmt, reflect, runtime and text/template/parse test hosts (value sites rewritten).
Not measured: whether identity (Pointer equality) is RED at the base.
CENSUS: added lines clean; 2 refusals are base text (Reference doc, reflect all_test).
SUGGEST: `type F func(...)` rendered inline plus `f := g` of that signature emits
`F f = g;` with F undeclared (CS0246), and the base does the same. Out of scope.
STANDBY.
