# C2 REC-C §B extendslice: footprint, red-first, golden, CNR, 0fcdba94d5
from: C2 · to: COORD · 2026-09-24T06:54:12Z

C2 -> COORD: REC-C §B (extendslice) SEAT ANNOUNCED; push follows this line.
REF claude/c2-rec-c-append-make ff90d5681a -> 0fcdba94d5, off fa18863b94, UNMERGED until the 1.24.13.1 release.
 ff90d5681a (posted as PARKED/WIP; title kept, a posted SHA is not replaced) is the converter+golib change;
 0fcdba94d5 is everything it owed.
FORM append(x, make([]T, n)...) renders its operand as makeꓸꓸꓸ<T>(n) and nothing else in the call moves;
 golib appendꓸꓸꓸ -> slice<T>.AppendZeroed over Append's own arms (zero-size, nil, in place, growth), so no
 cap() moves. Refused: a capacity arg, a constructed-zero element, fixed-array dims, a make bound to a name.
FOOTPRINT two-seeded -stdlib, three targets serial, written counts equal (1,856/1,935/1,930):
 19 lines in 16 files windows, 18 in 15 linux/darwin, each the one-operand swap; 0 GoPositionMap lines.
 Applied lines == emitted lines exactly; residual vs the head arm 0 in all 16 files. The windows arm
 re-emitted from THIS commit's converter source is byte-identical to the measured arm.
RED-FIRST TestAppendOfMakeEmitsTheLengthOnlyOperandOnlyForGosExtendSliceShape (end to end, 4 accepted,
 4 refused): RED at fa18863b94 on exactly the 4 accepted; a regressed copy (cap and array checks off) is
 RED on exactly withCap and arrays.
BEHAVIORAL AppendOfMake: golden differs from base on exactly the 3 accepted sites; registered in the four
 classes and go2cs.slnx. CNR over 736 tracked projects: the same 8 unmeasured, 0 tracked files differ.
FIX IN PASSING appendOfMakeOperand had split makeLenArgs from its doc comment; moved, so convCallExpr.go's
 diff against the base is additions only.
GATES go test ./... rc=0; identifier census clean on every staged file (ConversionStrategies-Reference.md
 carries 1 pre-existing hit at base and 1 at head; the 26 added lines are clean).
LOCAL, unofficial, not a gate: AppendOfMakeTests 5/5; AppendOfMake builds for head and base; its run stops
 at fmt's init on kernel32 (the Windows-flavour corpus on linux), so the Output leg is unexecuted.
OMITTED LEGS (hardware lane): the behavioral Output/Compile legs on Windows; slices TestGrow/TestConcat/
 TestInsert and slog attrs6/attrs9 before/after (C1's predictions stand, UNMEASURED); GolibTests full;
 check-solution-integrity.ps1 (no pwsh here); one go2cs.slnx build.
SUGGEST: the untracked src/tests/Behavioral/package_info.cs that census conversions accumulate at the
 Behavioral root came out in a different member order in two same-order runs: a determinism question
 in its merge (seen at §A's CNR too).
NEXT the revision-3 pass on claude/c2-literal-cache-draft, then the Float*bits seat.
