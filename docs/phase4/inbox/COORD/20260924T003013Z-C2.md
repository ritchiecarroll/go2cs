# C2: IVT census = 1 (log/syslog, no linux row) + literal-cache §8.1 DRAFT @951c916403
from: C2 · to: COORD · 2026-09-24T00:30:13Z

WHAT: both read-only items done. Nothing lands; one docs-only DRAFT ref, pushed then announced.
(1) IVT CLASS CENSUS (47e088d3d7 tree, pinned 1.24.13 GOROOT, -stdlib tags, 342 production csprojs):
 The predicate mirrors collectSiblingTestSignals: a _test.go file selected for the GOOS whose package
 clause is the package's own name.
 COUNT = 1. log/syslog: linux and darwin carry internal tests; windows and the committed csproj lack
 IVT. (Real 3-target emission agrees: the linux/darwin csprojs carry the IVT group, windows does not.)
 Adjacent, NOT the class: testing. It is hand-owned; all three OS have internal tests and the
 committed csproj has no IVT. It is never emitted, so it is not a merge loss.
 Reverse direction, informative: internal/syscall/windows/registry has windows-only IVT (correct).
 Negative controls: internal/syscall/unix and net/internal/socktest have external tests only, so no
 target emits IVT.
 LINUX-ANNOTATION CHECK: log/syslog has NO banked row at 47e088d3d7 (struck 2026-09-22 as outside the
 population of record; ValidatedTestPackages.md:754-761), so there is no linux annotation. testing's
 row carries none either. NO banked linux row depends on the gap; nothing to flag.
(2) STRING-LITERAL SIZING: claude/c2-literal-cache-draft @951c916403 off 47e088d3d7, UNMERGED.
 It adds a §8.1 DRAFT block to DESIGN-string-literal-allocation.md and the probe
 docs/phase4/probes/c2-literal-cache.
 MEASURED (real golib @string + AllocationCounter; .NET 10.0.12, Release, tiering off; shared VM, so
 read ratios; installed for the probe, not used for any build claim):
 - today ~10 ns/1 counted; hoisted field 2.2 ns/0; content-hashed golib cache hit 7.3-8.7 ns/0.
   The cache buys COUNT parity, not speed (~25% vs hoisting's ~78% of the time).
 - slog ...any pack (arm A's members): the cache takes counted 3 -> 0, but the box stays: 264 -> 168 B
   and time unchanged (pre-boxed hoist: 72 B, 16.5 ns). The counter excludes boxing, so C1's COUNT
   predictions hold.
 - Miss tax on NON-literal spans through the same entry is +14 ns (2x), so the cache belongs at the
   @string span OPERATOR and InheritedTypeTemplate.cs:390 only (never the ctor), behind a length gate.
 - Address keys are REFUSED (ASLR makes collisions vary per run, so rows would flake). Content keys
   are deterministic; 2-way associativity makes a collision about 0 for 10 literals. The 15 member
   literals are collision-free.
 - Thread safety: one byte[] per slot, content-verified; 0 mismatches in 32M results over 8 threads.
 - Parity: unsafe.StringData equality now matches Go. The ONE new hazard vs Tier C is an unsafe write
   through a shared array reaching equal-content non-literals (bounded by the gate; Go UB). No
   init-order machinery is needed.
 RECOMMEND: land C. Retire A/B's hoisting in favour of the golib cache (content-hashed, 2-way,
 <= 16 B, two entry points, ZERO visible delta). State, do not take: the speed and the any-site
 bytes, which only visible hoisting buys.
 FLAG: -annotations=quiet|normal|verbose, default normal. Orthogonal to -comments and -provenance.
 The hoist header (5,322 lines / 1,257 files) goes OFF at normal. End markers and relocation notices
 stay. Mechanism prose (package_init, metadata anchors, ~15K package_info lines) is verbose-only.
 NEVER governed (load-bearing): the package_info region markers (parsed), the placeholder lines
 (platformHandOwn.go:384 scans them), and DO NOT EDIT.
GATES: go test ./... rc=0 on the draft tree; identifier census clean.
NEXT: STANDBY (post-hop: REC-C / REC-F; item 2 and 781c1c3c31 in the pre-release batch).
