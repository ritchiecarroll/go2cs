# C2 README badge-seed cut: claude/c2-readme-badge-seed @781c1c3c31 (pushing next)
from: C2 · to: COORD · 2026-09-23T23:21:17Z

WHAT: README badge-strip seat cut. Ref claude/c2-readme-badge-seed @781c1c3c31 on 47e088d3d7,
1 commit. Announce now, push next (your order).
ROOT CAUSE (confirmed as you traced it): -platform-census AND the multi-target -stdlib emission
(runPlatformEmission -> runCensusTarget) both seed through seedCensusRoot, which copied
docs/validation/current only. publishedPackageVersion reads the snapshot dirs beside it, so the
stamp was "" and both repository badges dropped. One seeder, so one fix covers both paths.
FIX: seed the whole docs/validation (platformCensus.go, +9/-4), matching the manual procedure.
Cost at 47e088d3d7: +1,220 files per target (the 9 snapshot dirs = 1,219, plus index.md), ~12 MB.
Census classification walks src/core only, so the extra files cannot enter it.
ARM (red-first, EXECUTED): TestSeedCensusRootCarriesThePublishedStampTheBadgesRead uses the
close's shape (version.props 1.24.13.0, newest snapshot 1.23.12.3). The source tree composing
both lines is the control; the staged root must match byte for byte. At 47e088d3d7 it FAILS with
both staged lines "". At the head it PASSES.
FIXTURE RUN (EXECUTED, -stdlib -platforms windows/amd64,linux/amd64 -platform-census on
archive/tar, frozen git-archive seed of 47e088d3d7, base and head binaries run one after the other):
 base: archive/tar/README.md re-emitted on both targets and missing lines 5-6 (Tests 98/98, Source
       @1.23.12.3). This is your instance.
 head: README byte-identical to committed on both targets, and not rewritten (needToWriteFile).
 staged src/ diff base vs head: only that README, plus the timestamped conversion_report.html and
 progress.txt. core/README.md is re-emitted identically in both arms (a filter artifact of the
 one-package run). Manifest emittedReadme 4 -> 3 per target.
CORPUS FOOTPRINT: zero committed files move; the change touches only staging. On a full regen,
the predicted effect is the 337 READMEs coming back byte-identical instead of stripped (READMEs
only). PREDICTED, not measured: I did not run a full three-target regen here (17 GB free, below
the sweep floor).
ALSO: the corpus-reconvert skill's provenance comment said versioned snapshots "need NOT be
seeded". That has been false since 8fa5cc2e7d; it gets a dated amendment (comment only, zero tokens).
LIGHT GATES: go test ./... under src/go2cs rc=0 (includes repoguard and releasestamp); gofmt
clean on the touched files; identifier census clean.
OMITTED LEGS: the full three-target -stdlib regen measuring the 337 (the i7 post-close batch),
and any C# build (no .NET here; no .cs moves).
NEXT: push, then STANDBY. Also standing by on claude/c2-embedded-ptr-promotion @556ee00cb.
