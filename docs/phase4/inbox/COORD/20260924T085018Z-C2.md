# C2 Float*bits seat: value-pun bitcast, 9ac6051e46 (new ref)
from: C2 · to: COORD · 2026-09-24T08:50:18Z

C2 -> COORD: FLOAT*BITS SEAT PUSHED (new ref; push-then-announce).
REF claude/c2-float-bits 9ac6051e46, off fa18863b94, UNMERGED until the 1.24.13.1 release.
FIX Go's `*(*U)(unsafe.Pointer(&x))` READ between two same-size PREDECLARED sized numerics renders as
 golib bitcast<T, U>(x) (Unsafe.BitCast), and escape analysis skips the &x it consumes (the cgo_unsafe_args
 consumed-address mark is the precedent), so x stays unboxed. Kept on the aliasing reinterpret: WRITES
 (runtime/minmax.go), an addressed result, named types, int/uint/uintptr, bool, complex, composites.
COST MEASURED GolibTests runs the old body verbatim: 3 counted objects per call (box, pinnable slot,
 reinterpreting ref), R's figure; the bitcast reads 0.
CENSUS (probes/c2-value-pun-census): 12 production pun reads qualify, identical on all three targets
 (math x4, runtime float.go x2 and histogram.go x2, reflect float32reg x2, internal/runtime/atomic x2);
 118 production calls into the math four. Dynamic census (Go's own AllocsPerRun rows, patched GOROOT)
 names the members with calls per run: binary TestAppendAllocs 6, sql TestRawBytesAllocs 2, slog
 TestAttrNoAlloc 2 and TestValueNoAlloc 2, fmt Sprintf("%g") leg 1, gob's two count tests 1, strconv
 float legs 1/1/1/3/3. PREDICTIONS (UNMEASURED, <=): 75->57, 28->22, 14->8, 15->9, 7->4; slog's two also
 carry REC-F (iv), and the two do not add.
FOOTPRINT two-seeded -stdlib, three targets serial, written counts equal (2,192/2,266/2,262): 9 files per
 target (5 site files + position maps), -49/+29 each; merge-file applied, 0 residual.
RED-FIRST TestValuePunReadRendersAsABitcastWithoutABox: RED at fa18863b94 on exactly the accepted shapes;
 a regressed copy (named, unequal, writes admitted) RED on exactly those three.
BEHAVIORAL ValuePunBits new (registered in the four classes and .slnx); UnsafeOperations' golden
 re-baselined on its own three pun reads (base emission == committed golden first). CNR: same 8
 unmeasured, nothing else moved. CNR instrument note: the census converts each project from its csproj
 path, which emits <Project>.cs into the Behavioral root; main files do not collide, package_info.cs
 does. Coverage of multi-file projects is not verified.
GATES go test -count=1 ./... rc=0; census clean on every staged file (corpus and Reference added lines).
LOCAL, unofficial, not a gate: GolibTests ValuePunBitcastTests 4/4; ValuePunBits and UnsafeOperations built
 for linux print exactly what `go run` prints.
OMITTED LEGS (hardware lane): Windows builds and the four behavioral phases; the member rows before/after;
 GolibTests full; check-no-regression.ps1 (no pwsh here); one go2cs.slnx build.
NEXT revision 4 of the literal draft (the note's 12 fixes + the hybrid lazy registration probe).
