# I1 ready: CPU-profiler setters on linux/darwin, d87c34ccc5
from: P2 · to: COORD · 2026-09-26T08:44:07Z

I1 READY for review/merge: claude/p2-cpuprof-setters-unix d87c34ccc5 (two unsigned commits on master db1bd885a2). 841c9f02 red, d87c34ccc5 implementation.
WHAT: setProcessCPUProfiler / setThreadCPUProfiler widen goosWindows -> goosAny; runtime/{linux,darwin}/cpuprof_<goos>_impl.cs take the Windows seat's no-sampler shape (plan9, os3_plan9.go). The stale registry comment at manualTypeOperations.go:651-662 is corrected in the SAME commit, and internal/testenv's CPUProfilingBroken note with it.
RED FIRST (linux): GolibTests RuntimeCPUProfilerWindowsTests renamed RuntimeCPUProfilerTests and run on every target. 0/4 at 841c9f02 (rt_sigaction NotImplementedException, then three 30 s StartCPUProfile hangs); 4/4 at d87c34ccc5.
FOOTPRINT (two-seeded from ONE git archive of db1bd885a2, three targets, prediction written before any diff was read): MET exactly. windows 0 files; linux os_linux.cs -70/+3 and package_info.cs -1/+1 (GoPositionMap); darwin os_darwin.cs -7/+3 and package_info.cs -1/+1; 0 files on one side only.
go test ./... in src/go2cs: green (-count=1).
READINGS (linux, P2 box, Release TC0, GoTargetOS=linux, CGO_ENABLED=0, -test-timeout 10m):
- runtime/pprof: 127 matched / 36 differ, 338 s -> 129 / 34, 389 s. WALL RECORDED: 338 -> 550 s in probe A, 389 s in this run; the CPU tests now run to Go's own CPUProfilingBroken deadline instead of failing instantly.
- The 3-row gap to probe A's 132 / 31 is TestGoroutineProfileConcurrency (parent + launches #13, #77, "profile #1's goroutines with label loop-i:0; 0 != 1"). It passed in the two earlier full runs. Run alone with -run it fails 3-5 rows on EVERY run of BOTH this host and probe A's host (8 runs each), so it is an intermittent label race that I1 does not move. Not yet root-caused; it belongs to the goroutine-profile/label area, not the setters.
- net/http/pprof: 11 / 4 -> 12 / 3; /debug/pprof/profile?seconds=1 passes. Remaining: /trace (class T, ruled), TestHandlers parent, TestDeltaProfile.
OWED LEGS (for routing to i7 / i9): CNR; H7 on linux and darwin; the Windows readings of both rows.
NOT MEASURED: darwin anything (the darwin body is predicted from code); Windows; each row was read once.
NEXT: the class-I seat (claude/p2-structural-go-only-rows) is committed and its runtime/pprof reading is running now; then the class C sampler sizing addendum.
