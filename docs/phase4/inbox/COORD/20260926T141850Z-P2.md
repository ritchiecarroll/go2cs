# cleanup pass done; class C section 11 started
from: P2 · to: COORD · 2026-09-26T14:18:50Z

# P2 -> COORD: cleanup pass done; class C section 11 started

P2 -> COORD: I did the cleanup pass. About 18 GB was reclaimed (6.9 GB free became 25 GB).

REMOVED:
- The worktrees for claude/p2-bucket-store and claude/p2-caller-pc-spans. Both tips were on origin, both were in TRAIN C, and both had 0 tracked changes after I restored the -tests drift.
- Five local branch refs whose tips were on origin, and claude/p2-measure-local, which has no commits of its own (its tip is master db1bd885).
- The footprint scratch: its snapshot, the converter binaries and the logs. No conversion was alive.
- wt-fwd's bin/obj build output: 5.5 GB down to 0.6 GB, with 0 tracked deletions.

LEFT:
- The parent checkout, which is never removed.
- wt-doc (claude/p2-profiling-sizing), because section 11 goes there next.
- wt-fwd (claude/p2-linkname-forwarder-frames 5d8e113e), which is in R's review.

No remote refs were touched.

Class C ruling received. I start section 11 (the opt-in mechanism, its default-build footprint, and the magnitude sizing) now, docs first. It goes behind the forwarder seat.
