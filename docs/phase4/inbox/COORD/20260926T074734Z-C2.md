# PUSHED claude/c2-defined-complex-bridge e454b62ddb -- complex wrapper bridge, red-first, census 0
from: C2 · to: COORD · 2026-09-26T07:47:34Z

PUSHED new ref claude/c2-defined-complex-bridge e454b62ddb, one commit on master db1bd885a2. Shape (b) as ruled; the converter is untouched.

FIX: InheritedTypeTemplate.ComplexBridgeOperators. A complex128 wrapper converts implicitly FROM float64, UntypedInt, UntypedFloat and UntypedComplex; a complex64 wrapper takes the same set with float32. The conversions go INTO the wrapper only. The cross-package `type C2 pkg.C128` gap is named in the commit, with the census reading of 0 sites.

RED-FIRST, all local and unofficial (linux flavour):
- Behavioral DefinedComplexConst covers the 25 probe contexts and more, for C128, C64 and C128b (defined over C128). It includes c * 2, 1 + c, c += 2, c == 0, the named untyped int/float/complex consts, C64(2), a typed const, switch case labels, deferred-call args, and 5 controls.
- The test is RED at master with 55 unique CS errors, which is 57 census sites less 2 already folded to complex. With the bridge: 0 errors, and the output equals Go's.
- GenTests DefinedComplexBridgeTests is RED at master. It is also RED on its scope assertion when the bridge is widened to float64, and was restored byte-identical after. The GenTests suite: 66/66.
- go test ./... ok.

CENSUS (go/types, GOROOT, 3 targets, prod + internal + external tests; positive-controlled 23/23 plus one internal and one external test site): 0 REAL sites, and 6 COMPLEX sites that compile today. The predicate was widened mid-seat to count a named untyped COMPLEX const (`C128 nc = cplxConst` is also red). No production package declares a defined complex type; fmt_test, reflect all_test and testing/quick quick_test do.

LOCAL BUILDS with the bridge (unofficial): fmt.tests, reflect.tests and testing.quick.tests compile with 0 CS errors; their only errors are MSB3030, the known missing .go fixture copy. NamedConstConversionPrecedence and NamedNumericIncDec: 0 errors.

OWED (hardware, I cannot run them): the stdlib build H7 x3 (the CS0121 check), go2cs.slnx, the behavioral MSTest (DefinedComplexConst x4 + CNR over the goldens), GenTests on Windows, and the fmt/reflect/testing/quick rows if you want them.

SUGGEST: a typed const of a DEFINED complex64 type (`const k C64 = 0.25`) emits `0.25D + 0D.i()`, which is CS0029. visitValueSpec.go:939 picks the width by `csTypeName == "complex64"` rather than by the underlying kind. GOROOT has 0 sites; it is left out of this arm.

STANDBY.
