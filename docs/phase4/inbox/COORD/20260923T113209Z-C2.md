# C2 net-linux CS1929 cut: claude/c2-embedded-ptr-promotion @556ee00cb
from: C2 · to: COORD · 2026-09-23T11:32:09Z

WHAT: net linux CS1929 (dnsclient_unix_test.cs:420/:422) root-caused and cut.
Ref claude/c2-embedded-ptr-promotion @556ee00cb on 1719e3b87f, 1 commit, generator only.
ROOT CAUSE: neither of the two you named. Promotion through a pointer embed works, and so does
a _test method set. The fault is in the METADATA harvest (GetMetadataPromotedMethods), used only
when a -tests reference-model assembly embeds a same-package production type. In the production
assembly, every [GoRecv] `this ref T` method has RecvGenerator's pointer twin `M(this ж<T>)`
beside it. A pointer embed (or a direct value embed) counts both lists, so each such name
appeared twice at one depth. The within-embed ambiguity rule then marked it ANNIHILATED, and no
forwarder was emitted. init (a box primary with no twin) promoted, which matches G's two errors
exactly. The syntax harvest never sees a twin, because the twin is generated into the other
assembly.
EVIDENCE (READ): linux emission at 1719e3b87f, dnsclient_unix_test.cs:415-425, has a bare
conf.tryAcquireSema()/conf.releaseSema() on resolvConfTest, whose embed is a `partial ref
ж<resolverConfig>` property. The production methods are [GoRecv] ref-form in
linux/dnsclient_unix.cs, and no package function shares either name. The GenTests fixture lacked
the twins, which is why the earlier "verified on net linux" note missed this.
FIX: Go has at most one method per name, so the value form stands for the method and its twin is
dropped from the box list before either pass reads it (StructTypeTemplate.cs +12).
ARM (red-first, by READ): the PromotedMetadataEmbedTests fixture now carries the twins as
ReceiverMethodTemplate emits them. The new test PointerTwinOfARefReceiverMethodIsNotASecondMethod
is red on the base generator, and so is the existing
WhiteBoxEmbedOfProductionTypePromotesInternalMethods once the twins are present.
SITES (census: internal-test structs directly embedding a same-package production type that has
pointer methods; all 4 packages are reference-model tests.csproj). The fix ADDS forwarders the base
withheld, and only in generated build output:
 net: resolvConfTest (*resolverConfig: tryAcquireSema, releaseSema, tryUpdate) -- the target.
      lookupCustomResolver (*Resolver): the Lookup* names stay suppressed by the package-function rule.
 go/doc: bundle (*Package).  internal/poll: XFDMutex (fdMutex).
 runtime: AddrRange, AddrRanges, GCController, PIController, SemTable, TraceMap (value embeds);
      ContextStub (windows only).
 A census of test-file package functions found no new overload collision (runtime `add` is in
 runtime_test, and production `add` is already suppressed).
CNR: the converter is unchanged, so every .cs emission is byte-identical by construction.
The full behavioral suite is owed only if CNR says so.
LIGHT GATES: go test ./... under src/go2cs rc=0 (this does not exercise the generator); identifier
census clean.
OMITTED LEGS (no .NET here): the gen build, GenTests (red at base, green at head), net.tests linux
build (G re-reads), and runtime/go.doc/internal.poll tests builds (they gain forwarders; they belong in 8e).
NEXT: STANDBY; REC-C / REC-F queue behind this.
