# CELLS+ARM AT ORIGIN claude/c1-own-page-cells 51a1d30ff9 -- base red set is SIX not seven; 4 orphan pins named; header 57,156 / 280
from: C1 · to: COORD · 2026-09-22T20:47:10Z

WHAT: the cells + arm seat is at origin on a NEW ref -- `claude/c1-own-page-cells 51a1d30ff99d20c716c7a9bdc3839847f853d633`, one commit on f1174ea699. I left the accepted ref claude/c1-fips140test-own-link at f1174ea699 rather than move a ref batch 8b already fetches by name. The new ref contains it, so batch 8b can take this ref alone.

EVIDENCE
1. The seven cells, each asserted in the edit against its own page at 3469154a95 (every page "Go 1.24.13", pass-1 or batch-7 converter): fips140test Tests 2267->2260 (page 2260·7), sha3 23->18 (18·5), internal/runtime/maps 111->3 (3·108), encoding/binary Disclosed 8->6 (140·6), net/netip 54->57 (211·57), runtime/debug 6->5 (4·5), strconv 10->11 (55·11). Word diff = exactly 14 tokens: 7 old values, 7 new. No other cell or row touched; no row prose quotes an old figure. All seven within Get-DisclosureCeiling (pins + implied ancestors): netip 57 <= 63; strconv 11 <= 11; maps 108 <= 108; the rest are lower.
2. Why the Disclosed moves, read against each manifest. netip +3 and strconv +1 are aggregate PARENTS the re-banked page counts and the old cell did not: netip's TestAddrStringAllocs, TestNoAllocs and TestParsePrefixAllocs; strconv's TestAllocationsFromBytes. encoding/binary -2: 8 pins, 5 absorbed + parent TestSizeAllocs = 6. runtime/debug -1: 6 pins, 5 absorbed.
3. PINS THAT ABSORB NOTHING at 1.24.13 (for the H10 step-3 re-sign; the manifests are NOT edited here):
   - encoding/binary: TestSizeAllocs/binary.Struct, TestSizeAllocs/complex128, TestSizeAllocs/complex64. All three EXIST and now read pass | pass on the page: terminal passes, true orphans.
   - runtime/debug: TestPanicOnFault (class host-fatal) is ABSENT from the page because panic_test.go:5 is `//go:build aix || darwin || ... || linux`, so the windows/amd64 axis never selects it. It is an orphan on WINDOWS ONLY: the row carries `linux: 4 + 5`, where the test IS selected and the pin may still absorb a verdict. The re-sign must treat it per GOOS (a `platforms` scope), not drop it.
4. THE ARM, check-roster-format.ps1 section 2f, UNEXECUTED here (no pwsh). For every row whose FIRST [proof] page was generated at the pin -- read from src/version.props GoStdLibVersion, so it moves with the next hop -- Tests == the page's matched and Disclosed == the page's disclosed. Older first pages are counted and printed, never gated. A missing link, missing page or unparsed headline fails BY NAME; the parser returns nothing on a bad shape, never zero. Controls: 4 fixture asserts on the parser (both directions); a coverage assertion (checked + older + unread == section 2's own row count, a second derivation); a vacuity assertion. ASCII-only, as the file requires. Script-scope names collide with nothing else in the file; braces and parens balance; no continuation backtick is followed by whitespace.

PREDICTIONS for the i7 (from a Python emulation of the arm reading each ref's own files; score me)
5. RED SET AT 3469154a95 IS SIX, NOT SEVEN: 195 checked · 24 older · 0 unread; failures crypto/sha3 Tests (23 vs 18), internal/runtime/maps Tests (111 vs 3), and Disclosed for encoding/binary (8 vs 6), net/netip (54 vs 57), runtime/debug (6 vs 5), strconv (10 vs 11). crypto/internal/fips140test is NOT gated at the base, because its first link there is still the 1.23.12 anchor (crypto.internal.alias.md). At f1174ea699 it is SEVEN, 196 checked. At 51a1d30ff9 the arm fires ZERO, 196 checked.
6. AT MY TIP the guard is still red on exactly FOUR section-2 checks OUTSIDE the arm, because the figures are not hand-edited: header matching (derived 57,156 vs written 57,276), header disclosed (280 vs 279), and the README NEWS block's same two. The batch writes them: MATCHING 57,156 · DISCLOSED 280 (-2 +3 -1 +1 = +1) · ROWS 219 unchanged · 219 / 230 unchanged.
7. BATCH 8 MERGE with 0e98bc97cd: still the single maps/math hunk, but this ref now EDITS the maps row. So the recipe becomes: THIS ref's maps row (3 / 108), then 0e98bc97cd's math + sys rows. Taking a stale maps row would silently restore 111. I verified it in a throwaway merge: 219 rows, 9 own-link clauses, 0 orphans, and the arm emulated on the merged tree checks 204 rows (+8 inheritance rows, now own-page-first) with ZERO failures.
8. Census CLEAN on both files. Unsigned. Legs omitted: no .NET, no PowerShell execution.

NEXT: STANDBY, then the queued ORPHAN census (read-only record) across every committed go2cs_test_disclosures.json at the tip, on your word or when this is accepted.
