# //go:embed SEAT CUT: claude/c2-embed-directive 3ace3efd6a -- 11 arms / 12 plants (one reddened nothing), 2 self-found defects, embed_impl.cs uncompiled
from: C2 · to: COORD · 2026-09-22T06:28:10Z

WHAT: SEAT CUT + PUSHED. claude/c2-embed-directive 3ace3efd6a on 0adf2e4318 (ancestor asserted), ONE commit, 8 files
+1542/-1, unsigned. //go:embed now resolves at conversion time and the variable is initialized.
EVIDENCE:
- FILES (blobs). NEW embedDirective.go 0d49d5d2d7 · embedDirective_test.go 574d0503a0 · src/core/embed/embed_impl.cs
  e60892c37c (UNCOMPILED). Edited visitValueSpec.go eb9f126da4 · projectFileWriter.go ce829fb736 · testConversion.go
  bd7d76d3d6 · conversionDriver.go 1e2595ec59 · go2cs-src.projitems 86c8b7f56b.
- MECHANISM as ruled: payloads STAGED into the output package dir, emitted as <EmbeddedResource> with LogicalName
  `go.embed/<import-path>[_test]/<path>`; static field initializer at the declaration; the whole walk in the
  converter; gen untouched. The Include names the STAGED copy -- aimed at the Go source tree it carried a ten-level
  relative path into GOROOT, different on every machine (the first emission read found it).
- THE READ is go/ast over BOTH comment groups, indent-tolerant, multi-line, quote-aware, each load-bearing: 11 of
  embedtest's 16 vars put the directive on the ValueSpec's own doc, indented, 5 quoted; a line-start scan of the
  GenDecl doc alone finds 34 of 42, missing fips140test's.
- TWO DEFECTS MY OWN INSTRUMENTS FOUND, fixed and now armed: (1) the loud refusal fired on EIGHT embedtest variants
  -- a scalar pattern in a SUBDIRECTORY also synthesizes the `testdata/` marker, so the entry list is not a
  singleton though the file set is (embedScalarEntries); (2) the registry was reset in resetPackageState, which runs
  per VARIANT, so embedtest's tests csproj carried FIVE items (the external half's) while every file the internal
  half's three embed.FS vars name was absent. Reset moved to the package boundary.
- ARMS 11, PLANTS 12, each at its OWN site with the code LIVE and compiling (GenDecl-doc-only read; no hidden rule
  below a named dir; the asymmetry collapsed; all: ignored; dirs recorded as walked; lexical order; a no-match
  pattern yielding an empty FS; shared half identity; markers dropped; items un-deduplicated; payloads uncollected;
  the scalar reduction a no-op). ELEVEN REDDENED, THE TWELFTH REDDENED NOTHING -- which is why embedScalarEntries
  is a function, not an inline loop. Restored byte-identical each time.
- `all:` ships as Go specifies it, proved by the synthetic arm ALONE: zero `all:` directives exist anywhere in
  GOROOT/src at 1.24.13, so it ships with no row behind it and I say so rather than claim coverage.
- EMISSION at the pin, a READ, A/B'd against a baseline re-converted from 0adf2e4318 into a separately seeded root,
  ONE axis. fips140test: `slice<byte> capabilitiesJson;` gains `= embed.ΔEmbedBytes<byte>(asm, "go.embed/crypto/
  internal/fips140test_test/", "acvp_capabilities.json")`; payload staged at 5,857 B, byte-identical to GOROOT.
- embedtest: BOTH halves emit; 11 resource items (one per distinct file across the halves); testHiddenDir's entry
  list is EXACTLY TestHidden's first listing and testHiddenStar's EXACTLY its second -- `.hidden/` and `_hidden/`
  in, `.more`/`_more` out. All five named-type variants emit their conversion.
- PRODUCTION ARM (traceviewer, temp root, committed nowhere): staticContent initialized under the production prefix,
  both payloads staged byte-exact (2,618,942 + 118,419). The latent production defect is real; its ~2.7 MB enters
  the corpus at that package's next reconvert, a later seat as ruled.
- CONTROL compress/flate byte-identical (the manifest's only move is converterRevision and the inputDigest from
  it); emission reproduces byte-identical from the final tree. REQUIRED HOOK DONE + ARMED: payloads join
  testInputDigest (a payload need not live under testdata), deduped so the digest depends on the SET.
- GATES: suite -count=1 on the FINAL tree -- go2cs ok 159.1 s, repoguard ok 24.1 s, releasestamp ok, empty fail set.
  TWO EXISTING GUARDS CAUGHT THIS AND BOTH WERE RIGHT: the harness-resolvable-subset guard read my test FIXTURE's
  directive text as real directives (it now composes the keyword at runtime -- the census hit the same false
  positive on go/build/read_test.go:302), and the licensing guard required the BSD header on a src/core hand-own.
  gofmt parity checked not applied. Census clean 8/8 + message. UNCOMPILED, exactly: embed_impl.cs, the hand-owned
  constructor (the one file that may set FS.files) -- never compiled or run, no .NET and no pwsh here.
PREDICTED: embedtest's 7 tests PASS (the converted embed.cs runtime was already complete; only construction was
missing); TestACVP past its zero-byte config, so fips140test's 52 diverged drops by 1 atop R's check hand-own. Owed
to a hardware lane: embed_impl.cs compiles, embedtest's 7, TestACVP. NEXT: STANDBY.
