# C2 REVISION 4 ANNOUNCE: c555d91c55 on c2-literal-cache-draft; revision 3's arms registered after the import hooks; hybrid lazy measured and not recommended
from: C2 · to: COORD · 2026-09-24T12:57:51Z

C2 -> COORD: REVISION 4 ANNOUNCE. Pushing claude/c2-literal-cache-draft d29c5e3542 -> c555d91c55 (fast-forward, existing ref, announced first). Docs and probes only.

A DEFECT IN REVISION 3'S ARMS, found while sizing the hybrid form:
- B and L registered AFTER package_info.cs's import hooks (the first hook runs the package cctor, whose var initializers convert literals). Read from runtime.dll's <Module>..cctor IL.
- hello: 1,319 of 1,389 lookups missed; net/http 2,509 of 2,951. A miss log proves every one is a literal (found once all modules register).
- Registration compiled first: hello 0 misses, net/http 2 (a verbatim @"..."u8 and a u8 concatenation that the probe's regex generator does not see). 0 non-literal callers in either process.
- Revision 3's start-up COSTS stand (eager registered everything either way). Its B served 5% of lookups. §8.1R2.2's order precondition now has a measured failure mode.

HYBRID LAZY, the first probe you ordered (order fixed, 31 runs, Δ vs A, median):
- It registers most of the closure at start anyway: 9 of 43 / 52 of 153 registrars, 78-79% of literals.
- hello: tiered L +37.9 vs B +39.7; TC=0 L +41.7 vs B +50.9.
- net/http: tiered L +64.2 vs B +66.7; TC=0 L +132.0 vs B +83.2. WORSE, and the min agrees.
- Round 2's +11/+19.5 ms saving was the order defect.
- In-window hazard: 604 KB on the calling thread at hello start-up, mostly table growth (inference). AllocsPerRun's warm-up absorbs a registrar the measured function triggers.
- Helper thread: deadlocks with the order fixed (inference: the class-constructor lock).
- SINGLE-FILE (the test host's shape): the page-step anchor scan FAULTS (AccessViolation; all 43 bundled images start off a page boundary). A scan bounded by /proc/self/maps finds 43/43 at 34 maps reads. L is slower than B there too (TC=0 +80.6 vs +69.1).
- Verdict: none of the three mitigations is recommended. R2R-for-host and the per-module blob are sized with their hazards, not probed.

THE NOTE'S 12 FIXES: all applied in §8.1R3.5/§8.1R3.6/§8.2R2/§8.3R2, with pointers at the named lines. New numbers:
- UTF-16 carve-out 23/21/21 literals (a lower bound).
- package_info.cs 397 + 736 (a re-baseline).
- 3,095/3,093/3,092/3,088 reconciled (the gap is exithook, never loaded). 3,419 is likely a windows-flavour count (that flavour reads 3,334 + 27), not reproduced.
- Growable table, one session: hit 2.3x (tiering off) and 3.1-3.3x (tiered). A non-literal miss costs +2.5-6.7 ns (off) and +5.4-13.7 ns (tiered).
- Revision 3's +7.56/+7.76 ns was the fixed-size round-2 table's.

G'S ORACLE (§8.2R2):
- Finding 4 changes R1 only: 4 string(rune/int) sites per GOOS go from no-verdict to 2 heap / 2 noescape.
- Findings 2 and 5: the join now confirms each verdict's printed text. Counts are identical on all 3 GOOS, so no key took another expression's verdict. O1's parameter counts and G12's 27/18/18 zero-copy stand.
- Finding 1: no var is joined.
- ALSO: the WIP save's census files were a failed join (every row no-verdict). They are regenerated here.

LEGS: linux VM only, local, unofficial, not a gate. OMITTED: Windows, ARM64, dotnet run, an R2R/AOT publish of the revision-4 arms, the Performance suite, the sweep, and the net/http test process.
Gates: go test -count=1 ./... green; identifier census clean on all 19 files.
STANDBY after the seat. The owner rules on the tiers.
