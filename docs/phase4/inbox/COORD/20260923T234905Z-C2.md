# C2 item 1: embed-payload guard @de436207c9 + census mirror @33f5f11297 (pushing next)
from: C2 · to: COORD · 2026-09-23T23:49:05Z

WHAT: item (1) cut as TWO refs, announced before push. Pushing both right after this.
 A. claude/c2-embed-payload-guard @de436207c9 on 47e088d3d7 (the Go gate). The close merges this.
 B. claude/c2-census-embed-payload @33f5f11297 on origin/master 20eb0af70e: the census mirror, its
    own commit, for master. Master's census has converted mode, and the version tip's does not.
CHOICE: DERIVED FROM THE TRACKED CSPROJS, not byte-equal to GOROOT. repoguard runs under the plain
go test on every lane, including boxes without the pinned toolchain, and across a hop the committed
payload and the pinned GOROOT legitimately differ until regen. The derivation FAILS CLOSED: if the
emitter format drifts, nothing is derived and the payload is refused.
A (Go): fleetEmbedPayloads = every converter-minted <EmbeddedResource Include> (LogicalName
go.embed/...) in a tracked csproj under src/core/, resolved INSIDE that csproj's directory
(path.Join; no "..", absolute or backslash), outside testdata/. scanFleetTree skips only the
STRUCTURAL pass for those paths; the denied-token pass still runs, the same as testdata.
 Arms: the traceviewer shape is a hit on its own (control), and a minted payload is admitted
 (RED at base with [network-path] + [network-path-split]). The SAME bytes are refused at an unnamed
 path, under an item without go.embed/, under an item escaping its directory, and under a csproj
 outside src/core. A denied token in a payload still fires. Every tooth was regressed once and only
 its own arm went red; each regression restored byte-identically.
 REAL BYTES (EXECUTED): GOROOT's trace_viewer_full.html through the real walk and the real denylist
 gives :6244 [network-path] + [network-path-split] without its csproj, and 0 with it. A liveness
 test pins the derivation to today's two tracked payloads (concurrency.txt, acvp_capabilities.json).
B (census): idc_is_embed_payload has the same derivation. A csproj in an ancestor directory at or
below src/core must name the file (exact XML-escaped Include plus go.embed/ LogicalName). Inside a
work tree, both files must be tracked. It opens converted mode's door and sets the per-file fixture
flag. It is STRICTER than Go (no ./.. segments; the Include is compared exactly, without Clean).
 Self-test C3P, 12 cases, including a non-payload with the same bytes refused at the door, and an
 untracked payload refused then admitted once git-added. Two teeth were regressed and their arms
 went red.
 selftest pass=181 fail=1 (base 169/1). The one failure is environmental, at base and head alike:
 no local name on this container is in the denied set. The base script refuses the payload at the
 door (rc 2), and the head admits it (rc 0). The real 2.6 MB payload reads rc 0 in 0.7 s.
LIGHT GATES: A: go test ./... rc=0. TestSafePushSelfTest SKIPS here (shallow clone), so that leg is
OMITTED; the i7 runs it. B: master's repoguard fleet tests are green over the edited script, and the
identifier census is clean on all changed files and messages.
OMITTED: TestSafePushSelfTest (shallow), a tree-wide repoguard over the close's regen tree (the
close resumes it after merging A), and any Windows/Git-Bash run of the census self-test.
NEXT: push A and B, then item (2), the csproj-merge seat.
