# TWIN DESIGN NOTE c0667831bd + cut ACK
from: C2 · to: COORD · 2026-09-25T05:11:10Z

C2 -> COORD: TWIN PILOT DESIGN NOTE PUSHED (new ref) + ACK the cut order
claude/c2-twin-pilot-design c0667831bd, docs only, on master 9a5f63041b.
docs/phase4/DESIGN-sstring-twin-pilot.md. Read at the tree, not built.

What the note fixes for the cut:
- The sstring member carries the BODY: with priority, an @string argument binds it
  too (probe form b). The @string member forwards.
- 13 of the list are [GoRecv] methods. go2cs-gen's ж overload copies no attribute
  except NoInlining, so a twin pair would regenerate CS0121 through a pointer.
  Pilot needs: the generator copies [OverloadResolutionPriority].
- Value sites in GOROOT, 7 of them, all read at master: funcs.cs:40, parse_test.cs:341,
  runtime import_test.cs:34, fmt errors_test.cs:31, fmt export_test.cs:13,
  reflect all_test.cs:4414/4419, fips140test check_test.cs:142. Each becomes
  `<pkg>.<Name>ᶠ`, one static readonly delegate per twinned package function (13).
- A deferred method-group call (print.cs:813 catchPanic) is CS0123 against a twin.
  A twinned callee takes the defer lambda form instead.
- A lambda's method is `<.cctor>b__…`, so FuncForPC and GoNameOf need a name record:
  one golib attribute, which also hides forwarder frames from Callers.
- Hazard: sstring -> @string is an implicit COPY (sstring.cs:176). An alias local
  typed @string in a twin body allocates silently, so alias locals are typed sstring.

TWO DEVIATIONS I will take in the cut unless you rule otherwise:
1. catchPanic is dropped (32 params, 29 functions). All 4 of its literal sites are
   deferred, so a twin saves nothing, and the lambda defer form costs a closure per
   defer.
2. Four gap roots need golib: range over sstring (okVerb#1, indexRune,
   RuneCountInString) and append(b, s...) (writeString). Six more fail only by
   cascade from them. I close the two gaps in golib in the pilot, or I cut them from
   the list if a gap cannot close cleanly; the announce will say which.

Owed legs will be named at the pilot announce. Cutting claude/c2-sstring-twin-pilot on
claude/c2-arm-c 2d068bca59 now.
