# C2 sync_package shadowing on claude/c2-package-ref-shadow a91b638cf9: one corpus hunk (pprof), net/http tests at re-bank
from: C2 · to: COORD · 2026-09-22T22:42:48Z

WHAT: sync_package shadowing seat on new ref claude/c2-package-ref-shadow = a91b638cf9 (one commit, parent 3469154a95), pushed.

EVIDENCE
- Root: C# consults a namespace's MEMBERS, then that level's using-namespace directives, then moves out.
  rootQualifyIfAmbiguous saw enclosing classes, never a using. qualifyPackageReference adds it at the two
  body-reference sites (embedded field, method-shadowed package ident); alias targets untouched (resolved
  without usings). The late using source (method calls) is pre-computed per file.
- CENSUS, per compilation (project + transitive ProjectReferences, C# lookup order): the only captured
  name at 3469154a95 is sync_package; the colliding set is errors/maps/math/sync/syscall (root class also
  under go.@internal[.types|.runtime]). GOROOT AST census of every site that can spell one bare: 40
  embeds in 17 packages, 0 method-shadowed idents. fips140 / trace: nothing captured today.
- i9's 17: files under  (net, os, reflect, syscall...) find go.sync_package as a MEMBER
  before any using, so they bind correctly; the captured ones are those in a sub-namespace.
- FOOTPRINT, two seeded -stdlib reconverts, 3 targets, rc 0, written 1862/1941/1936 both arms: ONE hunk,
  runtime/pprof/pprof.cs -> go.sync_package.Mutex, same on all targets, 0 map lines; applied (committed ==
  base emission). -tests pairs, all 17 packages: only net/http's test emission moves, 5 lines in
  client_test/clientserver_test/transport_test (each file has using global::go.@internal at 1.24.13);
  base twice is byte-identical. Tracked net/http test sources are pre-hop (18 of 37 differ) -> not
  hand-edited; the row re-emits them at re-bank.
- Behavioral: 735 projects identical under both binaries -> CNR CHANGED predicted EMPTY.
- Red-first (unit, rootShadowQualification_test.go): relative / rooted / method-only using arms + 4
  controls; plants: parent behaviour reds the 3 colliding arms, no pre-pass reds the method arm only,
  unresolved rooted using reds the rooted arm only; restored identical. go test ./... green; census clean.
- Retracted before posting: a "clientserver_test.cs false positive" I read from a root the next run had
  re-seeded; the clean re-run shows the site genuinely captured.

OMITTED (hardware lane): C# build, runtime/pprof and net/http rows. NEXT: STANDBY.
