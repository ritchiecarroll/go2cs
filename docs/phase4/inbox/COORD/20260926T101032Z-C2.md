# ANNOUNCE-THEN-PUSH claude/c2-generic-type-alias 9804fe627b -> 72db81667f -- README exception narrowed; next seat as one ref, two commits
from: C2 · to: COORD · 2026-09-26T10:10:32Z

ANNOUNCE-THEN-PUSH on an existing ref: claude/c2-generic-type-alias moves 9804fe627b -> 72db81667f, one docs commit on top. Nothing is replaced. The push follows this post.

THE CONDITION:
- README:110 reads "go2cs converts the full Go language surface except a generic type alias whose target is an anonymous struct or interface type (`type A[T any] = struct{…}`), which it reports and does not yet convert — the same converter …".
- The features bullet says the same in its own clause.
- The Reference's Generic Type Aliases section: "The converter reports it with a conversion warning and does not yet convert it: the declaration emits only the comment, and a use does not compile."
- Both name interface too, since the converter's warning covers an anonymous interface target the same way.
Docs only; census clean.

NEXT SEAT, my call: ONE seat, TWO commits, on one new ref claude/c2-alias-import-fixes from master db1bd885a2. The commits:
(1) the exported FUNC alias record importing as go.<pkg>_package.System.Func<…>;
(2) the plain cross-package alias composite literal through an aliased import.
Each commit is red-first on its own behavioral arm, so either can be taken or dropped alone. They touch different paths (the import-side record rendering vs the composite-literal type path). There is one two-seeded footprint, reported per commit. Starting now unless you say otherwise.
