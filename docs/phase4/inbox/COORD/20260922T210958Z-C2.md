# C2 time TestLinkname on claude/c2-time-linkname 568c17380f: forward rows to time's legacy definitions
from: C2 · to: COORD · 2026-09-22T21:09:58Z

WHAT: time TestLinkname seat on new ref claude/c2-time-linkname = 568c17380f (one commit, parent 3469154a95), pushed.

EVIDENCE
- Spellings at the pin match yours: time.go:1923/1928/1933 carry //go:linkname legacyTimeTimeAbs
  time.Time.abs, legacyAbsClock time.absClock, legacyAbsDate time.absDate; linkname_test.go pulls
  timeAbs/absClock/absDate by those symbols. Only these two files in GOROOT name them.
- A plain "true" row cannot work: no C# member absClock/absDate exists (the bodies are legacy*), and
  time.Time.abs split at its last dot names package "time.Time" (the red emission called
  <pkg>.T_package.abs and queued a project reference to that path). So: three linknameForwardTargets
  rows + linknameForwardDefinitions (row -> "time.legacyX"); funcLinknameForward forwards to the
  definition; packageFuncAccess emits each definition public (reverse index, as the push-source arm);
  isLinknameExposed includes them. A row without a definition forwards to its own name, unchanged.
- Corpus: src/core/time/time.cs, 3 lines internal -> public. Tracked file == base reconvert, byte for byte.
- Red-first: TestRecurseLinknameForwardDefinition (module fixture: method-shaped + free symbol under
  legacy names, control row) red on 6 asserts at the parent, green after; access arm removed -> exactly
  the 2 publicize asserts red. Registry guard now verifies each definition against Go's directive:
  plants respelled / swapped / entry removed / orphan -> each red naming the row; restored identical.
- Emission READ (time -tests convert, seeded root): the three pulls forward to Δtime.legacyTimeTimeAbs /
  legacyAbsClock / legacyAbsDate with matching tuple shapes; nothing else in time moves.
- Reach incl. src/tests/Behavioral: all 735 projects transpiled with the parent binary and this one
  emit identically (per-root path in position maps normalised; the same 8 Windows-only projects
  unmeasured by both). CNR changed list predicted EMPTY. No Behavioral source names these symbols.
- go test ./... under src/go2cs green. Census clean on message, subject and 5 of 6 files; time.cs has a
  pre-existing ipv4-shaped hit at line 851 (same at the parent; my lines are 1958-1968).

OMITTED: the time row (TestLinkname, wantAbs oracle) and every C# build -- hardware lane.

NEXT: STANDBY -> queued items (caller-line pair; fmt func-value Stringer), one ref each, on the tip current at cut.
