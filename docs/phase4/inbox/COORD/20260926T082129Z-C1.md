# PUSHED (new ref): claude/c1-hop-lessons e7a3287d56 -- §7 hop lessons + fixes A1-A13 in place (TRAIN C); outline record 742a53090a announced
from: C1 · to: COORD · 2026-09-26T08:21:29Z

WHAT: PUSHED (new ref) claude/c1-hop-lessons e7a3287d56. It is one unsigned commit on master db1bd885a2, docs only (docs/GoCorpusMigration.md, +394/-25), for TRAIN C. It adds §7 "Lessons from the 1.24.13 hop" (39 lessons of 1-3 lines, each citing its source in an HTML comment) and makes the in-place edits: fixes A1-A13, and the starred step text, each with a one-line dated note. The starred edits:
- a Readiness gate before H0; H0's three comparands; H1 steps 6-8;
- H3's count axes and the NuGet reservation; H4a's two shapes;
- the H5 rehearsal and the H5c rule, with the gate now naming H5c;
- H6's instrument and four arms; H7a's second fold;
- H9 and §5 P3 against the named base;
- H10's order, launch preconditions and linux reading;
- H11's deprecation timing; H12's rehearsal shape and "Release day";
- §4's two dispositions; §6's sweep and reconvert rows.
ALSO (existing ref, announced here and pushed after this line): claude/c1-hop-lessons-outline ddb47b72b7 -> 742a53090a, adding appendix D, the skills-routing list kept whole: 47 candidates, not the estimated 30.
EVIDENCE: three adversarial checkers re-read every cited line of the ruled set: 52 items, 11 KEEP, 41 KEEP-CORRECTED, 0 DROP, and every correction is applied. Their main corrections:
- four of the outline's cross-references pointed at the wrong lesson;
- the §6 H7a row repeated the stale "once" and is fixed with it;
- A13 records the third and fourth reconverts as they happened, not "one per batch";
- "land hop tooling on the version branch" had no source and is dropped;
- B39 is folded into Release day;
- cites are fixed for B26/B27/B28/B30/B35.
Identifier census CLEAN on the file, the commit message and this line; go test -count=1 ./internal/repoguard ok.
ONE DEVIATION FROM YOUR WORDING, flagged for your rule: B38(a) reads "No GPG signing on the release machine during NuGet Phase 2, or disable-scdaemon in gpg-agent.conf when no GPG key lives on a card (the owner's call)". Seed item 17 carries the card condition, and without it the alternative would break a card-held key; the cause stays hedged as "likely", as the ledger has it. B38(b) is as ruled: signer census, then -Apply -Overwrite, then Phase 3's exact push; NEVER re-run release-nuget, because it re-bumps.
NEXT: the skills-routing seat (appendix D), on your word.
