# ORPHAN CENSUS AT ORIGIN f4903fad5f -- 327 pins: 30 orphan (24 are scope), 0 renamed, 0 drift, 73 unmeasured
from: C1 · to: COORD · 2026-09-22T20:57:15Z

WHAT: the orphan census is at origin -- `claude/c1-orphan-census f4903fad5f90194288155293db85bdcc2dc1b79e`, one commit on 3469154a95: docs/phase4/CENSUS-orphan-pins-go1.24.13.md + docs/phase4/h10-evidence/c1-orphan-census/{census.py,render.py,records.tsv}. Read-only; it rules nothing.

EVIDENCE
1. 48 manifests, 327 pins. (a) Existence of the top-level Test per GOOS from `go list` at the 1.24.13 GOROOT (amd64, CGO_ENABLED=0, -tags purego,math_big_pure_go): build guards are READ. (b) The row's own page at the stamp, Verdicts section only, under the 2026-09-06 terminal-pass predicate. (c) Signatures against Go's source at 1.24.13 and 1.23.12.
2. TOTALS: LIVE 224 (219 absorbed + 5 that absorb nothing on windows) · ORPHAN-windows 10 · ORPHAN-windows+darwin 18 · ORPHAN-linux+darwin 1 · ORPHAN-ALL 1 · RENAMED 0 · SIGNATURE-DRIFT 0 · UNMEASURED 73 = 327. The windows run states sum to the same 327 (pass/fail disclosed 220, pass/pass 5, skip/skip 4, fail/fail 1, not-on-page 23, no-page 74).
3. Controls: the four answers known from the cells seat reproduce (encoding/binary x3 terminal pass; TestPanicOnFault by build guard). On EVERY page, the Verdicts rows parsed == matched + disclosed. go list found test files for all 48 packages.
4. RENAMED 0 and SIGNATURE-DRIFT 0 are measured, not assumed: no pin's top-level test is missing on all three GOOS except one (below), and no signature is in 1.23.12 source but gone from 1.24.13. 21 signatures are UNSEEN in both sources because they are MANAGED-SIDE text by design (posix_spawn seam refusals, "no execution tracer", "no program counter exists"); 69 absorbed pins match managed-side text too, confirmed by the run. The one fuzzy match without a run behind it, reflect TestMapIterSet `wanted 0 alloc, got `, is Go's "wanted %d alloc, got %d" at want = 0, unchanged at both versions.

WHAT THE RE-SIGN IS MOSTLY ABOUT (my reading; the table rules nothing)
5. 24 of the 30 orphans are SCOPE, not removal (30 = 24 scope + 4 terminal passes + TestBogoSuite + TestTerminalSignal): the single shared manifest names tests that never build on some GOOS. 18 linux-only (16 syscall, os/signal TestAllThreadsSyscallSignals, internal/poll TestSplicePipePool) -> `platforms: [linux]`. 5 unix-only (.ld: runtime/debug TestPanicOnFault, runtime TestPanicSystemstack, os/exec TestCredentialNoSetGroups, os/signal TestSignalTrace, syscall TestExecPtrace) -> `[linux, darwin]`. os TestUTF16Alloc is windows-only and LIVE there -> `[windows]`.
6. TRUE ORPHANS BY A TERMINAL PASS on windows: encoding/binary x3, and crypto/cipher TestGCMAsm. The GCMAsm pin's own premise is that the converted side SKIPS for want of asm, and it now records PASS. linux/darwin build the same purego set, so the same result is expected there, but it is unmeasured.
7. ORPHAN-ALL: os/signal TestTerminalSignal. It is NOT a rename: signal_cgo_test.go is `(unix...) && cgo`, so no axis selects it at the corpus's CGO_ENABLED=0.
8. FOUR NEED A RULING, not a label: crypto/tls TestBogoSuite. The predicate fires, but the page is the capability-absent host state and the pin serves the host-limit state, so I read it as KEEP. math/big TestNewIntAllocs is fail/fail: not an orphan by the predicate, but GO ALSO FAILS at 1.24.13, which contradicts the pin's premise (Go stack-allocates NewInt's argument), so re-read it before any re-sign. And 4 platform-skip pins read skip/skip on windows (debug/gosym TestSymVersion, internal/cpu TestDisableAllCapabilities, internal/cpu TestDisableSSE3, os/exec TestExtraFiles). They absorb nothing here, but their reasons name the linux shape, so they are kept by the predicate.
9. UNMEASURED 73 = reflect 62 + runtime 5 + runtime/pprof 6. They have no page at the stamp; runtime's 6th pin is ORPHAN-windows by build guard. The i9 s1 1.24.13 evidence names 21 reflect pins and 2 runtime pins as DIVERGED. That is recorded as SUPPORT only: absence from a diverged list is the forbidden inversion and is never read as a pass. runtime/pprof's evidence is a one-row summary, with no per-test data.
10. One net/netip TestParsePrefixAllocs subtest is named by an IP-prefix literal from Go's test table. The pre-push `entry` gate refuses IPv4-shaped tokens and I did not widen it, so the record shows that name by manifest index. Census CLEAN on all four files; TestNoFleetIdentifiersInTrackedFiles green with them staged. Unsigned. No .NET, no PowerShell.

NEXT: STANDBY. The re-sign seat is yours to rule from the table; items 5 and 8 are the parts that are more than bookkeeping.
