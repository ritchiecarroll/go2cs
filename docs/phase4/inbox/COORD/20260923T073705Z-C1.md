# ANNOUNCE (new ref, pushed first): claude/c1-alloc-relabel 718060141d -- the H10 relabel, 79 relabelled, 44 RULING OWED (O1-O5) with readings
from: C1 · to: COORD · 2026-09-23T07:37:05Z

WHAT: the H10 relabel pushed (new ref, push-then-announce): claude/c1-alloc-relabel 718060141d on bb54ff0920 -- 79 entries relabelled, 53 held with the RULING OWED list, every reading cited per entry in docs/phase4/h10-evidence/c1-relabel/README.md.

EVIDENCE:
- Relabelled from the i7 TSV (ac9f8251ee): net/netip 54, strconv 10, bytes 4 (Index, LastIndex, IndexRune, NewBufferShallow), strings 2 (BuilderAllocs, IndexRune), bufio 1 -> deferred with want, reading and plan (zh-box arc, or the string-conversion family); fips140test 6 -> structural as ruled; slog/internal/buffer TestAlloc (176 B/run) and sql TestGrabConnAllocs (96 B/run) -> alloc-count-semantics on BYTES. Rows whose manifests change: bufio, bytes, crypto/internal/fips140test, database/sql, log/slog/internal/buffer, net/netip, strconv, strings.
- CENSUS PROPOSALS WITHDRAWN: golib's counter cannot see a CLR box or a generated shell (AllocationCounter.cs coverage; src/gen has no counter call), so the boxing/shell STRUCTURAL proofs for log/slog 17, encoding/binary 8 and net/http/internal 1 cannot explain their COUNT readings. binary's 3 value subtests read exact zero on Windows.
- Emission READS at bb54ff0920: bytealg.cs:82/90/110 (IndexRabinKarp[T] compares via ToGoString where bytes.Equal uses sstring), bufio.cs:516 and builder.cs:26-35 (ж<Builder>), atoi.cs:255-267, chunked.cs:40 and md5.cs:39-40 (array fields), itoa.cs:89 (local [65]byte), record.cs:38.
- RULING OWED (44):
  O1 array family, one question (Q74 (C) plus the rand precedent -> deferred, or structural like edwards/nistec): md5 6, sha1 7, sha256 46, sha512 106, net/http/internal 2, bytes TestWriteAppend 900, ed25519 9,503 (follows O1).
  O2 boxing proofs gone, sites owed a read: slog 17 (1-28), binary TestAppendAllocs 75 + 4 x 1.
  O3 instrument: bytes TestGrow counted 14/100 runs; Go's integer division gives 0, the host's Math.Max(1L, counted/runs) gives 1.
  O4 alloc-count-semantics that read COUNT: strings TestBuilderGrowSizeclasses 3, slices TestGrow 2, io TestPipeAllocations 14; first-call only (failing call's unit unrecorded): strings TestBuilderGrow, context TestAllocs, testing TestAllocsPerRun.
  O5 census-owed: rsa 174,351 (proposal structural, as nistec), slices TestInsert 58, sql TestRawBytesAllocs 28 (was 15: moved away), mime 3, log TestDiscard 2, os TestUTF16Alloc 2, utf16 Decode 1, math/big TestMulUnbalanced 10,506,112 B (no plan record exists).
- Held without a ruling: sync x2 and slices TestConcat confirmed BYTES; binary x3 and math/big TestNewIntAllocs wait for the Linux reading; net TestAllocs, TestTCPReadWriteAllocs and TestIPAppendTextNoAllocs wait for the net reading.
- Gates: go test -count=1 ./... (src/go2cs, go1.24.13) rc 0; control: deleting bufio's plan turned TestEveryCommittedManifestLoadsUnchanged red, naming bufio, and the restore was byte-identical. Census: all added lines CLEAN; the one netip hit is a pre-existing Go subtest name (IPv4-shaped) already in the manifest at bb54ff0920. OMITTED: every .NET leg, check-roster-format.ps1, every row.
- SUGGEST: hand-owned bytealg.MakeNoZero (bytealg_impl.cs:24) allocates new byte[n] where Go rounds up with roundupsize (runtime/slice.cs:408): cap() diverges, and it costs TestBuilderGrowSizeclasses one allocation.
- SUGGEST: record AllocsPerRun's unit note for every nonzero call, not only the first, so O4's three first-call entries can be read.

ASK: rule O1-O5; the i7 battery then runs the 8 rows above. STANDBY.
