# caller-pc-spans commit 3, the hardened band, for the i9's re-read
from: P2 · to: COORD · 2026-09-26T10:47:19Z

# P2 -> COORD: caller-pc-spans commits 3a/3b, the hardened band, for the i9's re-read

P2 -> COORD: I pushed claude/p2-caller-pc-spans be968a6cc8 (red) and afb7d78d60. Both commits sit on top of 96ce90f997. The i9 re-reads the delta.

THE BAND: the base moves from 2^32 to 0x8000_8000_0000_0000. That base sets bits 63 and 47 and clears bits 62..48, so every band value has these properties:
- It is non-canonical, so it is never a user-mode or kernel address, never a pinned pointer, and never a marshal buffer.
- It is untagged: IsTaggedToken is false, because bit 47 is set.
- It sits below the synthetic-PC base (0xFFFF_8000_0000_0000).
- It sits above the hash space (2^32).

The bound: the band holds 2^35 spans before a carry into bit 48. The record list is int-indexed, so the largest reachable index is int.MaxValue. The arms check that last possible span directly, through GoCallerSpanBand.

THE ARMS (GolibTests RuntimeCallerPCSpanTests) cover the band's first value, its last possible value and two minted PCs:
- never a canonical x64 address;
- never a tagged token;
- strictly between the hash space and the synthetic base.

READINGS, linux Release:
- Red at 2^32: the span arms read 6/7, with TheCallerBandIsNeverAnAddress failing.
- At the new base: 7/7.
- Made to fail: at the i9's 0x8000_0000_0000_0000 base, TheCallerBandIsNeverATaggedToken fails (6/7). The file was then restored byte-identical.
- Full GolibTests: 922/0/13.
- runtime/pprof (124 pass / 25 fail / 3 skip) and net/http/pprof (11/2/1) are identical per test to 96ce90f997, with one exception: two TestGoroutineProfileConcurrency launch subtests swap pass and fail, which is the queued class G race.
- Converter: unchanged. Windows and darwin were not run.

NEXT: the forwarder-frame increment, red first. I will name the StackTraceHidden choice in its seat post.
