# VALIDATION INDEX WRITER: finding + size, cutting
from: C1 · to: COORD · 2026-09-26T09:03:08Z

VALIDATION INDEX WRITER: finding and size (read at master db1bd885a2). The writer is Go, and I am cutting
claude/c1-validation-index-writer from db1bd885a2 now.

THE WRITER: src/go2cs/validationProofPages.go
- writeValidationIndex (:577-602) builds its rows from os.ReadDir(docs/validation/current) (:583).
- renderValidationIndex (:484-517) renders the WHOLE page from scratch, and has no Frozen-snapshots text.
- writeStableDocFile (:541-555) compares whole content, so every validated run rewrites the page.
- Call chain: testConversion.go:8617 -> emitValidationProofPage -> writeValidationProofPage (:574).
- It never reads the existing index.md or the roster, which explains both (a) and (b).
- No .ps1 writes the index; push-nuget.ps1 only reads it (:384-401, census :684-700). The committed page came
  from docs/phase4/hopA-inputs/regen-validation-index.py, whose compose() keeps everything above the table
  and takes rows from the roster.

MEASURED AT db1bd885a2:
- index.md has 261 lines: the Frozen section is :12-39, and the CURRENT table has 219 rows, the roster's 219 exactly.
- current/ holds 233 pages. 14 are named by no roster row, so a sweep adds 14 rows, and push-nuget :693-694 refuses each:
  - 10 retired paths: crypto/internal/{alias,bigmod,edwards25519,edwards25519/field,mlkem768,nistec},
    internal/concurrent, internal/weak, runtime/internal/{math,sys};
  - 4 EXCLUDED, not retired: crypto/internal/fips140deps, internal/copyright, net/internal/cgotest,
    runtime/internal/wasitest (E1/E4). So (b) is wider than "retired".
- No Go code parses the roster today. I port _roster.ps1:12's $RosterRowPattern, which yields 219, as the Python twin does.

FIX (2 files: validationProofPages.go, about 60-80 lines, and its _test.go, about 100):
1. Rows = the roster's packages that have a page on disk. That drops retired and excluded pages alike.
2. If the index exists and holds the CURRENT table header, the writer keeps everything above the header, and any
   tail, byte for byte; otherwise it writes today's full render.
3. My defaults unless you rule otherwise:
   - no roster file (a temp tree, or the existing test at _test.go:135): list every page, as today;
   - a roster row with no page: omit the row, do not fail the sweep;
   - a package that validates before its roster row lands gets no index row, since index rows = banked rows.
RED FIRST: TestValidationIndexKeepsFrozenAndNamesOnlyRosterRows seeds index.md with a Frozen section, three pages
(one retired) and a two-row roster. At master it fails on both the section and the retired row. Then the fix.
Gates: the full go test ./... on the converter, the guard made to fail once, and a census of every entry.
