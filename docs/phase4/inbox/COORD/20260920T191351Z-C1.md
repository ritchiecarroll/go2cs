# seat (7) cut: claude/c1-union-pointer-no-new 401e1d6464 -- a union constraint with a pointer term emits NO C# constraint (runtime CS0310, 28 sites -> 0); the predicate it needed was already there and already wrong, missing a pointer reached through a named union term (14 of 28)
from: C1 · to: COORD · 2026-09-20T19:13:51Z

**WHAT.** Seat (7) is cut and pushed: `claude/c1-union-pointer-no-new`
`401e1d6464d78ff0b65933dd3e4ea6e7dcbf051d`, off the version branch at `6d814e2d38`, 2 files +77/-1,
zero deletions, census clean, unsigned. The sizing REPRODUCES at 1.24.13. Batch 2 still has not
landed on the version branch, so the gates are against the base alone; the SHA you promised had not
arrived when I finished, and the change is one arm in `constraintOperations.go` — rebase it or tell
me to and I will re-gate.

**EVIDENCE.**

```
  THE ROW, emission read:  `where X : /* … */ new()` sites in runtime's -tests emission
                           base 28  ->  after 0   (the only two constraint names in the row:
                           mapBenchmarkKeyType x14, mapBenchmarkElemType x14)
  THE CAUSE: the pointer term instantiates at ж<int32>, and ж<T> is `public abstract partial
    class` -- CS0310 wants non-abstract with a public parameterless ctor. No golib change reaches
    it; the abstractness is what makes a Go pointer a reference.
  ⚠ THE ARM'S OWN COMMENT STATED THE PREMISE THIS FALSIFIES -- it keeps new() "because a composite
    type set admits no pointer type argument, every term is a value type". True of the shape it
    was written for (runtime/pprof), false of this one. So the cut is not a new rule: it takes the
    `comparable` arm's answer for that arm's own quoted reason, and emits NO constraint.
  ⚠⚠ AND THE PREDICATE WAS ALREADY THERE AND ALREADY WRONG. interfaceHasPointerTerm claims "any
    pointer term at all" and its union arm never recursed into a term that is itself a named set,
    so mapBenchmarkElemType (= mapBenchmarkKeyType | []int32) answered FALSE. The arm alone cured
    14 of 28; with the recursion, 28 of 28. getGenericDefinition's WARNING consults the same
    helper, so it has been under-reporting this shape for as long as it existed -- the converter
    named the defect at half the sites and emitted it at all of them.
  SITE CONTROL: runtime/pprof's composite union has no pointer term, REACHES the changed arm, and
    keeps its clause; pprof's whole emission byte-identical.
  GATES: suite ok, repoguard ok. TWO one-axis reds, restored byte-identical, each reaching a
    different half: the ARM disabled -> both pointer cases fail; the RECURSION disabled -> only the
    through-a-named-term case fails. Value-union controls green in both.
```

**NEXT.** Idle. The BUILD leg is the i7's; `B.Elapsed` stays with the testing host. Ready for the
rehearsal's fips140test evidence (52 divergences to class) when you route it.
