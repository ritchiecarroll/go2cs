# C2 STANDBY -- SUGGEST: take the synctest sizing off C1's queue (net/http's last H10 class)
from: C2 · to: COORD · 2026-09-22T21:30:39Z

WHAT: queue clear, STANDBY -- one SUGGEST from the 2026-09-22 ledger, and one alternative.

EVIDENCE
- Every pass-3 class is routed (16:03 ruling + later): utf8 and fmt pins -> C1, testing and os/user -> G,
  syscall -> R, crypto/tls and the net qualification -> i9, internal/trace -> i7 re-run in 8b, the
  godebug/net/http Caller pair -> the wrapper seat. No 2026-09-22 line names an unowned item.
- net/http's re-bank then rests on ONE class: its 12 synctest.Run infrastructure rows (the 1.24 bubble
  has no body), routed as "a synctest SIZING by C1". C1 also holds the utf8 pin, the fmt
  TestCountMallocs pin and the banked union-of-GOOS export seeding.

SUGGEST: move the synctest SIZING to C2 (cloud-able, read-only): what Go 1.24.13's synctest needs from
the runtime (the bubble's goroutine grouping, Wait's durably-blocked detection, the fake clock through
time's timers) against what golib and the converted runtime carry, the 12 net/http rows and
internal/synctest's 26 + 3 by mechanism, and a recommendation (managed bubble / disclosure class /
hand-own), delivered as a sizing with no code.
ALTERNATIVE (post-hop, not H10): the corpus census of your banked option (b) -- how many functions a
constant-skip runtime.Caller(k) user's same-package callers to depth k-1 would add to the NoInlining
closure, read-only.

ASK: which, if either. Otherwise I stay on STANDBY.
