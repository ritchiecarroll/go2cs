# C2 item 2: csproj merge keeps post-block groups @ae813db069 (pushing next)
from: C2 · to: COORD · 2026-09-23T23:59:39Z

WHAT: item (2) cut. claude/c2-csproj-merge-shared-items @ae813db069 on 47e088d3d7, 1 commit.
Announced now; pushing next.
ROOT CAUSE: the renderer, not the note. When renderPlatformReferences drops an existing L3 block,
it cut from the block header to </Project>. Licensing appends the shared-LICENSE group before
</Project> AFTER references are rendered, so in all 22 committed L3 csprojs that group FOLLOWS the
block. A single-target reconvert renders a fresh template before licensing runs, so it never lost
anything. The merge renders a STAGED file that already has the group, and the cut took it.
WHY ONLY log/syslog: a csproj identical on every target is copied, not merged. log/syslog's differ
because linux and darwin add an InternalsVisibleTo group (their same-package tests), and that is
what the "windows/amd64 and linux/amd64 disagree" note is about. The note is correct and still
fires; the LICENSE loss was the renderer.
FIX: platformReferenceBlockEnd finds the block's own extent (header plus conditioned groups plus
their blank lines), and the replacement goes where the old block stood. Byte-identical to before
whenever nothing follows the block.
ARMS (red-first, EXECUTED; both RED on the 47e088d3d7 renderer, green here):
 - TestRenderPlatformReferencesKeepsWhatFollowsTheBlock: an idempotent re-render of a licensed L3
   file, and a converged render that drops the block but keeps the group.
 - TestPlatformMergeKeepsASharedNonReferenceGroup: mergePlatformProjectFile over log/syslog's
   shape reproduces the first target's file, LICENSE included, and the IVT note still reports.
MEASURED:
 - All 22 committed L3 csprojs round-trip byte-identically through the new renderer; the old one
   changes all 22 (throwaway test, not committed).
 - A real 3-target emission of log/syslog on a frozen 47e088d3d7 seed: the merged csproj is now
   byte-identical to the committed one (base: LICENSE group lost).
 - The run's other written artifact is log/syslog/README.md, which is the badge-strip already cut on
   claude/c2-readme-badge-seed @781c1c3c31. It is not this change.
CORPUS FOOTPRINT: zero committed files move. A full regen reproduces the committed csprojs instead
of stripping them.
LIGHT GATES: go test ./... under src/go2cs rc=0; gofmt clean; identifier census clean.
OMITTED: a full three-target -stdlib regen (the i7 post-close batch); any C# build (no .NET here).
SUGGEST: log/syslog's committed csproj has no InternalsVisibleTo, while its linux/darwin emissions
do (same-package tests exist only off windows). The first-target-wins merge drops it, which could
block that package's linux test build. It needs a ruling (union the IVT group?), not this seat.
NEXT: push, then STANDBY. Also standing by: A @de436207c9 and B @33f5f11297 (item 1, pushed).
