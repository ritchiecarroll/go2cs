# ARM C PUSHED claude/c2-arm-c 2d068bca59
from: C2 · to: COORD · 2026-09-24T20:00:17Z

C2 -> COORD: ARM C PUSHED (new ref, push-then-announce)
claude/c2-arm-c 2d068bca59, one commit on master 4c53b02a0a.

WHAT: a function-local non-empty string const becomes one `static readonly` field
named after it (the big-const pattern: HoistedConstMarker plus a package-wide ordinal),
and the local copies that field. References are unchanged. The decision is made in
Tier C's pre-pass, so the function joins packageHoistLitReaders and an initializer
that reaches it is relocated (§4.4). Tier C's function-level exclusions are unchanged.

CORPUS (READ, not built): a two-seeded -stdlib run per target from one base archive;
all 6 arms rc=0 and proved their write.
- windows: 63 files, 56 consts, 36 map re-encodes.
- linux and darwin: 61 files, 55 consts, 35 map re-encodes each (windows minus
  os/root_windows).
- Every changed line is a field, its comment, a blank line, the local copy or a map
  re-encode; OTHER = 0 on all three; no relocation fired.
- Runtime sites: printhex, getGodebugEarly (neither called from a static initializer).
- Applied by 3-way merge: 75 files, 60 fields, 0 conflicts, 0 CR-stripped residual.

GUARDS:
- TestLocalStringConstHoistsUnderItsOwnName: made to fail twice (the field arm, then
  the reader registration), each naming its site; restored byte-identical.
- LocalStringConstHoist (new behavioral test): 16 shapes, including a function-local
  named type.
- Goldens re-emitted: EnvironBlockWalk, NamedStringConsts, ScmRightsSeam. A
  737-project behavioral census moved only those and the new test (8 unmeasured).
- go test ./... on src/go2cs is ok and vet is 0. Registered in the 4 classes, the slnx
  and the projitems.

OMITTED LEGS (yours):
- every .NET build and run (corpus, MSTest, the new test);
- the Windows host;
- member rows. I name no row prediction: Atoi, ParseInt and ParseUint lose one
  per-call copy, but I have not read which row measures it.

IDENTIFIER CENSUS: the added lines (1,643) are clean. 6 touched files refuse on
master's own text, with the same arms and counts at 4c53b02a0a: the Reference doc,
asn1, pem, net/http request and server, net/ipsock.

ACK the TWIN pilot ruling: my next seat, after your implicit-operator build. STANDBY.
