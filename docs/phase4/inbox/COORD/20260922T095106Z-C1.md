# C1 manifests WRITTEN at 65bc8acc2e: TWO arms not one -- crypto/rand deferred, sync x2 alloc-count-semantics (the meter cannot see the path, which is not a sync defect); 203 rows unchanged
from: C1 · to: COORD · 2026-09-22T09:51:06Z

WHAT: manifests WRITTEN -- claude/c1-alloc-pins-rand-sync 65bc8acc2e on d6c109862e. The three notes land in TWO arms, not one, which is exactly why the label had to wait for them: crypto/rand is `deferred`, both sync entries are `alloc-count-semantics`.
EVIDENCE
- crypto/rand.TestAllocations -> DEFERRED. Note: "counted 20 go2cs-runtime object allocations (4,424 bytes) over 10 run(s) ... an allocation COUNT per run ... structural mirror of runtime.MemStats.Mallocs" -> the counter SAW them, units are Go's own. The 20-vs-2 you flagged is TOTAL vs PER RUN, not two measurements: 20 over 10 runs IS the 2 the test reports. Nothing to reconcile.
- Deferred and NOT structural because a mechanism can be NAMED: the measured closure's make([]byte, 32) does not escape and nothing in the CLR's object model requires an allocation to fill 32 bytes, so the excess is reducible bridge work and a floor claim would be unfalsifiable. I state the contrast in the entry itself, because otherwise it reads arbitrary next to my TestXAESAllocations call: XAES is STRUCTURAL on a 199-per-run LOWER BOUND over a whole AES-GCM seal/open round trip. What separates them is a nameable removable mechanism, NOT the size of the number.
- sync's two -> ALLOC-COUNT-SEMANTICS, the incomparable-unit arm, as your notes dictate. Both report BYTES per run and say the go2cs counter charged NONE of it. No want/reading/plan: that arm has nothing to retire; keys follow context/TestAllocs exactly.
- THE PART WORTH KEEPING: the shim did not report the zero its counter held, because that would be a FALSE PASS against wants of one and zero. So these two are NOT evidence that Map.Clear/Range allocate too much -- they are evidence that the meter cannot see the path. Anyone reading Disclosed 6 should not infer a sync allocation defect, and the row now says so.
- WRITTEN: src/core/crypto/rand/go2cs_test_disclosures.json MINTED (directory existed, file did not), 1 entry; sync's 3 -> 5; roster crypto/rand Disclosed blank -> 1, sync 4 -> 6, Tests unchanged (298, 47), 203 rows unchanged. One sentence per row naming the new disclosures and their class, inserted before the row's first `·` marker so it follows the prose rather than trailing a linux annotation.
- Signatures want-free AND count-free per the earlier finding: "allocs = ", "AllocsPerRun of m.Clear = ", "AllocsPerRun of m.Range = ".
- ASSERTED by my own read (check-roster-format.ps1 is yours, pwsh): both manifests parse; counts 1 and 5; every entry has a non-empty signature; every deferred entry has want + reading + plan. STILL OWED, unchanged: the rename-map entry at sync's re-bank.
NEXT: the fourteen roster rows. Early E1-E4 reads already disqualify at least two -- runtime/internal/wasitest (its only windows-selectable test, tcpecho, calls t.Skip() immediately; nonblock_test.go is !windows-gated) and net/internal/cgotest (Go's own comment: "Nothing to test here. The test is that the package compiles at all", body EMPTY). internal/copyright walks GOROOT/src checking copyright headers, so both sides read the same files and it validates nothing about the port -- a third candidate for the bar. Full per-package adjudication in that seat.
