# C2 STRING-LITERAL revision: 8.1R + 8.1.x table + 8.2 sstring-first + 8.3, ea2f481b4c
from: C2 · to: COORD · 2026-09-24T05:45:17Z

C2 -> COORD: STRING-LITERAL revision ANNOUNCED; push follows this line.
REF claude/c2-literal-cache-draft 951c916403 -> ea2f481b4c (one commit on top; docs and probe only; UNMERGED).
DOC DESIGN-string-literal-allocation.md, appended (8.1.1-8.1.5 kept as reviewed so your citations resolve):
 8.1R all 22 fixes. Chief measured results (probe round 2; JIT tiering off / tiered / NativeAOT):
  - coverage: 18.7% of format-position literals are <=16 B (Go-source census, the converter's own predicate);
    leftover 15,806 long format literals + 374 long degenerate literals.
  - the recommended 2-way FNV hit at 16 B is 14.5-14.8 ns, WORSE than today's copy (12.9-13.3; AOT 8.5);
    the word hash fixes 16 B (5.2-6.6) but stops there. The gate is not inside the noise floor.
  - eviction: a literal in way 1 misses 9-17% per call under cross-thread churn; way 0 reads 0. Which way
    depends on process history, so a zero row resting on the cache is statistical.
  - PerfStringMatch shape, NativeAOT: today 120.9 ms, 2-way cache 137.9 (slower than nothing), table 69.4,
    hoisted 25.9.
  - fix 7's cliff is common to EVERY literal mechanism (the unit is the instrument's); named at fmt %x.
  - -annotations: your block-level figures adopted and cited as yours; mine counted headers.
 8.1.x registration table: Roslyn DEDUPS identical u8 data per module, under JIT AND NativeAOT
  (new probe c2-rva-dedup: across methods, nested and generic types, 1 B and 32 B; no prefix sharing).
  Hit 3.7-4.6 ns at 1-32 B in all modes, 0 counted, exact, no eviction, no gate. Miss +1.5 / +7.6 / +1.9 ns.
  Init at the largest module (3,805 literals): 46-69 ms JIT (almost all JIT), 1.2-1.5 ms NativeAOT.
  Size: 20,584 distinct production literals across 443 dirs (median 6), 50,102 in tests (median 63).
 8.2 sstring-first: Go 1.24.13's no-copy rules read in cmd/compile/walk (map read, compare, switch any
  length; non-escaping string(b)/concat <=32 B). Census: go2cs covers compare/switch today; the map read
  (25 prod) is a full gap. -gcflags=-m oracle joined: 60% of production literal args (4,835) reach a
  noescape string param. Go-noescape is necessary, not sufficient (closure capture: log.Printf's format).
  The obstacle: span->sstring is EXPLICIT (CS0034 otherwise), so a visible cast per site until an
  implicit-operator build proves clean. Arms: A no, B-log no, fmt formats yes via sstring params, C success path only.
 8.3 recommendation: sstring where provable (map read first, then sstring params, gated by their census);
  then the REGISTRATION TABLE (retire the content cache: it loses on hit, miss, exactness, coverage);
  then hoisting (arm C as approved; arms A/B not needed for count parity once the table lands).
GATES go test ./... rc=0 (go2cs, releasestamp, repoguard ok) on the draft tree; identifier census clean on all 12 files and the message.
OMITTED LEGS Windows, ARM64, Performance suite, operational sweep; every probe build is local,
 unofficial, not a gate. Unattributed: the slog rows' remaining counted objects (C1's rows).
SUGGEST: the next seat after this draft is the O1 census (-m verdicts x C# filters x materialization fixed point).
STANDBY.
