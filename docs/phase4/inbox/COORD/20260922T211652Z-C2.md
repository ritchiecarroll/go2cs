# C2 caller-line pair: not the position map -- the TC0-only residuals, read without the release-tiered pin; ruling asked
from: C2 · to: COORD · 2026-09-22T21:16:52Z

WHAT: caller-line pair STOPPED before a ref -- it is not the position map; both rows are the measured
TC0-only residuals, read at Release+TC0 by a wrapper that never applies the roster's execution pin.

EVIDENCE
- Map read at 3469154a95 (go1.24.13, internal/godebug -tests convert, linux AND -platforms windows/amd64,
  identical emission): godebug_test.cs 181/182/183 (var c/d/e = s.Value()) -> Go 146/147/148, decoded
  from the emitted record. The map is right; the frame is handed C# line 180 (-> 145) for the c call.
- Every frame in godebug's chain is already NoInlining (stack, Stack, hand-owned Value), and the skip
  arithmetic lands stk[0] on Stack as Go does. The remaining shift is the non-leaf line of an optimized
  TestBisectTestCase frame: no JIT boundary at the statement start, so the call reads as the previous line.
- CENSUS-release-tc0-delta.md §2 + its 2026-09-02 amendment measured exactly these two as one-axis A/Bs:
  godebug TestCmdBisect FAIL at Release+TC0, PASS 5/5 with -TestTiered; net/http TestRegisterErr//a
  FAIL at TC0, pass with tiering (only those 2 verdicts moved). Both rows carry `execution: release-tiered`
  (_roster.ps1 -> -test-config Release -test-tiered). The opt-out list is godebug, log/slog, net/http.
- G's pass 3 ran "-test-config Release, tiered JIT off" through run-h10-recon.ps1 (8fc439415f): line 862
  passes -test-config $TestConfig and an empty @extra -- no -test-tiered, no Get-RosterExecutionArgs.
- net/http mechanism, read: registerErr's runtime.Caller(3) walks register -> Handle -> TestRegisterErr;
  registerErr is NoInlining but register (panics on error) and Handle (use121 branch) are not thin
  forwarders, so computeNoInliningClosure leaves them inlinable and TC0 drops them -> "unknown location".

OPTIONS (a ruling, not mine to pick)
 (a) Instrument: the H10 wrapper applies each row's execution pin as the sweep does. Both rows read green
     with no converter change (predicted from the census A/Bs, not re-measured).
 (b) Converter, net/http only: seed a constant-skip runtime.Caller(k) user's same-package callers to depth
     k-1 into the NoInlining closure (register, Handle here). Makes TestRegisterErr TC0-robust and could
     retire net/http from the opt-out list; widens NoInlining beyond the thin-forwarder policy the
     analysis states, so its corpus blast radius needs a census first. It cannot fix godebug: no
     attribute gives an optimized frame a statement-start boundary.
 (c) Both.

ASK: rule (a)/(b)/(c). Meanwhile I start item (2), fmt TestSprintf, on its own ref from the current tip.
