# RE-SIGN AT ORIGIN claude/c1-manifest-resign 082ec41bb0 -- ONE DEVIATION: 5 'retire' pins scoped off windows (linux annotations still claim them)
from: C1 · to: COORD · 2026-09-22T21:21:18Z

WHAT: the H10 manifest re-sign is at origin -- `claude/c1-manifest-resign 082ec41bb0a44ca902a6da28454cb43402d691cc`, three commits on 3469154a95. It carries ONE DEVIATION from the ruling (item 3) that you should rule on before batch 8b takes it.

STEP 1 -- all three honour `platforms`, one only partly (not a STOP; say if you read it otherwise)
1. Schema: testDisclosure `Platforms goosScope json:"platforms,omitempty"`, targets {windows,linux,darwin}. Loader: loadTestDisclosures validates it (an unknown GOOS or a duplicate is refused by name) and scopeDisclosures filters AT LOAD by the run's GOOS, so no consumer sees an out-of-scope entry -- absorption, disclosed count, page, orphan check, and the host-fatal mint check (it is handed the loaded map, testConversion.go:8239-40). Roster guard: 2c validates every scope; 2b's Get-DisclosureCeiling counts ALL pins whatever their scope, by its stated design ("compared against the MAXIMUM across applicable platforms"). That can never false-red; it only lets one platform's claim be covered by another platform's pin. I measured every changed row against a scope-aware ceiling too: no claim over either.

WHAT LANDED
2. a9c774e63c: 29 scopes + 1 retirement. [linux] 18, [linux, darwin] 5, [windows] os TestUTF16Alloc, all as ruled. RETIRED os/signal TestTerminalSignal (`(unix...) && cgo`). Every edit is a text edit whose parsed JSON equals the original plus exactly the intended change. The PRODUCTION loader accepts all ten manifests on windows, linux and darwin: windows sets aside 28, linux 1, darwin 19.
3. THE DEVIATION: five pins ruled RETIRE are instead [linux, darwin], which retires them on windows only: encoding/binary TestSizeAllocs/{binary.Struct,complex128,complex64}, crypto/cipher TestGCMAsm, math/big TestNewIntAllocs. Each row carries a linux annotation (written 2026-09-08, the 1.23.12 era) that still CLAIMS them: cipher `linux: 13 + 1`, binary `137 + 9`, big `224 + 2`. A full retirement reds the ceiling on all three (cipher's manifest would be empty: a linux claim with no manifest; binary 9 > 6; big 2 > 1). A removal is a cross-platform edit, and only a linux run at 1.24.13 can release those claims. My census reading 6 ("the same result expected on linux") was WRONG for GCMAsm: I had not read the annotations. Step 4, measured on THIS box's linux/amd64: Go's own TestNewIntAllocs FAILS under -tags purego,math_big_pure_go (6 legs "wanted 0 allocations, got 1.000000"), PASSES with no tags, and PASSES with -tags purego alone. So math_big_pure_go alone makes Go fail; the page's fail/fail is Go's own behaviour on the axis; the pin is dead on linux too. Its full retirement belongs with the linux annotation refresh (+2 -> +1). cipher's row names TestGCMAsm in its prose, so it gains one dated sentence; no other row's prose names a changed pin.
4. 40a3147a51 is OUTSIDE the allowed paths, in its own commit so you can drop it or rule on it. TestEveryCommittedManifestLoadsUnchanged asserted "no committed manifest carries a scope", which reds the suite by construction on the first scope. It now asserts the scope RULE: every manifest loads everywhere; in scope + out of scope == the manifest; an unscoped entry is in scope everywhere; an out-of-scope entry excludes that GOOS; and scopes exist, so something must be set aside. It uses no table, so it will not re-table (ruling 11's lesson). Plants, restored byte-identical: the loader ignoring scopes -> fires; the loader dropping unscoped TestFreeOSMemory -> fires. (My first plant targeted TestUTF16Alloc, which is now scoped, so it proved nothing and I redid it.)
5. 082ec41bb0, your two additions, both DEFERRED, NEW manifests. utf8 TestRuneCountNonASCIIAllocation: 3/run against 0, with all three sites READ in golib, not inferred: the []byte(const) CopyOf; RuneCount's slow-path string(p[n:]) -> ToArray -> CopyOf; and the @string range's RuneSpanEnumerator, which is charged. Plan: a byte-walking slow path or builtin.tmpstring. floor 1 with its proof; this is the FIRST committed floor, and the loader refuses a planted -1. fmt TestCountMallocs: 15 of 16 legs over their bound, from recon run R's recorded output (CR-tolerant parse; my entry's 15 got/want pairs equal the record exactly; my own "12 of 16 want <=1" was wrong and is fixed to 13). ONE mechanism is named and read: `params Span<any>` is the stack pack, and aʗp.slice() copies it via CopyOf, one charged array per call with args; the no-arg leg is the one that passes. The rest of the surplus is NOT attributed, and the entry says so.
6. Suite `go test ./... -count=1` ok (go2cs 143.6s, repoguard 21.2s). Census CLEAN on every file. Merge: vs 51a1d30ff9 clean; the 0e98bc97cd conflict is ONLY the known maps/math hunk, identical without this ref. Unsigned. No .NET.

PREDICTIONS, windows (re-run every row whose manifest changed; linux scopes are read on the linux axis)
7. Cells UNCHANGED, word VALIDATED: crypto/cipher 27272/0, encoding/binary 140/6, internal/poll 19/0, math/big 230/1, os 1103/2, os/exec 116/0, os/signal 1/0, runtime/debug 4/5, syscall 65/0. Every windows-out-of-scope pin already absorbed nothing there (pass/pass, fail/fail or not built). Their comparison records gain `outOfScopeDisclosures` (cipher 1, binary 3, poll 1, big 1, exec 1, signal 2, debug 1, syscall 17); the page generator never renders that key, so the pages keep their shape. runtime has no row; its scope does not reach windows.
8. unicode/utf8: DIVERGED -> VALIDATED at 14 matched · 1 disclosed (the header's disclosed +1, 280 -> 281 on top of 8b). fmt: still DIVERGED at 61 · 1 disclosed · 1 unabsorbed, because TestSprintf ("%v" of a func with a String method prints <nil>) is a real defect and is NOT pinned. It re-banks at 62 · 1 once that is fixed.

NEXT: the internal/synctest sizing, on its own ref.
