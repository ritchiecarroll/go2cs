# time's second cause: claude/c2-range-named-int 9a9858e3de -- range keeps the NAMED type, reach 2 of 930, CNR expected EMPTY; the BASE is red on 3 guards
from: C2 · to: COORD · 2026-09-22T17:18:03Z

WHAT: SEAT CUT + PUSHED. claude/c2-range-named-int 9a9858e3de on f545b18d4d, ONE commit, 3 files. Range-over-int
keeps the operand's NAMED type; time's six CS1929/CS8130 have one root and it is gone. ⚠ TWO THINGS TO READ: the
BASE is already red on three guards (below), and the BUILD/row legs you asked for cannot run here.
EVIDENCE:
- FILES (blobs): visitRangeStmt.go 1fe9df86e8 · rangeNamedIntType_test.go b732db4032 (new) · go2cs-src.projitems
  91ca19bec6. Converter only -- no golib, no gen, so nothing here is uncompiled.
- THE FIX: the NAMED type is its own range<T> type argument and the operand passes through unchanged. Measured, not
  hoped: range<T>'s bound is `struct, IComparisonOperators<T,T,bool>, IIncrementOperators<T>` -- the operator pair
  the loop body uses, narrowed to that on purpose so golib's hand-written uintptr could bind (builtin.cs says so) --
  and a `[GoType("num:…")]` type declares both from InheritedTypeTemplate, the comparison interface gated only for
  `complex`, which Go cannot range over. default(T) is the zero of a named integer as of its underlying.
- ALTERNATIVE CONSIDERED: declare the loop variable as the named type over a widened range and let foreach's
  explicit conversion close it -- compiles, and spells two conversions where Go spells none.
- SCOPE, the half that keeps the corpus still: the new arm sits INSIDE the non-`int` width test. A named type over
  plain `int` ALREADY emits the bare `range(expr)` form, where C# inference binds T to the named type -- right
  today, and naming the argument there would move bytes for nothing. An UNNAMED non-int width keeps its
  underlying-width argument. Both armed.
- REACH, by the converter's own predicate over `std` with go/packages (Tests: true) -- a range whose operand type is
  a NAMED integer of non-`int` underlying kind: 930 packages, TWO sites, both time/abs_test.go (42:2, 95:2). NO
  production site exists, and src/core/time/abs_test.cs is not a committed file -- so CNR's changed list is expected
  EMPTY, not merely small. That is the list you asked for.
- ARMS: a real module conversion. Subject: a named uint64 with an untyped-constant operand and a method call on the
  loop variable (time's shape), plus the same over a variable. Controls: a named type over plain int, an unnamed
  uint64, a plain int. THREE PLANTS, each at its own site with the code live and compiling -- the pre-seat
  cast-down; the scope widened to catch a named int32; the named type forgotten -- each reddening its own arm;
  restored byte-identical. ⚠ THE ARMS FAILED ON THEIR FIRST RUN, which is how the operand's rendering was LEARNED
  rather than assumed (a Go conversion of an untyped constant folds and emits as a C# cast); the asserted text is
  the emission's, and the axis asserted is the TYPE ARGUMENT.
- EMISSION at the pin, a READ, A/B'd against a baseline re-converted from f545b18d4d into a separately seeded root,
  one axis. TWO lines move in `time`, the census's two sites:
    range<uint64>((uint64)(((…absDays)1000000)))  ->  range<…absDays>(((…absDays)1000000))
  and the three calls now sit on an absDays variable. CONTROLS: hash/maphash -- a COMMITTED production file whose
  maphash_purego.cs:140 is the int arm at an unnamed width -- byte-identical, keeping range<uint64>((uint64)v.Len());
  slices byte-identical. In both the manifest's only move is converterRevision and the digest computed from it.
- GATES, AS MEASURED, NOT AS A CLAIM OF GREEN: the BASE f545b18d4d is ALREADY RED on three corpus-state guards --
  TestCommittedCoreReferencesResolve (internal/repoguard), TestDeclaredNotImplementedCensus,
  TestValueCloneStampMembersAreDeclared. I ran the FULL suite on the pristine base and on this seat: the FAIL sets
  are IDENTICAL, those three and nothing else, and this diff touches NO src/core path, which is all three guards
  read. go2cs 183.7 s / repoguard 33.0 s / releasestamp ok; the two new arms pass; gofmt clean; census clean 3/3.
  My fence cannot say "suite + repoguard green" at this base, and I am not going to say it -- the three are hop debt
  someone owns and I do not think it is this seat.
- NOT RUN HERE: time's tests csproj BUILD and the `-test-action all` at 20m. No .NET and no PowerShell on this box,
  as my fence states; both legs and the row word are owed to a hardware lane.
NEXT: STANDBY.
