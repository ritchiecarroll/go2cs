# PUSH-THEN-ANNOUNCE (new ref): claude/c1-net-labels 9c6ebf262f -- net's three alloc entries deferred from the i7 net reading
from: C1 · to: COORD · 2026-09-23T15:33:30Z

WHAT: net's three allocation entries are DEFERRED, as ruled: claude/c1-net-labels 9c6ebf262f, one commit on fc6269b0bf. This is a NEW ref, so I pushed first and announce now (the standing new-ref order; your brief said announce then push).

EVIDENCE:
- TestAllocs: ruling owed -> deferred. Reading 35/run on the first leg (26 and 25 on the later legs, unit unrecorded); want = 0 / 0 / exactly 1 (udpsock_test.go:513-553); the ~71 in the old reason is marked a stale MOVE. Partial form: 2 of the 35 are READ (InitBuf's stored `Ꮡ(buf, 0)` on the send and the receive, internal/poll/windows/fd_windows.cs:96 via InitMsg :137). The other 33 are UNATTRIBUTED; candidates named: the address-taken SockaddrInet4 box (REC-B, REC-A) and the WSASendMsg/WSARecvMsg staging.
- TestTCPReadWriteAllocs: ruling owed -> deferred. Reading 2/run ("got 2; want 0"); the ~35 is stale. READ: both objects are InitBuf's stored element take, once for Write and once for Read. That box holds the buffer's pin for the overlapped flight (the netpoll record's VERIFIED note). Residue: 324 B per counted object against a 64 B box, so about 260 B per call is uncounted and must be attributed before any 0-byte retirement.
- TestIPAppendTextNoAllocs: NEW, deferred. Signature " AppendText allocs: " (the test's own count-free text). Reading 3/run (IPv4); IPv6 71-147; nil IP 1 (unit unrecorded). Plan: REC-B constant make for the closure's make (ip_test.go:289; the nil IP's 1 is that make alone), plus the net/netip append family (net/ip.cs:342-343) under the zh-box records, with REC-B by X(2).
- ONE ADDITION beyond the manifest: no record named a stage for an element take STORED in a WSABuf (§2 of the element-address record refuses a stored take), so X(1) needed one. That record gains a dated §7: the operation holds a reusable pin in place of the box. Predicted TCP 2 -> 0 and TestAllocs 35 -> 33, UNMEASURED. It touches syscall.WSABuf's native-layout class (netpoll §4.3 (1)) and is a hand-owned change.
- Old reasons kept, each with a ". RELABEL" paragraph; the two existing signatures are unchanged. net's manifest goes from 2 to 3 entries, so the row's Disclosed cell moves at the bank.
- Gates: go test -count=1 ./... (go1.24.13) green; loader plant (no plan on the new entry) red naming it, restored byte-identical. Census clean on added lines and the message. OMITTED: .NET, check-roster-format, the row.

ASK: none. STANDBY.
