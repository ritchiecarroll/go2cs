# C1 BATCH 7 eeed65f66d: both manifests minted (6 + 4 pins), fips140test 2267/7, sha3 23/5, embedtest 7/0 -- maps HELD, its manifest is 58 pins = 60 disclosed, not 108
from: C1 · to: COORD · 2026-09-22T16:22:23Z

WHAT: BATCH 7 pushed -- claude/c1-alloc-pins-fips140test-sha3 eeed65f66d, ONE commit on f545b18d4d. Both manifests minted, 216 -> 218 rows. TWO of the three rows are in; internal/runtime/maps is HELD on an arithmetic disagreement, not a preference.
EVIDENCE
- fips140test: SIX pins, class alloc-profile, signature `expected zero allocations, got `. The five re-pins carry class and signature VERBATIM from the retiring crypto/internal/edwards25519 and crypto/internal/nistec manifests -- I read both on master to confirm rather than assume, and the relocation record's "expected to survive verbatim" holds exactly. XAES reused verbatim from my classing record. Readings: Edwards25519 75/run, P224 8,480, P256 16,149, P384 12,568, P521 17,086, XAES 199 (a lower bound). Row: Tests 2196 -> 2267, Disclosed -> 7 = 6 pins + the parent TestNISTECAllocations by aggregation.
- crypto/sha3: new row 23 / 5, FOUR pins (New 18/run, NewSHAKE 16, Sum 16, SumSHAKE 13) + parent by aggregation. Labelled DEFERRED. I put the discriminator in every entry because it is the part that could read as arbitrary next to my own XAES call: all four are the COUNT arm, and a mechanism can be NAMED -- the sponge state object New256()/NewSHAKE128() returns and the closures' non-escaping byte slices, none of which the CLR's object model requires an allocation for -- so the excess is reducible bridge work and a floor claim would be unfalsifiable. XAES is STRUCTURAL on a 199-per-run LOWER BOUND over a whole AEAD round trip. What separates them is whether a removable mechanism can be named, NOT the size of the number; each entry also records that a later floor measurement moves it deferred -> structural, which never re-opens.
- Signatures verified from the 1.24.13 sources, not carried: edwards25519_test.go, nistec_test.go, xaes_test.go and sha3_test.go:223's family all print `expected zero allocations, got ` (edwards25519 uses %0.1v where the others use %0.1f -- after the signature, so the want-free form is identical across all ten).
- embed/internal/embedtest: new row 7 / 0. Page asserted present at f545b18d4d by `git cat-file -e` before minting; no manifest, so Disclosed 0 is measured, not assumed.
- internal/runtime/maps HELD: you ask for Disclosed 108, but G's manifest carries 58 pins (46 subtest + 12 bare) at BOTH f401c4ea07 and f545b18d4d, and by the disclosed-parent aggregation rule that is 58 + 2 parents riding = 60, not 108. Writing 108 against a 58-pin manifest is precisely what check-roster-format's disclosed check reads, and it would refuse -- correctly. One line to land once it is 60 or the missing 48 pins arrive.
- FLAGGED, not acted on: crypto/sha3's proof page does not exist at f545b18d4d, so its row links a page your re-run banks after this push. That is the sequence you set; it does mean the index tool refuses on TWO page-less rows in the interim (sha3 and the pre-existing fips140test hop debt), transient and expected. 218 rows, no duplicates, correct alphabetical placement; the one inversion (encoding/xml before encoding/pem) is still the pre-existing one. No header, no denominator touched.
ASK: confirm maps at Disclosed 60 (or push the missing pins) and it lands as a second commit. Then STANDBY.
