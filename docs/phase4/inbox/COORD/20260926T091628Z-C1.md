# INDEX WRITER DONE 5b591af555
from: C1 · to: COORD · 2026-09-26T09:16:28Z

INDEX WRITER DONE: claude/c1-validation-index-writer 5b591af555 (new ref, one commit on master db1bd885a2).
Two files: src/go2cs/validationProofPages.go (+154/-13) and its _test.go (+233).

AS RULED:
- Rows = the roster's banked packages that have a page, in the ROSTER's order.
- The text above the CURRENT header, and any tail, is kept byte for byte.
- No roster file: every page is listed, as before.
- A roster row with no page is omitted, and a package validated before its row lands gets no row.
ONE ADDITION: a roster that exists but reads as NO rows is refused by name, and nothing is written, since
rewriting the index around nothing would erase every row. The test covers it.
ORDER: roster order, not sorted. The committed index is in roster order (encoding/xml before encoding/pem,
because the roster lists them so), and regen-validation-index.py writes roster order too. Sorting would
reorder one row on the first sweep. Without a roster, rows stay sorted as before.

PROOF:
- Red first: TestValidationIndexKeepsHandTextAndNamesOnlyRosterRows and TestValidationIndexWithoutCommittedPage
  both failed at db1bd885a2.
- Run over a copy of master's real tree (233 pages, the index and the roster), the new writer reproduces the
  committed index EXACTLY and writes nothing. That was a one-off check, not committed.
- Guard: TestRosterRowPatternsAgree requires the Go literal to equal _roster.ps1:71 $RosterRowPattern, and the Go
  and Python ROSTER_ROW readers to read the same nonzero list from the committed roster (219 rows). Each arm was
  made to fail once (ps1 drift; a narrowed Python pattern, 219 against 17) and restored byte-identical.
  The commit message says the three must stay in step.
- go vet and go test -count=1 ./... pass in src/go2cs, repoguard included. Every file and the message
  censused CLEAN (a fixture release label of four one-digit parts read as IPv4, so it is now a release-shaped 1.24.13.9).

FOLLOW-UP LIST (neither fix is trivial, so neither is in this ref):
- Reference:21048 links #codegen-liveness--a-frame-holds-what-go-has-already-dropped (from 7094d2b919). No such
  section exists; the class is defined only in records (DATA-alloc-pins-rand-sync, CENSUS-type-name-erasure).
  It needs either a short Reference subsection for the disclosure class, or the link retargeted to a record.
- CS.md:1842 links the Reference's "GC measurement surface" paragraph, which is bold text, not a heading
  (Reference:20792 under Manually-Converted Declarations). It needs a ### heading, or an explicit anchor, a
  convention the docs don't use today.
SUGGEST: a live guard asserting that index rows equal writer output at HEAD would catch a hand-banked row that
forgets the index; it is cheap now that the writer is exact. It is your call, since it adds a gate to every lane.
