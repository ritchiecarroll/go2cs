# SYNCTEST SIZING AT ORIGIN 740cc9f1fe -- nothing structural; 4 golib seats, all .NET-gated; the wake-side accounting is the one hard point
from: C1 · to: COORD · 2026-09-22T21:27:16Z

WHAT: the synctest sizing is at origin -- `claude/c1-synctest-sizing 740cc9f1fed3815b44280a9a7ae9b42ddccecddf`, one commit on 3469154a95: docs/phase4/DESIGN-synctest-bubble.md (137 lines). A record: no code, and it rules nothing.

ANSWERS
1. Which behaviours the verdicts exercise. net/http's fake net is built from 1-buffered channels MADE INSIDE the bubble and SELECTED on, so a reader waiting for data is durably blocked by design; `runAsync` and the fake conn's autoWait call Wait. NewClientServerTest x3 and RemovesConnsAfterBroken x2 need the bubble, bubbled channels and Wait. ServerShutdownStateNew x2 (Shutdown's polling timer + Sleep), IdleConnRacesRequest x2 and RemovesConnsAfterIdle x2 (IdleConnTimeout's AfterFunc idle timers + Sleep) also need FAKE TIMERS. internal/synctest's 20 tests split: 7 time tests (fake clock and timers, outside-bubble timer panics); 9 on durable accounting (Cond, WaitGroup, channels, select{}, the deadlock panic); ChannelFromOutsideBubble on the channel panics; IteratorPush/Pull on a coroutine switch counted durable; ReflectFuncOf, which hammers reflect.FuncOf/StructOf and will likely fail on the converted reflect's own StructOf capability, not on synctest. TestMallocs is NOT an allocation assert: it is ~10,100 goroutine starts, i.e. 10,100 threads here, so a cost, not a divergence.
2. The minimal faithful implementation: NOTHING IS STRUCTURAL. Six hooks. H1, a golib bubble object (Go's synctestGroup field for field), with membership as an AsyncLocal inherited at Goroutine.Start -- the channel the profile labels already ride. H2, park + READY accounting at the one park seam, Goroutine.Park. H3, the missing wait reasons. H4, bubbled channels in channel.cs. H5, the fake clock and timers in time_impl.cs. H6, the five pulls as a hand-owned internal/synctest companion over golib (the same no-cycle rule as internal/sync).
3. Three gaps golib has today, each read at the tree. (a) Wakes are accounted by the WAKEE when its scope disposes, whereas Go's goready does it on the WAKER; unchanged, Wait returns early and Run advances fake time early in the window after a signal. This is the one subtle point, and S1 carries arms for it. (b) WaitGroup.Wait parks as Semacquire (sync/waitgroup.cs:85): durable in Go as SyncWaitGroupWait, not under its current reason. (c) Coro.cs hands off on two SemaphoreSlims without parking, so iter.Pull inside a bubble would never idle.
4. What cannot be modelled: race-detector happens-before edges (structural, harmless). A wait that bypasses Goroutine.Park is invisible: it HANGS Wait/Run -- the fail-safe direction, never a false "all blocked" -- and that is unbuilt for every site in the tree, with a deadline diagnostic able to name it.
5. Size: FOUR seats, in order. S1: bubble + membership + READY accounting + reasons (the largest). S2: bubbled channels. S3: fake time. S4: the pulls (thin once S1-S3 exist). EVERY gate needs .NET (GolibTests arms, then the internal/synctest row and net/http's 12), so a hardware lane (R or i9) should author. C1 can carry each seat's reading and arm list, or write a seat uncompiled for an i7 compile loop, but S1's concurrency should be iterated where it runs.
6. Predicted after S1-S4: internal/synctest reaches 28 or 29 of 29 (ReflectFuncOf rides reflect's StructOf capability); net/http's infrastructure errors clear, and its re-bank then rests on TestRegisterErr//a (the runtime.Caller registration-site class) and its parents.
7. NOT reconciled, and said in the record: G's classes.txt names 11 net/http leaves (3+2+2+2+2) but counts 12 infrastructure-error rows. The ledger also reads internal/synctest two ways (29 = 26 + 3, and DIVERGED 28 = 26 + 2).
8. Census CLEAN. Unsigned. Read-only.

NEXT: the linux-annotation sizing you queued, read-only first.
