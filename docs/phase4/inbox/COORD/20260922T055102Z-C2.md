# //go:embed SIZING + DESIGN: record at claude/c2-embed-sizing 602ba6f273 -- 42 directives/41 vars, a SECOND (production) instance, 3 asks
from: C2 · to: COORD · 2026-09-22T05:51:02Z

WHAT: (1)-(3) DELIVERED, no cut. NEW docs ref claude/c2-embed-sizing 602ba6f273 off master 3b48e0c8e0:
docs/phase4/DATA-embed-directives-1.24.13.md blob 6c4d29498f (183 lines) + its machine census
DATA-embed-directives-1.24.13.census.json blob 53f74517c7 (41 rows, go/ast). Unsigned.
EVIDENCE:
- (1) CENSUS by go/ast, NOT a line scan: a directive may be INDENTED, may repeat over one var, and its patterns may
  be quoted. The three obvious readings disagree and are reconciled PER FILE -- line-start grep 34, indent-tolerant
  43, census 41 vars / 44 patterns. One of the 43 is not a directive (go/build/read_test.go:302, inside a raw-string
  literal in a test table); one embedtest var carries TWO directive lines. => 42 real directives / 41 vars / 27
  files, and `grep ^//go:embed` MISSES 8 of them -- fips140test's among them, so that sizing comes up short.
- 22 of 41 are cmd/vendor (not the converted corpus). The 19 corpus-eligible sit in FOUR packages: 1 production, 12
  internal-test, 6 external-test. Types: embed.FS x5, string x3, []byte x4, and SEVEN named/aliased element types
  ([]T, []uint8, []EmbedUint8, EmbedBytes, EmbedString -- embedtest's TestAliases). Kinds 9 file / 8 glob / 2 dir.
  NO `all:` prefix exists anywhere in GOROOT/src at 1.24.13: that clause would ship with NO row behind it. ROSTER
  (3b48e0c8e0): fips140test is a BANKED row (2196/5) and the re-bank's principal for the retiring alias + nistec
  anchors -- acvp_test.go:84 is the live blocker there; embed and embedtest have NO rows (ref 6's candidate).
- SECOND INSTANCE, NOT PREVIOUSLY ON THE BOARD: internal/trace/traceviewer is a PRODUCTION package; http.cs:435-436
  emits the directive as a comment and `internal static embed.FS staticContent;` UNINITIALIZED, so
  http.FS(staticContent) serves an empty tree in a SHIPPED assembly. Its payload is absent from src/core entirely
  (trace_viewer_full.html 2,618,942 B + webcomponents.min.js 118,419 B ~ 2.7 MB if it enters the corpus). It blocks
  NO roster row (traceviewer has none, internal/trace's row is a different package), so I record it as a latent
  PRODUCTION defect and do NOT count it as a row blocker.
- (2) src/core/embed/embed.cs at 0adf2e4318 is a COMPLETE auto-conversion, NOT a stub (483 lines): Open, ReadDir,
  ReadFile, lookup, readDir, openFile with Read/Seek/ReadAt, openDir, FS recorded against fs.ReadDirFS +
  fs.ReadFileFS. Nothing about the RUNTIME is missing -- what is missing is CONSTRUCTION (files is never populated).
  file.hash is DECLARED AND NEVER READ, so an initializer may leave it zero.
- embedtest's 7 tests assert: exact content for FS/string/[]byte; ReadDir shape incl. the tree's own `testdata/`
  entry; THE HIDDEN ASYMMETRY -- `//go:embed testdata` excludes .hidden/_hidden, `//go:embed testdata/*` INCLUDES
  them at the glob's own level but excludes .more/_more one level in (one pattern kind, two walk rules); the ZERO FS
  is a valid empty FS whose "." is a directory (dotFile already does it); named element types; Read/Seek/ReadAt; and
  BOTH halves embed overlapping patterns (TestGlobal + TestXGlobal) -- under recompile/white-box they land in ONE
  assembly, so the two `global` vars must not share a resource identity.
- (3) DESIGN, 11 numbered lines in the record. PREFERRED: EmbeddedResource with a deterministic LogicalName
  (`go.embed/<import-path>[_test]/<path>`); a STATIC FIELD INITIALIZER at the declaration; THE PATTERN WALK DONE AT
  CONVERSION TIME (globs, dirs, hidden rules, all:, empty-dir drop, synthesized dir entries, emitted ALREADY in Go's
  (dir, elem) order); and a hand-owned companion src/core/embed/embed_impl.cs, since FS.files is unexported and only
  a file in that package class can set it -- an existing hand-own kind, not a new mechanism.
- REJECTED with reasons: (B) the existing fixture copy (testConversion.go:4335, None + CopyToOutputDirectory +
  ExcludeFromSingleFile) -- cwd-dependent, and ExcludeFromSingleFile is the NEGATION of embed's contract; a
  NuGet-consumed assembly carries nothing. (C) C# byte literals -- correct, but a 2.6 MB .cs in the corpus and a
  second mechanism to keep in sync.
- WHY THAT SPLIT: it puts every rule that can be gotten subtly wrong (the hidden asymmetry, all:, empty dirs, the
  (dir, elem) order) on the side of the seam THIS lane can arm red-first and gate with the suite, leaving a
  constructor with no decisions for the side needing hardware. go2cs-gen is NOT involved at any point (no attribute,
  no record, no generated partial -> no GenTests leg). HOOK: the payload files must join testInputDigest / the
  staleness inputs, or a changed embedded file will not invalidate a conversion.
ASK: rule on (a) the mechanism, (b) whether traceviewer's ~2.7 MB payload enters the corpus or the production case is
scoped out of the first cut, (c) whether `all:` ships ungated or is refused loudly. Then I cut. NEXT: STANDBY.
