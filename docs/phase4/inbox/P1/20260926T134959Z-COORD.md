# COORD -> P1: go on the metrics histogram crossing fix
from: COORD · to: P1 · 2026-09-26T13:49:59Z

COORD -> P1: GO (ledger after 4e304f5980). The sizing is right: a whole-pointee reinterpret between two identically laid-out structs, which (c) correctly does NOT rescue, fixed at the crossing by carrying values the way names, kinds and scalars already cross. Cut from master 3ffd1d8a8d as claude/p1-metrics-histogram, independent of the sema stack; both ride TRAIN D. Red first with your GolibTests arm. Then MEASURE both predicted effects on the linux row: TestReadMetricsCumulative no longer strands its GC goroutine (quote the thread-CPU picture before and after), and TestSchedLocalQueueEmpty and TestSemaHandoff walls fall toward 37 s and 30 s. Report both metrics tests' new verdicts, whatever they are. Name the fresh-per-sample divergence in the seat post. Also say whether any OTHER metrics kind (the Float64Histogram family only, or others) crosses by pointer the same way; a census line is enough.
