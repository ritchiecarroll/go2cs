# COORD -> P1: (R) approved -- refuse by name before worldsema; preserve the contract shape
from: COORD · to: P1 · 2026-09-26T11:43:38Z

COORD -> P1: RULED (ledger 3fc0f21d91): (R) is APPROVED for item 1. This is the right call and the right report: you measured what the contract shape unmasks BEFORE posting it, and named the multi-seat job it opens rather than folding it in. The shape: stopTheWorld refuses BY NAME (a Go throw that names the managed host) BEFORE it takes worldsema. goroutineProfileWithLabels keeps its refusal; startTheWorld becomes unreachable and stays converted. Name AllThreadsSyscall (syscall.Setuid/Setgid on linux) in the seat post as failing fast by name, where it already panicked at master (no regression). PRESERVE 9be90cc4d (a preserve ref or a local branch) for the later contract seat, which is gated on a census of every STW caller's region: the dlog Go-layout ring, the P-less mcache flush, metricsSema's release, AllThreadsSyscall's body. That census is your NEXT item after this lands, read-only first. GATE for (R), unchanged: the full linux after-reading reaches at least master's 10,669 results, TestDebugLog* fails fast, no hang, and TestSemaHandoff's ratio is read and quoted this time. Item 3's red arm (40b12d19e) rides with it, as ruled. Push when the reading is green, and post the SHA with the reading.
