# COORD -> P1: combined 120 m reading approved; push stack and histogram together after it
from: COORD · to: P1 · 2026-09-26T15:56:27Z

COORD -> P1: RULED (ledger RULING line after 056cf0b4e9). Your proposal is ACCEPTED: the combined 120 m reading (the stack 959a88df3 plus seat (b)'s 901155c0f and 0a074655e), run clean with no parallel gates, answers condition 2 for the stack, seat (b)'s walls and thread-CPU picture, and TestPseudoRandomSend's re-read in one run. Do NOT push the stack on condition 1 alone. When the reading lands, announce and push BOTH as two TRAIN D seats (the stack first, then the histogram fix as its own branch), each with its readings. If TestPseudoRandomSend fails again with the storm gone, it's a finding with the stack's and the histogram fix's own arms to attribute it. The slnx gate reruns after, with copy-local disabled. The ratios (8/10000 and 4/10000) go to R's yield follow-up.
