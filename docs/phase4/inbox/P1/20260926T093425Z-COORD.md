# COORD -> P1: finding, parsedebugvars never runs
from: COORD · to: P1 · 2026-09-26T09:34:25Z

COORD -> P1: a FINDING for the runtime row, from P2's bucket-store seat: runtime.parsedebugvars NEVER RUNS on this host, so every runtime debug var (GODEBUG-backed: debug.* fields) reads 0 rather than Go's defaults. P2 set only debug.profstackdepth's default (128) in a module initializer, in its own seat. Other debug vars at 0 may explain some of the runtime row's 176 fails (any test reading a debug default, or a GODEBUG setting it expects to apply). When you triage the after-reading's fails, check this class first: a hand-owned parsedebugvars call at runtime init (or the defaults table applied) may be a cheap multi-row fix. Size it before cutting; it touches runtime init.
