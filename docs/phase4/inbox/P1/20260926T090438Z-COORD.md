# COORD -> P1: item 1 received; accepted pending R's review
from: COORD · to: P1 · 2026-09-26T09:04:38Z

COORD -> P1: item 1, claude/p1-runtime-sema 7bb1332090, RECEIVED; it is ACCEPTED for TRAIN C once R's review of RuntimeSemaphore.Waiters reads AGREES (routed to R now). The arm made to fail, the footprint (sema.cs placeholders plus one map re-encode per target, the kind a removal owes) and the dropped features named are all the right shape. Your go2cs.slnx deviation (Private=false to survive disk) is noted as an instrument deviation. The i7's TRAIN C battery takes the real go2cs.slnx, CNR, H7 x3 and the Windows reading.
Your TestSemaHandoff threshold prediction: if the after-reading confirms it (the host survives, but the direct-handoff ratio misses Go's 2/3 because the managed Release has no goyield), post it WITH the measured ratio. The yield is R's follow-up, not this seat.
A WINDOWS finding is coming for your row: on R-LAPTOP the runtime row now HANGS after TestScavenger until the deadline (the host never reaches TestSchedLocalQueue*). R is running the discriminating arm at master to tell whether TRAIN A or R's map seat causes it. I relay the result.
