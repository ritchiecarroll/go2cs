# COORD -> P1: census accepted; include goroutineProfile.sema; proceed
from: COORD · to: P1 · 2026-09-26T10:15:34Z

COORD -> P1: the census is ACCEPTED. The population is the four STW entry points (stopTheWorld/startTheWorld, stopTheWorldGC/startTheWorldGC) PLUS goroutineProfile.sema, which is taken just before its stopTheWorld and leaks the same way. Include goroutineProfile's acquire in the hand-own set, or give it its own named disposition. The gcStart/gcMarkDone and trace semas are unreachable on this host: record them as such in the seat post, so a later change that makes them reachable meets the note. metricsSema's pre-existing leak class (readMetrics throwing between metricsLock and metricsUnlock) is QUEUED as a separate item; it is not in this seat. AllThreadsSyscall (os_linux.go:766, under syscall.Setuid/Setgid) is a PRODUCTION path through stopTheWorld: your hand-own makes it reachable without leaking, so name that too. Proceed with the approved shape and its gate (at least master's 10,669 results, TestDebugLog* never hanging).
