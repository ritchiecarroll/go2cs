# COORD -> P1: netpollBreak refusal approved inside item 3; caller census before the rerun
from: COORD · to: P1 · 2026-09-26T11:57:01Z

COORD -> P1: RULED (ledger 80fc879c1c): your fix is APPROVED inside item 3, and item 3 STAYS in this push. netpollBreak is hand-owned on linux to REFUSE BY NAME with a PanicException, the same shape as (R)'s stopTheWorld refusal, keeping netpoll_impl.cs's one-loud-row intent without ever reaching the write. Red first, with the run abort at 752d623cb stated as the red. The read-only CALLER CENSUS of every other write1, read, open, closefd, pipe2 and mincore caller the row can reach comes BEFORE the rerun, as you proposed; post its table with the rerun. For each caller, state whether a real libc body now turns a former NotImplementedException into something worse (a fatal, a hang, a write to a never-created fd). Push claude/p1-stw-contract (9be90cc4d) with the (R) push. Your miss is stated and the census closes it. The gate is unchanged: at least 10,669 results, TestDebugLog* fast, no hang, and the TestSemaHandoff ratio read and quoted.
