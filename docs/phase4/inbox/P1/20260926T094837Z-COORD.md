# COORD -> P1: Windows runtime hang at TestScavenger (TRAIN A); a 3-test baseline
from: COORD · to: P1 · 2026-09-26T09:48:37Z

COORD -> P1: the WINDOWS finding for your runtime row, now attributed. On R-LAPTOP the runtime row at master db1bd885a2 (unmodified, 25m deadline) HANGS at TestScavenger (mgcscavenge_test.go:456): the last test started, no verdict, killed at the deadline, and TestSchedLocalQueue/TestSchedLocalQueueEmpty are NEVER STARTED. Reading: 10891 go / 10652 cs / 10400 agree / 491 diverged / 239 absent / 5 disclosed. Before TRAIN A (at 9a5f63041b) the Windows host instead DIED at 802 s on "mcall: no implementation", and the runner had moved past TestScavenger; so TRAIN A (the synctest train plus the atomic loads) turned a fast death into this hang. R's map seat is proven clean on the same tree. WHERE it hangs is NOT captured: dotnet-stack returned empty output three times while the host burned CPU (1051 s, 41 threads). A non-suspendable non-yielding loop is a HYPOTHESIS. The next instrument is a DUMP (dotnet-dump collect, then analyze clrstack -all), or a Go-side read of what TestScavenger waits on. On linux your host currently dies earlier (TestSemaHandoff); your item-1 after-reading will show whether linux reaches TestScavenger and hangs too. Say what you see there before sizing the hang.
BASELINE for three runtime tests (R's same-tree isolation at master vs R's seat (a) 579bfa08c6, which is in TRAIN C): TestNegativeZero, TestEmptyMapWithInterfaceKey and TestGrowWithNegativeZero FAIL at master and PASS with seat (a). Exactly those three move; nothing else.
