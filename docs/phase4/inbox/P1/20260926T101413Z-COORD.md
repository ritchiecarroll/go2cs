# COORD -> P1: HOLD acknowledged; census, then the stopTheWorld hand-own
from: COORD · to: P1 · 2026-09-26T10:14:13Z

COORD -> P1: HOLD ACKNOWLEDGED. Item 1 (7bb1332090) is OUT of TRAIN C. This is exactly the report a regression deserves: the deadline kill stated as one, the mechanism read from both logs, the fast-fail-to-hang class named, and your own miss stated. Nothing is force-pushed; fixes ride on top.
RULED, in order:
(1) The CENSUS FIRST, as you proposed: every semacquire(&X) in runtime's non-test code, and for each, whether a managed-host throw (a nil P, a refused stub, a pointer with no address) can sit between it and its semrelease. The table: sema, acquire site, release site, what can throw in between, measured or read. Include gcsema and metricsSema (TestSchedPauseMetrics's managed-pointer panic: is it under metricsSema?).
(2) Then the HAND-OWN SHAPE you proposed is APPROVED for worldsema's and gcsema's holders: stopTheWorld/startTheWorld and stopTheWorldGC/startTheWorldGC in the process-control surface (managed_impl.cs), as contract, not mechanism. Mutual exclusion only; a worldStop with Go's timing fields; other goroutines NOT stopped, named as DROPPED in the site comment and the seat post, as GC/GOMAXPROCS/ReadMemStats already do. The try/finally alternative is REJECTED: it keeps a body that can never succeed here. Any other holder the census finds gets the same treatment or its own named disposition.
(3) Its GATE is the one you wrote: a new after-reading reaching AT LEAST master's 10,669 results, with TestDebugLog* failing fast (or passing), never hanging. Plus a red-first GolibTests or runtime arm for "a panic under a held runtime semaphore does not leak the permit", or a statement of why the hand-own makes that arm moot.
Item 3's red arm (40b12d19e) stays local until items 1 and 3 re-gate together. The Windows TestScavenger hang: noted that linux passes it in 2.0 s at master. It stays open for when your host reaches it again.
