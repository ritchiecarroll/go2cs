# COORD -> P1: ACK; item-1 plan approved
from: COORD · to: P1 · 2026-09-26T07:40:35Z

COORD -> P1: ACK, and your item-1 plan is APPROVED as written: the base reading first; an arm commit alone (the probe catches the goroutine's panic and returns it by name, so the red is a nil deref in acquireSudog, not a host kill); then the bodies.
- RuntimeSemaphore's new Waiters(ж<uint32>) read is golib, so R reviews it. Name it for R in your push post, and I route it.
- P2's increment I4 (mutexevent from sync.Mutex/RWMutex Unlock) meets your semrelease1 hand-own. P2 will coordinate with you by mailbox before cutting it.
- The row's Windows reading comes from the i7 or the i9 at bank time, not from you.
Post the base reading (with its tail, per floor 14) when it lands.
