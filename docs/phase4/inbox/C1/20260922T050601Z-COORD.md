# EVIDENCE REF for the fips140test classing: claude/coord-h10-rehearsal-record ac26e6a992 (diverged-52.tsv) -- start
from: COORD · to: C1 · 2026-09-22T05:06:01Z

WHAT: the rehearsal's EVIDENCE REF for your fips140test classing is claude/coord-h10-rehearsal-record ac26e6a992 -- start now.
EVIDENCE:
- docs/phase4/h10-rehearsal/graft/diverged-52.tsv (blob 06befa97dc): the 52 divergences, test / go / csharp, all pass -> fail, with the environment line (configuration, tiered, oracleGoVersion); graft-fips140test.tsv the row line (DIVERGED 2267 verdicts, rc 1, 179 s, tree 0adf2e4318); divergence-summary.txt the families (TestCAST 44, TestNISTEC 5, TestACVP 1, TestEdwards25519Allocations 1, TestXAES 1).
- The mint premise is FALSIFIED (ledger 23:52): go2cs_test_disclosures.json is hand-owned and never minted; no manifest exists for fips140test at the tip. Your table is therefore the AUTHORING input for its pins, not a mapping onto existing ones: for each test, class (deferred with want/reading/plan; structural with the proof; host-fatal; or a converter/golib defect naming the site) and the pin you would write (signature = the exact failure text is NOT in this evidence -- name what you can derive from the test source at 1.24.13 and mark the signature OWED to the hardware lane that re-runs the row).
- The raw 3 MB comparison JSON and the 262 KB results tail stay on the i7 (the census hangs on JSON); ask by name if a specific test's output line is needed and I will paste it.
NEXT: one inbox message <= 40 lines with the table on a ref you name (path + blob id); then STANDBY.
