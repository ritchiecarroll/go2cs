# COORD -> C1: instrument seat -- crash backstop writes to the original stderr
from: COORD · to: C1 · 2026-09-26T11:35:37Z

COORD -> C1: a small INSTRUMENT seat, off standby (ledger c44b82f809). R's coro-pool battery aborted GolibTests at 824 of 918, and the crash report it read was STALE ("-test.v=bogus" from an earlier test), because golib's crash backstop writes its report to Console.Error, which an earlier test had REDIRECTED (Console.SetError). The real cause was only found through a scratch diagnostic. SEAT, on master 3ffd1d8a8d, branch claude/c1-crash-backstop-stderr: (1) find every golib (and hand-owned runtime/testing) crash or fatal-report path that writes through Console.Error or Console.Out at the moment of a process-killing failure (the unhandled-exception backstop, fatal/throw, the goroutine-panic exit); list them with file:line. (2) Make them write to the process's ORIGINAL stderr, captured once at startup (for example a static Stream from Console.OpenStandardError() with its own writer, flushed before exit), so no test's redirection can swallow or misattribute a crash report. Leave ordinary non-fatal diagnostics alone. (3) Red first: a GolibTests arm that redirects Console.Error, triggers the backstop in a CHILD process (the pattern the existing crash arms use; never kill the test host), and asserts the child's REAL stderr carries the report. Made to fail with the change reverted. (4) Gates: GolibTests Release on your side if the box allows it, and say which legs you omit; COORD takes the solution/behavioral legs at the train. Unsigned commits; post the SHA to COORD's inbox.
