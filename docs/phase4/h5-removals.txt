# h5-removals.txt — the EXECUTABLE form of REHEARSAL-h5-go124.md §2's removal list.
#
# WHY THIS FILE EXISTS. The list has lived only in the REHEARSAL record, and this repo's own doctrine
# says a RECORD is "never executed from" — so nothing consumed it, and MEASURED 2026-09-08 against the
# ladder tree at master 8a1b7e71c: all 14 directories were STILL PRESENT. The list had never been
# applied. Four of the twelve sites on that rung are leftover seed from three of these.
#
# WHAT THESE ARE. Packages that exist at go1.23.12 and DO NOT EXIST at go1.24.13. The corpus seed
# carries them in; the target release's conversion never overwrites them (there is nothing to convert);
# so they survive beside their live replacements — e.g. crypto/internal/edwards25519 sits beside
# crypto/internal/fips140/edwards25519, internal/concurrent beside internal/sync, internal/weak
# beside weak. A build then counts 1.23.12 leftovers as though they were corpus.
#
# ⚠ REMOVAL IS NOT A DELETE. Measured the hard way on 2026-09-08: deleting the directories and
# line-filtering their entries out of go2cs-stdlib.slnx produced INVALID XML (28 x MSB4025, zero
# assemblies, one second). A .slnx is PARSED, never text-matched. And restoring a deleted directory
# from master is NOT seed-equivalent — it grafts a 1.23 package into a 1.24 tree and mints fresh
# 1.23-vs-1.24 API errors. These are removed BY THE SEEDED RE-CONVERT, which regenerates the solution,
# never by hand on a live tree.
#
# Paths are corpus-relative (under src/core). Derived programmatically from the record's own brace
# groups, and the expansion count (14) matches the count the record states.
crypto/internal/alias
crypto/internal/bigmod
crypto/internal/edwards25519
crypto/internal/edwards25519/field
crypto/internal/mlkem768
crypto/internal/nistec
crypto/internal/nistec/fiat
go/internal/typeparams
internal/concurrent
internal/weak
runtime/internal/math
runtime/internal/sys
vendor/golang.org/x/crypto/hkdf
vendor/golang.org/x/crypto/sha3
