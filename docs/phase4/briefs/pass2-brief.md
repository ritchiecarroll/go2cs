You are an i7 sub-agent for the go2cs fleet coordinator. PASS 2 OF THE H10 RE-BANK on the i7, in ONE worktree, three acts in order: (A) LEG 9 -- merge R's s2b shard ref; (B) THE RE-DISPATCH -- re-run the rows the batches fixed, through the pipeline, and bank them as a shard commit; (C) THE INDEX -- regenerate docs/validation/index.md once and let the guard derive the header. Then report. **DO NOT PUSH.** Read `CLAUDE.md` first (floors 3, 7, 8, 13, 14, 16).

## Fixed facts (the coordinator fills TIP5; assert by `git ls-remote origin`)
- BASE: `claude/version-go1.24.13` at `TIP5` (the batch-5 STAMP; `git merge-base --is-ancestor ae2f251988 TIP5` must hold).
- LEG 9: `claude/r-h10-rebank-s2b` = `a7cbca2637` (R; parented on c6fdbe73c3; 19 files: the test artifacts of crypto/internal/fips140/{aes,ecdsa,nistec} + their three proof pages; no roster edit -- their rows are already in TIP5 via C1's 5d11c8d3f9). Merge `--no-ff -S` with the legs brief's rules (blobs EQUAL; no production .cs; `git diff --diff-filter=D` empty; repoguard's TestNoFleetIdentifiersInTrackedFiles PASS; the format guard's page-less set shrinks from four rows to ONE -- fips140test -- quote it).
- Worktree: `git worktree add -b i7/h10-pass2-<mmddHHMM> /c/go2cs-tmp-coord/p2 TIP5` (a LINKED worktree ON A BRANCH -- the pipeline's artifacts are BANKED from it); lock `mkdir /c/go2cs-tmp-coord/p2.lock` + PID; scratch `<this session's scratchpad>/p2/`; the converter REBUILT in the tree under the pin; `git status --ignored=matching` = only `src/go2cs/go2cs.exe` before row 1; disk >= 25 GB. Pins as the batch briefs (GOROOT backslash spelling read back; the pinned bin first on PATH; GOTOOLCHAIN=local; CGO_ENABLED=0; pwsh 7 net8 apphost launched with DOTNET_ROOT unset AND the dotnet10 root off the parent's PATH, the .NET pins set inside via `$env:`). Sibling check per the batch briefs. Census: master's tool (blob 7da4915a2f or later).

## (B) THE RE-DISPATCH -- the rows and their PREDICTED words at TIP5 (the wrapper `src/run-h10-recon.ps1` from TIP5 by blob; `-tests -test-action all`, the floor from the sweep's long-timeout table per row -- hash/maphash 60m, fips140test 20m, runtime its floor; output dir the SECOND positional inside p2/src/core/<row>; results tails read FIRST; artifacts cleared before each row)
| row | predicted | why |
|---|---|---|
| crypto/internal/fips140test | DIVERGED 7 -> VALIDATED if C1's manifest (six pins) is in TIP5, else DIVERGED 7 UNDISCLOSED | batch 4 (check hand-own, println, embed); the pins are C1's manifest seat |
| crypto/sha3 | DIVERGED 5 -> VALIDATED if C1's TestAllocations pins are in TIP5, else DIVERGED 5 | batch 4 sha3 pair; the pins C1's |
| hash/maphash | Validated 59 | batch 4 escapeForHash; roster Tests 22 -> 59 |
| embed/internal/embedtest | Validated 7 (a candidate: its roster row is C1's to mint if absent -- report rowless) | batch 4 embed |
| crypto/x509 | Validated 518 / 0 | batch 5 template fix |
| internal/runtime/maps | VALIDATED 111 with 108 disclosed (a candidate row; report) | batch 5 pointer-model + pins |
| log/slog | PASS 197 with 19 disclosed; roster Tests 194 -> 197 | batch 5 forwarder NoInlining |
| crypto/rand | VALIDATED with 1 disclosed (roster Disclosed 1) | batch 5 pins |
| sync | VALIDATED with 6 disclosed | batch 5 pins |
| runtime | by cause: the host past TestMemmoveAtomicity; dies at getfp unless R's getfp ref is in TIP5 -- the word and the counts | batch 5 memmove; a reserved row, banks only if VALIDATED |
| internal/synctest | DIVERGED 28 (synctest.Run intrinsic, banked debt) -- NOT re-run unless a seat landed | -- |
| time | BUILD unless C2's anon-struct seat is in TIP5 (batch 6) -- NOT re-run unless it is | -- |
Every row's word and counts reported; a mismatch with the prediction is a FINDING (report the first divergence names), never reconciled by hand. BANK (a shard commit on the p2 branch, then `git branch claude/coord-h10-pass2-s1 HEAD`): the banking rule -- TEST artifacts only (`*_test.cs`, package_test_info.cs, package_info_internal_test.cs, go2cs_test_host.cs, `<pkg>.tests.csproj`), the proof pages of rows that VALIDATED, README badges if modified, and the roster ROW edits (Tests / Disclosed to the measured values for rows that VALIDATED; a row that did not validate gets no edit); NEVER a production .cs (restore and count), NEVER a manifest (hand-owned; if the pipeline rewrote one byte-identically it is unmodified; a changed one is a STOP), NEVER `docs/validation/index.md` (act C regenerates it), NEVER the header. Rows that VALIDATED but have no roster row (embedtest, internal/runtime/maps if rowless): bank their artifacts + pages and REPORT them rowless (C1 mints). Census `converted` on staged converted files, `entry` on the message; repoguard on the staged tree with a planted control.

## (C) THE INDEX and the header
After (A) and (B): `python docs/phase4/hopA-inputs/regen-validation-index.py` (master's tool at facb693040 or later, materialised by blob; it admits anchor-linked and bar-excluded pages) -- it must WRITE (`--write`) with ZERO orphans and ZERO page-less rows (if fips140test is still page-less because its pins are not in, it REFUSES: report and stop act C; do not force). Then `src/check-roster-format.ps1`: the header figures it derives are WRITTEN into the header lines and the README NEWS block exactly (the guard's expected values; never hand-chosen), re-run to `0 of N` or to a named set; ONE signed commit `-S`. Report the row count, verdict sum, disclosed sum, the percentage, and the index's row count == the roster's.

## Report (final message)
TIP5 asserted; leg 9's merge SHA and gates; per row the command line, word, counts, the results tail's last line, wall; the shard commit SHA with its path count per row and the roster edits made; the rowless VALIDATED rows; the production files restored; the index tool's line and the guard's before/after with the header figures written; `git ls-files` arithmetic; disk; the lock released; anything not verified. Worktree `/c/go2cs-tmp-coord/p2` IN PLACE. NOTHING PUSHED.
