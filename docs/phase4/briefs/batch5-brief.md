You are an i7 sub-agent for the go2cs fleet coordinator. APPLY BATCH 5: the SIX seat refs listed below merged in order (1-6) onto the version branch in a throwaway worktree, ONE battery, report. **DO NOT PUSH.** Read `CLAUDE.md` first (floor 3: the output directory is the SECOND positional; floor 7: capture every rc before any pipe; floor 8: never `git add -A`; floor 14: read the results tail). The conflict rules of the batch-4 brief apply: a `go2cs-src.projitems` adjacent-insert resolves to the UNION (asserted by `TestProjitemsRegistersEveryGoSource`); any other conflict is a STOP with the hunks quoted -- the coordinator rules per hunk.

## The refs (the coordinator fills SHAs marked TBD before launch; assert each by `git ls-remote origin`; STOP on any mismatch)
1. `claude/r-lookup-windows-getaddr-box` = `acb6b5a38a` (on c6fdbe73c3; gen/go2cs-gen/Templates/InheritedType/PointerTypeTemplate.cs: a Go named pointer type's outbound uintptr / void* operators defer to the box (ж<T>'s own operator) instead of a `fixed` DEREF; a nil named pointer -> 0; GolibTests NamedPointerTokenCarrierTests.cs 5 arms + a windows-only Compile Remove group in GolibTests.csproj; R gated GenTests 57, GolibTests 793/3 env/14, stdlib 344/0, crypto/x509 518 / 0, behavioral LookupServicePort PASS). ALLOWED paths: `src/gen/**`, `src/core/golib/**` (the GolibTests project). Rows: crypto/x509 VALIDATED 518 / 0; the behavioral suite's LookupServicePort PASS (gate (c) covers CNR; the Output phase for that one package by name).
2. `claude/g-pointer-model-layout-compat` = `f401c4ea07` (on c6fdbe73c3; FIVE commits -- the projection, the maps.rand companion, the map(nint) hint clamp, the 44-leaf manifest, the refuse-by-name companions with the four census rows deleted; G measured maps VALIDATED 111 = 3 + 108 disclosed, sync 106; the description that follows predates the final shape: fd830d5e1c the PrefixProjection registry in ж.Reinterpret + abi's Type -> mapType registration (golib 3 files +114); a second commit = the maps.rand body (Go's bodyless `func rand() uint64` linknamed to runtime.rand); a third = a NEW hand-owned `src/core/internal/runtime/maps/go2cs_test_disclosures.json` with the structural parent pin TestTableGroupCount). ALLOWED paths: `src/core/golib/**`, `src/core/internal/runtime/maps/go2cs_test_disclosures.json`, the linkname push-target registration in `src/go2cs/**` if the body is a converter row. Rows: internal/runtime/maps predicted VALIDATED 111 with 108 disclosed (G's measured figure); internal/sync 106 unmoved (the control); `TestDeclaredNotImplementedCensus` passes in gate (a).
3. `claude/c1-alloc-pins-rand-sync` = `65bc8acc2e` (C1; manifests: crypto/rand's NEW go2cs_test_disclosures.json with one entry; sync's manifest 3 -> 5 entries, want-free signatures; the roster rows crypto/rand Disclosed -> 1, sync Disclosed 4 -> 6; a DATA record with sync's owed rename-map entry TestMapClearNoAllocations -> TestMapClearOneAllocation). ALLOWED paths: the two manifests, `docs/ValidatedTestPackages.md` (two row cells), `docs/phase4/DATA-*.md`. Rows: crypto/rand and sync re-run at this tip: predicted VALIDATED with the new pins absorbing exactly the named tests (rand 1; sync 2 more).
4. `claude/c1-h10-new-rows` = `d66b3e288c` (TEN rows minted; wasitest/cgotest/copyright/fips140deps refused at the bar) (C1; docs only: up to FOURTEEN new roster rows for the first-time PASS packages -- crypto/hkdf 3, internal/pkgbits 2, crypto/internal/fips140deps 1, crypto/pbkdf2 5, internal/copyright 1, net/internal/cgotest 1, crypto/internal/sysrand, crypto/internal/fips140/rsa, crypto/internal/fips140/ecdh, go/ast/internal/tests, internal/coverage/test, runtime/internal/wasitest, unique, crypto/mlkem -- each read against the exclusion bar; C1 states which were minted). ALLOWED paths: `docs/ValidatedTestPackages.md` (rows only, no header). Gate: `src/check-roster-format.ps1`'s failure set unchanged or smaller; the header figures the guard derives recorded (the header itself is written from the guard's figures at the LEGS, not here).
5. `claude/c2-anon-struct-accessibility` = `TBD` (C2; the synthesized anonymous-struct type's accessibility follows its least-accessible field type -- time/abs_test.go's table over `Action<testingT>`; Go side or generator half uncompiled, as C2 states). Rows: `time` by BUILD (`-tests` convert + `dotnet build time.tests.csproj`, 0 errors -- was CS0052/CS0050/CS0051) and, if it builds, `-test-action all` at the sweep's time floor: the row's word by cause.
6. `claude/g-recv-forwarder-noinline` = `6492844624` (G, on c6fdbe73c3; gen/go2cs-gen RecvGenerator.cs + Templates/ReceiverMethod/ReceiverMethodTemplate.cs: the ж-forwarder carries [MethodImpl(MethodImplOptions.NoInlining)] when the source method does; GenTests arm RecvForwarderNoInliningTests red-first; 16 of 5,821 [GoRecv] ref-receiver forwarders gain the attribute -- log/slog 13, flag 2, internal/pkgbits 1). ALLOWED paths: `src/gen/**`. Rows: log/slog predicted PASS 197 with 19 disclosed (was DIVERGED 197/1: TestCallDepth's caller file/line lost to JIT inlining under the pipeline's tiered-off config) -- roster Tests 194 -> 197 owed at the leg; log 8, flag 24 unchanged; runtime/pprof DIVERGED 155/37 unmoved (its own state).
7. (the census `converted`-mode and public-handle seats landed on MASTER be8e137af5 / dd18e5e2ab -- the battery's census uses master's tool at dd18e5e2ab or later, blob 7da4915a2f.)
TARGET: `claude/version-go1.24.13` at `TIP4` (the batch-4 STAMP; assert by ls-remote).

## Setup, merges, battery, report
As the batch-4 brief (`docs/phase4/briefs/batch4-brief.md` on this branch): worktree `/c/go2cs-tmp-coord/ab5` detached at TIP4, lock + PID, scratch `<scratchpad>/ab5/`, the pins read back by output, the sibling check, disk >= 25 GB; merges `--no-ff -S` in order with the additive blob proof per COMPOSED file and the `src/core` path list asserted against the allowed sets; battery (a) suite + repoguard, (b) gen 0 errors + GenTests + GolibTests (counts verbatim; NamedPointerTokenCarrierTests 5 arms named), (c) CNR `NO REGRESSION` (ref 1 predicts ONE behavioral golden moving only if LookupServicePort's emission changed -- it should not: the template is generator-side; CHANGED packages listed verbatim), (d) stdlib 344 / 0, (e) the rows above each with the output dir the SECOND positional inside the ab5 seed, results tails read first, (f) census on every changed file and merge message with `selftest` at its current count. Restore the pipeline's side effects; status empty; `^ D` empty. The worktree stays IN PLACE at `/c/go2cs-tmp-coord/ab5`. NOTHING PUSHED.
