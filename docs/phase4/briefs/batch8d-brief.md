You are an i7 sub-agent for the go2cs fleet coordinator. APPLY BATCH 8d -- the H10 allocation RELABEL: two refs merged onto the version branch in a LINKED worktree ON A BRANCH, a PAGE-BANKING battery (every page whose disclosure text moved is regenerated; no verdict may move), the testing README badge repaired, the guard's new arm proven red-first; report. **DO NOT PUSH.** Read `CLAUDE.md` first (floors 1, 3, 4, 7, 8, 13, 14, 16) and `.claude/rules/harness-gates.md`, `.claude/rules/corpus.md` and `.claude/skills/validation-bank/SKILL.md` when you touch those trees. Never modify the main checkout or any other worktree under the coordinator's temp root. The coordinator's prompt supplies the absolute values for every <placeholder>.

## Why
The hop retires the legacy `alloc-profile` disclosure label (H10 step 3). COORD ruled the relabel (ledger 2026-09-23 03:37, lines X + O1-O5, mailbox 90c7968420; the seat brief `docs/phase4/briefs/h10-relabel-seat.md`); C1 implemented it; C2 made the proof-page heading class-aware. A label-only manifest edit cannot move a verdict (matching reads the class only for host-fatal and skip shapes), so this battery BANKS PAGES: every verdict pair and every roster cell must come out unchanged, and any movement is a STOP-finding.

## Fixed facts (assert each by `git ls-remote origin`; STOP on any mismatch)
- BASE: `claude/version-go1.24.13` = `1719e3b87f` (the batch-8c STAMP).
- Worktree `git worktree add -b i7/h10-batch8d-<mmddHHMM> <coord-tmp>/ab8d 1719e3b87f`; lock `<coord-tmp>/ab8d.lock` + PID; scratch `<scratchpad>/ab8d/`; disk >= 25 GB before the battery and before every row; `bin/` `obj/` purged after each row.
- Pins, GPG probe, census, sibling check: exactly as in batch 8c's brief (`docs/phase4/briefs/batch8c-brief.md`), read back by OUTPUT.

## THE REFS, merged `--no-ff -S` IN THIS ORDER (each asserted by ls-remote; the additive-blob proof per ref; `git diff --diff-filter=D 1719e3b87f HEAD` must name no file)
1. `claude/c1-alloc-relabel` = `030fb084cc` (four commits on bb54ff0920: 718060141d the first relabel, bae906e86c the records, 7ddecbf1de the manifests, 030fb084cc the guard arm `2g` in src/check-roster-format.ps1). C1 dry-ran the merge onto 1719e3b87f clean. ALLOWED paths: docs/phase4/**, docs/ConversionStrategies-Reference.md, src/core/**/go2cs_test_disclosures.json, src/check-roster-format.ps1.
2. `claude/c2-proofpage-class-heading` = `68960ac100` (one commit on bb54ff0920): src/go2cs/validationProofPages.go + its testdata/validationproof golden + a new control test. ALLOWED: those paths.
CONFLICT RULES as in 8c; anything else => STOP with the hunk quoted.

## Battery (converter source FROZEN while it runs)
(a) go2cs.exe rebuilt under the pin; the FULL converter suite `go test ./... -count=1 -timeout 40m` in src/go2cs (C2's golden + control, C1's manifests through the loader test `TestEveryCommittedManifestLoadsUnchanged` and friends): expected GREEN; quote any red in full.
(b) CNR (the merged tree's check-no-regression.ps1): `NO REGRESSION`, CHANGED predicted EMPTY (no C# emission changes). No golib/gen/C# change is aboard: GolibTests, the stdlib build and the behavioral suite are NOT owed -- say so.
(c) THE GUARD ARM 2g, RED-FIRST (floor 13): at the merged tree run `src/check-roster-format.ps1` from `src/` (expected: every section green, 2g included -- C1 predicted 212 rows at the pin, 0 refused). Then PLANT: set one entry of one BANKED 1.24.13 row's manifest (e.g. bufio) back to `alloc-profile`; the guard must fail naming that row and entry; restore byte-identical (`git diff --quiet`) and re-run green. Quote both runs' 2g lines.
(d) THE ROW SET: derive it, never guess -- (i) every roster row whose `go2cs_test_disclosures.json` changed in `1719e3b87f..HEAD`, UNION (ii) every banked roster row whose manifest at HEAD carries a `deferred` entry (C2's heading moves those pages by the claim line + one sentence, +211 bytes LF each per C2). Print the list with the reason per row. Expected about 30 rows (C1's 26 manifest rows; crypto/rand, crypto/sha3, fmt and unicode/utf8 by the heading alone). EXCLUDE net (its chain is pending) and reflect (unbanked); say so.
(e) THE PAGE-BANKING RUN over (d), through a per-run COPY of the merged tree's `src/run-h10-recon.ps1` (it applies each row's roster execution pin, e.g. log/slog's release-tiered), `-tests -test-action all -test-config Release`, the output dir the SECOND positional inside the worktree's src/core/<row>; results tails FIRST; test artifacts cleared before each row; production re-emissions restored after each (count them by class). PER ROW, all must hold, else STOP-finding naming the row:
  - the verdict set (go/cs per test) EQUALS that row's committed page at 1719e3b87f, verdict for verdict;
  - the roster cells (Tests = matched, Disclosed = disclosed) are UNCHANGED;
  - the regenerated page differs from the committed page ONLY in disclosure text (class/reason/want/reading/plan cells), C2's heading and sentence (rows with a deferred entry), and the page date -- quote any other hunk;
  - no page carries `alloc-profile`;
  - every AllocsPerRun COUNT note the host prints for a relabelled entry REPRODUCES the reading-run TSV (`claude/coord-h10-readings` ac9f8251ee, docs/phase4/h10-evidence/i7-readings/readings-go1.24.13.tsv) -- a different figure is REPORTED per entry (not a stop); a UNIT flip (COUNT <-> BYTES) is a STOP-finding.
(f) THE TESTING README BADGE (hand-owned package; the converter emits no README for it): `src/core/testing/README.md`'s Tests badge still reads the 1.23.12.3 figures (37/52). Set it BY HAND to the testing page's own figures at this tree (53 matched of the 68 compared -- derive the numbers from the page and use the exact badge format the converter uses for other rows' README badges; quote before/after).
(g) THE GUARD after the bank: every section green (`0 of N` failures); the HEADER and README NEWS figures as the guard derives them (predicted UNCHANGED: 219 / 230, 57,197 matching, 282 disclosed -- a change is a finding, since no cell may move); the linux line untouched.
(h) THE INDEX: `python docs/phase4/hopA-inputs/regen-validation-index.py` self-test, dry run, `--write` only if it changes (predicted unchanged).
(i) COMMITS on the branch, each signed: (1) the pages' bank (pages, badges) + the testing README badge; (2) the header/NEWS/index only if (g)/(h) changed them. Census (`entry` on pages/roster/README/index and your report); repoguard TestNoFleetIdentifiersInTrackedFiles -count=1 with a planted control.
Restore side effects; `git status --porcelain` empty (unfiltered), no ` D`; go2cs.exe count 0; lock released. Worktree IN PLACE. NOTHING PUSHED.

## Report (final message)
Base and refs asserted; merges with any conflict resolution; (a)-(c) verdicts (2g's red and green lines quoted); the derived row set with reasons; the rows table (word, counts vs page, page-diff classes, COUNT-note agreement vs the TSV per row, wall); the badge before/after; the guard's line and the header figures; the index line; `git log --oneline --first-parent 1719e3b87f..HEAD` with %G?; disk; everything not verified, stated as such.
