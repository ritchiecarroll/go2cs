# Repair log: postclose-h11-h12-brief.md

All 38 defects checked against the sources at `fa18863b94` (runbook, push-nuget.ps1, release-nuget.ps1, stdLibConverter.go, projectFileWriter.go, readmeValidationBadge.go, PLAN-corpus-upgrade.md, MILESTONE-75pct-prep.md, the close obligations, the LEDGER, the abclose scripts and stage, and the tag list).

- **Result:** 38 SUSTAINED and applied, 0 REJECTED. One defect (D-09) is sustained only in part.
- **Brief size:** 420 -> 450 lines.
- **Where overlapping defects landed:** D-11 and MN-1 in one edit; D-02 and MN-3 in one; D-10 and MJ-6 in one; D-19 and MN-4 in one; D-17 and MJ-2 in one; D-09 and MJ-7 in one; D-12 and MN-7 in one.

| id | verdict | evidence confirmed / note |
|:--|:--|:--|
| D-01 | SUSTAINED+APPLIED | Confirmed: the brief had no CP3. rb :3201-3209 says the tag cannot be moved. push-nuget.ps1:535 runs `tag -s` at HEAD. release-nuget.ps1:171 runs `-BumpBuild`. Added COORD CHECKPOINT 3 (READY predicate, announce+push, landing freeze, final re-reads at the release HEAD, console block, post-Phase-3 record). Added obligations row REL-0. Line 1 now names CP3. |
| D-02 | SUSTAINED+APPLIED | Confirmed: the pre-flight throws at :362 before the metadata gate (:404). The bump is :428-436, the tag :535, the freeze :624, the fifth-number throw :853, and the keep-tag branch :528-529. Rewrote C6's red branch. Prefixed C7(3) with the seat-dependent conditional. Added the READY requirement and the real-run hazard to the readiness report. |
| D-03 | SUSTAINED+APPLIED | Confirmed: rb :3590 and :3216-3219 require the tag mint exercised. MILESTONE-75pct-prep.md:421 says no dry run mints a tag. The NEVER RUN list forbade the `-BumpBuild` that RN-7's clone needs. Release facts now say the cutover FOLLOWS the publish under the DEFAULT. RN-7 gains the DEFAULT consequence. NEVER RUN gains the `<coord-tmp>/abh12-clone` carve-out (renamed from abrel-clone because of D-12). |
| D-04 | SUSTAINED+APPLIED | Confirmed: PLAN :555 names the P2 instrument as the full `run-validated-sweep.ps1` roster, and §6 rb :3609 says "once, at the parity gate". PAR-2 already listed it as owed, so the brief contradicted itself. Release facts: P1 and P3 discharged, P2's arithmetic holds but its instrument is OWED. PAR-1 re-worded to match. |
| D-05 | SUSTAINED+APPLIED | Confirmed: CP2 ruled only RN-7/8/10/11. docs/CNAME = go2cs.net at the tip. rb :3579-3583 lets master merge only at all five gates. CP2 item 2 now rules RN-12 and RN-13, plus RN-14/15 when RN-12 = master. Part C STOPS before C4 without RN-12 and RN-13. RN-12 gains the critical-path clause, and the readiness report states that path. |
| D-06 | SUSTAINED+APPLIED | Confirmed: release-nuget.ps1:126-131 dies on any porcelain line. <repo> carries `?? du.exe.stackdump`. push-nuget.ps1:404 runs an ambient `go test`, and :535's tag failure is fatal after the bump. release-nuget.ps1:214-217 asks for the typed word. The 1.23.12.3 precedent had two PIN prompts, and two dialogs dismissed on an RD session cancelled packages. Replaced the owner's-run line with the console block. Line 1 now says "signing needs the owner's card PIN at the physical console". REL-1 and OWNER HANDS re-worded. The precedent is cited by release, never by a local file path. |
| D-07 | SUSTAINED+APPLIED | Confirmed: obligations a10 and d4 route comment and manifest prose to H12. Hits exist at every cited line, and manifest :14-15 carries "3,242 cases" and "3,242 BoGo rows". C4(3) now sorts hits into classes (a), (b) and (c). The ruling on (c) and the signature byte-identity is folded into RN-10. |
| D-08 | SUSTAINED+APPLIED | Confirmed: rb :2999-3001 step 7 is unconditional. A3 now takes the ignored-residue census with a plant. Skipping it needs an R-P ruling. PC-7 updated. |
| D-09 | SUSTAINED (in part)+APPLIED | Confirmed: testing is in nonConvertedStdLibPackages (stdLibConverter.go:235), and :212-219 describes the second testing_package collision. LEDGER 04:03 says "the converter emits no README for it", and b6746ab185 hand-derived it. CORRECTION to the checker: a single-package emission whose output dir lies under `-go2cspath`/core DOES emit a README (projectFileWriter.go:241-250, :628-630), so the "yields nothing" branch is false. The second-package and precedent branches hold. C2 now COMPOSES from readmeDocsBadgeLine :302-334 and readmeGoSourceBadgeLine :227-256, and proves the lines against testing/fstest's converter-emitted lines (README.md:5-6 @1.24.13, which A4(6) proves reproduced). The emission control is routed to a new RN-17. |
| D-10 | SUSTAINED+APPLIED | Confirmed: `nuget-stdlib-2026-07-14` is in the tag list. PLAN :661 (OQ-11) says the check is "added to H11". B2 now filters to four-component tags, lists the others by name, and states NuGetVersion equivalence. RN-9 now calls the scratchpad an INTERIM. |
| D-11 | SUSTAINED+APPLIED | Confirmed: the NEVER RUN list contradicted B2 and C8. It now forbids only the creating, moving and deleting forms, and permits `--list`. |
| D-12 | SUSTAINED+APPLIED | Confirmed: floor 11, and Part C was a separate dispatch into abrel. Part C now cuts `<coord-tmp>/abh12` (branch i7/h12-<mmddHHMM>) from `<part-c-base>`, with its own lock and scratch. abrel stays read-only. C2, C5, C6, C7, C8, CP3 and the rehearsal tree (`abh12-dry`) are re-pointed. Part B's branch is renamed i7/h11-<mmddHHMM>. `<tree>` is defined per part. |
| D-13 | SUSTAINED+APPLIED | Confirmed: 95daed0073 is docs(M1). ca4066a74c is an ancestor of nuget-1.23.12.1. 78d8894e5a and 88def0eff2 are ancestors of nuget-1.23.12.3 (b6746ab185). C5's precedent is replaced. |
| D-14 | SUSTAINED+APPLIED | Confirmed: Part A's ALLOWED paths never touch push-nuget.ps1. Added a CP0 bullet so COORD rules RN-6 and dispatches the seat at Part A's launch. CP1 item 5 and RN-6 updated. |
| D-15 | SUSTAINED+APPLIED | Confirmed: `<stage>` was preserved only until acceptance. A4(10) now copies the survivors' linux and darwin README/csproj emissions, with their sha256, to `<scratchpad>/abpost/survivors/`, and C2 reads them from there. |
| D-16 | SUSTAINED+APPLIED | Confirmed: rb :3155-3156. C7(3)(i) now checks the packed ID set BY NAME against nuget-1.23.12.3 minus B4's 14 plus B4's 51. |
| D-17 | SUSTAINED+APPLIED | Confirmed: the brief set a DEFAULT that overrode COORD's dispatch pin. R-P2 now has NO default, and a missing ruling STOPS before A4(1), quoting both. The blanket "STOP before A1" is narrowed per ruling. Report A's header names the arm. RN-2 and PC-8 updated. |
| D-18 | SUSTAINED+APPLIED | Confirmed: rb :1339, and AUDIT-h6-handown-go124.md is blob-equal (empty diff). `git log fa18863b94..074a12c4ae` has 13 commits. Minor correction: the three-dot diff touches 6 files, but regen-validation-index.py is already blob-equal two-dot, so the brief names the five without a count. dd18e5e2ab is on master and not on the tip. Release facts extended. |
| D-19 | SUSTAINED+APPLIED | Confirmed: floor 13, and LEDGER 18:23's red reads both staged badge lines empty. A2(1) now gates on rc != 0, `--- FAIL: <name>`, and no `undefined:`/`[build failed]`. A build failure is a STOP. |
| D-20 | SUSTAINED+APPLIED | Confirmed: rb :3186-3192 ("wants rewording at the next pass"). Added C4(6), obligations row H12-9, and an RN-10 clause. |
| MJ-1 | SUSTAINED+APPLIED | Confirmed: b8-overlay.sh runs `tar -xf` into $WT/src before its comm, and 13 quoted `ж` paths exist under src/core. The copy list is now explicit and excludes b8-overlay.sh. A4(8) spells out the absent set with quotePath=false, pipefail and a plant. |
| MJ-2 | SUSTAINED+APPLIED | Confirmed: b8-bin.sh hardcodes REGEN=54dec61728. The close's bin/A reads e0b2a4c1… and bin/B reads 7014b789…. The copy sets REGEN=<arm-a> per R-P2 and echoes it. A leftover 54dec61728 without a ruling is a STOP. A4(1) asserts the hash per arm, and A4(4)(c) picks its comparand per arm. |
| MJ-3 | SUSTAINED+APPLIED | Confirmed: classify.py takes `S, A, B, OUT = sys.argv[1:5]`, the core dirs plus an outdir, and the close launched python with cygpath -w. A4(5) now gives the exact command. The plant uses S-plant and cls-plant. |
| MJ-4 | SUSTAINED+APPLIED | Confirmed: gate-forensics SKILL :1504-1511 (exit 150 under the pin). h5c.sh used powershell 5.1 with cygpath'd, quoted arguments. migrate-gorelease.ps1 has `#Requires -Version 5.1`. Rules now define ONE bash parent and the 5.1 and pwsh 7 launch shapes (with ORIG_PATH derived by removal). H5c runs through h5c.sh. C4(1) uses 5.1. A6, B6(1) and C4(5) use the pwsh 7 shape. CNR runs through cnr-driver.ps1. |
| MJ-5 | SUSTAINED+APPLIED | Confirmed: C7(1) used `$env:` while C7(2) used bash `rc=$?`. The parent is now bash. B6(2), C6 and C7(2) use `env -u … powershell … "$(cygpath -w …)" < /dev/null > log 2>&1`, then `rc=$?`, with an optional UTF-8 driver ending `exit $LASTEXITCODE`. |
| MJ-6 | SUSTAINED+APPLIED | Confirmed: the non-version tag exists. B2 drops peeled `^{}` rows, filters, and lists EXCLUDED names. A third plant covers the excluded tag. |
| MJ-7 | SUSTAINED+APPLIED | Confirmed: emitsPackageReadme (projectFileWriter.go:628-630) and rewriteOfCorePackage (:241-250). corpus-reconvert SKILL :86 seeds version.props and docs/validation. Because D-09 made composition the default, MJ-7's spelled-out command lands as the emission control and runs only if RN-17 rules one. |
| MJ-8 | SUSTAINED+APPLIED | Confirmed: the Bash tool's timeout is capped at 600000 ms and foreground sleep is blocked. The legs' budgets exceed or approach the cap. SERIAL now requires backgrounding for A2(2), A4(3), A5 and C7, with a .result file and notification-based awaiting. The GPG re-probe is a backgrounded until-loop. A tool-timeout kill is a STOP. |
| MN-1 | SUSTAINED+APPLIED | Duplicate of D-11; the same edit fixes both. |
| MN-2 | SUSTAINED+APPLIED | Confirmed: check-no-regression.ps1:217 and :240 resolve by $PSScriptRoot. `git check-ignore` returns no match for a .runpost.ps1 copy. CNR exits 0 on NO REGRESSION and 1 on CHANGED. A5 now places the copy beside the original, runs it through cnr-driver backgrounded, gates on rc 0 plus the line, deletes the copy, and prints porcelain. |
| MN-3 | SUSTAINED+APPLIED | Merged into D-02's C7(3) prefix: UNREACHED, not RED, with elements UNEXERCISED (blocked at (a)) and P5 NOT MET named. |
| MN-4 | SUSTAINED+APPLIED | Confirmed: the `-run` alternation was unquoted. The quoted form, cwd and rc capture are added for merges, go build, go test, the index selftest and dry run, and B3. Gating follows D-19. |
| MN-5 | SUSTAINED+APPLIED | Confirmed: clean-bin.ps1 exits 0/1/2/3/4, and regen-validation-index.py:45-48 roots itself on `__file__`. DISK now gives clean-bin's 5.1 launch with an rc gate. A6 and C4(5) give the index path. `<tree>` and the go test cwd are defined on line 1. |
| MN-6 | SUSTAINED+APPLIED | Confirmed: push-nuget writes READMEs in the dry-run tree, gated only by ShouldProcess. C7(4) now asserts the child, records the tracked count, then runs `worktree remove --force` and `prune` and re-asserts. The Part B and Part C cuts assert git-dir != common-dir. |
| MN-7 | SUSTAINED+APPLIED | Confirmed: floor 11, and abrel.lock was never released between dispatches. Report B now releases abrel.lock after porcelain is empty. Part C takes its own abh12.lock and asserts no abrel.lock remains (never kills). |
| MN-8 | SUSTAINED+APPLIED | Confirmed: floor 13, and the corpus-reconvert well-formed-0 trap. Added a plant to A4(4)(c). B5 gains known-hit plants (1.23.12.* dirs, nuget-1.23.12* tags), re-run at C8. |
| MN-9 | SUSTAINED+APPLIED | Confirmed: the close's B-convert log prints `toolchain: GOROOT <profile path>`, and env.sh's pincheck echoes it. The Nicknames rule now requires `<goroot>`/`<dotnet10>` substitution and census `entry` before sending. |
| MN-10 | SUSTAINED+APPLIED | Confirmed: floor 15; the census runs from the disk copy. Added a `git diff --quiet 074a12c4ae -- <three census files>` rc-0 assert after the HEAD assert. |

## COORD RULING NEEDED (final list, verbatim from the brief)
- **RN-1** G's crypto/tls linux rebank: in Part A, or post-release (L 22:44: COORD rules at assembly).
- **RN-2** Arm A of the proving regen. NO default; a missing ruling STOPS Part A before A4(1). Either `fa18863b94`'s converter, identity `7014b789…`, the brief's reading of rb :3010-3012 ("the last corpus-wide regen"), or COORD's dispatch pin `REGEN_BASE 54dec61728`, identity `e0b2a4c1…`, with A-only of about 455. Record it as §6's fourth seeded reconvert.
- **RN-3** A non-zero regen footprint: STOP (DEFAULT) or an in-batch overlay with step 9's full gate set.
- **RN-4** Where a zero-footprint regen's readings live. DEFAULT: Report A plus the STAMP ledger line. The alternative is a committed docs/phase4 record (rb :1136-1138, "ask COORD").
- **RN-5** The three platform-exclusive seed survivors (macos, runtime/syscall, x/net/route: stale badges, and csprojs without the package_info-first order): a pre-release converter seat, an H12 restore class (C2's default), or post-hop.
- **RN-6** The release-census instrument seat. It teaches push-nuget's four-number guard and fifth-number check the named identities, and it must land before H11 goes green and before the rehearsal. The ruling covers who cuts it, on which branch, and whether it rides a batch. It also decides whether the 1.24.13.1 snapshot freezes all 232 pages (anchors and exclusions included), and whether a counter-0 dry run keeps writing `<base>.0` into READMEs. Ruled and DISPATCHED at CP0, in parallel with Part A (the seat touches none of Part A's paths); CP1 only names `<census-seat-ref>`.
- **RN-7** Ritual element 2, the pre-pack signed tag. DEFAULT: UNEXERCISED by the pack-only run, stated, with the proof being the owner's run. The alternatives are an isolated CLONE with no remote (`<coord-tmp>/abh12-clone`, the NEVER RUN list's one `-BumpBuild` carve-out), `-BumpBuild` and a local tag, discarded afterwards; or MILESTONE §3.6.3's redirect copy. DEFAULT consequence: §5 P5 is NOT MET before the publish (no dry run mints a tag, MILESTONE-75pct-prep.md:421), so RN-13's cutover follows the publish unless COORD records a ruling that the partial rehearsal discharges P5.
- **RN-8** The rehearsal host. DEFAULT: Windows PowerShell 5.1, the release's own host; guard obligation d22 is host-dependent. The alternative is pwsh 7.
- **RN-9** The home of the H11 monotonicity script. OQ-11 (PLAN :661) says the `NuGetVersion.Compare` assertion "is added to H11"; COORD rules whether that means a committed instrument. The scratchpad script, with its output in the report, is an INTERIM, not the ruled home. The alternative is a committed guard.
- **RN-10** The H12 prose: re-anchor or retire migrate-gorelease's rotted roster anchor, or hand-edit the DOC-STATEMENT sites. Also decide which roster lines (:4, :172, :664) are present tense and which are the 1.23.12 record; and when CLAUDE.md:69 changes, and by whom, under TestContextBudget, given that it merges at the cutover. Also (C4(3)): whether the present-tense 3,243/3,242 figures in CODE COMMENTS (class c) are edited at H12 (repoguard owed; CNR not owed for `.ps1` comments), and confirmation that the crypto/tls manifest prose edit leaves every `signature` byte-identical. And (C4(6)): reword rb :3186-3192's "top-level docs" line here, or route it to the lessons-learned seat.
- **RN-11** The announcement: who drafts it, what it says about net/http's demotion and the linux figures, and the owner's approval before the tag.
- **RN-12** THE RELEASE BRANCH: the version tip after H12 (the tag names the exact tree, but Pages and index links 404 until master catches up), or master after the §5 cutover (release-nuget's expectation, and the site stays coherent). Ruled at CP2, before Part C's C4: it decides which branch C4's prose and C5's announcement land on, and what the announcement can honestly say about `validation/1.24.13.1`. If (b), the release waits on P2's sweep (RN-14) and P4's gate (RN-15); the readiness report states that critical path.
- **RN-13** THE CUTOVER, version into master: before or after the publish (under RN-7's DEFAULT, after), and its mechanics. Ruled at CP2, before Part C's C4. Is it a `--no-ff -S` merge? Where is it sized, given that the i7's git lacks `--write-tree`? It must carry the UNION of `fleetIdentifierCensus_test.go`: master's public-handle admits plus the tip's embed-payload admit. How promptly does the snapshot reach master?
- **RN-14** §6's one full validated-roster sweep: before the release, or only before the cutover? On which machine (never the i9)? Does it carry crypto/tls's raised-wall environment pin? Is `-VerifyOnly` re-run after it?
- **RN-15** The remaining parity items. H6's completeness gate script (rb :1332-1333: "COORD rules the gate script"), including row 130's (c) and the T4 `.cs.auto` refresh. The §5 P3 line for the ruled base two. The timing of the gofmt pass (d15). Whether close items c6-c9 gate the release or go post-hop.
- **RN-16** Owner-hand timing: the 14 removed-ID deprecations before or after the publish; the `go.*` prefix answer; and whether `release/go1.23` (at `10c78227a7`) advances to `nuget-1.23.12.3`, and whether `release/go1.24` is minted.
- **RN-17** C2's hand-owned README derivation for `testing` and `unsafe`. DEFAULT: COMPOSE the two H1-following lines from `readmeValidationBadge.go`'s forms and prove them against a converted sibling's lines (`testing/fstest`), re-composed as the control. The alternative is an EMISSION control (a single-package conversion of `<goroot>/src/testing` into a seeded scratch root, spelled out in C2), which converts a package `nonConvertedStdLibPackages` deliberately keeps out and mints a second `testing_package` in that root.

Inline markers in the brief that point into this list: R-P1 (RN-1), R-P2 (RN-2), R-P3 (RN-3), and C4(3)'s class-(c) ruling (folded into RN-10).
