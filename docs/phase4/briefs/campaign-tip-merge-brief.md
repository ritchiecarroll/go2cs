You are an i7 sub-agent for the go2cs fleet coordinator. THE CAMPAIGN-TIP MERGE: merge master INTO the version branch in a throwaway worktree, run ONE battery, report. **DO NOT PUSH.** Read `CLAUDE.md` first (floor 3: the output directory is the SECOND positional; floor 7: capture every rc before any pipe; floor 8: never `git add -A`; floor 13: a gate never made to fail proves nothing; floor 14: read the results tail).

## Why this merge exists (read once)
`claude/version-go1.24.13` and `master` forked at `9d80085377` (2026-09-16). Master alone carries the H10 ROSTER SEAT (`docs/ValidatedTestPackages.md`, 203 rows at the relocated paths), the plan `docs/phase4/hopA-inputs/h10-dispatch-plan.tsv`, `shardmap.py`, the driver `src/run-h10-dispatch.ps1`, the wrapper `src/run-h10-recon.ps1` and the roster-guard seat. The re-bank refs edit roster ROWS, so they must be cut off a tip that carries the post-seat roster: the CAMPAIGN TIP is master merged into the batch-3 STAMP.

## Fixed facts (the coordinator fills the two SHAs before launch; assert both by `git ls-remote origin`)
- VERSION TIP after batch 3: `TIP3` on `claude/version-go1.24.13` (the ledger's last STAMP line).
- MASTER: `MASTER` = 9e12c3e7d0 or later (the roster-guard seat landed there: `src/check-roster-format.ps1` on master's roster reads EXACTLY `2 of 672 checks FAILED`, the two relocation orphans `crypto/internal/fips140/edwards25519` and `crypto/internal/fips140test` -- 'disclosed count is backed by a committed go2cs_test_disclosures.json' -- the roster's own named hop debt, cleared at the fips140test re-bank).
- Worktree: `git -C /c/Projects/go2cs fetch origin master claude/version-go1.24.13 && git worktree add --detach /c/go2cs-tmp-coord/ctip TIP3` (assert `/c/go2cs-tmp-coord/ctip` does not exist first). Lock: `mkdir /c/go2cs-tmp-coord/ctip.lock` (refuse if present), your PID into `ctip.lock/pid`, removed at the end. Scratch: `<this session's scratchpad>/ctip/`.
- Shells: build `export DOTNET_ROOT=$HOME/dotnet10; export PATH="$HOME/dotnet10:$PATH"` (assert `dotnet --version` starts with 10); converter `export GOROOT='<the go1.24.13 SDK, backslash spelling exactly as go env GOROOT prints it>'`, `export PATH="$HOME/sdk/go1.24.13/bin:$PATH"` (the pinned bin FIRST), `GOTOOLCHAIN=local`, `CGO_ENABLED=0`; assert `go version` = go1.24.13 and `go env GOROOT` = the pin (the machine-scope GOROOT is `C:\Program Files\Go` -- pin explicitly). pwsh 7 for the .ps1 gates (`env -u DOTNET_ROOT pwsh -NoProfile`).
- Disk >= 25 GB before each heavy gate. Sibling check: `Get-CimInstance Win32_Process | Where-Object { $_.Name -match '^(dotnet|MSBuild|VBCSCompiler|csc|go2cs|go)\.exe$' -and -not ($_.CommandLine -match 'pwsh\.dll') }` -- heavy > 0 means wait in 120 s loops (cap 40 min); you kill nothing.

## The merge
`git merge --no-ff -S MASTER` with a message in the repository's style (`git log -3 --format=%B` on the version branch): `Merge master (MASTER) into claude/version-go1.24.13 -- the campaign tip: ...`, a body naming what was verified, ending `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; nicknames only.
- **`version.props` keeps the VERSION branch's side** (1.24.13; master's reads 1.23.12). If git reports a conflict there, resolve it to the version branch's file byte-for-byte (`git checkout TIP3 -- version.props`) and say so; any OTHER file in `src/**` in conflict is a STOP (`git merge --abort`, the file list + hunks quoted). A conflict confined to `docs/**` or `.claude/**` is reported with its hunks and the merge left in progress (do not abort) -- the coordinator resolves docs.
- After the merge: `%G?` G or U; parents ours-then-theirs; `git diff --diff-filter=D --name-only TIP3 HEAD` and `... MASTER HEAD` both listed (deletions master made are expected -- name each; deletions of anything master still has are a STOP); no `<<<<<<<`/`>>>>>>>`/`__go2cs_lift` markers; every file master changed since the fork blob-EQUAL to master's tip unless the version branch also changed it (list those as COMPOSED with the hunk classes); `src/core` path list of the merge (expected: master's docs/README/badge changes only if any -- report).
- `version.props`: assert the merged file's GoStdLibVersion reads 1.24.13 and equals TIP3's blob.

## The battery (every rc captured on the next line, no pipe; logs by redirection)
(a) `cd src/go2cs && go build -o go2cs.exe . && go vet ./... && go test -timeout 90m ./...` every package ok (repoguard included, `-count=1` on repoguard).
(b) `dotnet build src/gen/go2cs-gen` (0 errors) and GenTests + GolibTests as the repository runs them (read `.claude/rules/golib-gen.md` and `harness-gates.md` for the exact invocations; quote them): pass counts verbatim.
(c) `check-no-regression.ps1` from pwsh with `$env:GOROOT` the literal SDK path and `$env:DOTNET_ROOT` set; the final line `NO REGRESSION`; CHANGED packages, if any, listed verbatim with the master commit that explains each (master's converter seats since the fork, if any -- `git log --oneline 9d80085377..MASTER -- src/go2cs src/gen src/core/golib` is the candidate list).
(d) `dotnet build src/go2cs-stdlib.slnx -c Debug`: 344 assemblies, 0 errors; `git status --porcelain -- src/core` empty after.
(e) `src/check-roster-format.ps1` from `src/` under pwsh 7: the failure SET must be EXACTLY the two orphan lines above (`2 of 672`, by name -- quote them); any other line, or fewer, is a STOP for the STAMP with every line verbatim.
(f) The plan's digest reproduces over the merged tree's `docs/phase4/hopA-inputs/h10-dispatch-plan.tsv` (run the driver with `-DryRun` on the i7's W=4 slice 1 in a SEPARATE linked branch worktree off the merge commit -- never this detached one: `git worktree add -b ctip-dry/<mmddHHMM> /c/go2cs-tmp-coord/ctipdry HEAD` -- with `-Worker 'i7-5820K (coordinator)' -FleetSize 4 -OnlySlice 1 -Mode rebank -ExpectTip <the merge SHA>` and the wrapper/plan BY BLOB from the merge commit; rc 0, seven rows in the plan's order: crypto/internal/fips140/edwards25519, internal/godebugs, database/sql, internal/types/errors, compress/flate, crypto, html/template; remove that worktree after, `git worktree remove`).
(g) Census: `bash .claude/coord-scripts/coord-identifier-census.sh entry` on every file the merge composed and on the merge message; `selftest` 117/0.

## Report (final message)
TIP3 and MASTER asserted; the merge SHA, `%G?`, parents; EQUAL/COMPOSED/DIFFER counts with names; both deletion lists; the version.props reading; every gate's figures verbatim; the DryRun's seven rows; census figures; the worktree left IN PLACE at `/c/go2cs-tmp-coord/ctip` (the coordinator pushes from it); disk free; the lock released; anything not verified. NOTHING PUSHED.
