You are an i7 sub-agent for the go2cs fleet coordinator. APPLY BATCH 3: TEN seat refs merged in order onto the version branch in a throwaway worktree, ONE battery, report. **DO NOT PUSH.** Read `CLAUDE.md` first (floor 3: the output directory is the SECOND positional; floor 7: capture every rc before any pipe; floor 8: never `git add -A`).

## The refs (assert each by `git ls-remote origin`; if any differs, STOP)
1. `claude/coord-ecdh-alias-collision` = `8ce22ff7c32df1bbcf48dc9a19572660468c7b3d` (two commits on `f83ba29ffb`: the seat `b5d3687634` — the recompile model's two-source merge, the variant's colliding key rendered fully qualified + a writer refusal for a duplicate `global using` — and the const-key guard `8ce22ff7c3`; files `src/go2cs/importOperations.go`, `src/go2cs/typeNameResolution.go`, `src/go2cs/packageInfoWriter.go`, `src/go2cs/seededAliasNameCollision_test.go` and whatever else `git diff --name-only f83ba29ffb..8ce22ff7c3` lists — report that list).
2. `claude/c1-publicize-internal-test-seed` = `bf7e31d037b30a429b6793951bb0fa55f35c9882` (one commit on `6d814e2d38`: 7 files +416/-0 -- `conversionDriver.go`, `go2cs-src.projitems`, `packageGlobalState.go`, `publicizeSiblingTestSeed_test.go`, `testAliasShadowOperations.go`, `testConversion.go`, `typeAccessibilityOperations.go` -- the production conversion's in-package test-half scan gains ACCESSIBILITY seeding: an exported internal-test member widens a production type; a test-only type resolves to nothing; an external `_test` package never reaches the scan). EXPECTED COMPOSED: `src/go2cs/go2cs-src.projitems` (batch 2 registered files too) -- after the merge assert BOTH registrations are present and `TestProjitemsRegistersEveryGoSource` passes; no other COMPOSED file is expected (report any).
3. `claude/c2-synctest-seat` = `db570064e9` (full SHA by ls-remote; one commit on `6d814e2d38`, three files under `src/go2cs/`: (a) convCallExpr's named func-type ARGUMENT rule now substitutes a generic parameter through the recorded instantiation -- `iter.Pull(seq)` emits `iter.Pull(new iter.Seq<time.Time>(seq))`; (b) visitRangeStmt's range-over-func arm declares a heap-boxed yielded variable per iteration inside the body, the slice/array/map shape). Report its file list (`git diff --name-only 6d814e2d38..<sha>`). EXPECTED COMPOSED: `src/go2cs/convCallExpr.go` (batch 2's mlkem seat also changed it on the target tip) and `go2cs-src.projitems` (all three refs register files) -- after merge 3 assert with `git diff <ref tip> HEAD -- src/go2cs/convCallExpr.go` that the ONLY differences are batch 2's hunks (i.e. the ref's own hunks are present verbatim) and that the projitems carries every registration; a conflict in convCallExpr.go is a STOP with the conflict hunks quoted, not a hand-resolution.
4. `claude/g-untyped-typeparam-inference` = `c19ae68fe3a214f597987c7459b99977c066fa30` (one commit on `6d814e2d38`, 3 files +263/-1: a fifth predicate on convCallExpr's explicit-type-args chain -- a generic call whose type parameter meets an untyped constant argument beside a typed one spells its type arguments; the internal/sync row past BUILD, CNR 729 byte-identical on the base). COMPOSED: `src/go2cs/convCallExpr.go` FOUR ways in this batch (batch 2's mlkem seat on the tip, ref 3's argument rule, ref 4's predicate, ref 5's TypeFor companion binding) and `go2cs-src.projitems`; assert ref 4's hunks verbatim after merge 4; a conflict is a STOP with the hunks quoted.
5. `claude/r-maphash-typefor` = `8b98102bfeffbfdb45f02c9877a540a525e03528` (one commit on `6d814e2d38`, files: `src/go2cs/convCallExpr.go`, `src/go2cs/descriptorCompanion.go` -- the reflect.TypeFor[T] lowering's companion type parameter bound at the call; the BARE `chan T(nil)` conversion emitted as a cast, the parenthesised sites untouched; hash/maphash BUILD 0 errors (was CS0411 x42 + CS1955), unique BUILD CS0030 x1 base-present; CNR 729 byte-identical on the base). Report any COMPOSED file against refs 1-4 and batch 2 (the overlap the coordinator measured: src/go2cs/convCallExpr.go, src/go2cs/descriptorCompanion.go).
6. `claude/c1-testonly-bridge-anchor-using` = `77f978074f18d6ba98e2dcd36b7a2dca689bc7df` (one commit on `6d814e2d38`, 2 files +222/-2: `src/go2cs/testConversion.go` -- internalTestPackageInfoSeed's production `using static` now gated on productionClassEmitted, the one site of the family that was unconditional -- and `testOnlyPackageEmission_test.go` with the mixed-variant fixture). COMPOSED with ref 2 on `testConversion.go` (both C1 refs off the same base; different hunks expected) -- assert both refs' hunks verbatim after merge 6; a conflict is a STOP.
7. `claude/c1-union-pointer-no-new` = `401e1d6464d78ff0b65933dd3e4ea6e7dcbf051d` (one commit on `6d814e2d38`, 2 files +77/-1: `src/go2cs/constraintOperations.go` -- a union constraint with a pointer term emits NO C# constraint (the `comparable` arm's answer), and `interfaceHasPointerTerm` recurses into a term that is itself a named set; runtime's -tests emission `where X : ... new()` sites 28 -> 0). COMPOSED with batch 2's mlkem seat on `constraintOperations.go` (different regions expected: the mlkem seat's funcResultProjectionArg near :1899) -- assert ref 7's hunks verbatim after the merge; a conflict is a STOP.
8. `claude/c1-testing-b-elapsed` = `a5249dc702` (full SHA by ls-remote; ONE commit ON the tip 0adf2e4318, one file `src/core/testing/testing.cs` +32/-3: `B.Elapsed()` returning a zero `time_package.Duration` beside the no-op timer members, with the remark; the stale doc sentence replaced; supportedTestCapabilities NOT widened -- no Test reaches B.Elapsed). Merge it LAST (after ref 5); the only `src/core` path allowed to change in this batch is `src/core/testing/testing.cs` (report the `src/core` path list).
9. `claude/c1-struct-conv-construction` = `da6cf0f5dd` (full SHA by ls-remote; ONE commit on the tip, files `src/go2cs/convCallExpr.go`, `src/go2cs/emptyStructConversion_test.go`, `src/go2cs/go2cs-src.projitems`: a conversion between ZERO-FIELD struct types (named or unnamed, not identical) with a pure operand emits `new T()`; `T(f())` keeps the cast on purpose). Merge it after ref 5 and before ref 8. If `src/go2cs/convCallExpr.go` is among its files it composes a FIFTH way (ref 5 carries batch 2 + G + R; ref 3's argument rule; this arm near the composite-underlying hop ~:1441) -- assert its hunks verbatim; a conflict is a STOP.
10. `claude/g-abi-maptype-projection` = `75be74fff3` (full SHA by ls-remote; ONE commit on the tip, files `src/core/internal/abi/package_info.cs`, `src/core/internal/abi/type.cs`, `src/core/internal/abi/type_impl.cs`, `src/go2cs/manualTypeOperations.go`: Type.MapType joins the hand-owned prefix-downcast family in internal/abi (the synthetic Map-kind descriptor's SwissMapType projection with the seed-only Hasher; MapType on a synthetic descriptor names itself); G measured internal/sync VALIDATED 106/106 with ref 4's predicate applied). Merge it after ref 9, before ref 8. Its `src/core/internal/abi/**` files are ALLOWED `src/core` paths in this batch (with `src/core/testing/testing.cs` from ref 8); report the full `src/core` path list.
TARGET: `claude/version-go1.24.13` at `0adf2e4318ef8313febb38048a9b0e947b256723` (assert by ls-remote).

## Setup
`cd /c/Projects/go2cs && git fetch origin claude/version-go1.24.13 claude/coord-ecdh-alias-collision claude/c1-publicize-internal-test-seed claude/c2-synctest-seat claude/g-untyped-typeparam-inference claude/r-maphash-typefor claude/c1-testonly-bridge-anchor-using claude/c1-union-pointer-no-new claude/c1-testing-b-elapsed claude/c1-struct-conv-construction claude/g-abi-maptype-projection && git worktree add --detach /c/go2cs-tmp-coord/ec3 0adf2e4318ef8313febb38048a9b0e947b256723`. Work only there. Shells: build `export DOTNET_ROOT=$HOME/dotnet10; export PATH="$HOME/dotnet10:$PATH"` (assert `dotnet --version` starts with 10); converter `export GOROOT='%USERPROFILE%\sdk\go1.24.13'` (LITERAL backslash spelling), `export PATH="$HOME/sdk/go1.24.13/bin:$PATH"` (the pinned bin FIRST on PATH), `GOTOOLCHAIN=local`, `CGO_ENABLED=0`; assert `go version` = go1.24.13. Disk ≥ 25 GB. Sibling check before each heavy gate: any `dotnet|MSBuild|VBCSCompiler|csc|go2cs|go` process by NAME anywhere → wait in 120 s loops (cap 40 min); never a substring match over CommandLine. You kill nothing.

## CHAIN COMPOSITION (ruled 15:35): R is the one resolver of convCallExpr.go's explicit-type-args chain. Before this battery launches, ref 5 (`claude/r-maphash-typefor`, tip 174403b3d2558bb1488afabeabfeab2485a37daa as re-announced by R) CONTAINS ref 4 (G's `c19ae68fe3`) and the batch-2 tip by merge commits. Assert `git merge-base --is-ancestor c19ae68fe3 174403b3d2558bb1488afabeabfeab2485a37daa` and `git merge-base --is-ancestor 0adf2e4318ef8313febb38048a9b0e947b256723 174403b3d2558bb1488afabeabfeab2485a37daa`; merge ref 4 NOT AT ALL (it is an ancestor of ref 5 -- report `git merge-base --is-ancestor` true) and merge ref 5 LAST. After merge 5 assert by grep that the chain in `src/go2cs/convCallExpr.go` names all SIX predicates (the four pre-existing: calleeHasConstraintOnlyTypeParam, callHasMethodGroupArg, calleeTypeParamUnsuppliedByCall, callNeedsConstraintProxy; plus calleeTypeParamMixesUntypedAndTypedArgs and calleeReadsDescriptorName) and run the chain guard BY NAME in gate (a): `go test -run TestExplicitTypeArgsChainCarriesEveryPredicate ./...` must pass (R's instrument: the arm files ask the predicate functions directly and stay green when the chain drops one; only this test reads the chain window). R's ref tip after G's merge was 2b677cd9d8; 174403b3d2558bb1488afabeabfeab2485a37daa is its tip after R merges the batch-2 tip too. You hand-resolve NOTHING: any conflict is a STOP with the hunks quoted.

## The merges, in order (refs 1, 2, 3, 5, 6, 7, 9, 10, 8; ref 4 is an ancestor of ref 5 and is not merged), each
`git merge --no-ff -S <sha>` with a message in the repository's style (`git log -3 --format=%B` on the version branch: `Merge <ref> -- <one sentence>`, a body naming what was verified, ending `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; nicknames only; sites with file extensions). Conflicts none (a conflict → `git merge --abort`, STOP with the file list); `%G?` G or U; parents ours-then-theirs; every file the ref touched since its merge base blob-EQUAL to the ref's tip blob (`git -c core.quotePath=false`), COMPOSED files named; no `<<<<<<<`/`>>>>>>>`/`__go2cs_lift` markers; `git diff --diff-filter=D --name-only 0adf2e4318ef8313febb38048a9b0e947b256723 HEAD` EMPTY; nothing under `src/core` changes (report the `src/core` path list — expected empty).

## The battery
(a) `cd src/go2cs && go build -o go2cs.exe . && go vet ./... && go test -timeout 90m ./...` every package ok (repoguard included; `-count=1` on repoguard).
(b) `check-no-regression.ps1` from Windows PowerShell with `$env:GOROOT` the literal SDK path and `$env:DOTNET_ROOT` set; rc captured directly; the final line `NO REGRESSION` (the seat's prediction: ecdh's marking site needs seeded production metadata only a `-tests` recompile reaches; C1's seeding runs in the production conversion's in-package test scan, which the behavioral packages exercise -- if CNR reports changed packages, STOP and report the list verbatim (that is a finding about C1's narrowness gate, not a failure to hide)).
(c) `dotnet build src/go2cs-stdlib.slnx -c Debug`: 344 assemblies, 0 errors; `git status --porcelain -- src/core` empty after the build (report any file). (c2) THE CORPUS FOOTPRINT of C1's seeding: reconvert the corpus per the `corpus-reconvert` skill (`.claude/skills/corpus-reconvert/SKILL.md`: the two-seeded diff, seeded from `src/core` BEFORE the `-stdlib` reconvert, into a temp root, never the worktree's own `src/core`), and report the hunk list -- the prediction from C1's population: at most the 9 types in 7 packages gain `public`, nothing else moves.
(d) THE ROWS, each with the output dir as the SECOND positional: (1) `crypto/ecdh` via the converter, the output dir the SECOND positional (`<GOROOT>\src\crypto\ecdh` → `<worktree>\src\core\crypto\ecdh`), `-tests -test-action all -test-timeout 20m`: it must COMPILE (before the seat: CS1537 duplicate using / the alias re-binding a `global using` name); report its `Validated`/divergence line verbatim, or the first error class with file:line. (2) `encoding/json` (`-tests -test-action all -test-timeout 20m`): the prediction is COMPILE with `isZeroer` now `public partial interface` in `encode.cs` (C1's emission read); report its Validated/divergence line. (3) The other SIX packages of C1's class by BUILD only (`-tests` conversion + `dotnet build` of the tests project, or the row with `-test-action build` if the converter offers it): crypto/internal/fips140/mlkem, internal/reflectlite, internal/runtime/maps, os, runtime, strconv -- report each's first error class if any (the population is a syntactic upper bound). (4) `internal/synctest` (`-tests -test-action all -test-timeout 20m`): the prediction is COMPILE (before: CS0411 + CS0029 at the iter.Pull site, CS0136 at the boxed yield) and its Validated/divergence line; report the first error class with file:line if not. (5) `internal/sync` (`-tests -test-action all -test-timeout 20m`): the prediction is VALIDATED 106 tests with 0 divergences (ref 5's predicate retires CS0411 x4; ref 10 retires the *SwissMapType no-address panics: TestHashTrieMapTruncHash 35/35) -- a GATE; report its Validated line, or the first error class / first panic line verbatim. (6) `hash/maphash` (`-tests -test-action all -test-timeout 20m`): the prediction is past BUILD (0 errors) and a RUN that panics in production at `escapeForHash` (`throw panic("intrinsic")` -- a Go compiler intrinsic converted faithfully; a separate seat); report its line and the first panic frame. (7) `unique` (`-tests -test-action all -test-timeout 20m`): the prediction is COMPILE (CS0411 retired by ref 5, CS0030 retired by ref 9: handle_test.cs:65 now `new testZeroSize()`) and its Validated/divergence line; report the first error class with file:line if not. (8) `embed/internal/embedtest` (`-tests -test-action all -test-timeout 20m`): the prediction is COMPILE (was CS0234 from an unconditional production `using static` for a test-only mixed suite) and its Validated/divergence line; `container/list` by BUILD as the site control (production half present, both variants: unchanged, compiles). (9) `runtime` by BUILD ONLY (`-tests` conversion + `dotnet build` of its tests project; NEVER `-test-action all` -- the row's host crash is known): the prediction is CS0310 x28 -> 0 AND the missing-member class for `b.Elapsed()` gone (ref 8); the remaining error classes, if any, reported verbatim with counts -- findings, not this seat's failure; `runtime/pprof` by BUILD as the site control (its composite union keeps its clause; emission byte-identical). Then restore every pipeline side-effect INSIDE the worktree (assert cwd): `git checkout -- .` and `git clean -fdq -- src/core docs/validation` (list what it removes); `git status --porcelain` empty; `grep '^ D'` empty.
(e) Census: materialise master's tool, patterns and hashes (`MSYS_NO_PATHCONV=1 git show origin/master:.claude/coord-scripts/coord-identifier-census.sh` etc. into scratch; sizes > 80,000 / > 20,000 / > 1,000; content-hash against `git rev-parse origin/master:<path>` via `git hash-object`); `entry` on every changed file and on the merge message: hits 0, arms 21, self-test 116/0.

## Report (final message)
The base tip; the merge SHA, `%G?`, parents; EQUAL/COMPOSED/DIFFER counts with names; the deletion count; the `src/core` path list; every gate's figures verbatim; the row's line; census figures; the worktree left IN PLACE at `/c/go2cs-tmp-coord/ec3` (both merges) (the coordinator pushes from it); disk free; anything not verified. NOTHING PUSHED.
