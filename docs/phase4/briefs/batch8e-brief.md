You are an i7 sub-agent for the go2cs fleet coordinator. APPLY BATCH 8e -- the LINUX LEG's bank, the vgetrandom seat, the linux pin retirement and a generator fix, merged onto the version branch in a LINKED worktree ON A BRANCH, one battery, the header derived; report. **DO NOT PUSH.** Read `CLAUDE.md` first (floors 1, 3, 4, 7, 8, 13, 14, 16) and `.claude/rules/harness-gates.md`, `.claude/rules/corpus.md` and `.claude/rules/golib-gen.md` when you touch those trees. Never modify the main checkout or any other worktree under the coordinator's temp root. The coordinator's prompt supplies the absolute values for every <placeholder>.

## Fixed facts (assert each by `git ls-remote origin`; STOP on any mismatch)
- BASE: `claude/version-go1.24.13` = `74bae27672` (the batch-8d STAMP).
- Worktree `git worktree add -b i7/h10-batch8e-<mmddHHMM> <coord-tmp>/ab8e 74bae27672`; lock `<coord-tmp>/ab8e.lock` + PID; scratch `<scratchpad>/ab8e/`; disk >= 25 GB before the battery and before every row.
- Pins, GPG probe, census, sibling check: exactly as in batch 8c's brief (`docs/phase4/briefs/batch8c-brief.md`), read back by OUTPUT.

## THE REFS, merged `--no-ff -S` IN THIS ORDER (each asserted by ls-remote; the additive-blob proof per ref; `git diff --diff-filter=D 74bae27672 HEAD` must name no file)
1. `claude/c2-embedded-ptr-promotion` = `556ee00cbf` (one commit on 1719e3b87f; GENERATOR only, src/gen): the metadata harvest (GetMetadataPromotedMethods) no longer counts a [GoRecv] method's RecvGenerator pointer twin as a second method (Go has one method per name), so a -tests reference-model assembly that embeds a same-package production type through a pointer gets its forwarders -- net's linux CS1929 (dnsclient_unix_test.cs:420/:422) was the symptom. It ADDS forwarders only in generated build output (net resolvConfTest, go/doc bundle, internal/poll XFDMutex, runtime's seven test structs).
2. `claude/g-vgetrandom-unsupported` = `971d919113` (on 1719e3b87f: bdec3ae4d8 the seat -- runtime.vgetrandom joins linknameForwardTargets, Go's own unsupported answer (-1, false); b293973e9f GolibTests.csproj excludes two windows-only guards from the non-windows group (GolibTests had not built under GoTargetOS=linux since 2026-09-15); 8e832c194 a merge of claude/g-linux-leg 5da426433b (the linux leg's 31 annotations); 971d91911 the vgetrandom re-read bank (9 more annotations moved, the guard's linux line 48,685 matching)). Emission: 5 files, the predicted ones (G's two-seeded A/B, 3 targets).
3. `claude/g-linux-pin-retire` = `6c35ea9b3c` (one commit on 74bae27672): retires the relabel's four [linux, darwin] pins (encoding/binary TestSizeAllocs complex64/complex128/binary.Struct read pass/pass on linux; math/big TestNewIntAllocs fail/fail, Go fails it too) and moves their linux claims. G measured its union with the linux leg green (1799/1799) after one adjacent-row roster conflict.
CONFLICT RULES: docs/ValidatedTestPackages.md from two refs => take each side's intended ROWS (annotations from G, os 1105 + 2 from 8d, etc.), then let THE GUARD re-derive the HEADER and the README NEWS figures -- never hand-choose a figure; quote every resolved hunk. A registry table, a projitems or a test-project item list with adjacent inserts => the UNION, asserted by its own guard tests. A converter-emitted file conflicting between two refs => RE-EMIT it (seeded single-package conversion, fresh root, taken whole), never hand-merge. Anything else => STOP with the hunk quoted.

## Battery (converter/gen/golib source FROZEN while it runs)
(a) go2cs.exe rebuilt under the pin; the FULL converter suite `go test ./... -count=1 -timeout 40m`: expected GREEN.
(b) gen 0 errors; GenTests: C2's new arm `PointerTwinOfARefReceiverMethodIsNotASecondMethod` and the existing `WhiteBoxEmbedOfProductionTypePromotesInternalMethods` must pass at the head -- AND, red-first (floor 13), fail with C2's StructTypeTemplate.cs change reverted in place (restore byte-identical, rebuild, green). GolibTests (Release, windows): a CONTROL at 74bae27672 first, then the head; predicted count UNCHANGED on windows (G's csproj edit touches the non-windows group only) -- account for any delta by name.
(c) CNR (the merged tree's check-no-regression.ps1): `NO REGRESSION`, CHANGED listed. Predicted EMPTY for the windows behavioral packages (G's emission change is linux-/darwin-target runtime and internal/syscall/unix files; C2's is generator build output). If CHANGED is not empty, run the full behavioral suite with a control at 74bae27672 and name every changed project.
(d) stdlib `src/go2cs-stdlib.slnx` Release: 344 / 0.
(e) WINDOWS ROWS through a per-run COPY of the merged tree's `src/run-h10-recon.ps1` (`-tests -test-action all -test-config Release`; the output dir the SECOND positional; results tails FIRST; artifacts cleared before, re-emissions restored after):
  - CONTROLS for C2's forwarders (verdicts identical to their banked pages; bank nothing unless a page legitimately moves): go/doc, internal/poll.
  - net: its windows TEST BUILD must compile at the merged tree (build only if the wrapper allows `-test-action build`; do NOT take net's reading -- its DNS chain is pending the owner). Quote the build line.
  - NOT run: runtime (a candidate), crypto/tls (AZ1), net/http (to the candidates).
(f) THE GUARD at the merged tree: every section green (`0 of N`); the HEADER and README NEWS written EXACTLY as the guard derives them after the merge (predicted: windows figures unchanged at 219 / 230, 57,199 matching, 282 disclosed; the LINUX line moves to G's derivation, 188 / 217 with 48,685 matching -- adjusted by the pin retirement if the guard says so). Quote the guard's linux line.
(g) THE INDEX: self-test, dry run, `--write` only if it changes.
(h) COMMITS on the branch, each signed: the merges; then ONE commit for the header / NEWS / index if (f)/(g) changed them. Census `entry` on the roster, READMEs, index and your report; repoguard TestNoFleetIdentifiersInTrackedFiles -count=1 with a planted control.
Restore side effects; `git status --porcelain` empty (unfiltered), no ` D`; go2cs.exe count 0; lock released. Worktree IN PLACE. NOTHING PUSHED.

## Report (final message)
Base and refs asserted; merges with every resolved roster hunk quoted; (a)-(d) verdicts with the GenTests red-first and the GolibTests control comparison; the rows table; the guard's line and the header figures (windows and linux); the index line; `git log --oneline --first-parent 74bae27672..HEAD` with %G?; disk; everything not verified, stated as such.
