You are an i7 sub-agent for the go2cs fleet coordinator. APPLY BATCH 8b -- twelve refs merged onto the version branch in a LINKED worktree ON A BRANCH, one battery, rows re-run and BANKED, badges repaired, the index and the header derived; report. **DO NOT PUSH.** Read `CLAUDE.md` first (floors 1, 3, 4, 7, 8, 13, 14, 16) and `.claude/rules/harness-gates.md` and `.claude/rules/corpus.md` when you touch those trees. Never modify the main checkout or any other worktree under the coordinator's temp root. The coordinator's prompt supplies the absolute values for every <placeholder>.

## Fixed facts (assert each by `git ls-remote origin`; STOP on any mismatch)
- BASE: `claude/version-go1.24.13` = `43d149b87d` (the batch-8a STAMP).
- Worktree `git worktree add -b i7/h10-batch8b-<mmddHHMM> <coord-tmp>/ab8b 43d149b87d` (ON A BRANCH: rows bank from it); lock `<coord-tmp>/ab8b.lock` + PID; scratch `<scratchpad>/ab8b/`; disk >= 25 GB before the battery and before every row; `bin/` `obj/` purged after each row.
- Pins (read each back by OUTPUT): the machine-scope GOROOT is a DEAD 1.23.1 pin -- unset it; the 1.24.13 SDK's bin first on PATH; `go version` = go1.24.13; `go env GOROOT` prints the BACKSLASH spelling (that spelling is the pin); GOTOOLCHAIN=local; CGO_ENABLED=0; the dotnet10 root as DOTNET_ROOT and first on PATH for every `dotnet`; pwsh 7 (net8 apphost) launched with DOTNET_ROOT UNSET and the dotnet10 root OFF the parent PATH, pins set inside via `$env:`, `Get-Command git` asserted inside before CNR. Explicit `-timeout` on every `go test`; `-count=1` on every guard. Exit codes captured on the next line before any pipe. Sibling check before starting (another go2cs.exe outside ab8b => STOP; the harness's own dotnet-hosted pwsh shells are not siblings; never kill by name).
- GPG: probe before every `-S`: `echo test | <gpg4win-gpg> --batch --pinentry-mode error --clearsign -u 941694536F21BAFF` (rc 0 = warm); cold => re-probe every 60 s up to 30 min, then STOP; never bypass, never kill gpg-agent, never run a non-batch gpg.
- Census: the main checkout's `.claude/coord-scripts/coord-identifier-census.sh` (master 20eb0af70e, blob 6c12103750): `converted` on staged converted test sources and fixtures, `entry` on pages, the roster, the index and your report.

## THE REFS, merged `--no-ff -S` IN THIS ORDER (each asserted by ls-remote; the additive-blob proof per ref; `git diff --diff-filter=D 43d149b87d HEAD` must name no file)
1. `claude/c2-h10-runbook-amend` = `c3cb3b3101` -- docs/GoCorpusMigration.md (six dated H10 amendments) + docs/phase4/hopA-inputs/recon-basis.tsv (two pprof rows costed).
2. `claude/c1-own-page-cells` = `51a1d30ff9` (contains f1174ea699) -- the roster's seven cells to their own pages + the fips140test row repair + check-roster-format.ps1 section 2f (Tests == page matched, Disclosed == page disclosed, for own-first-page rows). MERGE RECIPE (C1): the internal/runtime/maps row comes from THIS ref (Tests 3 / Disclosed 108) over the base's stale 111 row, and the base's math + sys rows (8a's own-page links) are kept -- a stale maps row silently restores 111.
3. `claude/c1-identity-badge-arms` = `fb1ecbfe55` -- check-roster-format.ps1 sections 2b2 (tracked tests.csproj == rows + exclusion rows keeping artifacts + rowless candidates) and 2b3 (a validated badge on EXACTLY the roster rows that have a README).
4. `claude/c1-manifest-resign` = `082ec41bb0` -- the hop's manifest re-sign (29 platform scopes, 1 retirement, two NEW deferred manifests: unicode/utf8, fmt) + the manifest-loading guard's scope-rule arm.
5. `claude/g-h10-pass3-s1` = `c660a17d8c` -- G's pass-3 bank: math/rand 47, mime/multipart 52, internal/coverage/cfile 15 + 1 (artifacts, pages, badges, cells).
6. `claude/g-atomic-xchg8` = `8519e1b44c` -- internal/runtime/atomic's Xchg8 under the 8-bit latch + the row banked at 16.
7. `claude/r-syscall-startupinfo` = `c4594c55b8` -- syscall GetStartupInfo through the native mirror (registry key, companion, zsyscall_windows.cs re-emission, the token-door census control re-anchored) + the syscall row banked at 65.
8. `claude/c2-time-linkname` = `568c17380f` -- the converter's linkname forward definitions (time_test's three pulls) + time.cs's three declarations public.
9. `claude/g-handown-bridge-declared` = `1f2724c755` -- the converter's hand-owned-host exclusion admits a bridged name the host declares.
10. `claude/c2-fmt-func-stringer` = `1913737522` -- golib TryTypeAssert: a typed nil func satisfies its type's interfaces; a NEW behavioral test TypedNilFuncMethodSet (no golden yet).
11. `claude/r-runtime-lfnode` = `c10500aa12` (contains 66e6b9905) -- golib: a native-rooted field ref's identity is its address; runtime: Windows sysAllocOS / sysFreeOS by direct P/Invoke.
12. `claude/i9-runtime-blockevent` = `1abe5678e4` -- runtime: the CPU-profiler setters in Go's no-sampler shape, readProfile pushed, saveblockevent refuses by name; GolibTests arms.
CONFLICT RULES: a registry table (manualTypeOperations.go, linknameOperations.go), go2cs-src.projitems, a test-project item list, or declaredNotImplemented_test.go with adjacent inserts or deletions from two refs => the UNION (each side's intended rows present, each side's deletions honoured), asserted by that file's own guard tests. The roster with different rows => both (and the maps recipe above). A CONVERTER-EMITTED file (e.g. runtime/windows/*.cs, runtime/*/package_info.cs, syscall/zsyscall_windows.cs) conflicting between two refs => NEVER hand-merge: RE-EMIT that file at the merged tree with the merged converter (a seeded single-package conversion for that target into a fresh root, taken whole), and prove the result differs from each parent only by the other parent's intended hunks. Anything else => STOP with the hunk quoted.

## Battery (converter/gen/golib source FROZEN while it runs)
(a) go2cs.exe rebuilt under the pin; the FULL converter suite `go test ./... -count=1 -timeout 40m` in src/go2cs: expected GREEN; quote any red in full.
(b) gen 0 errors; GenTests; GolibTests (Release) -- a CONTROL at 43d149b87d FIRST, then the head: name every new arm and account for the count delta exactly.
(c) CNR: `NO REGRESSION`, CHANGED listed (predicted EMPTY for the converter refs; the NEW TypedNilFuncMethodSet has no golden until step (e)).
(d) stdlib `src/go2cs-stdlib.slnx` Release: 344 / 0.
(e) THE FULL BEHAVIORAL SUITE (two golib refs aboard): a CONTROL run at 43d149b87d first, then the head; the fail sets must be EQUAL except the new test. R's R-LAPTOP runs failed FuncLiteralCallerNames and GoroutineWaitState at baseline -- report whether the i7's control fails the same two. A test failing at the head and passing at the control is a STOP-finding naming the ref. TypedNilFuncMethodSet: PROVE it first (transpile, compile, output vs Go's 13-line oracle, 1/1/1), THEN mint its golden and its MSTest registrations with the repo utility (`UpdateTestTargets --createTargetFiles --only TypedNilFuncMethodSet`, which re-transpiles first; never a hand copy), commit the pair.
(f) ROWS through a per-run COPY of src/run-h10-recon.ps1 -- the wrapper has two known defects you correct IN YOUR COPY and name in the report: a derived floor must never lower an asked -TestTimeout (take the max), and each row runs with its roster execution pin exactly as the sweep's Get-RosterExecutionArgs does. `-tests -test-action all -test-config Release`; the output dir the SECOND positional inside the worktree's src/core/<row>; results tails FIRST; test artifacts cleared before each row; production re-emissions restored after each (count them by class).
  - BANK if VALIDATED (Tests = the page's MATCHED count, Disclosed = the page's disclosed): time (floor 40m; C2's linkname fix: predicted VALIDATED), fmt (predicted VALIDATED 62 + 1 with C1's deferred pin and C2's Stringer fix), unicode/utf8 (predicted VALIDATED 14 + 1), crypto/internal/fips140/aes, /ecdsa, /nistec (at their roster cells; their README badges REGENERATE from their pages -- the 2b3 repair).
  - CONTROLS, verdicts identical to their banked pages (bank nothing unless a page or badge legitimately moves): internal/runtime/atomic 16, syscall 65, math/rand 47, mime/multipart 52, internal/coverage/cfile 15 + 1, crypto/internal/fips140test 2260 + 7, crypto/sha3 18 + 5, internal/runtime/maps 3 + 108, crypto/x509 518.
  - The rows whose MANIFESTS changed in ref 4, cells predicted UNCHANGED on windows: crypto/cipher, encoding/binary, internal/poll, math/big, os, os/exec, os/signal, runtime/debug.
  - runtime (floor 30m): predicted DEATH at mcall(park_m) from TestScavenger, about 10,654 of 10,891, asmcgocall 2 roots / 0 echoes, 'bad lfnode address' 0. Report the word, counts, the tail verbatim, the NotImplementedException classes. Bank nothing.
  - NOT run here: testing (G's seat pending), runtime/pprof (a candidate; measured by i9), net/http, net, crypto/tls, internal/trace.
(g) THE GUARD, scored against C1's predictions BEFORE any repair: run src/check-roster-format.ps1 at the merged tree with no row banked yet and quote its failure set (C1 predicted, at 3469154a95: section 2f red on six -- crypto/sha3 Tests, internal/runtime/maps Tests, Disclosed for encoding/binary, net/netip, runtime/debug, strconv -- which ref 2 itself turns green; section 2b3 red on four -- fips140/aes, fips140/ecdsa, fips140/nistec, fips140deps; 2b2 green 225 = 219 + 4 + 2). Then the repairs: the three fips140 badges from their rows (above), and crypto/internal/fips140deps's README badge RESTORED to its state before the run that set it green (find that commit with `git log -- src/core/crypto/internal/fips140deps/README.md`; quote it). Re-run the guard: every section-2 arm green; then the HEADER figures and the README NEWS block WRITTEN exactly as the guard derives them (never hand-chosen), re-run to `0 of N`. The linux line stays what the guard derives (still 1.23.12 evidence until the linux leg -- say so, do not edit it by hand).
(h) THE INDEX: `python docs/phase4/hopA-inputs/regen-validation-index.py` self-test, dry run, `--write` if it changes (zero orphans, zero page-less; index rows == roster rows).
(i) COMMITS on the branch, each signed: (1) the rows' bank (artifacts, pages, badges, cells) + the fips140deps badge restore; (2) the TypedNilFuncMethodSet golden pair + registrations; (3) the index + header + README NEWS. Census on everything staged; repoguard TestNoFleetIdentifiersInTrackedFiles -count=1 with a planted control (floor 13).
Restore side effects; `git status --porcelain` empty (unfiltered), no ` D`; go2cs.exe count 0 at exit; lock released. Worktree IN PLACE. NOTHING PUSHED.

## Report (final message)
Base and refs asserted; each merge's SHA, its conflicts and their resolutions (re-emissions proven); (a)-(e) verdicts with the control comparisons; the rows table (word, counts, the tail's last line, wall, banked or control); the guard's failure set before and after, scored against C1's predictions; the header figures written; the index line; `git log --oneline --first-parent 43d149b87d..HEAD` with %G?; disk; everything not verified, stated as such.
