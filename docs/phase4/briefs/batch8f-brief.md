You are an i7 sub-agent for the go2cs fleet coordinator. APPLY BATCH 8f -- net BANKED at go1.24.13 on the now-qualified i7, plus the linux syscall host seat, merged onto the version branch in a LINKED worktree ON A BRANCH, one battery, the header derived; report. **DO NOT PUSH.** Read `CLAUDE.md` first (floors 1, 3, 4, 7, 8, 13, 14, 16) and `.claude/rules/harness-gates.md`, `.claude/rules/corpus.md` and `.claude/skills/validation-bank/SKILL.md`. Never modify the main checkout or any other worktree under the coordinator's temp root. The coordinator's prompt supplies the absolute values for every <placeholder>.

## Fixed facts (assert each by `git ls-remote origin`; STOP on any mismatch)
- BASE: `claude/version-go1.24.13` = `fc6269b0bf` (the batch-8e STAMP).
- Worktree `git worktree add -b i7/h10-batch8f-<mmddHHMM> <coord-tmp>/ab8f fc6269b0bf`; lock `<coord-tmp>/ab8f.lock` + PID; scratch `<scratchpad>/ab8f/`; disk >= 25 GB before the battery and before every row.
- Pins, GPG probe, census, sibling check: exactly as in batch 8c's brief (`docs/phase4/briefs/batch8c-brief.md`), read back by OUTPUT.
- THE HOST: the i7 is a QUALIFIED net host since 2026-09-23 (its resolver is DNS-conforming -- public IPv4 resolvers, IPv6 unbound; Go's own `go test -count=1 -timeout 40m net` failed only TestLookupCNAME twice). Re-qualify ONCE before net's row (same command, pins as above, from a directory outside any module; judge: TestLookupCNAME tolerated, anything else => SKIP net's bank and report).

## THE REFS, merged `--no-ff -S` IN THIS ORDER (each asserted by ls-remote; the additive-blob proof per ref; `git diff --diff-filter=D fc6269b0bf HEAD` must name no file)
1. `claude/g-syscall-nofile-host` = `ce065f8aa9` (three unsigned commits on 971d919113, which 8e carries): src/core/testing/HostDescriptorLimit.cs (NEW, hand-owned; linux/darwin snapshot/restore of RLIMIT_NOFILE around each top-level test; a no-op on windows) + TestRunner.cs hook; GolibTests LinuxDescriptorLimitTests (linux-only arms) + csproj; syscall's manifest (TestPrlimitFileLimit host-limit linux, TestUnshareUidGidMapping platform-skip linux); the roster's syscall linux annotation 38 + 17 -> 45 + 11 (the linux line 48,692 matching / 162 disclosed). G measured it merging clean onto fc6269b0bf (guard 1799/1799).
2. `claude/c1-net-labels` = `9c6ebf262f` (one commit on fc6269b0bf): net's manifest -- TestAllocs and TestTCPReadWriteAllocs relabelled DEFERRED (readings 35 and 2 per run from the i7 net reading), TestIPAppendTextNoAllocs NEW DEFERRED (3/run) -- plus a dated §7 in docs/phase4/DESIGN-syscall-buffer-element-address.md. net's Disclosed cell moves 2 -> 3 at the bank.
CONFLICT RULES as in 8e (roster rows unioned, the header by the guard; registries unioned; converter-emitted files re-emitted); anything else => STOP with the hunk quoted.

## Battery (converter/gen/golib source FROZEN while it runs)
(a) go2cs.exe rebuilt under the pin; `go test ./... -count=1 -timeout 40m`: expected GREEN (the manifest loader over syscall's and net's new entries).
(b) GolibTests (Release, windows): a CONTROL at fc6269b0bf first (8e read 836 / 15 / 851), then the head; predicted UNCHANGED on windows (LinuxDescriptorLimitTests is linux-only) -- account for any delta by name. gen/GenTests not owed (no gen change) -- say so.
(c) CNR: `NO REGRESSION`, CHANGED predicted EMPTY (no converter change). The testing host changed (HostDescriptorLimit.cs + TestRunner.cs hook, a no-op on windows) -- so (e)'s controls prove the windows host unchanged.
(d) stdlib `src/go2cs-stdlib.slnx` Release: 344 / 0 (the testing host compiles).
(e) ROWS through a per-run COPY of the tree's `src/run-h10-recon.ps1` (`-tests -test-action all -test-config Release`, each row's roster pin; the output dir the SECOND positional; results tails FIRST; artifacts cleared before, re-emissions restored after):
  - **net: BANK** (after the re-qualification above; `-TestTimeout` 120m): predicted VALIDATED with 3 disclosed -- the divergence set exactly TestAllocs, TestTCPReadWriteAllocs, TestIPAppendTextNoAllocs, each now disclosed; every DNS test pass/pass and TestLookupCNAME fail/fail (a matched failing pair); the matched count as the page derives it (the i7 reading at fc6269b0bf: Go 479 = 434 pass / 1 fail / 44 skip). Bank the page, the README badge and the roster cells (Tests = the page's matched, Disclosed = 3). A net verdict that differs from the reading at fc6269b0bf (C1's evidence claude/coord-handover 4f8d747dda) is a STOP-finding.
  - CONTROLS for the testing-host change (verdicts identical to their banked pages; bank nothing unless a page legitimately moves): syscall (windows), testing, fmt, os/exec.
(f) THE GUARD: every section green; the HEADER and README NEWS written EXACTLY as the guard derives them (windows figures move by net's bank; the linux line to G's 48,692 / 162); quote the guard's lines. 2g must read net as banked at the pin with 0 refused.
(g) THE INDEX: self-test, dry run, `--write` only if it changes.
(h) COMMITS on the branch, each signed: the merges; (1) net's bank (page, badge, cells); (2) the header / NEWS / index if changed. Census `entry` on the page, roster, READMEs, index and your report (net's page carries Go's own IP-literal test names -- a census ipv4 refusal on pre-existing test names is reported, not a stop; repoguard is the gate of record); repoguard TestNoFleetIdentifiersInTrackedFiles -count=1 with a planted control.
Restore side effects; `git status --porcelain` empty (unfiltered), no ` D`; go2cs.exe count 0; lock released. Worktree IN PLACE. NOTHING PUSHED.

## Report (final message)
Base and refs asserted; merges with any resolved hunk quoted; the re-qualification's failing set; (a)-(d) verdicts with the GolibTests control comparison; the rows table (net: word, counts, the divergence set vs the reading, the page's disclosed entries); the guard's lines and the header figures (windows and linux); the index line; `git log --oneline --first-parent fc6269b0bf..HEAD` with %G?; disk; everything not verified.
