You are an i7 sub-agent for the go2cs fleet coordinator. APPLY BATCH 8c -- five refs merged onto the version branch in a LINKED worktree ON A BRANCH, one battery, rows re-run and BANKED, the header and the index derived; report. **DO NOT PUSH.** Read `CLAUDE.md` first (floors 1, 3, 4, 7, 8, 13, 14, 16) and `.claude/rules/harness-gates.md` and `.claude/rules/corpus.md` when you touch those trees. Never modify the main checkout or any other worktree under the coordinator's temp root. The coordinator's prompt supplies the absolute values for every <placeholder>.

## Fixed facts (assert each by `git ls-remote origin`; STOP on any mismatch)
- BASE: `claude/version-go1.24.13` = `bb54ff0920` (the batch-8b STAMP).
- Worktree `git worktree add -b i7/h10-batch8c-<mmddHHMM> <coord-tmp>/ab8c bb54ff0920` (ON A BRANCH: rows bank from it); lock `<coord-tmp>/ab8c.lock` + PID; scratch `<scratchpad>/ab8c/`; disk >= 25 GB before the battery and before every row; `bin/` `obj/` purged after each row.
- Pins (read each back by OUTPUT): the machine-scope GOROOT is a DEAD 1.23.1 pin -- unset it; the 1.24.13 SDK's bin first on PATH; `go version` = go1.24.13; `go env GOROOT` prints the BACKSLASH spelling (that spelling is the pin); GOTOOLCHAIN=local; CGO_ENABLED=0; GOFLAGS empty; the dotnet10 root as DOTNET_ROOT and first on PATH for every `dotnet`; pwsh 7 (net8 apphost) launched with DOTNET_ROOT UNSET and the dotnet10 root OFF the parent PATH, pins set inside via `$env:`, `Get-Command git` asserted inside before CNR. Explicit `-timeout` on every `go test`; `-count=1` on every guard. Exit codes captured on the next line before any pipe. Sibling check before starting and before every row (another go2cs.exe outside ab8c => STOP; the harness's own dotnet-hosted pwsh shells are not siblings; never kill by name).
- GPG: probe before every `-S`: `echo test | <gpg4win-gpg> --batch --pinentry-mode error --clearsign -u 941694536F21BAFF` (rc 0 = warm); cold => re-probe every 60 s up to 30 min, then STOP; never bypass, never kill gpg-agent, never run a non-batch gpg.
- Census: the main checkout's `.claude/coord-scripts/coord-identifier-census.sh` (master 20eb0af70e): `converted` on staged converted test sources and fixtures, `entry` on pages, the roster, the index and your report.

## THE REFS, merged `--no-ff -S` IN THIS ORDER (each asserted by ls-remote; the additive-blob proof per ref; `git diff --diff-filter=D bb54ff0920 HEAD` must name no file)
1. `claude/coord-instrument-h10` = `d095fe8108` (two signed commits on 43d149b87d) -- src/run-h10-recon.ps1 (max(asked, floor); every row with its ROSTER EXECUTION PIN via the sweep's Get-RosterExecutionArgs), src/run-validated-sweep.ps1 (H10 step 5's deadline floors), src/tests/Behavioral/check-no-regression.ps1 (refuses, exit 2, when git or go cannot be resolved). After this merge the tree's OWN wrapper is the corrected one: rows run through a per-run COPY of the MERGED tree's src/run-h10-recon.ps1 (blob proven), no hand patch.
2. `claude/g-tests-runtime-closure` = `629f97752b` (three commits on 43d149b87d) -- the TEST HOST preloads its managed reference closure (src/core/testing/TestHost.cs); os/user re-banked 17 (roster cell 5 -> 17, page, README, re-emitted tests.csproj / test host / package_test_info, user_windows_test.cs); GolibTests arm ImpersonatedAssemblyLoadTests.
3. `claude/g-testing-row-s5` = `8ed02f7dce` (three commits on 1f2724c755, which 8b carries as ref 9) -- the testing row's two 1.24 benchmark-iteration tests join the Phase-4D exclusion (family 3, pinned by name in the capability guard); TempDir inside a Cleanup is removed; the testing page (VALIDATED 53 + 15). The ROSTER CELL 37 -> 53 is NOT in this ref: the row run below banks it.
4. `claude/c2-package-ref-shadow` = `a91b638cf9` (one commit on 3469154a95) -- the converter root-qualifies a package reference in a file body when the file's own usings would capture it (visitImportSpec.go + guards), and runtime/pprof/pprof.cs's one captured line re-emitted. Predicted: CNR CHANGED empty.
5. `claude/i9-runtime-blockevent` = `5b5f35ff21` (fast-forward of 1abe5678e4, which 8b carries as ref 12; one commit) -- internal/testenv.CPUProfilingBroken = true on the managed host (registry key in manualTypeOperations.go, companion testenv_impl.cs, three package_info.cs position lines); GolibTests arm TestenvCPUProfilingBrokenTests.
CONFLICT RULES: a registry table (manualTypeOperations.go, linknameOperations.go, visitFuncDecl.go's linkname map), go2cs-src.projitems, a test-project item list (GolibTests.csproj) or declaredNotImplemented_test.go with adjacent inserts from two refs => the UNION, asserted by that file's own guard tests. The roster with different rows => both. A CONVERTER-EMITTED file conflicting between two refs => NEVER hand-merge: RE-EMIT that file at the merged tree with the merged converter (a seeded single-package conversion into a fresh root, taken whole), and prove the result differs from each parent only by the other parent's intended hunks. Anything else => STOP with the hunk quoted.

## Battery (converter/gen/golib source FROZEN while it runs)
(a) go2cs.exe rebuilt under the pin; the FULL converter suite `go test ./... -count=1 -timeout 40m` in src/go2cs: expected GREEN; quote any red in full.
(b) gen 0 errors; GenTests; GolibTests (Release) -- a CONTROL at bb54ff0920 FIRST (8b read 834 / 15 / 849), then the head: name every new arm (ImpersonatedAssemblyLoadTests, TestenvCPUProfilingBrokenTests) and account for the count delta exactly.
(c) CNR (the MERGED tree's check-no-regression.ps1, which now refuses without git/go -- assert its preflight line): `NO REGRESSION`, CHANGED listed. Predicted EMPTY. If CHANGED is NOT empty, (c2) run THE FULL BEHAVIORAL SUITE with a control at bb54ff0920 (8b's control and head both failed exactly FuncLiteralCallerNames and GoroutineWaitState) and name every changed project; if CHANGED is empty, the behavioral suite is NOT run (no golib change is aboard and every emission is byte-identical) -- say so.
(d) stdlib `src/go2cs-stdlib.slnx` Release: 344 / 0.
(e) ROWS through a per-run COPY of the merged tree's `src/run-h10-recon.ps1` (ref 1's): `-tests -test-action all -test-config Release`; the output dir the SECOND positional inside the worktree's src/core/<row>; results tails FIRST; test artifacts cleared before each row; production re-emissions restored after each (count them by class).
  - BANK if VALIDATED (Tests = the page's MATCHED count, Disclosed = the page's disclosed): `testing` (predicted VALIDATED 53 + 15, roster cell 37 -> 53, the row's prose per ref 3), `os/user` (predicted VALIDATED 17, as ref 2 banked it -- confirm at this tree), `internal/godebug` (its roster execution pin -test-tiered now applied by the wrapper: predicted the runtime.Caller rows MATCH -> VALIDATED; state the pin as the wrapper applied it), `internal/trace` (the i7 read 92 / 0 at 1.23.12-era; TestTraceCPUProfile x4 must run here, not refuse 'use of internal package').
  - CONTROLS (the test host changed for EVERY row via ref 2's preload): verdicts identical to their banked pages, bank nothing unless a page legitimately moves: fmt, strings, sync, os, net/url, encoding/json, crypto/x509.
  - NOT run here: crypto/tls (AZ1), net (the reading run), net/http (moving to candidates), runtime, runtime/pprof (candidates).
(f) THE GUARD: `src/check-roster-format.ps1` from `src/` at the merged tree before any bank (quote its failure set), then after the banks: every section green; the HEADER figures and the README NEWS block WRITTEN exactly as the guard derives them (never hand-chosen); re-run to `0 of N`. The linux line stays what the guard derives (G's linux leg is in flight -- do not edit it).
(g) THE INDEX: `python docs/phase4/hopA-inputs/regen-validation-index.py` self-test, dry run, `--write` if it changes (zero orphans, zero page-less; index rows == roster rows).
(h) COMMITS on the branch, each signed: (1) the rows' bank (artifacts, pages, badges, cells); (2) the index + header + README NEWS. Census on everything staged; repoguard TestNoFleetIdentifiersInTrackedFiles -count=1 with a planted control (floor 13).
Restore side effects; `git status --porcelain` empty (unfiltered), no ` D`; go2cs.exe count 0 at exit; lock released. Worktree IN PLACE. NOTHING PUSHED.

## Report (final message)
Base and refs asserted; each merge's SHA, its conflicts and their resolutions (re-emissions proven); (a)-(d) verdicts with the control comparisons (and (c2) if it ran); the rows table (word, counts, the tail's last line, wall, banked or control); the guard's failure set before and after; the header figures written; the index line; `git log --oneline --first-parent bb54ff0920..HEAD` with %G?; disk; everything not verified, stated as such.
