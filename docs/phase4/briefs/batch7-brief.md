You are an i7 sub-agent for the go2cs fleet coordinator. APPLY BATCH 7 -- two refs merged onto the version branch in a LINKED worktree ON A BRANCH, three rows re-run and BANKED, the index regenerated, the header re-derived; ONE battery; report. **DO NOT PUSH.** Read `CLAUDE.md` first (floors 1, 3, 4, 7, 8, 13, 14, 16). Never modify the main checkout or any other worktree under /c/go2cs-tmp-coord.

## Fixed facts (the coordinator fills TIP7; assert each by `git ls-remote origin`; STOP on any mismatch)
- BASE: `claude/version-go1.24.13` at `TIP7` (the batch-6 STAMP; `git merge-base --is-ancestor f545b18d4d TIP7` must hold).
- REF 1: `claude/c1-alloc-pins-fips140test-sha3` = `101d44d596` (C1; two commits on f545b18d4d): the two hand-owned manifests (crypto/internal/fips140test SIX alloc-profile pins; crypto/sha3 FOUR TestAllocations pins, DEFERRED) and roster ROW edits only -- fips140test Tests 2267 / Disclosed 7; NEW rows crypto/sha3 23 / 5, embed/internal/embedtest 7 / 0, internal/runtime/maps 111 / 108 (RULED: G's 58 leaf pins + 50 implied ancestors = the guard's ceiling 108) -> 219 rows. ALLOWED paths: the two `go2cs_test_disclosures.json`, `docs/ValidatedTestPackages.md`. Anything else: STOP and quote.
- REF 2: `claude/coord-h10-closing-arith` = `e43b8f3cd` (COORD; one signed commit on f545b18d4d, 7 files): `docs/phase4/hopA-inputs/recon-lists/population-go1.24.13.txt` (230 names, the population of record), roster header 216 / 230 -- 93.9% and 216 / 224 -- 96.4% with the 'The 230, derived' block and the four E1 rows struck (ledger 10 -> 6), README figures, `src/check-roster-format.ps1` section 2b (population arms), `docs/phase4/hopA-inputs/shardmap.py` closing check, runbook annotation, `.gitattributes` eol=lf pin. Merge conflicts EXPECTED in `docs/ValidatedTestPackages.md` between the two refs: ref 1 edits ROWS, ref 2 edits the HEADER, the exclusion table and adds a dated block -- resolve to BOTH (rows from ref 1, header/table/block from ref 2); quote every conflict hunk you resolved; anything that is not that shape is a STOP.
- Worktree `git worktree add -b i7/h10-batch7-<mmddHHMM> /c/go2cs-tmp-coord/ab7 TIP7` (ON A BRANCH: the rows are banked from it); lock `mkdir /c/go2cs-tmp-coord/ab7.lock` + PID; scratch `<this session's scratchpad>/ab7/`; pins as the batch briefs (GOROOT backslash read back; GOTOOLCHAIN=local; CGO_ENABLED=0; the dotnet10 root; pwsh net8 apphost launched with DOTNET_ROOT unset and the dotnet10 root off the parent PATH); sibling check; disk >= 25 GB before every row; `bin/` `obj/` purged after each.

## Merges
`--no-ff -S` in order ref 1 then ref 2; per ref the additive-blob proof (blobs EQUAL except the resolved roster hunks, which you quote), `git diff --diff-filter=D TIP7 HEAD` = only the four struck exclusion rows' LINES (no file deletions), the allowed-path check.

## Rows -- the wrapper `src/run-h10-recon.ps1` from the tree by blob, per-run COPY, one row per call; `-tests -test-action all -test-config Release`, output dir the SECOND positional inside ab7/src/core/<row>; results tails read FIRST; artifacts cleared before each row
| row | floor | predicted |
|---|---|---|
| crypto/internal/fips140test | 20m | VALIDATED 2267 with 7 disclosed (the six pins + the parent by aggregation; TestACVP + TestCAST* PASS) |
| crypto/sha3 | 20m | VALIDATED 23 with 5 disclosed |
| internal/runtime/maps | 20m | VALIDATED 111 with 108 disclosed (a control: its page is already banked; the re-run must reproduce it byte-for-byte in verdicts) |
A mismatch is a FINDING (names), never reconciled by hand. If a row does NOT validate, bank nothing for it, and act C's index step will refuse on its page-less row: report and stop that step.

## BANK (one shard commit `-S` on the branch; then `git branch claude/coord-h10-batch7-s1 HEAD`)
TEST artifacts only (`*_test.cs`, package_test_info.cs, package_info_internal_test.cs, go2cs_test_host.cs, `<pkg>.tests.csproj`, Go's own testdata the suite reads), the proof pages of rows that VALIDATED (fips140test's page is NEW -- the row's `[proof]` link must resolve to it; sha3's page is NEW), README badges if modified; NEVER a production .cs (restore, count, name by class -- the pipeline's -tests emission and the reconvert class are known), NEVER a manifest (a byte-identical rewrite is unmodified; a CHANGED one is a STOP with the diff), NEVER index.md, NEVER the header. Roster cells: the rows' Tests / Disclosed already carry the measured values from ref 1 -- assert equality with the run, edit nothing unless the run disagrees (then a FINDING, no edit). Census `converted` on staged converted sources, `entry` on pages; repoguard `TestNoFleetIdentifiersInTrackedFiles` on the staged tree with a planted control (floor 13).

## THE INDEX and the header (one signed commit)
`python docs/phase4/hopA-inputs/regen-validation-index.py` (the tree's copy is master's facb693040 tool, materialised by pass 2): self-test, dry run, `--write` -- it must WRITE with ZERO orphans and ZERO page-less rows (219 roster rows == index rows). Then `src/check-roster-format.ps1` from `src/`: the header figures WRITTEN as the guard derives them (expected: 219 rows, 219 / 230 = 95.2%, 219 / 224 = 97.8%, verdicts = 57,064 + fips140test's delta + 23 + 7 + 111, disclosed = 164 + 7 + 5 + 108 -- report the actual; never hand-chosen); README NEWS block the same; re-run to `0 of N` (quote any named set). The full converter suite is NOT required here (batch 6 ran it); run `go test ./... -run 'TestNoFleetIdentifiersInTrackedFiles|TestContextBudget|TestCommittedCoreReferencesResolve|TestDeclaredNotImplementedCensus' -count=1 -timeout 30m` under src/go2cs and report each (the two guard-table tests may be red on the new artifacts -- quote the rows they name; that is a finding for C1, continue).

## Report (final message)
TIP7 and the two refs asserted; merge SHAs with the resolved hunks quoted; per row the command line, word, counts, the tail's last line, wall; the shard commit SHA with path counts; production files restored by class; the index tool's line; the guard's before/after with the header figures written; index rows == roster rows; `git ls-files '*.tests.csproj' | wc -l` vs 219 (the difference named: rowless-unbanked candidates); disk; `git status --porcelain` empty and `^ D` empty; lock released; `git log --oneline TIP7..HEAD`. Worktree `/c/go2cs-tmp-coord/ab7` IN PLACE. NOTHING PUSHED.
