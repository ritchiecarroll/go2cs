You are an i7 sub-agent for the go2cs fleet coordinator. THE H10 REHEARSAL: the first execution of the dispatch driver, on ONE shard, on this Windows box, against the version tip after APPLY BATCH 2. Read `CLAUDE.md` first (the safety floor: floor 1 one conversion per output root; floor 3 the output directory is the SECOND positional; floor 4 source frozen while the battery runs; floor 5 kill by path never by name; floor 7 capture every rc before any pipe; floor 8 never `git add -A`; floor 13 a gate never made to fail proves nothing; floor 14 read the results-file tail first). **NOTHING IS PUSHED** by you; the coordinator banks or refuses from your report.

## The ruled design (read these two sections of the draft before anything else)
`SCRATCH/driver-brief-DRAFT.md` sections **E. The rehearsal** and **F. Results and banking** (SCRATCH = this brief's directory). They hold the ruled shard, the acceptance predicate (five decidable clauses + one made-to-fail control), what a red returns to, and the TSV column floor. Where this brief and the draft differ, THIS brief wins on mechanics (paths, blobs, flags) and the draft wins on the predicate.

## Fixed facts
- Version tip to rehearse against: `TIP` on `claude/version-go1.24.13` — assert `git ls-remote origin refs/heads/claude/version-go1.24.13` reads exactly this before anything; if not, STOP and report the tip you read.
- Master tools, taken BY BLOB (never from a checkout): `origin/master:src/run-h10-dispatch.ps1` (blob `05ec63184b`), `origin/master:src/run-h10-recon.ps1` (blob `5d07919168`, the wrapper, `-AllowBranch`), `origin/master:docs/phase4/hopA-inputs/h10-dispatch-plan.tsv` (the plan; the driver recomputes `#digest` over its rows and refuses a mismatch — materialise it byte-exact: `MSYS_NO_PATHCONV=1 git show <ref>:<path> > file` from a shell whose cwd is the repo; LF only, assert zero CR bytes). State each file's git blob id (`git rev-parse origin/master:<path>`) AND the sha256 of the file as read, each labelled with what it hashed.
- Fetch first: `git -C /c/Projects/go2cs fetch origin master claude/version-go1.24.13`.
- The tree: a LINKED worktree ON A BRANCH (the driver's tree guard refuses a detached or main worktree): `git -C /c/Projects/go2cs worktree add -b rehearsal/h10-i7-$(date +%m%d%H%M) /c/go2cs-tmp-coord/reh TIP`. Work only there. Disk ≥ 25 GB before the run (`df -h /c`).
- Shells: build `export DOTNET_ROOT=$HOME/dotnet10; export PATH="$HOME/dotnet10:$PATH"` (assert `dotnet --version` starts with 10). Converter/go: `export GOROOT='%USERPROFILE%\sdk\go1.24.13'` (the LITERAL backslash spelling; a forward-slash or the machine-scope go1.23.1 is a dead pin), `export PATH="$HOME/sdk/go1.24.13/bin:$PATH"` (the pinned bin FIRST — GOROOT does not pin the child `go` the converter spawns), `GOTOOLCHAIN=local`, `CGO_ENABLED=0`; assert `go version` = go1.24.13. pwsh 7: `env -u DOTNET_ROOT pwsh` for the driver (it is a PowerShell script; pass the same pins through `$env:`), and pass `-GoRoot '%USERPROFILE%\sdk\go1.24.13'`.
- Sibling check before the run: any heavy worker anywhere (`Get-CimInstance Win32_Process | Where-Object { $_.Name -match '^(dotnet|MSBuild|VBCSCompiler|csc|go2cs|go)\.exe$' }`) means WAIT in 120 s loops (cap 40 min), never a substring match over CommandLine (it matches prose). You kill nothing.

## The shard
The plan's `W=4` rows for the worker spelled exactly as the plan spells the i7 (read the plan's worker column — it is the i7's row set with `(coordinator)`), **slice 1** — the draft's seven rows (seq 1 `crypto/internal/fips140/edwards25519` … seq 7 `html/template`). Assert the plan's W=4 / i7 / slice-1 rows ARE those seven in that order before running; if the plan differs, STOP and report the rows you read (the rehearsal is on the ruled shard or not at all).

## Steps
1. `-DryRun` first: the driver with `-Plan <plan> -Worker "<the i7 as spelled>" -FleetSize 4 -OnlySlice 1 -DryRun -Tree /c/go2cs-tmp-coord/reh -GoRoot ... -Scratch <scratch dir> -ExpectTip TIP -RebankWrapper <wrapper file> -Ledger <scratch>/rehearsal-ledger.tsv -TimingOut <scratch>/rehearsal-timing.tsv` (read the driver's `param()` block first and map every mandatory parameter; report the exact command line). rc must be 0 and the dry run must name exactly the seven rows; capture its full output to a log.
2. The real run: the same command without `-DryRun`. Capture rc directly (no pipe). It runs the wrapper per row (the pipeline `go2cs -tests -test-action all`), writes artifacts into the worktree and the TSV.
3. The graft, NON-BANKING: after the slice, run the wrapper directly on `crypto/internal/fips140test` in the same worktree with the same pins (the wrapper's own usage — read its header; `-AllowBranch`; the output directory as the SECOND positional if it takes one) and capture its row line. Its artifacts (proof page, badge, `.tests.csproj`, the five-pin `go2cs_test_disclosures.json` manifest) are READ, not committed; expected DIVERGED ~52 (Go=pass C#=fail, incl. TestACVP/TestCAST*).
4. The acceptance predicate — the draft's five clauses over the seven driver rows (the TSV by column NAME never position; LF only; `sweep_s` integer; artifacts per row with `LastWriteTime` after the row's own start, never CreationTime; `src/check-roster-format.ps1` rc 0 or its two named relocation orphans reported as hop debt; the roster header re-derived from the table) and clause (5) over the graft's manifest (five pins, each with class and signature; every `deferred` entry carrying want/reading/plan).
5. THE CONTROL (floor 13): delete one pin from the graft's manifest, run the format gate, confirm it NAMES that row, restore, verify byte-identical by sha256.
6. Leave the worktree IN PLACE with every artifact; `git -C /c/go2cs-tmp-coord/reh status --porcelain` listed in the report (the coordinator commits the seven banking rows or refuses). Do not remove the worktree.

## Report (final message)
The tip; each tool's blob id + sha256 as read; the exact driver command lines; the dry run's row list; the real run's rc and the TSV verbatim (seven rows, all columns); per-row artifact check (present/absent, LastWriteTime vs row start); the format gate's rc and every line it printed; the header re-derivation; the graft's row line and manifest check; the control's three readings (named / restored / identical); wall time per row vs the plan's `i9-s` estimate; disk free after; anything not verified. NOTHING PUSHED.
