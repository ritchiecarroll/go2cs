You are an i7 sub-agent for the go2cs fleet coordinator. THE LEGS: merge the campaign's shard refs onto the version branch as INCREMENTAL TRAINS -- one leg at a time, each gated before the next -- in a throwaway worktree, then regenerate the shared index once, and report. **DO NOT PUSH.** Read `CLAUDE.md` first (floor 7: rc on the next line before any pipe; floor 8: never `git add -A`, and after every cleanup `git status --porcelain | grep '^ D'` is empty; floor 13: a gate never made to fail proves nothing; floor 16: an unfiltered command answers "is it clean").

## Fixed facts (the coordinator fills TIP4; assert everything by `git ls-remote origin`)
- BASE: `claude/version-go1.24.13` at `TIP4` (the batch-4 STAMP; its parent chain contains the campaign tip c6fdbe73c3 -- assert `git merge-base --is-ancestor c6fdbe73c3 TIP4`).
- THE SHARD REFS, all parented on c6fdbe73c3 (assert each ref's commit parent == c6fdbe73c3, except where noted), in LEG ORDER:
  1. `claude/coord-h10-rebank-s1` 3aec6d8fe3 (i7; 7 rows PASS: crypto/internal/fips140/edwards25519 54, internal/godebugs 1, database/sql 140, internal/types/errors 155, compress/flate 64, crypto 6, html/template 243; roster edits database/sql 138->140, fips140/edwards25519 Disclosed 1->0)
  2. `claude/coord-h10-rebank-s2` a71adf7b91 (i7; 23 rows PASS; roster edit internal/syscall/windows 2->3)
  3. `claude/g-h10-rebank-s1` 06ac80d680 (G; 6 rows PASS; encoding/xml 386->387, debug/buildinfo 197->211)
  4. `claude/g-h10-rebank-s2` 8236d68a06 (G; 25 rows PASS; netip 210/57->211/54, ed25519 8->9, strconv Disclosed 11->10, rc4 2->75, runtime/debug Disclosed 5->6, net/url 48->49, encoding/binary 137/9->140/8)
  5. `claude/i9-h10-rebank-s1` 854b94107d (i9; 24 rows PASS; go/parser 176, archive/tar 98, go/types 574, encoding/json 532, crypto/rsa 568, math/big 230, crypto/ecdsa 77, net/smtp 20, go/doc 86, crypto/des 55, crypto/cipher 27272; ONE deletion crypto/cipher/package_info_internal_test.cs -- measured, admitted)
  6. `claude/i9-h10-rebank-s2` 06f37e478e (i9; 74 rows PASS; strings 69, bytes 83, internal/sync 106, crypto/sha256 22, slices 120, encoding/pem 18, weak 6, fips140/mlkem 10; Disclosed cells filled)
- Worktree: `git worktree add --detach /c/go2cs-tmp-coord/legs TIP4` (assert the path does not exist). Lock `mkdir /c/go2cs-tmp-coord/legs.lock` + PID; released at the end. Scratch `<this session's scratchpad>/legs/`. Shells: the build pins (`DOTNET_ROOT` = the dotnet10 root, first on PATH; pwsh via `env -u DOTNET_ROOT pwsh -NoProfile` with the .NET pins set INSIDE via `$env:`); the Go pin only if a gate converts (the filtered re-sweep does): GOROOT = the go1.24.13 SDK in backslash spelling read back from `go env GOROOT`, the pinned bin first on PATH, GOTOOLCHAIN=local, CGO_ENABLED=0. Disk >= 25 GB before each leg. Sibling check per the batch briefs (idle MSBuild daemons are not siblings).

## Per leg (repeat for refs 1..6, in order; a red leg STOPS the train -- report which leg and why; the landed legs stay)
(a) `git merge --no-ff -S <ref>` with the repository's merge-message style (`Merge <ref> -- h10 rebank <lane> s<N>: <rows>`, body listing the rows and the roster edits, ending `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; nicknames only). Conflicts: `docs/ValidatedTestPackages.md` is edited by EVERY leg (different rows) and by batch 4's tip -- a conflict there that is two legs editing DIFFERENT rows resolves to BOTH rows (assert the resolved table has each of the two legs' edited rows exactly once with its new values); a conflict on the SAME row, on the header, or in any other file is a STOP with the hunks quoted. No other file should conflict (the artifacts are disjoint by shard -- assert `git diff --name-only` between any two refs shares only the roster).
(b) After the merge: `%G?` G or U; parents ours-then-theirs; every file of the ref blob-EQUAL to the ref's tip (the roster COMPOSED, its hunks additive); `git diff --diff-filter=D --name-only TIP4 HEAD` lists ONLY the admitted deletion (leg 5's package_info_internal_test.cs); NO `docs/validation/index.md` change from any leg (assert the blob equals TIP4's); NO production `.cs` from any leg (`git diff --name-only TIP4 HEAD -- 'src/core/**/*.cs'` filtered to non-test names must be EMPTY -- the test-artifact set is `*_test.cs`, `package_test_info.cs`, `package_info_internal_test.cs`, `go2cs_test_host.cs`); markers none.
(c) THE FIVE DISCLOSED CELL FIXES, applied as ONE extra signed commit after leg 6 (i9 wrote the pin count where the disclosed verdict count differs): os/signal 3 -> 0, debug/gosym 1 -> 0, internal/cpu 2 -> 0, crypto/cipher 1 -> 0, math/big 2 -> 1 -- verify each against the row's `go2cs_test_comparison.json` disclosed list in i9's evidence refs (claude/i9-h10-s1-evidence 6dbe347c07, claude/i9-h10-s2-evidence b2eff468c7 carry the TSVs; the comparison JSON stays on the i9 -- if a value cannot be verified from the refs, apply i9's stated figure and say so).
(d) GATES per leg, each rc captured on the next line: (1) identifier census `entry` on the merge message; repoguard `go test ./internal/repoguard/... -run TestNoFleetIdentifiersInTrackedFiles -count=1` from src/go2cs in the worktree -- PASS (this is the gate of record for converted sources); (2) `src/check-roster-format.ps1` from `src/` under pwsh 7: the failure SET must be exactly the two relocation orphans (2 of N) OR fewer as legs clear them (the fips140test re-bank clears one) -- any NEW line is a STOP with the line quoted; the header's derived figures are printed by the guard: record them; (3) THE CHECKSUM: every row banked so far appears in exactly ONE leg (union the six refs' row lists from their commit bodies; assert no duplicate and that each ref's proof pages `docs/validation/current/<row-dotted>.md` are present once); (4) A FILTERED RE-SWEEP SAMPLE at the merge result: pick the leg's SMALLEST-cost PASS row and re-run it through the pipeline in the legs worktree (`go2cs -tests -test-action all <GOROOT>/src/<row> <worktree>/src/core/<row>`, the floor from the sweep table; output dir the SECOND positional) -- its Validated count must equal the leg's TSV verdict count for that row (a lane's proof binds its own tree; this is the one re-measurement at the merge result); restore the pipeline's side effects afterwards (`git checkout -- .` + `git clean -fdq -- src/core docs/validation`; status empty; `^ D` empty).
(e) After leg 6 + the cell fixes: REGENERATE `docs/validation/index.md` ONCE with the repository's index tool (read `.claude/rules/harness-gates.md` and `docs/validation/` for the tool -- the validation-index regeneration tool C1 landed 2026-09-20 with `--write`; quote the invocation); its row count must equal the roster's row count (the guard prints it); commit signed. Then the header: DO NOT hand-edit -- if `check-roster-format.ps1` reports the header's derived figures differing from the header lines, write EXACTLY the guard's expected figures into the header lines (and the README NEWS block it cross-checks) in the same commit, and re-run the guard to its two-orphan set (or fewer).
(f) FINAL: `git ls-files | wc -l` before and after every cleanup equal (state both); `dotnet build src/go2cs-stdlib.slnx -c Debug` 344 / 0 errors (the artifacts changed test projects only, but the solution is the floor); census `selftest` 117/0 and `entry` on every commit message.

## Report (final message)
TIP4 asserted; per leg: the merge SHA, `%G?`, EQUAL/COMPOSED, the roster conflict resolution if any, repoguard, the guard's line and header figures, the checksum reading, the re-sweep sample row and its two counts; the cell-fix commit; the index regeneration (invocation, row count); the header figures before/after; stdlib; census; the worktree IN PLACE at `/c/go2cs-tmp-coord/legs` (the coordinator pushes from it); disk; the lock released; anything not verified. NOTHING PUSHED.
