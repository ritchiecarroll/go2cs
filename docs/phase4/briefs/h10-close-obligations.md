# H10 close: the obligations list, classified

As of: version tip `claude/version-go1.24.13` = d1a0d314d5 (batch 8f). Fetched and ls-remote asserted at 13:25, with `claude/r-tls-bank` = 3ec2c9ff39 and `claude/g-tls-linux-bank` = b57703b1cd also on origin. Ledger read through 2026-09-23 12:56 (mailbox 58d2ec3ea6, lines 48-224). Handover record 7bd5417f94 (STATE DELTA 26). This is a read-only derivation: nothing was built, converted, run or pushed. An earlier cut read the ledger only through 12:10. The 12:36 ACCEPT and the 12:56 RULING moved a1, a3, a9 and b1, and the corrections are folded in below.

Classes:
- **(a)** discharged by PART A (batch 8g).
- **(b)** discharged by PART B (the close seat).
- **(c)** COORD-owned. Rulings, stamps and docs no sub-agent should cut alone. Items marked **PRE-8g-STAMP** or **CLOSE PRECONDITION** gate a stamp.
- **(d)** post-hop. Each is stamped on the ledger on **<CLOSE_DATE>**, the calendar date of the H10-close STAMP line, read with `date` at the stamp. At today's pace that is predicted to be 2026-09-24; that prediction is UNVERIFIED. The stamp gives every relative "post-hop" in manifests and rulings a dated origin. The "due" column says what gates each item.

Source key: `rb` = docs/GoCorpusMigration.md@d1a0d314d5 (unchanged at 3ec2c9ff39 and b57703b1cd); `roster` = docs/ValidatedTestPackages.md@d1a0d314d5; `L hh:mm` = ledger line (2026-09-23 unless marked 09-22).

---

## (a) Discharged by PART A: batch 8g

| # | Obligation | Evidence | How Part A discharges it |
|:--|:--|:--|:--|
| a1 | crypto/tls BANKS at 1.24.13 from R's raised-wall run: page, badge, cells 3643/1 -> 4759/1, row prose, notes removal | owner ruling L 11:59; ACCEPTED L 12:36; rb :2244-2255 (re-bank or demote BY NAME); roster:273 first page reads Go 1.23.12 at the tip | ref 4, `claude/r-tls-bank` 3ec2c9ff39: two signed commits (aaa61ef620 the notes removal, 3ec2c9ff39 the bank) fast-forwarding d1a0d314d5, 6 files. Its header (:137) and `docs/README.md` NEWS hunks are admitted as guard-derived carriers and overwritten at (f); that is an exception to rb :2777-2783, recorded by COORD at the stamp (c17). Checks: guard 2f; the static page arms (d)1-8; manifest blob bbc8268e00 == 4a960d31a3's |
| a2 | TestBogoSuite BlockSize 3243 -> 3419, in the SAME batch as the bank | 09-22 L 21:09 item 3; L 11:59; `_roster.ps1:1198-1204`, `:1478-1484` refuse a mismatch | CARRIED by ref 4 (`run-validated-sweep.ps1:647` reads 3419 at 3ec2c9ff39). (d)4 asserts it once; the page's block count == 3419 by the sweep's own reader, with a planted 3418 red |
| a3 | crypto/tls's re-emitted tests.csproj retires the core-refs table's two crypto/tls rows. The sweep itself is (b) | `coreReferencesResolve_test.go:73-82`; ruling 11 (09-22 L 12:15/12:21); at 3ec2c9ff39 the csproj still names `crypto/internal/mlkem768` (:291) and `runtime/internal/math` (:320) | discharged by a11's re-emission, then the guard `-count=1 -v` reads `declared 8 · measured 1 · retired 7`. `measured 3 · retired 5` is a STOP-finding |
| a4 | crypto/tls's test project BUILDS at the merged tree after 8f's testing-host change (HostDescriptorLimit.cs) | 8f stamp L 12:02; R measured at 4c5b0a3b7 (= fc6269b0bf + notes) | battery (c), after a11 (a build only, no run). Before a11 it would build the 1.23.12 project against absent core dirs |
| a5 | net's LINUX annotation 577 + 2 -> 581 + 3 | G `claude/g-net-linux-bank` 2b65f11474 (L 12:10) | ref 3 |
| a6 | H10 step 5: sync/atomic floor 90m -> 150m | L 11:51 (linux wall 5,868 s), L 11:59 accepted; G 48e1e4d245 | ref 1. Checked with (e): `-SelfTest` green, then the floors derived read-only with the wrapper's reader after R-A1's commit (a plant named), then shardmap exit 0 |
| a7 | H10 step 5: crypto/tls's 30m floor RE-CHECKED against the full-host run "when it lands" | 09-22 L 18:02 item 3 (rule: raise at wall >= 0.75 x floor, never lower). Readings: R's BANK row 1,761 s = 0.98x (3ec2c9ff39 message; L 12:36); R's 09-22 EVIDENCE row 2,216 s at d095fe8108 (7462befde0 rows.tsv; nothing banked, 09-22 L 21:55); AZ1 standard wall 467 s = 0.26x (fad839a224 rows.tsv) | per ruling R-A1. By the bank row the rule says RAISE (60m: 0.49x). HOLD needs COORD's ruling that the standard-wall reading governs. **A missing R-A1 is a STOP** |
| a8 | R's REC-G ChanCore design record lands on the version branch | L 11:43 ("to ride the next version-branch batch"); bb69175480 | ref 2 |
| a9 | The header, README NEWS and linux line derived after 8g | rb :2777-2783 | guard. Predicted 219/230, 58,319 / 283; linux 188/217, 49,636 / 163 (the union b57703b1cd's message states). The :167 conflict between refs 3 and 5 is resolved by the guard |
| a10 | Stale-figure census for 3,243/3,242 (the registration, row prose, comments, manifest `hostConditional`, roster :71) | roster:273; `check-roster-format.ps1` 1b2 comment; manifest `hostConditional` | battery (d)8. Only the registration and row prose are fixed in 8g; comment and manifest prose go to (d) H12 |
| a11 | **crypto/tls's 1.24.13 TEST ARTIFACTS re-emitted at the merged tree** (convert-only, deterministic emission, no re-run) and committed with the bank | L 12:36: "NOT banked by R ... the 8g battery re-emits them at the merged tree ... and commits them with the bank, as every other bank did"; 3ec2c9ff39's message ("Not banked: the run's re-emitted tests, tests.csproj and eight testdata recordings"); at 3ec2c9ff39 `example_test.cs` and `fips_test.cs` are absent | Part A step (0): `go2cs -tests -test-action convert -go2cspath <tree>/src <GOROOT>\src\crypto\tls <tree>/src/core/crypto/tls`, with the output dir as the SECOND positional; production .cs and the README restored and counted (09-22 L 04:01); testdata byte-equal to GOROOT from the row's own dir minus sub-packages (rb :2891-2898); census `converted`; its own signed commit |
| a12 | G's crypto/tls LINUX annotation 401 + 1 -> 1341 + 1, so no row carries two Go versions | L 08:51; L 12:10; L 12:36 ("G ... banks its annotation on top, so 8g carries it"); ruled L 12:56 | ref 5, `claude/g-tls-linux-bank` b57703b1cd (on 3ec2c9ff39). Its header linux line conflicts with ref 3's at :167; the guard derives 49,636 / 163 |
| a13 | ONE dated sentence on the crypto/tls row naming the linux FOURTH (oracle-broken) state: Go's shim loopback refusal on the WSL arm, unattributed | L 12:56 RULING ("the batch adds ONE dated sentence") | R-A2 (COORD's text or the brief's default), Part A commit (1) |

No transient remains at the 8g stamp. crypto/tls's page and its linux annotation are both 1.24.13 once refs 4 and 5 land.

---

## (b) Discharged by PART B: the close seat

| # | Obligation | Evidence | Part B step and gate |
|:--|:--|:--|:--|
| b1 | (moved to a12: G's crypto/tls linux annotation rides 8g, L 12:36/12:56) | | |
| b2 | net/http MOVES TO THE CANDIDATES: the roster row out, a candidate note with its classes NAMED, badge orange, stays in the population (230) | owner ruling 09-22 L 17:33; roster:392; `src/core/net/http/README.md` badge 1345/1345 green | B4 (a)-(d). Guard 2b / 2b2 (225 = 218+4+3) / 2b3 / 2f / 2g |
| b3 | net/http's page disposition: `current/net.http.md` would re-green the badge on any re-emission and be refused by the index tool | `readmeValidationBadge.go:436-446`; `regen-validation-index.py:206-226`; the 1.23.12.3 snapshot holds `net.http.md` (blob 4d5a95ac64) | B4(c) per R-B4 (default REMOVE, which matches how all five existing candidates sit) |
| b4 | "TestRegisterErr//a -- the execution pin, applied at the close" | 09-22 L 17:33; wrapper pins since d095fe8108 (09-22 L 18:02); 8c and 8e never ran net/http; the i9 record's 19 divergences = 11 synctest leaves + 6 synctest parents + `TestRegisterErr` + `TestRegisterErr//a:&http.handler{i:0}` (recon-evidence/i9/net/http comparison JSON); linux reads the 17 (L 08:51) | B3 close reading (R-B1), through the wrapper with `-AllowBranch` and `-ExpectTip`. Pin asserted from the wrapper's printed line. Predicted 17 if the pin settles the pair, 19 if not; B4(b)'s cell worded from the outcome |
| b5 | HOST RULE: a windows-DIVERGED row is re-read on linux AT THE SAME TIP before demotion by name | rb :2541-2549; the existing read was on the seat tree 971d919113 (L 08:51) | R-B3. It needs COORD's checkpoint item 5 first: dispatch G's re-read at the 8g STAMP, or rule the seat-tree read sufficient (c15) |
| b6 | **shardmap's closing check goes RED when net/http leaves the roster** (found by code read, not run): net/http has no row in `recon-basis.tsv`, and UNSCHEDULED is roster rows without cost (`shardmap.py:416-417`), so a demoted net/http is a LIVE unreached member and the script dies (`:1048-1076`) | recon-basis.tsv@tip has no net/http row (216 lines) | B4(e) per R-B9: append net/http's costed row (the pprof precedent, rb :2353-2363). Red-first then green |
| b7 | go/internal/srcimporter's 1.23.12 linux annotation DEMOTED BY NAME (ORACLE: Go's own TestImportStdLib fails on the linux host) | L 12:10; 06:18; roster:311 `· linux: 7 ·`; 6836e605ac rows.tsv. The oracle read was cgo OFF (g-linux-leg README line 5; L 06:18; the sweep's whole-run pin `run-validated-sweep.ps1:159-161`); the annotation was derived cgo-ON (`:1018-1021`) | B5 per R-B11 (c16). The sentence names cgo OFF and the ON -> OFF state change. Linux line predicted 187/216 |
| b8 | The core-refs table SWEPT TO ZERO: a standing row "names a banked roster row never re-run at 1.24.13" | `coreReferencesResolve_test.go:63-64`; ruling 11 (09-22 L 12:15); the `-v` caveat (09-22 L 12:21) | B6, with a plant. The net/http row retires only through R-B2's re-emitted csproj (B3) |
| b9 | The hop's FINAL SEEDED `-stdlib` RECONVERT, owed BEFORE H10 closes | 09-22 L 11:10 ruling 2 (maphash, named debt); 09-22 L 08:18; 09-22 L 14:49 (profBufReadMode); STATE DELTA 26 | B8, with the two-seeded pair (A = `<REGEN_BASE>` 54dec61728, B = tip), seeded from one `seed.tar` with a delete-and-re-seed retry rule (rb :542-544), every diff classified by §4 in order (rb :571, :3315-3325) with zero T5 and a planted T5 |
| b10 | maphash.cs escapeForHash body still `throw panic("intrinsic")` | `hash/maphash/maphash.cs:306-308`@tip | B8 B-only class, predicted present |
| b11 | traceviewer's `//go:embed` payload (~2.7 MB) absent; `staticContent` uninitialized: a latent production defect | `internal/trace/traceviewer/http.cs:435-436`@tip; 09-22 L 00:52, 05:57; the only production `//go:embed` in go1.24.13 std (a GOROOT grep) | B8 B-only; the B8(7) overlay is WIDENED by EmbeddedResource paths (the ruled recipe rb :1073 copies only .cs/.csproj/README.md) |
| b12 | EVERY non-testdata EmbeddedResource payload's bytes must survive checkout. `.gitattributes` pins only `**/testdata/**`, and `core.autocrlf=true` on the i7 | `text: unspecified` for traceviewer's `static/**`, `embed/internal/embedtest/concurrency.txt` (read as "Concurrency is not parallelism.\n", embed_test.cs:79) and `crypto/internal/fips140test/acvp_capabilities.json`; the tip's `.gitattributes` has no pattern matching any of them | R-B6: `-text` pins derived from every tracked and emitted csproj. Proved by RE-CHECKOUT (rm + `git checkout HEAD --`, sha256 == GOROOT), with a control that the same read without the pin yields CR bytes |
| b13 | runtime package_info.cs `profBufReadMode` accessibility drift (×3 GOOS) | 09-22 L 14:49; `runtime/{windows:466,linux:481,darwin:537}/package_info.cs` | B8 §4 T3, named instance (ii) |
| b14 | runtime/pprof linux/darwin artifacts: 8c re-emitted windows only | L 04:03 | B8 B-only (scored) |
| b15 | Batch 3's measured, never-committed 20-file footprint, incl. **os/exec.cs shared -> per-GOOS** (C1's union seat was never cut) | 09-22 L 02:36, 02:37; `src/core/os/exec.cs:26` still shared | B8 B-only. **The emitter itself deletes the flat copy (`platformEmit.go:259-295`), and the overlay cannot carry a deletion (rb :1083-1096)**, hence R-B7 |
| b16 | Batches 4-8's converter footprints were never measured corpus-wide | 154 converter-touching commits in 54dec61728..d1a0d314d5; the batch-4 brief ran no two-seeded diff | B8's attribution table (count not predicted) |
| b17 | H5c deletion pass, predicted empty for a same-release regen | rb :574-680; step 2 :860-876 (every flavour, `-ExpectSourceGo` ALWAYS); (f) :913-914 (flat-row flavour agreement) | B8(6): three flavours, each rc-gated, extracted per flavour, flavour agreement checked before any `-Apply` |
| b18 | `go generate .` + TestStdLibMetadataInSync after the regen | rb :1098-1117; `.claude/rules/corpus.md:277-278` | B8(8) (B7's one named freeze exception), B9(a) |
| b19 | CNR after a corpus overlay (channel 2 moves behavioral emission) | rb :59-61 | B9(b) |
| b20 | stdlib build on every buildable flavour | rb H7 :1348-1390 and its 2026-09-16 amendment :1420-1442 (arm 4 inert; unique sites 0; ASM == projects; ASM 343 vs 344 produced); §6 :3396 | B9(c): all three flavours by the amended recipe, each arm on its own rc, both units named |
| b21 | GolibTests control vs head | house battery | B2 control (in `abclose-ctl`) + B9(d) |
| b22 | The full behavioral suite (the overlay moves core packages every behavioral project compiles against) | rb §6 (H9/parity); the base two FuncLiteralCallerNames / GoroutineWaitState (8b) | B2 control (in `abclose-ctl`: preflight builds never in the leg's tree, rb :2568) + B9(e); APPEARED empty |
| b23 | A `go2cs.slnx` build: owed after golib/runtime API changes, recorded in NO 09-22/23 ledger line | rb :3398; CLAUDE.md architecture note | B9(f) + check-solution-integrity.ps1 |
| b24 | Rows whose own production the overlay moves owe a re-run at the merge result | validation-bank SKILL :33; train-assembly SKILL :211 | B9(g), derived set, capped by R-B8. hash/maphash sweep red -> green (the control's red is UNVERIFIED). crypto/tls, if moved, per R-B10 (c18) |
| b25 | THE FIGURES: header + README NEWS as the guard derives them; BOTH numbers against the 1.23.12 anchor (they move in opposite directions) | rb :2239-2242, :2777-2783; `docs/NEWS.md:13-16` (204/215 = 94.9%, 97.6% of 209, linux 198/202) | B10. Predicted 218/230 = 94.8%, 56,974 / 283; 218/224 = 97.3%; linux 187/216, 49,629 / 163 |
| b26 | The closing identities restated as a DATED successor (the 2026-09-22 block still reads 216 + 8 + 6 and "lands in batch 7"); the csproj census as a NAMED identity | roster:896-960 (:938, :946-957); 09-22 L 16:31 | B4(b): 218 + 6 + 6 = 230; 225 = 218 + 4 + 3 |
| b27 | The index regenerated ONCE after the last roster edit | rb :2797-2800 | B11: 218 = 218; 232 pages |
| b28 | Close-by-name census: every row's FIRST page at 1.24.13 (2f/2g "older" counts are printed but never gated) | rb :2244-2255; `check-roster-format.ps1:1713`, `:1835` | B12.1: 218/218, 0 older in both arms, with a planted 1.23.12 first page named |
| b29 | The linux leg complete: every numeric annotation read at 1.24.13 | rb :2541-2549; 09-22 L 16:32; L 12:10 | B12.2: residual set EMPTY (sources incl. b57703b1cd), with a planted wrong annotation named |
| b30 | The release census as named identities (tests.csproj / pages / badges / index), correcting rb :2257-2259's "must equal the roster's row count" | rb :2257-2259; 09-22 L 16:31 (the equality REFUTED as by design; restate at the close) | B12.4 quotes both: 225 / 232 / 214 / 218 (tip 225 / 233 / 215 / 219); 2b3 scored after b32's restore |
| b31 | Deadline floors hold at close (runtime owes one only when it banks) | 09-22 L 18:02 | B12.5 |
| b32 | **The regen re-greens fips140deps's README**: an E4 exclusion row with a committed tests.csproj AND a current/ page (1 matched · 0 disclosed), whose badge is orange BY HAND | `readmeValidationBadge.go:436-446` (both signals => GREEN, both arms); 8f6abad087 ("restore fips140deps's badge"); 09-22 L 16:31 | B8(5) named class (iv), predicted present; R-B12 default: restore the committed README after the overlay (the 8b precedent). Unrestored, it reads T5 or reds 2b3 and makes badges 215 |
| b33 | A `-tests` run may REMOVE a generated test artifact it no longer emits | 09-22 L 04:14 (i9 s1: `crypto/cipher/package_info_internal_test.cs`, "a measured deletion, admitted"); net/http carries three generated files with no GOROOT `_test.go` (`package_info_internal_test.cs`, `package_init_external_test.cs`, `package_init_internal_test.cs`) | B3 admits a measured removal by name in commit 2; B12.7 lists it. Part A's (0) takes the same rule for crypto/tls |

---

## (c) COORD-owned

| # | Obligation | Evidence | Note |
|:--|:--|:--|:--|
| c1 | Fill **R-A1** and **R-A2** in Part A's prompt; fill **R-B1..R-B12** and name `<B_BASE>`, `<REGEN_BASE>` in Part B's prompt | this brief | Each part STOPS on a missing ruling |
| c2 | STAMP 8g and STAMP the close: announce, then push (floor 9); ledger STAMP lines with the time read from `date`; record refresh | house protocol; CLAUDE.md floor 9 | two stamps |
| c3 | **CLOSE PRECONDITION. The B5 BOARD finding**, OVERDUE since 8d's landing. It covers: X(3) fips140test's six structural pins reversed to deferred (COORD's own 00:43 and 11:24 acceptances); O5 crypto/rsa's user-confirmed 2026-08-10 ratification OVERTURNED (BOARD:5776-5782, DESIGN-zh-box-reduction.md:82-83); O1's sha256/sha512 shell end state as X(4)'s explicit exception, and net/http/internal's chunked attribution superseded; X(5) `reading` only presence-checked until REC-F; O4 golib append's size-class posture "to the BOARD for the owner"; the memmove/unsafe.Pointer GC-safety hole that "joins the syscall-buffer-pin family on the BOARD" (09-22 L 05:14) | L 03:37 (X, O1, O4, O5); L 04:43 item 6 ("the BOARD block comes with COORD's H10-close finding"); record RESUME-SESSIONS.md:552 ("COORD owes ONE BOARD finding (B5) at 8d's landing"); `grep -c 2026-09-23` over the BOARD = 0 at d1a0d314d5 and master; its last commit is 6e082a09c5 (2026-09-20) | Gates the close STAMP. Carried as a COORD docs ref in B_BASE/B1, or named in Part B's prompt with its landing ref; B12.10 checks it. Reader-reported and UNVERIFIED by this pass: the P256 move 8,528 -> 16,149/run |
| c4 | **CLOSE PRECONDITION. database/sql TestRawBytesAllocs attribution**: "owed before the close, and a BOARD finding if it stays unexplained" (15 -> 28, a MOVE AWAY) | `src/core/database/sql/go2cs_test_disclosures.json:17`@tip; O5 (L 03:37) | ATTRIBUTION FIRST (e.g. a C1 static read of the counted sites). The BOARD finding (folded into c3) is the fallback ONLY if the attribution fails, never an alternative. B12.10 checks it |
| c5 | **CLOSE PRECONDITION. Runbook in-stage amendment for the close**: the runbook LEADS, and its H10 section has NO close checklist | `rb :2190-2930` (no final reconvert, core-refs sweep, demotion mechanics or basis row); CLAUDE.md "runbooks lead, amended in-stage" | Content: the demotion mechanics (row -> dated candidate note; page removed; badge orange; basis row); the final regen as a same-release two-seeded REGEN classified by §4; the core-refs sweep; the overlay widened by EmbeddedResource payloads + the `-text` pins; the emitter's flat->per-GOOS deletions; `-tests`-run generated-artifact deletions (b33); a hand-set badge line under a regen (b32); rb :2257-2259's equality corrected to the named identity (09-22 L 16:31); the header-in-lane-ref exception (c17); §6's "seeded full reconvert once per phase" vs this ledger-ruled close regen (rb :3400). A C2/COORD docs seat (the c3cb3b310 precedent). Gates the close STAMP; B12.10 checks it |
| c6 | crypto/cipher TestGCMAsm `[linux, darwin]` pin now absorbs nothing (linux reads `27272`, nothing disclosed; darwin has no run layer); roster:239 still says "the 1.23.12-era `linux: 13 + 1` annotation still claims it" | 09-22 L 16:22 (scoped because the old linux annotation claimed it); C1's orphan census said RETIRE (09-22 L 15:59); L 08:51 | Retire it (a C1/G manifest seat, the 6c35ea9b3c precedent) or keep it by ruling. Guard 2b is a ceiling and stays silent |
| c7 | The 29 rows with no linux annotation: 27 have a measured 1.24.13 linux PASS on record, but the ruled scope edits only existing annotations. os failed to start on the host; net/http is demoted | 971d919113 message; 09-22 L 16:32 ("banking only the annotations") | A COORD decision, none taken. It could lift the linux numerator by up to 27. Record the choice at the close |
| c8 | Linux-scoped pins' absorption was never censused at 1.24.13: syscall 19 pins vs `45 + 11`; runtime/debug 6 vs `4 + 5`; os/exec 2 vs `87 + 1`. The 1.23.12 annotations were ROOT readings and this leg ran unprivileged | L 10:13 (the privilege note); C1's census was windows-only (09-22 L 15:59) | Reader-reported, **UNVERIFIED** here. Candidate orphans for a later re-sign; not a close item unless ruled |
| c9 | os has no linux reading at 1.24.13 (run 1 914/914 fail; run 2 `empty path in RemoveDirectoryRecursive`), and the ledger carries no ruling | 6836e605ac rows.tsv | Name it, or route it |
| c10 | Master -> version: master holds 5 first-parent commits absent from the tip (census converted mode + fixture mirror, public-handle admits, the index tool (blob already equal), unc-escape revert, docs-records.md) | `git log --first-parent d1a0d314d5..origin/master` = 20eb0af70e, 998fc49d03, facb693040, dd18e5e2ab, be8e137af5 | A fold or the cutover merge must carry them. Unruled whether this happens before the close |
| c11 | testing README's Docs and Go-Source badges still @1.23.12, the only one of 321 core READMEs (hand-owned host; the converter emits none) | `src/core/testing/README.md`@tip; L 04:03, 07:12 | H12 by rule (rb :2955-2957); cheap to hand-fix at the close if COORD wants it |
| c12 | The owner's hands: NuGet 1.24.13.1 (a publish is always the owner's PIN); deprecate the 14 removed IDs on nuget.org; the go.* prefix follow-up | 09-22 L 19:06 (a RECOMMENDATION, not a ruling), L 19:24 | Relay through the status board (owner-hands-through-COORD) |
| c13 | **PRE-8g-STAMP. The raised-wall pin's HOME.** 09-22 L 21:55 requires the pin to be carried "elsewhere (page + registry)". The registry half is post-hop (L 11:59 -> d12). The PAGE records no wall (0 hits for `GOFLAGS`/`40m`/`timeout`), and the row reads "raised to 40m on both sides (GOFLAGS)" (the literal `GOFLAGS=-timeout=40m` occurs nowhere) | 09-22 L 21:55; L 11:59; 3ec2c9ff39 roster :273 and page | A COORD RULING before the 8g STAMP (checkpoint item 2): the roster prose carries the pin at the close, and the page half joins d12. Part A (d)7 reports the greps (`GOFLAGS`, `40m`, `213` separately); a report alone does not discharge it |
| c14 | **PRE-8g-STAMP. The page's provenance SHA** `4c5b0a3b7` is not an object on origin (R's pre-rebase local tree, "measured at fc6269b0bf + the notes commit, rebased onto 8f", L 12:36); 3ec2c9ff39 carries no bank evidence (no rows.tsv, no comparison record) | page line `*Validated 2026-09-23 · converter `4c5b0a3b7`*`; `git cat-file -e` fails after fetch; every other checked page names a resolvable commit (net 64128bb0b, x509/maphash f9a4b088f, sync/atomic c6fdbe73c, net/http 8f82b3f63) | R reports `git rev-parse 4c5b0a3b7^{tree}` (== `4a960d31a3^{tree}` 2827307647 resolves it) or pushes the measured tree or an evidence ref; otherwise COORD rules and records the unresolvable SHA (checkpoint item 1) |
| c15 | **The host-rule DISPATCH for net/http** before Part B's prompt is cut | rb :2541-2549; b5 | Checkpoint item 5: dispatch G's re-read at the 8g STAMP (`<G_NETHTTP_LINUX>`), or rule the 971d919113 seat-tree read sufficient |
| c16 | **srcimporter's ORACLE read may be the cgo state, not the host** | the leg's oracle read ran cgo OFF (g-linux-leg README line 5; L 06:18; `run-validated-sweep.ps1:159-161`); the 1.23.12 annotation was derived cgo-ON, and the sweep's comment says a session-wide zero "would bring them back short" (`:1018-1021`); srcimporter reaches `go tool cgo` only for packages go/build gives `CgoFiles` (`srcimporter.go:130-141`) | R-B11 before B5 lands (checkpoint item 7): demote with the ON -> OFF change named (default), or order a cgo-ON oracle read first |
| c17 | Record the **header-in-lane-ref exception** COORD's 12:36 acceptance implies (R's ref carries the roster header :137 and the README NEWS; rb :2777-2783 says a lane ref never does) | L 12:36 ("header + NEWS 57,203 -> 58,319 as the guard derives them"); 2b65f11474 and b57703b1cd carry the header linux line the same way | In the 8g STAMP line; folded into c5's amendment. Part A overwrites all carried header hunks with the guard's output |
| c18 | **crypto/tls at the close regen**: if commit 6 moves crypto/tls's non-test files, the row owes its sweep as the seat's own gate, and the i7 cannot run it | train-assembly SKILL :211; the 12:36 "no re-run" covers only 8g's re-emission | R-B10: R runs the raised-wall row at the close head before the close STAMP (default), or a named deferral to the §6 sweep |

---

## (d) Post-hop: stamp each on <CLOSE_DATE>

Every row: **stamp = <CLOSE_DATE>** (the H10-close STAMP's `date`; predicted 2026-09-24, UNVERIFIED). "Due" is the trigger or gate that falls due.

| # | Obligation | Evidence | Due |
|:--|:--|:--|:--|
| d1 | **H11**: published version = pin + counter (version.props 1.24.13 / GoBuildNumber 0, so next is 1.24.13.1). TestPublishedCounterMatchesTheRecordedReleases runs by script BEFORE the first publish (numeric per component). The 51 new packable IDs publish with real code (all 404 today). The 14 removed IDs are DEPRECATED with a pointer to 1.23.12.3, never unlisted (an owner step). The publish ritual writes the stamp | rb :2931-2951; `version.props:23-24`@tip; 09-22 L 19:06; 8fa5cc2e7d (guard landed 2026-09-19) | Before the first 1.24.13 publish. COORD's research recommends right after the close (a recommendation, not an owner ruling) |
| d2 | **The 21 dangling Tests-badge links**: green badges point at `validation/1.23.12.3/<id>.html` for pages absent from that write-once snapshot (new or relocated at 1.24) | reader measurement at d1a0d314d5 (215 targets, 21 absent), UNVERIFIED by this pass; `readmeValidationBadge.go:447-452` | Resolve at the first 1.24.13 publish (the snapshot plus the H2-following retarget) |
| d3 | **H12**: every validation badge moves as the H12 table says (H1-following now; H2-following at the first publish; state the expected diff size first); hand-owned READMEs re-derived as a control; GOROOT-vendored golang.org/x/* re-pinned; the Go version in prose (the migrate-gorelease.ps1 doc-statement census names the sites) | rb :2953-2990 | H12, after H11 |
| d4 | **H12 prose routed by the ledger**: roster prose and class definitions (roster :36-42; :4 "Go 1.23.12 _test.go suite"; :71 "3,242 cases"; :172 "the Windows record for the Go 1.23.12 era"; :122's performance-margin "Founding row: net/http" now names a candidate); the NEWS headline and framing still say "the Go 1.23.12 anchor" (`docs/README.md:12`, `:34`); Reference:21526 "8 entries" -> 5; the 3,243 comments and manifest `hostConditional` prose; the badge census as named identities at the rehearsal | 09-22 L 16:31; L 03:37 X(8); L 04:43 item 7 | H12 |
| d5 | **The release-ritual rehearsal**, five elements in order: the announcement text before the tag; the pre-pack signed tag; the write-once snapshot; BOTH badge retargets; the recomputed re-verification. Signing is single-machine (the owner's PIN) | rb :2991-3012 | At the parity gate (rb §5) |
| d6 | **§5 PARITY GATES** (compile / roster / behavioral / hand-own audit / ritual) and **§6's ONE full validated-roster sweep**. The sweep is coordinator-owned, backgrounded, on the fastest available machine, and never on the i9 while its CPU is defective. It includes crypto/tls's LIVE steady-state absorption with BlockSize 3419 (deferred by name from 8g) | rb :3372-3386, :3390-3407; 09-22 L 18:59, L 21:33 (the i9 rule) | Before master cuts over |
| d7 | **The H6 hand-own audit**, including refreshing the `*.cs.auto` twins (never overlaid; "the reconvert's auto-pair step owns them") and any B8 §4 T4 hunks | rb §5 row 4; 09-22 L 04:36; corpus-reconvert SKILL :94 | The parity gate |
| d8 | **The post-hop train**: R's synctest arc S1a..S4 + the S-host seat; net/http RE-ENTRY carrying its `execution: release-tiered` pin; internal/synctest; the S2 select-path handoff gap (untested); C2's atomic acquire loads 8ef7f65ec8 | L 04:20, 06:10, 08:14, 11:33 (all "POST-HOP train, never the version branch"); 09-22 L 17:33 | First post-hop train, after the master cutover |
| d9 | REC-G IMPLEMENTATION (ChanCore single-object), after the synctest arc's train (same Waiter/ChanCore paths) | L 11:43 | After d8 |
| d10 | Owner-ruled FIRST UP post-hop: string-literal §8 arm C; arms A/B held for an invisible alternative; the hoisted-literal comment off by default + a comment-verbosity CLI parameter (C2 + C1's §8 record) | L 10:10 items 2-3 | First post-hop work |
| d11 | The O3 REC-F instrument seat (the COUNT-arm integer quotient with a RESIDUE GUARD, per-call unit lines, full sweep), then bytes TestGrow's removal ordered against linux | L 03:37 O3 | First post-hop instrument seat |
| d12 | A standing ENVIRONMENT execution pin for crypto/tls's raised wall (GOFLAGS on both sides): the REGISTRY half, and (per c13's ruling) the PAGE half, so the proof page records the wall it was measured under | L 11:59 ("a post-hop instrument seat"); 09-22 L 21:55 ("page + registry") | Post-hop instrument seat |
| d13 | The relabel's relative-dated items: encoding/binary x5 and net TestAllocs attributions; crypto/ed25519 and math/big decompositions; context B′ admission read (G); log/slog's want-0 BYTES meter question (a BOARD question); unicode/utf8's floor re-examined at REC-B's acceptance; REC-A..F full designs at the phase-4D kickoff | L 03:37 (X(4), O2, O4, O5); the manifests at d1a0d314d5 | "post-hop" = on or after <CLOSE_DATE>; phase-4D kickoff |
| d14 | The deferred alloc class: each entry re-measured at every sweep until retired (owner-delegated ruling 2026-09-05); COORD compares `reading` by hand until REC-F's comparator lands | L 03:37 X(5) | Every sweep read |
| d15 | The gofmt pass on the version branch (18 unclean files in src/go2cs; it touches converter source, so suite + CNR follow) | 09-22 L 08:37 ("once the converter seats stop landing, never mid-campaign") | After the close, before the cutover merge |
| d16 | G's Store8 fix (the whole 8-bit atomic family on a byte CompareExchange loop, retiring the latch) | 09-22 L 16:19 ("BANKED (post-H10)") | Post-hop |
| d17 | The Caller(k) NoInlining-closure census (could retire net/http from the release-tiered opt-out) | 09-22 L 16:17 item b | Post-hop |
| d18 | TestIteratorPush: golib's range-over-func adapter runs `seq` on a pool thread, not a goroutine (rebuilding it on Coro touches every range-over-func) | L 11:33 | A post-hop design seat |
| d19 | runtime, reflect, runtime/pprof, net/http/pprof and internal/synctest stay candidates. runtime owes a deadline floor the day it banks | 09-22 L 18:02; roster :946-957 | When each banks |
| d20 | Owner decisions after the hop: the Darwin run-layer go/no-go; the i9 CPU replacement (warranty) | memory record: darwin option 2's go/no-go is the owner's after the 1.24 hop; the i9 note | Owner |
| d21 | **crypto/tls's linux FOURTH STATE attributed**: why loopback refuses Go's own BoGo shim on the WSL arm (769 of 3,418 cases, `bogo_shim_test.go:484`) | L 12:56 ("the attribution is a POST-HOP item for G"); b57703b1cd's message | Post-hop, G |
| d22 | The guard's 'ledger scope' arm is HOST-DEPENDENT (red on R's box under PS 5.1 at 8f and 3ec2c9ff39; green on G's and on the i7 under pwsh 7) | L 12:36, L 12:56 ("NOTED") | Noted, unrouted: a guard seat or a ruled shell requirement |

---

## New findings of this derivation (already folded in above)

These are the items no earlier reader stated, or stated only as unruled.

1. **b6:** shardmap reds on the demotion, by a code read. A basis row fixes it, with a red-first arm built in.
2. **b11 / b12:** the ruled overlay recipe misses `//go:embed` payloads, and no non-testdata EmbeddedResource payload carries a `-text` pin, so autocrlf would change the embedded bytes. The class is three payload sets, not one.
3. **b15:** the emitter's own flat-to-per-GOOS removal of os/exec.cs is a deletion the overlay cannot carry, and it would otherwise leave duplicate declarations.
4. **b3:** removing the page and hand-setting the orange badge before seeding makes the regen's README emission a scored prediction rather than a risk.
5. **R-B5:** the two-seeded base must be 54dec61728. A later base misfiles never-applied converter footprints (for example maphash) as committed drift that neither converter reproduces.

## Corrections after the adversarial pass (2026-09-23 13:25)

6. **a1 / a11 / a12 / a13 (L 12:36, 12:56):** the earlier cut read the ledger through 12:10 only. R's ref is `claude/r-tls-bank` 3ec2c9ff39 on d1a0d314d5, not a fc6269b0bf-based cut. It carries the header and NEWS and no test artifacts. The 8g battery owes the convert-only re-emission. G's linux annotation (b57703b1cd) and the 12:56 sentence ride 8g, so the "named transient" never exists.
7. **b4:** the owner's "7 aggregation parents" include TestRegisterErr. The synctest class is 11 + 6 = 17, and 19 with the pair.
8. **b7 / c16:** the oracle read is recorded cgo OFF. "Not recorded" was wrong.
9. **b32:** a deliberate regen re-greens fips140deps's hand-restored badge.
10. **b33:** a `-tests` run may delete a stale generated test artifact.
11. **c13 / c14:** the raised-wall pin's page half has no owner, and the page's provenance SHA does not resolve on origin. Both are PRE-8g-STAMP COORD items.
12. **c3 / c4 / c5:** these are close preconditions, not optional riders. c4 is attribution first, with the BOARD finding only as the fallback.
