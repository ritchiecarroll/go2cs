You are an i7 sub-agent for the go2cs fleet coordinator. APPLY BATCH 6: the seat refs listed below merged in order onto the version branch in a throwaway worktree, ONE battery, report. **DO NOT PUSH.** Read `CLAUDE.md` first (floors 3, 7, 8, 14). The conflict rules of the batch-4/5 briefs apply (a `go2cs-src.projitems` adjacent insert is a UNION asserted by `TestProjitemsRegistersEveryGoSource`; the roster's different-rows conflicts resolve to both rows; anything else is a STOP with hunks quoted).

## The refs (the coordinator fills SHAs marked TBD before launch; assert each by `git ls-remote origin`; STOP on any mismatch)
1. `claude/c2-anon-struct-accessibility` = `TBD` (C2; the synthesized anonymous-struct type's accessibility follows its least-accessible field type -- time/abs_test.go's table over `Action<testingT>`; Go side, or the generator half written UNCOMPILED by C2 -- C2 states which; on a gen red read its test file first, as batch 4 taught). Rows: `time` by BUILD (`-tests` convert + `dotnet build time.tests.csproj`, 0 errors -- was CS0052/CS0050/CS0051 at c6fdbe73c3) then `-test-action all` at the sweep's time floor: the row's word by cause (a RESERVED row; predicted PASS or DIVERGED by cause -- report the first classes).
2. `claude/c1-train-guard-tables` = `TBD` (C1; the three converter-suite guards the re-bank train's test artifacts moved -- TestDeclaredNotImplementedCensus, TestValueCloneStampMembersAreDeclared, TestCommittedCoreReferencesResolve -- their tables updated, red-first). Gate (a) must read the converter suite FULLY GREEN after this ref (the batch-4/5 batteries carried these three as pre-existing reds); if any test is still red, STOP on it.
3. `claude/c1-alloc-pins-fips140test-sha3` = `TBD` (C1; two hand-owned manifests: crypto/internal/fips140test's SIX entries (Edwards25519 + NISTEC P224/P256/P384/P521 structural, XAES structural) and crypto/sha3's TestAllocations family; the roster cells fips140test Disclosed 7 and sha3 Disclosed 4-or-5). ALLOWED paths: the two manifests, `docs/ValidatedTestPackages.md` (row cells). Rows: crypto/internal/fips140test `-test-timeout 20m` -- predicted VALIDATED 2267 with 7 disclosed (TestACVP + TestCAST* PASS); crypto/sha3 -- predicted VALIDATED 23 with the family disclosed.
4. `claude/r-runtime-getfp-body` = `TBD` (R; runtime's getfp companion body by the memmove precedent; GolibTests arms). ALLOWED paths: `src/core/runtime/**` companion files, `src/core/golib/**` tests. Rows: runtime by `-tests -test-action all` at its floor -- predicted: the host past TestPreemptionAfterSyscall; the next death site or a completion by cause (report the counts: Go / C# / diverged / absent and the first divergence classes -- i9's read at batch 5's base: 34 'building testprog', nil derefs, syscall token-argument panics, asmcgocall, AllocsPerRun).
TARGET: `claude/version-go1.24.13` at `TIP6` (the pass-2 STAMP, or batch 5's if pass 2 has not landed -- the coordinator states which; assert by ls-remote).

## Setup, merges, battery, report
As the batch-5 brief: worktree `/c/go2cs-tmp-coord/ab6` detached at TIP6, lock + PID, scratch `<scratchpad>/ab6/`, pins read back by output (GOROOT backslash; pwsh net8 apphost with the dotnet10 root off the parent's PATH), sibling check, disk >= 25 GB; merges `--no-ff -S` in order with the additive blob proof and the `src/core` path list against the allowed sets; battery (a) the FULL converter suite GREEN (see ref 2), (b) gen 0 errors + GenTests + GolibTests count-matched, (c) CNR `NO REGRESSION` with CHANGED listed, (d) stdlib 344 / 0, (e) the rows above (output dir the SECOND positional inside the ab6 seed; results tails first; artifacts cleared before each) + `src/check-roster-format.ps1` from `src/` (the failure set quoted; with the fips140test pins in, the page-less set should be EMPTY if pass 2 landed the page, else fips140test alone), (f) census (master's tool, `entry` on changed files and messages). Restore side effects; status empty; `^ D` empty. Worktree IN PLACE at `/c/go2cs-tmp-coord/ab6`. NOTHING PUSHED.
