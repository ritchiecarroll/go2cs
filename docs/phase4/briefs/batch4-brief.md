You are an i7 sub-agent for the go2cs fleet coordinator. APPLY BATCH 4: SIX seat refs merged in order onto the version branch (the CAMPAIGN TIP) in a throwaway worktree, ONE battery, report. **DO NOT PUSH.** Read `CLAUDE.md` first (floor 3: the output directory is the SECOND positional; floor 7: capture every rc before any pipe; floor 8: never `git add -A`; floor 14: read the results tail).

## The refs (assert each by `git ls-remote origin`; if any differs, STOP). All were cut on `0adf2e4318`; the target is the campaign tip `CTIP` (master merged in), so every merge composes with master's docs/tool changes -- a conflict in `docs/**` is reported with hunks and left in progress; a conflict in `src/**` is a STOP (`git merge --abort`, hunks quoted), EXCEPT the one ruled class: a `src/go2cs/go2cs-src.projitems` adjacent-insert (two `<None Include=...>` registrations at one anchor) resolves to the UNION, asserted by `TestProjitemsRegistersEveryGoSource` after the merge.
1. `claude/c2-sha3-production-facet` = `d2b6b44ed1` (THREE commits -- the KEY seat, the FACET seat, and the 2026-09-22 fixture fix (Replace-substituted placeholders, not string.Format over C# braces); GenTests' AdapterPassSeparationTests must read 4/4 -- the RE-RUN's measurement: the KEY seat `f4b6e2d3d2` -- adapterStructKey keys a local-package alias qualifier bare -- and the FACET seat on top: `GoImplement<S, I>(Pointer = true, Production = true)` stamped on the seeded production pointer records in package_test_info.cs, rules 1-4 in adapterProductionNameKeepers (converter) and KeepsProductionAdapterName (generator); 11 + 4 files; ImplementGenerator.cs, golib GoImplementAttribute.cs and GenTests/AdapterPassSeparationTests.cs UNCOMPILED by the lane -- on any red in gate (b) read AdapterPassSeparationTests.cs first). Row: crypto/sha3 predicted VALIDATED (was CS0246/CS0426 x4).
2. `claude/r-escapeforhash-intrinsic` = `5291f72c8e` (compilerIntrinsic.go + arms, visitFuncDecl.go the one site, projitems; a body that is exactly one call of the unshadowed universe `panic("intrinsic")` emits the zero-value return). Row: hash/maphash VALIDATED 59 (gated end to end on R-LAPTOP with ref 5 of batch 3 -- now in CTIP -- applied); `-test-timeout 60m` (the sweep's floor; the default deadline-kills it).
3. `claude/r-fips140-check-handown` = `b08fbbc3b4` (src/core/crypto/internal/fips140/check/check_impl.cs NEW [GoInit] init in the nistec p256_impl.cs shape -- Verified = true, the linker-layout WHY in the header; check.cs re-emitted; package_info.cs; manualTypeOperations.go registration; stdlib-metadata.txt regenerated). ALLOWED src/core paths: `src/core/crypto/internal/fips140/check/**` only from this ref.
4. `claude/r-println-lf` = `b574ac27c2` (golib: every site standing in for a Go newline write emits a bare "\n" regardless of OS -- builtin print/println at builtin.cs:2281 and the class R censused; R gated GolibTests + the behavioral suite + the fips140test row). ALLOWED src/core paths: `src/core/golib/**` from this ref.
5. `claude/c2-embed-directive` = `34d8a5be0b` (THREE commits: the seat + two 2026-09-22 fixes -- embed_impl.cs reinterprets by Unsafe.BitCast after a size-1 assert, and the initializer names go.embed_package fully qualified for named AND blank imports (the fips140test CS0103 regression); embedDirective.go + arms; visitValueSpec.go, projectFileWriter.go, testConversion.go, conversionDriver.go, projitems; //go:embed resolved at conversion time: payloads STAGED into the output package dir, `<EmbeddedResource>` with LogicalName `go.embed/<import-path>[_test]/<path>`, a static field initializer; src/core/embed/embed_impl.cs NEW, UNCOMPILED by the lane -- the constructor that sets FS.files). ALLOWED src/core paths: `src/core/embed/embed_impl.cs` from this ref.
6. `claude/c1-per-unit-alias-renames` = `be49d3c133` (importAliasOperations.go: includeSiblingClosure on computeImportAliasRenames -- false at the production pass conversionDriver.go:445 and the auto-sibling pass autoSiblingOperations.go:68, true at convertTestVariant testConversion.go:2774; two guards, TestProductionUnitExcludesSiblingClosure red-first; a -tests run no longer rewrites production .cs; 9 files). Merge LAST. COMPOSES with refs 1 and 5 on testConversion.go (different hunks expected; a conflict is a STOP). Rows: the tests csproj COMPILES for regexp and strings (`-tests` convert + `dotnet build <pkg>.tests.csproj`, 0 errors -- the test unit still builds with Δio), and the three testProjectRecompile packages' test compiles net/netip, text/tabwriter, crypto/ecdh (production sources compiled into the test assembly; C1 A/B'd their production emission byte-identical -- assert it again here: convert each under -tests into the seed and `git status --porcelain -- src/core/<pkg>/*.cs` names NO production .cs); gate (c) CNR reads the three-target footprint.
RE-RUN NOTE (2026-09-22): the first battery on the pre-fix tips ran green on (a) suite, (c) CNR, (d) stdlib, (f) census and read maphash 59, sha3 COMPILES (5 alloc residuals), embedtest 2/5 infra, fips140test CS0103; this re-run measures the three fixes: GenTests 61/61 with the facet arms PASS, embedtest Validated 7, fips140test COMPILES -> DIVERGED 7 with TestACVP PASS. The seat-5 testConversion.go adjacent insert resolves as a UNION (ours then theirs) by the standing ruling.
TARGET: `claude/version-go1.24.13` at `CTIP` (assert by ls-remote; the ledger's last STAMP).

## Setup
`cd /c/Projects/go2cs && git fetch origin claude/version-go1.24.13 claude/c2-sha3-production-facet claude/r-escapeforhash-intrinsic claude/r-fips140-check-handown claude/r-println-lf claude/c2-embed-directive claude/c1-per-unit-alias-renames && git worktree add --detach /c/go2cs-tmp-coord/ab4 CTIP` (assert the path does not exist first). Lock `mkdir /c/go2cs-tmp-coord/ab4.lock` (refuse if present), PID into `ab4.lock/pid`, removed at the end. Scratch `<this session's scratchpad>/ab4/`. Shells: build `export DOTNET_ROOT=$HOME/dotnet10; export PATH="$HOME/dotnet10:$PATH"` (assert dotnet 10); converter `export GOROOT='<the go1.24.13 SDK in backslash spelling>'`, `export PATH="$HOME/sdk/go1.24.13/bin:$PATH"`, `GOTOOLCHAIN=local`, `CGO_ENABLED=0`; assert `go version` = go1.24.13 and `go env GOROOT` = the pin. Disk >= 25 GB before each heavy gate. Sibling check: `Get-CimInstance Win32_Process | Where-Object { $_.Name -match '^(dotnet|MSBuild|VBCSCompiler|csc|go2cs|go)\.exe$' -and -not ($_.CommandLine -match 'pwsh\.dll') }` -- heavy > 0 waits in 120 s loops (cap 40 min); you kill nothing. NOTE: the campaign's W=4 workers run on OTHER boxes; the i7's own campaign slice, if running, IS a heavy sibling -- wait for it.

## The merges, in order 1, 2, 3, 4, 5, 6
Each `git merge --no-ff -S <sha>` with a message in the repository's style (`git log -3 --format=%B` on the version branch), ending `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; nicknames only. After each: `%G?` G or U; parents ours-then-theirs; every file the ref touched since its merge base blob-EQUAL to the ref's tip blob (COMPOSED files named with the other side's hunks classified); no `<<<<<<<`/`>>>>>>>`/`__go2cs_lift` markers; `git diff --diff-filter=D --name-only CTIP HEAD` EMPTY; the `src/core` path list of the whole batch = exactly the three allowed sets above (report the list).

## The battery
(a) `cd src/go2cs && go build -o go2cs.exe . && go vet ./... && go test -timeout 90m ./...` every package ok (repoguard `-count=1`); the metadata guard (TestStdLibMetadataInSync) must pass -- ref 3 regenerated stdlib-metadata.txt.
(b) `dotnet build src/gen/go2cs-gen` 0 errors; GenTests and GolibTests as the repository runs them (quote the invocations from `.claude/rules/golib-gen.md`): pass counts verbatim; AdapterPassSeparationTests' 4 arms named PASS/FAIL.
(c) `check-no-regression.ps1` under pwsh with the pins: the final line `NO REGRESSION`; CHANGED packages listed verbatim (ref 4's println could move goldens whose expected output was CRLF-dependent -- R's scan named none; ref 5 could add EmbeddedResource items to behavioral projects that embed -- name each).
(d) `dotnet build src/go2cs-stdlib.slnx -c Debug`: 344 assemblies, 0 errors (embed_impl.cs and check_impl.cs compile inside it); `git status --porcelain -- src/core` empty after.
(e) THE ROWS, each `-tests -test-action all`, the output dir the SECOND positional inside the ab4 worktree's src/core: (1) crypto/sha3 -- predicted VALIDATED, report the line; (2) hash/maphash `-test-timeout 60m` -- predicted Validated 59 tests (1 skipped identically, 3 excluded); (3) embed/internal/embedtest -- predicted Validated 7; (4) crypto/internal/fips140test `-test-timeout 20m` -- predicted DIVERGED 7 (TestXAESAllocations, TestEdwards25519Allocations, TestNISTECAllocations x4 + parent) with TestCASTPasses/Failures/Conditionals and TestACVP PASS; its manifest is hand-owned and the six pins are AUTHORED, not minted -- if the pipeline reports the 7 as undisclosed, report the seven names (the pins are a separate seat); (5) crypto/ecdh by BUILD (the sha3 pair's in-set negative control; unchanged); (6) compress/flate by BUILD (the embed seat's control); (7) ref 6's rows: regexp and strings tests-csproj compiles, and netip / tabwriter / ecdh test compiles with the no-production-.cs assert. Read each row's results tail before quoting counts. Restore every pipeline side-effect INSIDE the worktree: `git checkout -- .` and `git clean -fdq -- src/core docs/validation` (list removals); `git status --porcelain` empty; `grep '^ D'` empty.
(f) Census: `bash .claude/coord-scripts/coord-identifier-census.sh entry` on every changed file and on every merge message; `selftest` 117/0.

## Report (final message)
CTIP asserted; each merge SHA, `%G?`, parents; EQUAL/COMPOSED counts with names; the deletion count; the `src/core` path list; every gate's figures verbatim; each row's line; census; the worktree IN PLACE at `/c/go2cs-tmp-coord/ab4` (the coordinator pushes from it); disk free; the lock released; anything not verified. NOTHING PUSHED.
