You are an i7 sub-agent for the go2cs fleet coordinator. APPLY BATCH 8a -- two C1 refs merged onto the version branch in a throwaway worktree, ONE battery, report. **DO NOT PUSH.** Read `C:\Projects\go2cs\CLAUDE.md` first (floors 1, 4, 7, 8, 13, 14, 16), and `.claude/rules/harness-gates.md` when you touch src/tests. Never modify the main checkout `C:\Projects\go2cs` or any other worktree under /c/go2cs-tmp-coord.

## Fixed facts (assert each by `git ls-remote origin`; STOP on any mismatch)
- BASE: `claude/version-go1.24.13` at `3469154a95` (the batch-7 STAMP).
- REF 1: `claude/c1-own-page-links` = `0e98bc97c` (C1; one commit on 8fc439415f; docs/ValidatedTestPackages.md ONLY, +8/-8): the eight inheritance-anchor rows (crypto/internal/fips140/{bigmod,edwards25519,edwards25519/field,mlkem}, internal/runtime/{math,sys}, internal/sync, weak) link their OWN 1.24.13 page FIRST with the 1.23.12 anchor kept. ALLOWED: that file. The batch-7 tip changed OTHER roster rows and the header; a conflict that is not different-rows is a STOP with the hunk quoted.
- REF 2: `claude/c1-internal-sync-hooks` = `cceba4e728` (C1; two commits on 8fc439415f: `865fda89ad` = the arm ALONE, `cceba4e728` = the bodies). internal/sync's five runtime hooks (canSpin, doSpin, nanotime, SemacquireMutex, Semrelease) were PartialStubGenerator throws; the bodies live in src/core/internal/sync/runtime_impl.cs beside throw/fatal; src/go2cs/declaredNotImplemented_test.go drops the five latent rows by its own same-commit rule; src/tests/GolibTests/InternalSyncMutexContentionTests.cs + one ProjectReference in GolibTests.csproj. Both C# files were written UNCOMPILED on C1's box. ALLOWED: exactly those four paths.
- Worktree: `git -C C:\Projects\go2cs worktree add --detach /c/go2cs-tmp-coord/ab8 3469154a95`; lock `mkdir /c/go2cs-tmp-coord/ab8.lock` + PID (exists => STOP); scratch `<scratchpad>\ab8\`; disk >= 25 GB before the battery and before each row.
- Pins (read each back by output): UNSET the machine-scope GOROOT (it names 1.23.1); `<the 1.24.13 SDK>\bin` first on PATH; `go version` = go1.24.13 and `go env GOROOT` prints the BACKSLASH spelling of the 1.24.13 root (that spelling is the pin); GOTOOLCHAIN=local; CGO_ENABLED=0; DOTNET_ROOT=<the dotnet10 root> with that root first on PATH for every `dotnet`; pwsh 7 (net8 apphost) launched from a shell with DOTNET_ROOT UNSET and the dotnet10 root OFF the parent PATH, the pins set inside via `$env:`, and `Get-Command git` asserted inside before CNR. Explicit `-timeout` on every `go test`; `-count=1` on every guard. Exit codes captured on the next line before any pipe. Sibling check: another go2cs.exe outside ab8 => STOP; the harness's own shells (dotnet.exe hosting pwsh.dll) are not siblings; never kill by name.
- GPG: probe before every `git commit -S` / `git merge -S`: `echo test | "/c/Program Files (x86)/GnuPG/bin/gpg.exe" --batch --pinentry-mode error --clearsign -u 941694536F21BAFF >/dev/null 2>&1` (rc 0 = warm); if cold, re-probe every 60 s up to 30 min, then STOP; never bypass signing, never kill gpg-agent, never run a non-batch gpg.
- Census: `C:\Projects\go2cs\.claude\coord-scripts\coord-identifier-census.sh` (master 20eb0af70e, blob 6c12103750: converted mode mirrors the gate's fixture predicate).

## Merges
`git merge --no-ff -S` REF 1 then REF 2; per ref: the additive-blob proof (every path the ref touches equals the ref's blob, or the conflict resolution is quoted), `git diff --diff-filter=D 3469154a95 HEAD` empty, the allowed-path check.

## Battery (converter/gen/golib source FROZEN while it runs)
(a) rebuild go2cs.exe under the pin; the FULL converter suite `go test ./... -count=1 -timeout 40m` under src/go2cs (the declaredNotImplemented guard's edit is aboard): expected FULLY GREEN; quote any red in full.
(b) gen 0 errors; GenTests; GolibTests (Release) count-matched against a control run at 3469154a95 FIRST (batch 7 read 837). THE RED-FIRST PROOF of REF 2, both halves, in this tree: (1) plant 865fda89ad's runtime_impl.cs (the pre-body file: `git show 865fda89ad:src/core/internal/sync/runtime_impl.cs`) over the merged one, rebuild, run ONLY InternalSyncMutexContentionTests -- C1 predicts RED with "a contended internal/sync.Mutex.Lock threw: System.NotImplementedException: runtime_canSpin: no implementation reached this compilation"; quote the actual; (2) restore byte-identical (`git diff --quiet`), rebuild, run the full GolibTests -- the new arms GREEN. A red that is not the predicted canSpin throw, or a green that needed anything but the restore, is a FINDING.
(c) CNR `NO REGRESSION`, CHANGED listed (predicted EMPTY: nothing here changes a converter emission).
(d) stdlib `src/go2cs-stdlib.slnx` Release: 344 / 0 (internal/sync's companion is production code).
(e) ROWS (the tree's `src/run-h10-recon.ps1` by blob, a per-run COPY outside the tree, one row per call, `-tests -test-action all -test-config Release`, -test-timeout = max(the sweep's $longTimeouts floor, 20m) -- never lower an asked value; output dir the SECOND positional inside ab8/src/core/<row>; results tails FIRST; test artifacts cleared before each row; restore every re-emission after each): `internal/sync` (control: VALIDATED 106 / 0 must hold with the bodies), `sync` (control: VALIDATED 46 with 6 disclosed must hold). Bank nothing.
(f) `src/check-roster-format.ps1` from `src/` (expected: 719 checks pass, or the named set quoted), and `python docs/phase4/hopA-inputs/regen-validation-index.py` self-test then DRY RUN: if the eight rows' new links change index.md, `--write` it (zero orphans, zero page-less, 219 = 219) as ONE signed commit; if the dry run shows no change, commit nothing. Census `entry` on the roster and index; repoguard TestNoFleetIdentifiersInTrackedFiles -count=1.
Restore side effects; `git status --porcelain` empty (unfiltered) and `| grep '^ D'` empty; go2cs.exe count 0 at exit; lock released. Worktree IN PLACE at `/c/go2cs-tmp-coord/ab8`. NOTHING PUSHED.

## Report (final message)
Base and refs asserted; merge SHAs with any resolved hunk quoted; (a) verdict with any red quoted; (b) gen / GenTests / GolibTests vs the control, and the red-first proof's two halves quoted; (c) CNR line + CHANGED; (d) stdlib; (e) each row's word, counts, tail's last line, wall; (f) the guard's line, the index dry-run result and any commit; `git log --oneline --first-parent 3469154a95..HEAD` with %G?; disk; anything not verified, stated as such.
