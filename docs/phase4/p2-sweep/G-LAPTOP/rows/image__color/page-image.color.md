# `image/color` — validation proof

Go's own `image/color` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `image/color` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-09-22 · converter `c6fdbe73c`*

**10 matched · 0 disclosed** — Go 1.24.13, `windows/amd64`, converted package
[`src/core/image/color`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/image/color).

Measured at `Release` (tiered JIT off), oracle `go version go1.24.13 windows/amd64`.

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestCMYKGray` | pass | pass |
| `TestCMYKRoundtrip` | pass | pass |
| `TestCMYKToRGBConsistency` | pass | pass |
| `TestNYCbCrAAlpha` | pass | pass |
| `TestNYCbCrAYCbCr` | pass | pass |
| `TestPalette` | pass | pass |
| `TestSqDiff` | pass | pass |
| `TestYCbCrGray` | pass | pass |
| `TestYCbCrRoundtrip` | pass | pass |
| `TestYCbCrToRGBConsistency` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a capability the managed runtime does not provide — a `testing` member the host
has not implemented, or a platform behavior it provably cannot reproduce. Each is named with
the capability it needs.

- BenchmarkNYCbCrAToRGBA (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkRGBToYCbCr (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkYCbCrToRGB (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkYCbCrToRGBA (benchmark): benchmark execution is deferred to Phase 4D
