# `encoding/hex` — validation proof

Go's own `encoding/hex` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `encoding/hex` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-09-22 · converter `c6fdbe73c`*

**12 matched · 0 disclosed** — Go 1.24.13, `windows/amd64`, converted package
[`src/core/encoding/hex`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/encoding/hex).

Measured at `Release` (tiered JIT off), oracle `go version go1.24.13 windows/amd64`.

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestDecode` | pass | pass |
| `TestDecodeErr` | pass | pass |
| `TestDecodeString` | pass | pass |
| `TestDecodeStringErr` | pass | pass |
| `TestDecoderErr` | pass | pass |
| `TestDump` | pass | pass |
| `TestDumper` | pass | pass |
| `TestDumper_doubleclose` | pass | pass |
| `TestDumper_earlyclose` | pass | pass |
| `TestEncode` | pass | pass |
| `TestEncodeToString` | pass | pass |
| `TestEncoderDecoder` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a capability the managed runtime does not provide — a `testing` member the host
has not implemented, or a platform behavior it provably cannot reproduce. Each is named with
the capability it needs.

- BenchmarkDecode (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkDecodeString (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkDump (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkEncode (benchmark): benchmark execution is deferred to Phase 4D
- ExampleDecode (example): example execution is deferred to Phase 4D
- ExampleDecodeString (example): example execution is deferred to Phase 4D
- ExampleDump (example): example execution is deferred to Phase 4D
- ExampleDumper (example): example execution is deferred to Phase 4D
- ExampleEncode (example): example execution is deferred to Phase 4D
- ExampleEncodeToString (example): example execution is deferred to Phase 4D
