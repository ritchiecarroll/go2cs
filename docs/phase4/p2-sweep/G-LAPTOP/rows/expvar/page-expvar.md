# `expvar` — validation proof

Go's own `expvar` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `expvar` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-09-22 · converter `c6fdbe73c`*

**11 matched · 0 disclosed** — Go 1.24.13, `windows/amd64`, converted package
[`src/core/expvar`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/expvar).

Measured at `Release` (tiered JIT off), oracle `go version go1.24.13 windows/amd64`.

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestAppendJSONQuote` | pass | pass |
| `TestFloat` | pass | pass |
| `TestFunc` | pass | pass |
| `TestHandler` | pass | pass |
| `TestInt` | pass | pass |
| `TestMapCounter` | pass | pass |
| `TestMapDelete` | pass | pass |
| `TestMapInit` | pass | pass |
| `TestMapNil` | pass | pass |
| `TestNil` | pass | pass |
| `TestString` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a capability the managed runtime does not provide — a `testing` member the host
has not implemented, or a platform behavior it provably cannot reproduce. Each is named with
the capability it needs.

- BenchmarkFloatAdd (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkFloatSet (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkIntAdd (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkIntSet (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkMapAddDifferent (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkMapAddDifferentRandom (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkMapAddDifferentSteadyState (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkMapAddSame (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkMapAddSameSteadyState (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkMapSet (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkMapSetDifferent (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkMapSetDifferentRandom (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkMapSetString (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkMapString (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkRealworldExpvarUsage (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkStringSet (benchmark): benchmark execution is deferred to Phase 4D
