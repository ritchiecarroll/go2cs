# `index/suffixarray` — validation proof

Go's own `index/suffixarray` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `index/suffixarray` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-09-22 · converter `c6fdbe73c`*

**12 matched · 0 disclosed** — Go 1.24.13, `windows/amd64`, converted package
[`src/core/index/suffixarray`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/index/suffixarray).

Measured at `Release` (tiered JIT off), oracle `go version go1.24.13 windows/amd64`.

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestIndex32` | pass | pass |
| `TestIndex64` | pass | pass |
| `TestNew32` | pass | pass |
| `TestNew32/ababab...` | pass | pass |
| `TestNew32/exhaustive2` | pass | pass |
| `TestNew32/exhaustive3` | pass | pass |
| `TestNew32/forcealloc` | pass | pass |
| `TestNew64` | pass | pass |
| `TestNew64/ababab...` | pass | pass |
| `TestNew64/exhaustive2` | pass | pass |
| `TestNew64/exhaustive3` | pass | pass |
| `TestNew64/forcealloc` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a capability the managed runtime does not provide — a `testing` member the host
has not implemented, or a platform behavior it provably cannot reproduce. Each is named with
the capability it needs.

- BenchmarkNew (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkSaveRestore (benchmark): benchmark execution is deferred to Phase 4D
- ExampleIndex_Lookup (example): example execution is deferred to Phase 4D
