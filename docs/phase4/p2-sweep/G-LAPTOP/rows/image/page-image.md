# `image` — validation proof

Go's own `image` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `image` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-09-22 · converter `c6fdbe73c`*

**8 matched · 0 disclosed** — Go 1.24.13, `windows/amd64`, converted package
[`src/core/image`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/image).

Measured at `Release` (tiered JIT off), oracle `go version go1.24.13 windows/amd64`.

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `Test16BitsPerColorChannel` | pass | pass |
| `TestDecode` | pass | pass |
| `TestImage` | pass | pass |
| `TestNewXxxBadRectangle` | pass | pass |
| `TestRGBA64Image` | pass | pass |
| `TestRectangle` | pass | pass |
| `TestYCbCr` | pass | pass |
| `TestYCbCrSlicesDontOverlap` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a capability the managed runtime does not provide — a `testing` member the host
has not implemented, or a platform behavior it provably cannot reproduce. Each is named with
the capability it needs.

- BenchmarkAlpha16At (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkAlphaAt (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkAlphaSetAlpha (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkAlphaSetAlpha16 (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkAt (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkGray16At (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkGrayAt (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkGraySetGray (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkGraySetGray16 (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkNRGBA64At (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkNRGBA64SetNRGBA64 (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkNRGBAAt (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkNRGBASetNRGBA (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkRGBA64At (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkRGBA64SetRGBA64 (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkRGBAAt (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkRGBASetRGBA (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkSet (benchmark): benchmark execution is deferred to Phase 4D
- Example (example): example execution is deferred to Phase 4D
- Example_decodeConfig (example): example execution is deferred to Phase 4D
