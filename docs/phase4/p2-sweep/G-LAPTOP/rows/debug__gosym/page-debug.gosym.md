# `debug/gosym` — validation proof

Go's own `debug/gosym` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `debug/gosym` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-09-22 · converter `c6fdbe73c`*

**10 matched · 0 disclosed** — Go 1.24.13, `windows/amd64`, converted package
[`src/core/debug/gosym`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/debug/gosym).

Measured at `Release` (tiered JIT off), oracle `go version go1.24.13 windows/amd64`.

Both runtimes skip 3 of the matched tests identically.

> Linux only: on Windows both runtimes skip TestSymVersion identically (skipIfNotELF), so the Windows row stays 10 matched / 0 disclosed and this entry never fires there.

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `Test115PclnParsing` | pass | pass |
| `TestGenericNames` | pass | pass |
| `TestIssue29551` | pass | pass |
| `TestLineAline` | skip | skip |
| `TestLineFromAline` | skip | skip |
| `TestPCLine` | pass | pass |
| `TestRemotePackage` | pass | pass |
| `TestStandardLibPackage` | pass | pass |
| `TestStandardLibPathPackage` | pass | pass |
| `TestSymVersion` | skip | skip |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a capability the managed runtime does not provide — a `testing` member the host
has not implemented, or a platform behavior it provably cannot reproduce. Each is named with
the capability it needs.

- Benchmark115 (benchmark): benchmark execution is deferred to Phase 4D
