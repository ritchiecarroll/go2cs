# `go/scanner` — validation proof

Go's own `go/scanner` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `go/scanner` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-09-22 · converter `c6fdbe73c`*

**11 matched · 0 disclosed** — Go 1.24.13, `windows/amd64`, converted package
[`src/core/go/scanner`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/go/scanner).

Measured at `Release` (tiered JIT off), oracle `go version go1.24.13 windows/amd64`.

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestInit` | pass | pass |
| `TestInvalidLineDirectives` | pass | pass |
| `TestIssue10213` | pass | pass |
| `TestIssue28112` | pass | pass |
| `TestLineDirectives` | pass | pass |
| `TestNumbers` | pass | pass |
| `TestScan` | pass | pass |
| `TestScanErrors` | pass | pass |
| `TestSemicolons` | pass | pass |
| `TestStdErrorHandler` | pass | pass |
| `TestStripCR` | pass | pass |

## Excluded declarations

Declarations filtered from **both** sides of the comparison, and therefore not claimed above:
`Benchmark`, `Fuzz` and `Example` declarations the converted host does not execute, plus any
test requiring a capability the managed runtime does not provide — a `testing` member the host
has not implemented, or a platform behavior it provably cannot reproduce. Each is named with
the capability it needs.

- BenchmarkScan (benchmark): benchmark execution is deferred to Phase 4D
- BenchmarkScanFiles (benchmark): benchmark execution is deferred to Phase 4D
- ExampleScanner_Scan (example): example execution is deferred to Phase 4D
