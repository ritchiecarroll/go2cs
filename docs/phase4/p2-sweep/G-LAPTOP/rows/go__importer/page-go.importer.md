# `go/importer` — validation proof

Go's own `go/importer` test suite, converted to C# by [go2cs](https://github.com/ritchiecarroll/go2cs), built against the converted standard
library, run under the Go-semantics test host, and compared verdict for verdict against a clean
`go test -json` baseline of the same sources. This page is generated by the converter from that
comparison — it is the evidence behind the `go/importer` row in
[Validated Test Packages](../../ValidatedTestPackages.md).

*Validated 2026-09-22 · converter `c6fdbe73c`*

**3 matched · 0 disclosed** — Go 1.24.13, `windows/amd64`, converted package
[`src/core/go/importer`](https://github.com/ritchiecarroll/go2cs/tree/master/src/core/go/importer).

Measured at `Release` (tiered JIT off), oracle `go version go1.24.13 windows/amd64`.

Both runtimes skip 1 of the matched tests identically.

## Verdicts

| Test | `go test` | go2cs |
|:--|:--:|:--:|
| `TestForCompiler` | pass | pass |
| `TestForCompiler/LookupCustom` | skip | skip |
| `TestForCompiler/LookupDefault` | pass | pass |
