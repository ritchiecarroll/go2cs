{
  "package": "maps",
  "status": "validated",
  "go": {
    "TestAlignUpPow2": "pass",
    "TestCtrlSize": "pass",
    "TestEncodeDecode": "pass",
    "TestMapDelete": "pass",
    "TestMapDeleteClear": "pass",
    "TestMapIndirect": "pass",
    "TestMapPut": "pass",
    "TestMapSplit": "pass",
    "TestMapZeroSizeSlot": "pass",
    "TestTableClear": "pass",
    "TestTableGroupCount": "pass",
    "TestTableGroupCount/makemap": "pass",
    "TestTableGroupCount/makemap/n=-1": "pass",
    "TestTableGroupCount/makemap/n=-1/escape": "pass",
    "TestTableGroupCount/makemap/n=-1073741824": "pass",
    "TestTableGroupCount/makemap/n=-1073741824/escape": "pass",
    "TestTableGroupCount/makemap/n=0": "pass",
    "TestTableGroupCount/makemap/n=0/escape": "pass",
    "TestTableGroupCount/makemap/n=1": "pass",
    "TestTableGroupCount/makemap/n=1/escape": "pass",
    "TestTableGroupCount/makemap/n=12": "pass",
    "TestTableGroupCount/makemap/n=12/escape": "pass",
    "TestTableGroupCount/makemap/n=14": "pass",
    "TestTableGroupCount/makemap/n=14/escape": "pass",
    "TestTableGroupCount/makemap/n=15": "pass",
    "TestTableGroupCount/makemap/n=15/escape": "pass",
    "TestTableGroupCount/makemap/n=24": "pass",
    "TestTableGroupCount/makemap/n=24/escape": "pass",
    "TestTableGroupCount/makemap/n=29": "pass",
    "TestTableGroupCount/makemap/n=29/escape": "pass",
    "TestTableGroupCount/makemap/n=8": "pass",
    "TestTableGroupCount/makemap/n=8/escape": "pass",
    "TestTableGroupCount/makemap/n=9": "pass",
    "TestTableGroupCount/makemap/n=9/escape": "pass",
    "TestTableGroupCount/makemap64": "pass",
    "TestTableGroupCount/makemap64/n=-1": "pass",
    "TestTableGroupCount/makemap64/n=-1/escape": "pass",
    "TestTableGroupCount/makemap64/n=-1073741824": "pass",
    "TestTableGroupCount/makemap64/n=-1073741824/escape": "pass",
    "TestTableGroupCount/makemap64/n=0": "pass",
    "TestTableGroupCount/makemap64/n=0/escape": "pass",
    "TestTableGroupCount/makemap64/n=1": "pass",
    "TestTableGroupCount/makemap64/n=1/escape": "pass",
    "TestTableGroupCount/makemap64/n=12": "pass",
    "TestTableGroupCount/makemap64/n=12/escape": "pass",
    "TestTableGroupCount/makemap64/n=14": "pass",
    "TestTableGroupCount/makemap64/n=14/escape": "pass",
    "TestTableGroupCount/makemap64/n=15": "pass",
    "TestTableGroupCount/makemap64/n=15/escape": "pass",
    "TestTableGroupCount/makemap64/n=24": "pass",
    "TestTableGroupCount/makemap64/n=24/escape": "pass",
    "TestTableGroupCount/makemap64/n=29": "pass",
    "TestTableGroupCount/makemap64/n=29/escape": "pass",
    "TestTableGroupCount/makemap64/n=8": "pass",
    "TestTableGroupCount/makemap64/n=8/escape": "pass",
    "TestTableGroupCount/makemap64/n=9": "pass",
    "TestTableGroupCount/makemap64/n=9/escape": "pass",
    "TestTableGroupCount/mapliteral": "pass",
    "TestTableGroupCount/mapliteral/n=-1": "pass",
    "TestTableGroupCount/mapliteral/n=-1/escape": "pass",
    "TestTableGroupCount/mapliteral/n=-1073741824": "pass",
    "TestTableGroupCount/mapliteral/n=-1073741824/escape": "pass",
    "TestTableGroupCount/mapliteral/n=0": "pass",
    "TestTableGroupCount/mapliteral/n=0/escape": "pass",
    "TestTableGroupCount/mapliteral/n=1": "pass",
    "TestTableGroupCount/mapliteral/n=1/escape": "pass",
    "TestTableGroupCount/mapliteral/n=12": "pass",
    "TestTableGroupCount/mapliteral/n=12/escape": "pass",
    "TestTableGroupCount/mapliteral/n=14": "pass",
    "TestTableGroupCount/mapliteral/n=14/escape": "pass",
    "TestTableGroupCount/mapliteral/n=15": "pass",
    "TestTableGroupCount/mapliteral/n=15/escape": "pass",
    "TestTableGroupCount/mapliteral/n=24": "pass",
    "TestTableGroupCount/mapliteral/n=24/escape": "pass",
    "TestTableGroupCount/mapliteral/n=29": "pass",
    "TestTableGroupCount/mapliteral/n=29/escape": "pass",
    "TestTableGroupCount/mapliteral/n=8": "pass",
    "TestTableGroupCount/mapliteral/n=8/escape": "pass",
    "TestTableGroupCount/mapliteral/n=9": "pass",
    "TestTableGroupCount/mapliteral/n=9/escape": "pass",
    "TestTableGroupCount/nohint": "pass",
    "TestTableGroupCount/nohint/n=-1": "pass",
    "TestTableGroupCount/nohint/n=-1/escape": "pass",
    "TestTableGroupCount/nohint/n=-1073741824": "pass",
    "TestTableGroupCount/nohint/n=-1073741824/escape": "pass",
    "TestTableGroupCount/nohint/n=0": "pass",
    "TestTableGroupCount/nohint/n=0/escape": "pass",
    "TestTableGroupCount/nohint/n=1": "pass",
    "TestTableGroupCount/nohint/n=1/escape": "pass",
    "TestTableGroupCount/nohint/n=12": "pass",
    "TestTableGroupCount/nohint/n=12/escape": "pass",
    "TestTableGroupCount/nohint/n=14": "pass",
    "TestTableGroupCount/nohint/n=14/escape": "pass",
    "TestTableGroupCount/nohint/n=15": "pass",
    "TestTableGroupCount/nohint/n=15/escape": "pass",
    "TestTableGroupCount/nohint/n=24": "pass",
    "TestTableGroupCount/nohint/n=24/escape": "pass",
    "TestTableGroupCount/nohint/n=29": "pass",
    "TestTableGroupCount/nohint/n=29/escape": "pass",
    "TestTableGroupCount/nohint/n=8": "pass",
    "TestTableGroupCount/nohint/n=8/escape": "pass",
    "TestTableGroupCount/nohint/n=9": "pass",
    "TestTableGroupCount/nohint/n=9/escape": "pass",
    "TestTableIteration": "pass",
    "TestTableIterationDelete": "pass",
    "TestTableIterationGrowDelete": "pass",
    "TestTableIterationGrowDuplicate": "pass",
    "TestTableIterationGrowDuplicate/grow": "pass",
    "TestTableIterationGrowDuplicate/split": "pass",
    "TestTableKeyUpdate": "pass",
    "TestTablePutDelete": "pass"
  },
  "csharp": {
    "TestAlignUpPow2": "pass",
    "TestCtrlSize": "pass",
    "TestEncodeDecode": "pass",
    "TestMapDelete": "fail",
    "TestMapDeleteClear": "fail",
    "TestMapIndirect": "fail",
    "TestMapPut": "fail",
    "TestMapSplit": "fail",
    "TestMapZeroSizeSlot": "fail",
    "TestTableClear": "fail",
    "TestTableGroupCount": "fail",
    "TestTableGroupCount/makemap": "fail",
    "TestTableGroupCount/makemap/n=-1": "fail",
    "TestTableGroupCount/makemap/n=-1/escape": "fail",
    "TestTableGroupCount/makemap/n=-1073741824": "fail",
    "TestTableGroupCount/makemap/n=-1073741824/escape": "fail",
    "TestTableGroupCount/makemap/n=0": "fail",
    "TestTableGroupCount/makemap/n=0/escape": "fail",
    "TestTableGroupCount/makemap/n=1": "fail",
    "TestTableGroupCount/makemap/n=1/escape": "fail",
    "TestTableGroupCount/makemap/n=12": "fail",
    "TestTableGroupCount/makemap/n=12/escape": "fail",
    "TestTableGroupCount/makemap/n=14": "fail",
    "TestTableGroupCount/makemap/n=14/escape": "fail",
    "TestTableGroupCount/makemap/n=15": "fail",
    "TestTableGroupCount/makemap/n=15/escape": "fail",
    "TestTableGroupCount/makemap/n=24": "fail",
    "TestTableGroupCount/makemap/n=24/escape": "fail",
    "TestTableGroupCount/makemap/n=29": "fail",
    "TestTableGroupCount/makemap/n=29/escape": "fail",
    "TestTableGroupCount/makemap/n=8": "fail",
    "TestTableGroupCount/makemap/n=8/escape": "fail",
    "TestTableGroupCount/makemap/n=9": "fail",
    "TestTableGroupCount/makemap/n=9/escape": "fail",
    "TestTableGroupCount/makemap64": "fail",
    "TestTableGroupCount/makemap64/n=-1": "fail",
    "TestTableGroupCount/makemap64/n=-1/escape": "fail",
    "TestTableGroupCount/makemap64/n=-1073741824": "fail",
    "TestTableGroupCount/makemap64/n=-1073741824/escape": "fail",
    "TestTableGroupCount/makemap64/n=0": "fail",
    "TestTableGroupCount/makemap64/n=0/escape": "fail",
    "TestTableGroupCount/makemap64/n=1": "fail",
    "TestTableGroupCount/makemap64/n=1/escape": "fail",
    "TestTableGroupCount/makemap64/n=12": "fail",
    "TestTableGroupCount/makemap64/n=12/escape": "fail",
    "TestTableGroupCount/makemap64/n=14": "fail",
    "TestTableGroupCount/makemap64/n=14/escape": "fail",
    "TestTableGroupCount/makemap64/n=15": "fail",
    "TestTableGroupCount/makemap64/n=15/escape": "fail",
    "TestTableGroupCount/makemap64/n=24": "fail",
    "TestTableGroupCount/makemap64/n=24/escape": "fail",
    "TestTableGroupCount/makemap64/n=29": "fail",
    "TestTableGroupCount/makemap64/n=29/escape": "fail",
    "TestTableGroupCount/makemap64/n=8": "fail",
    "TestTableGroupCount/makemap64/n=8/escape": "fail",
    "TestTableGroupCount/makemap64/n=9": "fail",
    "TestTableGroupCount/makemap64/n=9/escape": "fail",
    "TestTableGroupCount/mapliteral": "fail",
    "TestTableGroupCount/mapliteral/n=-1": "fail",
    "TestTableGroupCount/mapliteral/n=-1/escape": "fail",
    "TestTableGroupCount/mapliteral/n=-1073741824": "fail",
    "TestTableGroupCount/mapliteral/n=-1073741824/escape": "fail",
    "TestTableGroupCount/mapliteral/n=0": "fail",
    "TestTableGroupCount/mapliteral/n=0/escape": "fail",
    "TestTableGroupCount/mapliteral/n=1": "fail",
    "TestTableGroupCount/mapliteral/n=1/escape": "fail",
    "TestTableGroupCount/mapliteral/n=12": "fail",
    "TestTableGroupCount/mapliteral/n=12/escape": "fail",
    "TestTableGroupCount/mapliteral/n=14": "fail",
    "TestTableGroupCount/mapliteral/n=14/escape": "fail",
    "TestTableGroupCount/mapliteral/n=15": "fail",
    "TestTableGroupCount/mapliteral/n=15/escape": "fail",
    "TestTableGroupCount/mapliteral/n=24": "fail",
    "TestTableGroupCount/mapliteral/n=24/escape": "fail",
    "TestTableGroupCount/mapliteral/n=29": "fail",
    "TestTableGroupCount/mapliteral/n=29/escape": "fail",
    "TestTableGroupCount/mapliteral/n=8": "fail",
    "TestTableGroupCount/mapliteral/n=8/escape": "fail",
    "TestTableGroupCount/mapliteral/n=9": "fail",
    "TestTableGroupCount/mapliteral/n=9/escape": "fail",
    "TestTableGroupCount/nohint": "fail",
    "TestTableGroupCount/nohint/n=-1": "fail",
    "TestTableGroupCount/nohint/n=-1/escape": "fail",
    "TestTableGroupCount/nohint/n=-1073741824": "fail",
    "TestTableGroupCount/nohint/n=-1073741824/escape": "fail",
    "TestTableGroupCount/nohint/n=0": "fail",
    "TestTableGroupCount/nohint/n=0/escape": "fail",
    "TestTableGroupCount/nohint/n=1": "fail",
    "TestTableGroupCount/nohint/n=1/escape": "fail",
    "TestTableGroupCount/nohint/n=12": "fail",
    "TestTableGroupCount/nohint/n=12/escape": "fail",
    "TestTableGroupCount/nohint/n=14": "fail",
    "TestTableGroupCount/nohint/n=14/escape": "fail",
    "TestTableGroupCount/nohint/n=15": "fail",
    "TestTableGroupCount/nohint/n=15/escape": "fail",
    "TestTableGroupCount/nohint/n=24": "fail",
    "TestTableGroupCount/nohint/n=24/escape": "fail",
    "TestTableGroupCount/nohint/n=29": "fail",
    "TestTableGroupCount/nohint/n=29/escape": "fail",
    "TestTableGroupCount/nohint/n=8": "fail",
    "TestTableGroupCount/nohint/n=8/escape": "fail",
    "TestTableGroupCount/nohint/n=9": "fail",
    "TestTableGroupCount/nohint/n=9/escape": "fail",
    "TestTableIteration": "fail",
    "TestTableIterationDelete": "fail",
    "TestTableIterationGrowDelete": "fail",
    "TestTableIterationGrowDuplicate": "fail",
    "TestTableIterationGrowDuplicate/grow": "fail",
    "TestTableIterationGrowDuplicate/split": "fail",
    "TestTableKeyUpdate": "fail",
    "TestTablePutDelete": "fail"
  },
  "matched": true,
  "skipped": [],
  "disclosed": [
    "TestMapDelete (runtime-capability): runtime-capability: the test builds a real swiss table through NewMap and drives it, and the table lives in RAW Go-layout group memory -- allocated, copied and cleared by the push block runtime supplies to this package (runtime/malloc.go maps_newobject and maps_newarray, runtime/mbarrier.go maps_typedmemmove and maps_typedmemclr), addressed by the descriptor GroupSize, SlotSize and ElemOff offsets. The managed model has no such memory: golib map\u003cK,V\u003e is a dictionary with no swiss table behind it, and abi MapType projection reports those offsets as an honest zero. Each of the four refuses BY NAME in this package runtime_impl.cs, the internal/abi FuncPCABI0 shape, so the row fails with the text below rather than as a missing implementation. No managed implementation can meet this assertion without a managed group store, which is a design, not a seat (not taken). internal/runtime/maps IS the raw-memory swiss table; the tests that remain live are its managed surface. Coordinator ruling, 2026-09-22.",
    "TestMapDeleteClear (runtime-capability): runtime-capability: the test builds a real swiss table through NewMap and drives it, and the table lives in RAW Go-layout group memory -- allocated, copied and cleared by the push block runtime supplies to this package (runtime/malloc.go maps_newobject and maps_newarray, runtime/mbarrier.go maps_typedmemmove and maps_typedmemclr), addressed by the descriptor GroupSize, SlotSize and ElemOff offsets. The managed model has no such memory: golib map\u003cK,V\u003e is a dictionary with no swiss table behind it, and abi MapType projection reports those offsets as an honest zero. Each of the four refuses BY NAME in this package runtime_impl.cs, the internal/abi FuncPCABI0 shape, so the row fails with the text below rather than as a missing implementation. No managed implementation can meet this assertion without a managed group store, which is a design, not a seat (not taken). internal/runtime/maps IS the raw-memory swiss table; the tests that remain live are its managed surface. Coordinator ruling, 2026-09-22.",
    "TestMapIndirect (runtime-capability): runtime-capability: the test builds a real swiss table through NewMap and drives it, and the table lives in RAW Go-layout group memory -- allocated, copied and cleared by the push block runtime supplies to this package (runtime/malloc.go maps_newobject and maps_newarray, runtime/mbarrier.go maps_typedmemmove and maps_typedmemclr), addressed by the descriptor GroupSize, SlotSize and ElemOff offsets. The managed model has no such memory: golib map\u003cK,V\u003e is a dictionary with no swiss table behind it, and abi MapType projection reports those offsets as an honest zero. Each of the four refuses BY NAME in this package runtime_impl.cs, the internal/abi FuncPCABI0 shape, so the row fails with the text below rather than as a missing implementation. No managed implementation can meet this assertion without a managed group store, which is a design, not a seat (not taken). internal/runtime/maps IS the raw-memory swiss table; the tests that remain live are its managed surface. Coordinator ruling, 2026-09-22.",
    "TestMapPut (runtime-capability): runtime-capability: the test builds a real swiss table through NewMap and drives it, and the table lives in RAW Go-layout group memory -- allocated, copied and cleared by the push block runtime supplies to this package (runtime/malloc.go maps_newobject and maps_newarray, runtime/mbarrier.go maps_typedmemmove and maps_typedmemclr), addressed by the descriptor GroupSize, SlotSize and ElemOff offsets. The managed model has no such memory: golib map\u003cK,V\u003e is a dictionary with no swiss table behind it, and abi MapType projection reports those offsets as an honest zero. Each of the four refuses BY NAME in this package runtime_impl.cs, the internal/abi FuncPCABI0 shape, so the row fails with the text below rather than as a missing implementation. No managed implementation can meet this assertion without a managed group store, which is a design, not a seat (not taken). internal/runtime/maps IS the raw-memory swiss table; the tests that remain live are its managed surface. Coordinator ruling, 2026-09-22.",
    "TestMapSplit (runtime-capability): runtime-capability: the test builds a real swiss table through NewMap and drives it, and the table lives in RAW Go-layout group memory -- allocated, copied and cleared by the push block runtime supplies to this package (runtime/malloc.go maps_newobject and maps_newarray, runtime/mbarrier.go maps_typedmemmove and maps_typedmemclr), addressed by the descriptor GroupSize, SlotSize and ElemOff offsets. The managed model has no such memory: golib map\u003cK,V\u003e is a dictionary with no swiss table behind it, and abi MapType projection reports those offsets as an honest zero. Each of the four refuses BY NAME in this package runtime_impl.cs, the internal/abi FuncPCABI0 shape, so the row fails with the text below rather than as a missing implementation. No managed implementation can meet this assertion without a managed group store, which is a design, not a seat (not taken). internal/runtime/maps IS the raw-memory swiss table; the tests that remain live are its managed surface. Coordinator ruling, 2026-09-22.",
    "TestMapZeroSizeSlot (runtime-capability): runtime-capability: the test builds a real swiss table through NewMap and drives it, and the table lives in RAW Go-layout group memory -- allocated, copied and cleared by the push block runtime supplies to this package (runtime/malloc.go maps_newobject and maps_newarray, runtime/mbarrier.go maps_typedmemmove and maps_typedmemclr), addressed by the descriptor GroupSize, SlotSize and ElemOff offsets. The managed model has no such memory: golib map\u003cK,V\u003e is a dictionary with no swiss table behind it, and abi MapType projection reports those offsets as an honest zero. Each of the four refuses BY NAME in this package runtime_impl.cs, the internal/abi FuncPCABI0 shape, so the row fails with the text below rather than as a missing implementation. No managed implementation can meet this assertion without a managed group store, which is a design, not a seat (not taken). internal/runtime/maps IS the raw-memory swiss table; the tests that remain live are its managed surface. Coordinator ruling, 2026-09-22.",
    "TestTableClear (runtime-capability): runtime-capability: the test builds a real swiss table through NewMap and drives it, and the table lives in RAW Go-layout group memory -- allocated, copied and cleared by the push block runtime supplies to this package (runtime/malloc.go maps_newobject and maps_newarray, runtime/mbarrier.go maps_typedmemmove and maps_typedmemclr), addressed by the descriptor GroupSize, SlotSize and ElemOff offsets. The managed model has no such memory: golib map\u003cK,V\u003e is a dictionary with no swiss table behind it, and abi MapType projection reports those offsets as an honest zero. Each of the four refuses BY NAME in this package runtime_impl.cs, the internal/abi FuncPCABI0 shape, so the row fails with the text below rather than as a missing implementation. No managed implementation can meet this assertion without a managed group store, which is a design, not a seat (not taken). internal/runtime/maps IS the raw-memory swiss table; the tests that remain live are its managed surface. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount (): ",
    "TestTableGroupCount/makemap (): ",
    "TestTableGroupCount/makemap/n=-1 (): ",
    "TestTableGroupCount/makemap/n=-1/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap/n=-1073741824 (): ",
    "TestTableGroupCount/makemap/n=-1073741824/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap/n=0 (): ",
    "TestTableGroupCount/makemap/n=0/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap/n=1 (): ",
    "TestTableGroupCount/makemap/n=1/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap/n=12 (): ",
    "TestTableGroupCount/makemap/n=12/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap/n=14 (): ",
    "TestTableGroupCount/makemap/n=14/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap/n=15 (): ",
    "TestTableGroupCount/makemap/n=15/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap/n=24 (): ",
    "TestTableGroupCount/makemap/n=24/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap/n=29 (): ",
    "TestTableGroupCount/makemap/n=29/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap/n=8 (): ",
    "TestTableGroupCount/makemap/n=8/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap/n=9 (): ",
    "TestTableGroupCount/makemap/n=9/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap64 (): ",
    "TestTableGroupCount/makemap64/n=-1 (): ",
    "TestTableGroupCount/makemap64/n=-1/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap64/n=-1073741824 (): ",
    "TestTableGroupCount/makemap64/n=-1073741824/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap64/n=0 (): ",
    "TestTableGroupCount/makemap64/n=0/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap64/n=1 (): ",
    "TestTableGroupCount/makemap64/n=1/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap64/n=12 (): ",
    "TestTableGroupCount/makemap64/n=12/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap64/n=14 (): ",
    "TestTableGroupCount/makemap64/n=14/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap64/n=15 (): ",
    "TestTableGroupCount/makemap64/n=15/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap64/n=24 (): ",
    "TestTableGroupCount/makemap64/n=24/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap64/n=29 (): ",
    "TestTableGroupCount/makemap64/n=29/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap64/n=8 (): ",
    "TestTableGroupCount/makemap64/n=8/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/makemap64/n=9 (): ",
    "TestTableGroupCount/makemap64/n=9/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/mapliteral (): ",
    "TestTableGroupCount/mapliteral/n=-1 (): ",
    "TestTableGroupCount/mapliteral/n=-1/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/mapliteral/n=-1073741824 (): ",
    "TestTableGroupCount/mapliteral/n=-1073741824/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/mapliteral/n=0 (): ",
    "TestTableGroupCount/mapliteral/n=0/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/mapliteral/n=1 (): ",
    "TestTableGroupCount/mapliteral/n=1/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/mapliteral/n=12 (): ",
    "TestTableGroupCount/mapliteral/n=12/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/mapliteral/n=14 (): ",
    "TestTableGroupCount/mapliteral/n=14/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/mapliteral/n=15 (): ",
    "TestTableGroupCount/mapliteral/n=15/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/mapliteral/n=24 (): ",
    "TestTableGroupCount/mapliteral/n=24/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/mapliteral/n=29 (): ",
    "TestTableGroupCount/mapliteral/n=29/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/mapliteral/n=8 (): ",
    "TestTableGroupCount/mapliteral/n=8/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/mapliteral/n=9 (): ",
    "TestTableGroupCount/mapliteral/n=9/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/nohint (): ",
    "TestTableGroupCount/nohint/n=-1 (): ",
    "TestTableGroupCount/nohint/n=-1/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/nohint/n=-1073741824 (): ",
    "TestTableGroupCount/nohint/n=-1073741824/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/nohint/n=0 (): ",
    "TestTableGroupCount/nohint/n=0/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/nohint/n=1 (): ",
    "TestTableGroupCount/nohint/n=1/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/nohint/n=12 (): ",
    "TestTableGroupCount/nohint/n=12/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/nohint/n=14 (): ",
    "TestTableGroupCount/nohint/n=14/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/nohint/n=15 (): ",
    "TestTableGroupCount/nohint/n=15/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/nohint/n=24 (): ",
    "TestTableGroupCount/nohint/n=24/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/nohint/n=29 (): ",
    "TestTableGroupCount/nohint/n=29/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/nohint/n=8 (): ",
    "TestTableGroupCount/nohint/n=8/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableGroupCount/nohint/n=9 (): ",
    "TestTableGroupCount/nohint/n=9/escape (runtime-capability): runtime-capability: TestTableGroupCount reads a Go map VALUE as its runtime header -- map_swiss_test.go testMap does mm := *(**maps.Map)(unsafe.Pointer(\u0026m)), emitted Reinterpret\u003cmap\u003cnint, nint\u003e, ж\u003cMap\u003e\u003e() (map_swiss_test.cs:157) -- and then asks that header for TableCount and GroupCount. A Go map value IS a pointer to the runtime maps.Map the swiss table lives in; golib map\u003cK,V\u003e is a managed dictionary with NO maps.Map behind it, by construction, so there is no header to reinterpret and no table or group count to report. The address route hands out a native box over the map box order token, and its first dereference takes the ruled Q44 section 10.3 arm-2a refusal by name -- the signature below -- rather than read a Go-layout offset into CLR-laid-out storage. No managed implementation can meet this assertion without replacing golib map with the runtime swiss table; the rest of the row runs the real internal/runtime/maps tables directly and validates. Pinned on the 44 leaves, each of which carries the refusal text; their 49 ancestors (the parent, its 4 constructor groups, 44 n= cases) have no failure output of their own and ride the disclosed-parent aggregation, 93 verdicts in all. Coordinator ruling, 2026-09-22.",
    "TestTableIteration (runtime-capability): runtime-capability: the test builds a real swiss table through NewMap and drives it, and the table lives in RAW Go-layout group memory -- allocated, copied and cleared by the push block runtime supplies to this package (runtime/malloc.go maps_newobject and maps_newarray, runtime/mbarrier.go maps_typedmemmove and maps_typedmemclr), addressed by the descriptor GroupSize, SlotSize and ElemOff offsets. The managed model has no such memory: golib map\u003cK,V\u003e is a dictionary with no swiss table behind it, and abi MapType projection reports those offsets as an honest zero. Each of the four refuses BY NAME in this package runtime_impl.cs, the internal/abi FuncPCABI0 shape, so the row fails with the text below rather than as a missing implementation. No managed implementation can meet this assertion without a managed group store, which is a design, not a seat (not taken). internal/runtime/maps IS the raw-memory swiss table; the tests that remain live are its managed surface. Coordinator ruling, 2026-09-22.",
    "TestTableIterationDelete (runtime-capability): runtime-capability: the test builds a real swiss table through NewMap and drives it, and the table lives in RAW Go-layout group memory -- allocated, copied and cleared by the push block runtime supplies to this package (runtime/malloc.go maps_newobject and maps_newarray, runtime/mbarrier.go maps_typedmemmove and maps_typedmemclr), addressed by the descriptor GroupSize, SlotSize and ElemOff offsets. The managed model has no such memory: golib map\u003cK,V\u003e is a dictionary with no swiss table behind it, and abi MapType projection reports those offsets as an honest zero. Each of the four refuses BY NAME in this package runtime_impl.cs, the internal/abi FuncPCABI0 shape, so the row fails with the text below rather than as a missing implementation. No managed implementation can meet this assertion without a managed group store, which is a design, not a seat (not taken). internal/runtime/maps IS the raw-memory swiss table; the tests that remain live are its managed surface. Coordinator ruling, 2026-09-22.",
    "TestTableIterationGrowDelete (runtime-capability): runtime-capability: the test builds a real swiss table through NewMap and drives it, and the table lives in RAW Go-layout group memory -- allocated, copied and cleared by the push block runtime supplies to this package (runtime/malloc.go maps_newobject and maps_newarray, runtime/mbarrier.go maps_typedmemmove and maps_typedmemclr), addressed by the descriptor GroupSize, SlotSize and ElemOff offsets. The managed model has no such memory: golib map\u003cK,V\u003e is a dictionary with no swiss table behind it, and abi MapType projection reports those offsets as an honest zero. Each of the four refuses BY NAME in this package runtime_impl.cs, the internal/abi FuncPCABI0 shape, so the row fails with the text below rather than as a missing implementation. No managed implementation can meet this assertion without a managed group store, which is a design, not a seat (not taken). internal/runtime/maps IS the raw-memory swiss table; the tests that remain live are its managed surface. Coordinator ruling, 2026-09-22.",
    "TestTableIterationGrowDuplicate (): ",
    "TestTableIterationGrowDuplicate/grow (runtime-capability): runtime-capability: the test builds a real swiss table through NewMap and drives it, and the table lives in RAW Go-layout group memory -- allocated, copied and cleared by the push block runtime supplies to this package (runtime/malloc.go maps_newobject and maps_newarray, runtime/mbarrier.go maps_typedmemmove and maps_typedmemclr), addressed by the descriptor GroupSize, SlotSize and ElemOff offsets. The managed model has no such memory: golib map\u003cK,V\u003e is a dictionary with no swiss table behind it, and abi MapType projection reports those offsets as an honest zero. Each of the four refuses BY NAME in this package runtime_impl.cs, the internal/abi FuncPCABI0 shape, so the row fails with the text below rather than as a missing implementation. No managed implementation can meet this assertion without a managed group store, which is a design, not a seat (not taken). internal/runtime/maps IS the raw-memory swiss table; the tests that remain live are its managed surface. Coordinator ruling, 2026-09-22.",
    "TestTableIterationGrowDuplicate/split (runtime-capability): runtime-capability: the test builds a real swiss table through NewMap and drives it, and the table lives in RAW Go-layout group memory -- allocated, copied and cleared by the push block runtime supplies to this package (runtime/malloc.go maps_newobject and maps_newarray, runtime/mbarrier.go maps_typedmemmove and maps_typedmemclr), addressed by the descriptor GroupSize, SlotSize and ElemOff offsets. The managed model has no such memory: golib map\u003cK,V\u003e is a dictionary with no swiss table behind it, and abi MapType projection reports those offsets as an honest zero. Each of the four refuses BY NAME in this package runtime_impl.cs, the internal/abi FuncPCABI0 shape, so the row fails with the text below rather than as a missing implementation. No managed implementation can meet this assertion without a managed group store, which is a design, not a seat (not taken). internal/runtime/maps IS the raw-memory swiss table; the tests that remain live are its managed surface. Coordinator ruling, 2026-09-22.",
    "TestTableKeyUpdate (runtime-capability): runtime-capability: the test builds a real swiss table through NewMap and drives it, and the table lives in RAW Go-layout group memory -- allocated, copied and cleared by the push block runtime supplies to this package (runtime/malloc.go maps_newobject and maps_newarray, runtime/mbarrier.go maps_typedmemmove and maps_typedmemclr), addressed by the descriptor GroupSize, SlotSize and ElemOff offsets. The managed model has no such memory: golib map\u003cK,V\u003e is a dictionary with no swiss table behind it, and abi MapType projection reports those offsets as an honest zero. Each of the four refuses BY NAME in this package runtime_impl.cs, the internal/abi FuncPCABI0 shape, so the row fails with the text below rather than as a missing implementation. No managed implementation can meet this assertion without a managed group store, which is a design, not a seat (not taken). internal/runtime/maps IS the raw-memory swiss table; the tests that remain live are its managed surface. Coordinator ruling, 2026-09-22.",
    "TestTablePutDelete (runtime-capability): runtime-capability: the test builds a real swiss table through NewMap and drives it, and the table lives in RAW Go-layout group memory -- allocated, copied and cleared by the push block runtime supplies to this package (runtime/malloc.go maps_newobject and maps_newarray, runtime/mbarrier.go maps_typedmemmove and maps_typedmemclr), addressed by the descriptor GroupSize, SlotSize and ElemOff offsets. The managed model has no such memory: golib map\u003cK,V\u003e is a dictionary with no swiss table behind it, and abi MapType projection reports those offsets as an honest zero. Each of the four refuses BY NAME in this package runtime_impl.cs, the internal/abi FuncPCABI0 shape, so the row fails with the text below rather than as a missing implementation. No managed implementation can meet this assertion without a managed group store, which is a design, not a seat (not taken). internal/runtime/maps IS the raw-memory swiss table; the tests that remain live are its managed surface. Coordinator ruling, 2026-09-22."
  ],
  "excluded": [
    "FuzzTable (fuzz): fuzz execution is deferred to Phase 4D"
  ],
  "errors": [],
  "environment": {
    "configuration": "Release",
    "tiered": false,
    "oracleGoVersion": "go version go1.24.13 windows/amd64"
  }
}
