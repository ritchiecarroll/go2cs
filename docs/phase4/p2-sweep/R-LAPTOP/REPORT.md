R S-R REPORT (P2 full-roster sweep, shard S-R, host R-LAPTOP) -- DRIVER_EXIT=0; P2 findings NONE; one CLEANUP named STOP (the worktree is held).
HEADER: sweep-base 61724860b4 (asserted: == version tip by ls-remote; fa18863b94 ancestor; corpus/instrument delta EMPTY; converter delta = the 4 platform* files); master 074a12c4ae. Converter plain sha256 4e4aaa77fad74aecb27b70cff2b0bd9c9d677f3cb688fc0e616a69ac3500d033, identity (-trimpath -buildvcs=false) 38be1ce6ea0bf31e1d63588c670cf645f2224def84342cd764935b9c9a978664, both go1.24.13. Driver 07:18:05Z-09:17:13Z; sum sweep wall 6,878 s, sum outer 6,966 s.
Q: pins by output: go1.24.13; go env GOROOT = the backslash SDK path (process only); GOFLAGS unset with go env GOFLAGS empty; GOTOOLCHAIN=local; CGO_ENABLED=0; dotnet 10.0.400; GoTargetOS absent; DOTNET_Tiered*/TC_*/COMPlus_* names none; both NuGet credentials absent. 16 logical CPUs, 31.3 GB RAM, boot 2026-09-19T19:00Z (108 h up). Disk C: (worktree and profile drive) 99.4 GB before, 90 GB at the end, 99 GB after cleanup. LongPathsEnabled=0; domain-joined=True; gcc/cc/clang absent; echo on PATH=True; symlink privilege ABSENT (TestOpenFileCreateExclDanglingSymlink skipped before InRoot/NoRoot); network Wi-Fi Private/Internet; WSL idle; 0 go2cs.exe. COORD's machine-global-hazards assertion recorded; no build-server shutdown or name-scoped kill ran here during the shard. SURVIVAL: sleeper PID 54112 alive across a turn boundary, canary.log advanced tick 5 (07:12Z) to tick 10 (07:17Z).
LIST: 89 = crypto/tls, internal/gover .. weak, minus internal/trace/net/os; processed == listed (89).
VERDICTS: 89/89 plain PASS, rc 0, every sweep line "sweep: 1 pass / 0 fail". crypto/tls "  PASS  crypto/tls  4759 [1805s]" in the FULL state (GOFLAGS=-timeout=40m in that child only; go env GOFLAGS [] before and after; -TestTimeout 90m). log/slog "  PASS  log/slog  197 [release-tiered] [142s]", header "  1 row(s) carry a per-row execution config: log/slog [release-tiered]". No header shows "an A/B measurement" or "target OS". Per-row lines are in ledger.tsv and driver.log.
SCORE: 89 discharged. Absorptions NONE (path/filepath plain 61; os/exec plain 116, TestExtraFiles not fired). Row-level findings NONE. Run-level stops NONE. Sum got 8,379 == sum banked Tests 8,379.
DISCLOSED (offline recompute per ruling): recompute-disclosed.ps1, which reads rows/<row>/go2cs_test_comparison.json through _roster's ConvertFrom-ComparisonRecord BY PROPERTY. Its planted control: the crypto/tls record reads 1, and the same record with one injected entry reads 2. Sum gotDisclosed 239 == sum banked Disclosed 239, row-for-row equal. The orphanedDisclosures field is absent from all 89 records.
PAGES (per the ruling, NOT REWRITTEN = content-stable EQUAL): 88 NOT REWRITTEN; 1 REWRITTEN, os/exec MOVED [TestString] skip/skip -> pass/pass, counts 116|0 unchanged. It is on the brief's count-neutral host-state list: TestString with echo on PATH, a READING. P-2(a) page N|D == run got|gotDisclosed on 89/89; P-2(b) banked == page on 89/89.
DRIFT: no REAL DRIFT.
- Class 1: CRLF-only porcelain M on 28 rows (numstat empty), plus index.md's frozen-snapshot phantom.
- Class 2, the -tests-closure initᴛᴛtests() hook (+7/-0) in package_init.cs, 8 rows: internal/profile, internal/syscall/windows (windows/package_init.cs), internal/testenv, internal/zstd, math/big, syscall (windows/package_init.cs), time, unicode. The hunk was quoted from a post-EXIT convert-only re-emission of those 8 (all identical: the call plus the partial declaration and its comment); the tree was then restored, tracked 15394, ' D' 0.
- Class 3, .cs.auto: internal/syscall/windows exec_windows_test.cs.auto 3/3; internal/syscall/windows/registry registry_test.cs.auto 5/3.
WALLS: floored: crypto/tls 1805 s vs 0.75x60m=2700; time 656 vs 1800; sync/atomic 156 vs 6750, all under. Unfloored >= 450 s: none (max regexp 350).
DEFERRED-ALLOC rows in S-R (29 manifests at the sweep base by '"class": *"deferred"'): io, log, log/slog, math/big, mime, net/http/internal, net/netip, slices, strconv, strings, unicode/utf16, unicode/utf8. Their records are at rows/<row with / as __>/go2cs_test_comparison.json.
DISK/PURGES: 2 in-run purges (30-row rule) rc 0, converter present, bin/obj/Generated 0 MB, 98.4/98.3 GB after. Cleanup purge rc 0. START GATE clean. No budget kills, no restores beyond per-row.
DEVIATIONS, stated:
(1) The lock is at <scratch>/p2/R-LAPTOP/p2r.lock: <wt>.lock is the drive root, not writable unelevated. The FIRST launch died at that write before row 1 (no ledger row; tree untouched) and was relaunched.
(2) The driver's gotDisclosed bug; the offline recompute above.
(3) The driver's drift columns count paths; classes 1-3 were assigned by hand from the preserved numstat and porcelain.
CLEANUP: steps 1-3 done (no oracle-flake dir; purge rc 0; per-run copy deleted; porcelain EMPTY; ' D' 0; tracked 15394 == S0). NAMED STOP: the ignored census shows src/gen/go2cs-gen/bin/ and src/gen/go2cs-gen/obj/ beyond the expected src/core residue. It is the analyzer build output the rows' ProjectReference created, and clean-bin's src/core root does not reach it. The worktree <wt> is NOT removed pending your ruling; the scratch and p2/R-LAPTOP-20260924T0703Z are kept until M9.
NOT VERIFIED: equality of the identity sha to A2(4) (your reference); SW-18's host-limit reading (not this shard). Nothing committed or pushed.
